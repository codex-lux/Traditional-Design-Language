{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://traditional-design-language.org/schema/parti.schema.json",
  "title": "Parti",
  "version": "0.1.0",
  "description": "A canonical plan diagram, used as a seed. A parti specifies TOPOLOGY and ROLES — which rooms, which walls are outside, what connects to what — and nothing else. Dimensions come from the room catalogue and are scaled to the brief, so the library never duplicates room data and never drifts from it. Every parti here is descended from something that was actually built, which is the argument for seeding a search rather than starting it from noise.",
  "type": "object",
  "required": ["id", "name", "description", "massing", "styles", "rooms"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
    "name": { "type": "string" },
    "aka": { "type": "array", "items": { "type": "string" } },
    "description": { "type": "string", "description": "What the diagram IS, and what it is good and bad at. The trade-off is the useful part." },
    "massing": { "type": "string" },
    "alternate_massings": { "type": "array", "items": { "type": "string" } },
    "styles": { "type": "array", "items": { "type": "string" }, "description": "style ids this parti is native to. A style not listed can still use it; the composer will say so." },
    "groupings": { "type": "array", "items": { "type": "string" } },
    "circulation_parti": { "type": "string" },
    "storeys": { "type": "integer" },
    "area_range_sf": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 2 },
    "bedroom_range": { "type": "array", "items": { "type": "integer" }, "minItems": 2, "maxItems": 2 },
    "rooms": {
      "type": "array", "minItems": 2,
      "items": {
        "type": "object", "required": ["id", "type", "level"], "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "type": { "type": "string", "description": "room catalogue id" },
          "name": { "type": "string" },
          "level": { "type": "integer", "description": "0 ground, 1 above, -1 cellar" },
          "required": { "type": "boolean", "default": true, "description": "False marks a room the composer may drop to hit an area target." },
          "repeats_with_bedrooms": { "type": "boolean", "default": false,
            "description": "The room multiplies with the bedroom count — a chamber, its closet." },
          "area_weight": { "type": "number", "description": "Relative share of the plan. Omit and the room catalogue's midpoint is used." },
          "exterior_walls": { "type": "array", "items": { "enum": ["N","NE","E","SE","S","SW","W","NW"] } },
          "lit_from": { "type": "array", "items": { "type": "string" }, "description": "Which of those walls actually carry glass." },
          "doors": { "type": "array", "items": { "type": "string" }, "description": "room ids, or 'exterior'" },
          "fixtures": { "type": "array", "items": { "type": "string" } },
          "stacks_over": { "type": "string" },
          "note": { "type": "string" }
        }
      }
    },
    "adjacencies": { "type": "array", "items": {
      "type": "object", "required": ["a", "b", "relation"], "additionalProperties": false,
      "properties": { "a": { "type": "string" }, "b": { "type": "string" }, "relation": { "type": "string" } } } },
    "scaling": {
      "type": "object", "additionalProperties": false,
      "description": "How the diagram grows. Most partis grow by widening rooms; some grow only by adding a link, and saying which is the difference between a diagram that scales and one that breaks.",
      "properties": {
        "grows_by": { "type": "array", "items": { "enum": ["widening", "deepening", "adding-bays", "adding-a-link", "adding-a-storey", "does-not-scale"] } },
        "bay_module_ft": { "type": "number" },
        "max_bay_count": { "type": "integer" },
        "note": { "type": "string" }
      }
    },
    "trades_away": { "type": "string", "description": "What this diagram gives up. Every parti gives something up, and a composer that cannot say what will pick badly." },
    "exemplars": { "type": "array", "items": { "type": "string" } },
    "sources": { "type": "array", "items": { "type": "string" } },
    "confidence": { "enum": ["high", "medium", "low"] }
  }
}

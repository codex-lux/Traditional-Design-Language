{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://traditional-design-language.org/schema/style-node.schema.json",
  "title": "Style Node",
  "description": "A single taxon in the Traditional Design Language style graph. Nodes form a directed acyclic graph via typed lineage edges, and a separate strict single-parent containment hierarchy via member_of.",
  "type": "object",
  "required": [
    "id",
    "name",
    "rank",
    "period",
    "geography",
    "description",
    "defining_characteristics"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Stable kebab-case identifier. Never reused, never renamed. This is the address the kit-of-parts directory hangs off."
    },
    "name": {
      "type": "string"
    },
    "aka": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Alternate and historical names. Feeds fuzzy lookup for agents and search."
    },
    "rank": {
      "enum": [
        "tradition",
        "family",
        "style",
        "variant"
      ],
      "description": "tradition = civilizational trunk; family = coherent grouping sharing a generative logic; style = the nameable style a client would select; variant = regional/period expression at which a kit of parts is actually buildable."
    },
    "member_of": {
      "type": [
        "string",
        "null"
      ],
      "description": "Single-parent containment for browsing/UI. NOT lineage. A variant's member_of is its style; a style's is its family; a family's is its tradition. Null only for traditions."
    },
    "status": {
      "enum": [
        "historical",
        "living",
        "revival",
        "extinct",
        "emergent"
      ],
      "default": "historical",
      "description": "living = still built by practicing traditional builders; revival = a later re-adoption node; emergent = contemporary (New Traditional, etc.)."
    },
    "period": {
      "type": "object",
      "required": [
        "floruit_start",
        "floruit_end"
      ],
      "additionalProperties": false,
      "properties": {
        "origin": {
          "type": [
            "integer",
            "null"
          ],
          "description": "First appearance year (approximate)."
        },
        "floruit_start": {
          "type": "integer",
          "description": "Start of the period of dominant/characteristic production."
        },
        "floruit_end": {
          "type": "integer",
          "description": "End of that period."
        },
        "decline_end": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Last meaningful vernacular production before revival, if any."
        },
        "circa": {
          "type": "boolean",
          "default": true,
          "description": "Dates are conventional and soft; true for nearly all nodes."
        },
        "revival_periods": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "start",
              "end"
            ],
            "additionalProperties": false,
            "properties": {
              "start": {
                "type": "integer"
              },
              "end": {
                "type": "integer"
              },
              "revival_node": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "id of the node that IS the revival, if it is modelled separately."
              },
              "note": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "geography": {
      "type": "object",
      "required": [
        "regions"
      ],
      "additionalProperties": false,
      "properties": {
        "regions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Where it was actually built. Use consistent region tokens (see schema/regions.json)."
        },
        "hearth": {
          "type": "string",
          "description": "The originating locus, if identifiable (e.g. 'Tidewater Virginia', 'Pasadena, California')."
        },
        "climate_zones": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "IECC climate zones where the style's assemblies are natively appropriate. Constrains buildability."
        }
      }
    },
    "lineage": {
      "type": "array",
      "description": "The DAG. Multiple parents are normal and expected. Edge TYPE carries most of the meaning.",
      "items": {
        "type": "object",
        "required": [
          "target",
          "type"
        ],
        "additionalProperties": false,
        "properties": {
          "target": {
            "type": "string",
            "description": "id of the ancestor/related node."
          },
          "type": {
            "enum": [
              "descends_from",
              "references",
              "reacts_against",
              "hybridizes_with",
              "regional_of",
              "revives"
            ],
            "description": "descends_from = actual transmission of building practice (builders, pattern books, trade knowledge). references = claimed/quoted ancestry the style imitates without descending from it. reacts_against = defined by inversion of a predecessor. hybridizes_with = reticulation, a co-parent of equal weight. regional_of = a variant's relation to its parent style. revives = deliberate resurrection after a gap."
          },
          "weight": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "default": 1,
            "description": "Relative strength of this inheritance. Drives cascade precedence and edge rendering."
          },
          "inherits_kit": {
            "type": "boolean",
            "default": false,
            "description": "If true, this edge participates in the kit-of-parts cascade. Only descends_from / regional_of edges should normally set this."
          },
          "note": {
            "type": "string"
          },
          "slots": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Slot ids this edge transmits, when it transmits only some. Absent means the edge carries the ancestor's whole kit, which is the historical behaviour and stays the default. Added by OQ 58 for `hybridizes_with`: an edge drawn to carry one narrow aspect of a donor's practice - a porch treatment, a roof form, a decorative vocabulary - transmitted that donor's ENTIRE kit, because the cascade could not partition a donor's bindings by which aspect the edge was drawn for. WP-4.2 hit about twenty-six real merge problems that way across 129 kits and patched every one at the node instead of at the mechanism. Scoping an edge fixes the class and makes those node-level patches removable one at a time."
          }
        }
      }
    },
    "description": {
      "type": "object",
      "required": [
        "short",
        "long"
      ],
      "additionalProperties": false,
      "properties": {
        "short": {
          "type": "string",
          "description": "One sentence. Used in the tree UI and in agent context windows."
        },
        "long": {
          "type": "string",
          "description": "2-4 paragraphs: what it is, what produced it, what it was reacting to, what it feels like to stand in."
        }
      }
    },
    "defining_characteristics": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "string"
      },
      "description": "The features without which it is not this style. Necessary conditions, not a feature list."
    },
    "diagnostic_tells": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Fast field-identification cues that distinguish it from its nearest siblings. The 'if you see X and Y together, it is this' set."
    },
    "distinguished_from": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "node",
          "difference"
        ],
        "additionalProperties": false,
        "properties": {
          "node": {
            "type": "string"
          },
          "difference": {
            "type": "string"
          }
        }
      },
      "description": "Explicit disambiguation against commonly-confused neighbours. High value for both humans and agents."
    },
    "proportional_system": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "governing_logic": {
          "type": "string",
          "description": "e.g. 'classical orders, module = column diameter', 'structural bay of timber frame', 'none — picturesque massing'."
        },
        "typical_ratios": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "bay_rhythm": {
          "type": "string"
        },
        "symmetry": {
          "enum": [
            "strict-bilateral",
            "near-symmetrical",
            "balanced-asymmetrical",
            "picturesque-asymmetrical",
            "additive-vernacular"
          ]
        }
      }
    },
    "massing_affinities": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "massing",
          "affinity"
        ],
        "additionalProperties": false,
        "properties": {
          "massing": {
            "type": "string",
            "description": "id from the massing catalog. Massings are style-independent skeletons."
          },
          "affinity": {
            "enum": [
              "canonical",
              "common",
              "possible",
              "atypical",
              "forbidden"
            ]
          },
          "note": {
            "type": "string"
          }
        }
      },
      "description": "Massing is NOT style. This is the join table between the skeleton catalog and the style grammar."
    },
    "kit": {
      "type": "object",
      "description": "Inline slot overrides. The full slot directory for a style lives at kits/<id>.kit.json; this field is for the small number of bindings worth carrying on the taxonomy record itself. Slots absent from both are inherited via the cascade; see docs/inheritance.md.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "binding": {
            "enum": [
              "specified",
              "inherited",
              "open",
              "forbidden"
            ],
            "default": "open"
          },
          "variants": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "parameters": {
            "type": "object"
          },
          "rule": {
            "type": "string",
            "description": "Prose constraint, pending formalization."
          },
          "note": {
            "type": "string"
          },
          "status": {
            "enum": [
              "empty",
              "stub",
              "drafted",
              "reviewed"
            ],
            "default": "empty"
          }
        }
      }
    },
    "constraints": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "kind",
          "statement"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9-]+\\.c[0-9]{2,3}$",
            "description": "Present once migrated (docs/open-questions.md #24). <style-id>.c01, c02, ... Stable, never reused; see schema/constraint.schema.json for the full shape this mirrors."
          },
          "kind": {
            "enum": [
              "co-occurrence",
              "proportional",
              "structural",
              "climatic",
              "code",
              "cultural"
            ]
          },
          "statement": {
            "type": "string"
          },
          "severity": {
            "enum": [
              "hard",
              "soft",
              "advisory"
            ],
            "default": "soft"
          },
          "scope": {
            "enum": [
              "plan",
              "elevation",
              "site",
              "section",
              "judgment"
            ],
            "description": "Present once migrated. See schema/constraint.schema.json."
          },
          "deprecated_in_favour_of": {
            "type": "string"
          },
          "test": {
            "type": "object",
            "description": "Present once migrated and only when scope != judgment and the statement supplies a number or enumerable set. Full shape validated separately by build/check_constraints.py against schema/constraint.schema.json — kept loose here (not $ref'd cross-file, to match this project's existing convention of self-contained schema files) so a style node can still be checked standalone."
          }
        }
      },
      "description": "Assembly rules. Hard constraints are what keep the generator from producing incoherent houses. `id` and `scope` are present once a constraint has been migrated to the executable rule language (WP-1.1); constraints not yet migrated are still valid records without them — migration is in progress across the corpus, not a hard cutover."
    },
    "exemplars": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "location": {
            "type": "string"
          },
          "year": {
            "type": [
              "integer",
              "null"
            ]
          },
          "note": {
            "type": "string"
          },
          "precedent": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "description": "WP-11.1. The id of the building's own record in `precedents/`, which carries the archival references (HABS, NRHP, a list entry, the institution) and, where one exists, the survey's written data quoted verbatim. An exemplar without one is a name a reader can find and a checker cannot resolve. `build/check_precedents.py` holds this to the record's own `nodes` in both directions."
          },
          "standing": {
            "enum": [
              "icon",
              "canonical",
              "regional",
              "documentary"
            ],
            "description": "WP-11.1. What this building is to THIS node, editorially: `icon` -- the building a fluent reader names first, the one the style is known by; `canonical` -- a type specimen, well documented, that the node's own figures can be held against; `regional` -- the local expression, right for the region and not the style's whole reach; `documentary` -- plain, surveyed, useful to measure and not to admire. A judgment, and `why` says whose."
          },
          "why": {
            "type": "string",
            "description": "One sentence for `standing`, in the project's voice, saying what makes it so and where the reason comes from."
          },
          "no_precedent": {
            "enum": [
              "archive",
              "body-of-work",
              "phase",
              "not-yet-researched"
            ],
            "description": "WP-11.4, ruled 5 Sep 2026. Why this exemplar carries no `precedent`, when the reason is a DECISION rather than a gap. `archive` -- it names a drawings collection, not a building (Royal Barry Wills Associates' garrison designs, collection AR029 at Historic New England). `body-of-work` -- it names an architect's output rather than one work (Cliff May's early Monterey-mode houses). `phase` -- the building HAS a record and this row names a phase the record is not about (`dogtrot-vernacular`'s 'Gaineswood (original core)': the record is the finished 1842-1861 mansion whose survey is silent on the dogtrot substrate, so joining them would hand the reader the wrong card). `not-yet-researched` -- an honest gap, and the default meaning when the key is ABSENT. The field exists because nothing else separates a stated refusal from an unresearched row: one number carried both, and a `why` is prose no counter can read. A row carrying `precedent` may not carry this. AND A REGIONAL POPULATION IS NOT ONE OF THESE (ruled 5 Sep 2026, the day the field was added): a row naming 'the Leopoldine farmhouses of the Val di Chiana' or 'the farmhouses of the Simmental' takes a `record_kind: type-model` RECORD, not a refusal. Europe raised this on first contact and two such rows were marked `body-of-work` and WITHDRAWN, because that value means an architect's output and these have no architect."
          }
        }
      }
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "confidence": {
      "enum": [
        "high",
        "medium",
        "low"
      ],
      "default": "medium",
      "description": "Editorial confidence in the dates and lineage claims of this node."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "proportion_packs": {
      "type": "array",
      "description": "The grammar this style is written in. Element slots supply the alphabet; a proportion pack supplies the rules that dimension them. A style with no pack bound has a vocabulary and no syntax.",
      "items": {
        "type": "object",
        "required": [
          "pack",
          "role"
        ],
        "additionalProperties": false,
        "properties": {
          "pack": {
            "type": "string",
            "description": "proportion pack id"
          },
          "role": {
            "enum": [
              "primary",
              "secondary",
              "interior",
              "opening",
              "facade",
              "room",
              "massing",
              "optional"
            ],
            "description": "Which layer of the building this pack governs. A style normally binds several: an order for the classical apparatus, a module system for the envelope, a trim system for the interior."
          },
          "authority": {
            "type": "string",
            "description": "Preferred authority overlay for this style, e.g. gibbs-ionic for colonial Georgian, benjamin-ionic for Federal."
          },
          "note": {
            "type": "string"
          },
          "precedence": {
            "type": "integer",
            "description": "Lower wins where two packs bind the same slot. Required because the Georgian exercise found 32 of 82 slots spoken to by two or more packs, with a 10-inch spread on the chair rail and no rule for choosing."
          },
          "slots": {
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^[a-z_]+(/[a-z_]+)?$"
            },
            "minItems": 1,
            "description": "Target slot ids this binding may contribute, when it contributes only some. Absent means the binding carries the pack's whole rule set, which is the historical behaviour and stays the default. Added by OQ 49, and it is OQ 58's mechanism one layer out: that ruling scoped a `hybridizes_with` edge so a donor could transmit one aspect rather than an entire kit; this scopes a BINDING so a pack can supply one rule to a node that is not an instance of its type. `french-normandy-revival` states `jetty-overhang`'s material-change rule almost verbatim and has no jetty; `mission-revival` carries a rule about `facade-portada`'s subject stating that there is none; two of `facade-peristyle`'s rules fit `egyptian-revival` exactly and thirteen do not. Without this field each of those is a choice between a false claim about the building and a true rule restated in the node, which is the duplication this library exists to remove. Each entry is either a bare slot id, which admits every rule the pack writes to that slot, or `slot/dimension`, which admits exactly one rule. BOTH GRANULARITIES ARE NEEDED and the coarse one alone would not have served the cases that motivated the field: `jetty-overhang` writes `material_change_rule` once, so a bare slot id is right there, but `facade-portada` writes `ornament_vocabulary` four times and only its rationing rule applies to `mission-revival`, and `facade-peristyle` writes `column` seven times of which two fit `egyptian-revival`. A scope that admitted three rules nobody asked for would be a smaller version of the problem it was added to solve."
          },
          "slots_except": {
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^[a-z_]+(/[a-z_]+)?$"
            },
            "minItems": 1,
            "description": "Target slot ids this binding may NOT contribute, when it carries the pack's whole rule set except a few. Same grammar as `slots` -- a bare slot id or `slot/dimension` -- and the two are mutually exclusive: `slots` is an allowlist, this is a denylist, and a binding states at most one of them. Added 27 Aug 2026 (WP-5.14) because the allowlist alone cannot express the case it was needed for without becoming a liability. `sash-light` writes fourteen addresses to a Georgian node and exactly one of them is wrong on a masonry wall: its `window_sill/projection` rule gives 2.25 in 'sloped about 1 in 6 with a drip', and its own note says why that is a frame figure -- 'in a masonry wall it is a rowlock or a stone and belongs to the brick-course pack, not this one.' Excluding one address with `slots` would mean listing the other thirteen by hand, and a hand-maintained allowlist silently stops delivering any rule the pack gains later. The denylist says what is being refused and nothing else, which is also what the note says."
          }
        },
        "not": {
          "required": [
            "slots",
            "slots_except"
          ]
        }
      }
    },
    "declined_packs": {
      "type": "array",
      "description": "Packs that reach this node BY DESCENT and do not belong on it. The refusal half of OQ 51's adjudication, and the exact mirror of adding a node to a pack's `applies_to`: one records that somebody read the cascade and agreed, this records that somebody read it and did not. Additive -- it strands nothing, because it removes only what an author names, and it is not the opt-in flip, which stays where the ruling put it. A decline is per (node, pack) and NOT per (node, pack, role): a node reaching one pack in two roles is ONE judgment, the same distinction `check_inheritance.py --unendorsed` already prints as NODES versus GAPS. `slots`/`slots_except` cannot do this job -- they live on the ANCESTOR's binding, so they change behaviour for every descendant and for the ancestor itself, and a child re-binding the pack unscoped shadows them entirely. A PER-EDGE deny was designed and refused (WP-8.2): only 111 of 222 gaps have a direct lineage edge to their delivering ancestor at all, and a subtree deny on `english-georgian`/`storey-graduation` would have touched 26 receivers to fix 18, undoing five of the eleven endorsements the 26 Aug pass had just made.",
      "items": {
        "type": "object",
        "required": [
          "pack",
          "reason",
          "basis"
        ],
        "additionalProperties": false,
        "properties": {
          "pack": {
            "type": "string",
            "description": "The proportion pack id being declined. It must actually reach this node by descent, and `check_pack_bindings.py` errors if it does not -- a decline that refuses nothing reads exactly like an adjudicated refusal and is worse than no decline at all."
          },
          "reason": {
            "type": "string",
            "description": "Why this pack does not belong on this node, in a sentence. Not 'wrong style' -- what about the building refuses it. `ranch-style` refuses `storey-graduation` because it has one storey and a rule for graduating a stack has no stack to graduate."
          },
          "basis": {
            "enum": [
              "node-record",
              "editorial"
            ],
            "description": "`node-record` means this node's own record decides it, and `quote` proves it. `editorial` is the honest alternative and never the default: a decline with no quote is a judgment, and this corpus's rule is that a judgment says so."
          },
          "quote": {
            "type": "string",
            "description": "A verbatim sentence from THIS node's own record. Required when `basis` is `node-record`, and checked against the file the way check_openings.py checks grammar.json's citations -- a citation that cannot be checked is a guess wearing a citation."
          },
          "quoted_from": {
            "type": "string",
            "description": "Where in this node's own JSON the quote came from, e.g. `distinguished_from[2].statement`. For a reader, not for a parser."
          },
          "decided": {
            "type": "string",
            "description": "ISO date the judgment was made."
          },
          "note": {
            "type": "string",
            "description": "Where the decline is known to leave the slot with a worse governor or none at all, say so here. `check_inheritance.py --impact` computes it; this records that the author looked. Scoping stops a wrong pack and does not supply a right one -- OQ 58's stated limit, one layer down."
          }
        }
      }
    },
    "inherits_packs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Pack ids this node OPTS IN to receiving by descent. The delivery half of OQ 51, re-ruled 3 Sep 2026, and the exact mirror of `declined_packs`: that one records a person reading the cascade and refusing a delivery, this one records a person reading it and wanting one. IT ONLY MEANS ANYTHING FOR A PACK THAT DECLARES `delivery: opt-in`. Until a pack declares it, the cascade delivers as it always has and an entry here is a no-op -- which is what makes the flip stageable: one pack moves at a time, and the nodes that should keep it say so first. PER-NODE, and that grain is measured rather than chosen. A per-EDGE opt-in mirroring `inherits_kit` was costed and refused: only about half of all gaps reach their delivering ancestor through a direct lineage edge at all, `_cascade` is flattened so the delivering edge is not recoverable from the chain, and `english-georgian` delivers some fifty gaps of which five are direct. It is NOT `applies_to` under another name. `applies_to` says a pack's author vouched for the node and additionally arms five live behavioural gates; this says the node receives the pack. Keeping them apart is why restoring a dimension cannot silently switch on `graduation_check` or the elevation generator. `check_pack_bindings.py` errors on an entry that opts into nothing -- a pack that does not reach this node by descent, or one the node already binds itself, which `resolve_packs` takes at chain[0] anyway -- and on opting into a pack this node also declines. An opt-in that admits nothing reads exactly like a considered one, which is the failure `declined_packs` already states for the refusal direction."
    }
  }
}

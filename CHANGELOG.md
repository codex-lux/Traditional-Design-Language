# Changelog

One line per layer per notable change. Versions here track the project's own `v0.x` designation, not any single schema's version number (those are tracked in the schema files themselves — `ontology_version`, `kit_version`, and so on).

## Unreleased (8–9 Sep 2026 — Phase 12 planned, WP-12.0 through 12.3 built)

- **The scene route (WP-12.3, 9 Sep 2026).** `workbench/server/corpus.py::scene()` returns the scene record, the PLACED plan and every named view's plate together, behind `POST /api/scene` with `_heavy` and `_plan`; `api.scene` is the client wrapper. **The measurement the package owed is about the meter and not the clock.** On `tidewater-georgian-careful` at `auto`: a cold solve 37.48 s, an already-placed record 0.00 s, `build_scene` 0.00 s, all six plates 0.38 s, the whole call 38.46 s — so everything but the placement is 2.6% of it, and the response is 376,574 bytes raw, about 71 KB gzipped. Six plates fetched one at a time beside the scene is seven calls against `limits.heavy_calls_per_hour()` of 60: eight record changes an hour, where one call buys sixty. **A reader who profiled this route would have concluded the plates were free and reached the opposite design.** `SCENE_PLATES` holds the six views that carry a plate and APPROACH is deliberately absent, because a perspective dimension is never true. The placed record comes back for a second reason and it is not a cache — it is WP-6.4's rule, and a client that keeps it pays 0.00 s on every later export or evaluate.
  **The package's two defects split the opposite way from what a reader would guess.** `B` and `_os` are per-function locals in the three neighbouring functions that use them, so two `NameError`s reached the tests' first two runs and neither was reachable by reading, because the surrounding code reads as though both were module-level. The one found *by* reading was the serious one: the first draft forwarded the resolved parti record to `drawing()`, which calls `core.load_parti` itself and takes a caller's string — handed a dict it does `os.path.basename(str(dict))`, finds no file and returns `None`, so every plate would have been drawn with no parti while the scene beside it used one. That is WP-12.0's own defect reintroduced by the package that depends on WP-12.0, and **this package's own digest guard cannot see it**, because `_placed` stamps the digest on the record it was handed and that record is the same either way. And the first plate figure published was 0.70 s against a reproducible 0.38 — measured in a process that had not yet loaded the drawing pipeline — corrected in five files by sweeping for the retired number. See `docs/reports/wp-12.3-the-scene-route.md`.

- **One opening rectangle, three callers (WP-12.2, 9 Sep 2026).** `(x0, x1, sill, head)` was written out three times — `render_elevation._window` for a sash, `render_elevation._entrance` for the door, and `export_dxf._win` — and **so was the loop around it**, which is the half that bit: when the blind bay arrived (OQ 85) the SVG learned to skip the bay a chimney stack stands on and the DXF did not, so the CAD file drew a window through a chimney and the export selftest could not see it, because it round-trips findings and not geometry. `build/elevation.py::opening_rects(elev, face)` is the one spelling now, and `build/scene.py` is its third caller: 40 and 32 `opening-frame` solids on the two shipped plans, `not_modelled` 2 → 1 and 3 → 2. **Its largest finding was invisible on the two shipped plans**: both renderers resolved the upper storey with a fallback to the ground one and then drew a second row of windows at that datum unconditionally, so each of the six one-storey plans that build an elevation was drawing a row of openings its record does not hold — 244 window rectangles become 124 over 24 plates. The first measurement of this package was taken over the two shipped plans, which are both two-storey, and reported that the lift changed nothing but four pixels; *verifying a corpus-wide change on the plans that happen to ship is verifying it on 2 of 164 styles* is in `CLAUDE.md` and was still the thing that nearly shipped. **Measured over all 44 elevation plates: 16 of 44 SVG and 20 of 44 DXF byte-identical** (on the two shipped plans, 6 of 8 and 8 of 8), both sides proved reproducible run-to-run first; the four that move without losing a row are one decimal tie at exactly 203.35 px, and **the unit was chosen by measurement** — over the 144 opening edges the two shipped plans actually draw, inches-first moves 0 DXF coordinates and 2 printed SVG ones, feet-first moves 0 and 64.
  **The branch the package exists to guard is unreachable from the corpus.** All sixteen plan records now produce zero blind bays, because WP-11.4 moved the stacks off the gable centre line and onto the flues the plan states, so a parity test over the shipped plans would pass with the skip deleted. The guard is driven and asserts its own premise; `CLAUDE.md`'s sentence calling the collision measurement "a measured zero — it resolved a collision" is corrected, because it is now a zero for the other reason.
  **It found three defects in WP-12.1 and two false claims in `docs/export.md`.** An opening extruded out of its wall on the north and east faces; **the same error in the gable ends, with WP-12.1's own test ratifying it** (that test compared the gable's `at` against the outer face of the wall extent, which is exactly where the wrong contract put it, so both gables stood 1.29 ft clear of the house above the eave under a green assertion); and a `bounds` block that was the right size in the wrong place, offset by one wall thickness, **which none of the three agreement figures could see because not one of them reads `bounds`**. The doc claims — a 0.9× window shrink in the SVG and a fixed 3 ft door — have been false since WP-6.2, and the test the paragraph cited could never have caught them, because it asserts the DXF's widths are a subset of the recorded ones, which is true whether or not the SVG shrinks. All three geometry defects were found by drawing the scene and looking at it.

- **The scene record (WP-12.1, 9 Sep 2026).** `build/scene.py` — constructed 3D geometry between a placed plan and any camera, pure arithmetic and a leaf, importing `export_ifc.slab_boxes`, `structure.wall_thickness`, `hearths.breast`, `elevation.opening_rects` and `compass` rather than restating one of them. `schema/scene.schema.json` 0.1.0: six primitives so a viewer needs no domain knowledge, `ink` and `tone` as closed enums of `tokens.css` names so a solid can never carry a hex, `kind` as the weakest provenance among the numbers that made it, and three states rather than two. The selftest runs inside `build/validate.py` so `TOTAL_CHECKS` does not move. **Its finding is that the roof was never over the house**: `roof.py` lays the roof out over the outside footprint from (0,0) and `structure.py` lays the walls out over the clear one from (0,0), so read literally the roof sits 1.29 ft east and north of what it covers — invisible for as long as no surface drew a roof and a room in one picture. **And the roof was a box with a lid**: each plane was written as a prism between the eave and ridge heights, which every agreement figure accepted because all three measure the plan extent, and only the picture refused. See `docs/reports/wp-12.1-the-scene-record.md` and `docs/scene.md`.

- **The drawing set is one building again (WP-12.0, 8–9 Sep 2026).** The elevation and roof plates were built with no section, so they took `structure.build_section`'s heuristic default while the plan beside them was a CP proof. Fixed at the one place both are built, and the four faces are addressable from the client for the first time since WP-3.2. **Re-derived across the merge with `main` rather than quoted, and the defect was worse than published**: the spec Colonial's `porch_clear_depth_ft` is 6.00 against a drawn 4.00, and the Tidewater plan now reaches a proof inside its budget, so ten of its measurements move and all four of its faces differ. See `docs/reports/wp-12.0-the-drawing-set-was-not-one-building.md`.


- **Phase 12, the sheet in the round (8 Sep 2026).** Lucas asked for the interaction in Dilum Sanjaya's post on 2D schematics transitioning into 3D — named views, a camera tween, annotations anchored to the model, furniture that appears only where it is true — and asked whether SVG was still the medium or a BIM or Blender layer was now required. The brief is a PRD, `docs/prd/phase-12-the-sheet-in-the-round.md`, the first document in a new `docs/prd/` directory, adopted after a review against the tree. **The answer is WP-5.11's finding one dimension up: the format was never the constraint.** A format serialises what is modelled and cannot invent what is not, so what is missing is the layer between — a constructed-3D scene record built in Python from the placed record, with three.js drawing what Python models, the SVG plates staying authoritative and laid over the model at every named view to keep the two honest, and Blender and IFC staying export targets. Nine packages, WP-12.0 through 12.8; all nine of the PRD's rulings taken the day they were put, and an APPROACH perspective view at 5′-6″ added to v1 with its dimensions withheld, because a perspective dimension is never true.
  **The review found the defect that made WP-12.0: the drawing set is not one building.** `corpus.drawing` routes the plan, section and bearing plates through the set's one placement and then builds the elevation and the roof with no section, so both take `structure.build_section`'s heuristic default — whose own comment says that default is for internal callers only — while the plan beside them is a CP proof. The elevation reads placement, so the two plates are of different houses wherever a proof is reached, under the banner WP-6.4 wrote saying one drawing set is one building or it is nothing. Pre-existing and invisible to every test. **In the same package: `render_elevation` has taken a `face` argument since WP-3.2 and no client has ever sent one**, so three of the four elevations the system can already draw have never been looked at.
  **And two citations in the PRD would have failed the build on commit** — seven report paths for reports that do not exist yet, which `check_ids.py` refuses by construction, and an open-question slug wrapped across a newline, which `check_citations.py` reads line by line and sees as its truncated left half. Both corrected before the commit, and **both guards driven to prove they fire** rather than reasoned about.

## Unreleased (24–26 Aug 2026 — Phase 4 closed on the data, Phase 5 part-built, and the 25 Aug merge)

*(Backfilled 26 Aug 2026 by the project review — the log had stopped at WP-4.1/4.2 while six further packages shipped. Entries below are summaries; each package's full record is its report.)*

- **The candidate score (26 Aug 2026).** The composer published a DEMERIT TOTAL under the label `score` — lower is better, no ceiling, sorted ascending — and the workbench set it in a 26px numeral under the word SCORE, so the worst plan of four read as the winner. It is now a weighted composite **out of 100, higher is better**, each of eight axes a share of its own denominator (what came back clean out of what was actually checked), so a bigger house is no longer marked down for being checked more times: the same old formula gave −66 to 146 on one brief and 176 to 283 on another, for plans of comparable merit. An axis with no evidence has its **weight dropped and the total renormalised**, never counted as a pass, with the dropped weight published. The Candidate Set gained three named orderings — highest score first (default), native to the style, fatal first — each printing the sentence that says what it did, because the ordinal in a column is a position in the current order and not a verdict; that conflation is what made the reported sequence read 82, 36, 74, 78. Every column now shows the whole arithmetic instead of the rulebook legend `100/fatal · 8/serious · 1/minor − fidelity (7)`. Found on the way and fixed: **`why_this_diagram` was rendered wrong in both branches of the shipped workbench** — it is a list, and React joined it with nothing between the reasons in the native case and with commas plus a duplicated "NOT native to this style" in the borrowed one. A fatal finding **disqualifies** a candidate — stated in words, last under every ordering, and still scored, because five briefs of eight come back with every candidate disqualified and four dashes tell a reader nothing. `demerits` stays on the record and ranks nothing.
  **An adversarial audit then found the change had moved the returned SET, not just the reading order** — `family-georgian` came back with an octagon and a tower villa in place of two native diagrams, which is the exact sentence WP-4.5 exists to delete. The composite cannot be made order-equivalent to a demerit total at any weighting, so fidelity's weight is now the one MEASURED weight: 25, from the observed 18.0/22.0-point spread of the whole non-fidelity subtotal, and the returned sets are more native than the demerit total's own answer on both briefs. The same audit found `area` clamping only one end against a schema with no `minimum`, publishing **902.8 "out of 100"** (schema bounds added; the ceiling now enforced at one tested point), and `BriefIntake` telling users **"93 of 132 styles have none"** — the pre-WP-4.5 figure, wrong by 90.
  **The first test suite was audited by mutation and thirteen of twenty wrong scorers survived it**; rewritten, twelve of twelve now die. **The workbench app had no tests at all** — `workbench/app/src/candidateOrder.js` + `candidateOrder.test.mjs`, ten cases, six of six mutants killed, wired into `check_all.py` (N/EV without node) and CI. It also exposed that `workbench/app/e2e/walk.mjs` — 28 assertions across all eleven surfaces since WP-5.2 — was run by **nothing**: not CI, not the Makefile, not `check_all.py`. It is wired into CI now, and it could never have run there anyway: it hard-coded one machine's Chromium path and its `require('playwright')` had no module to find. *(A first version of this entry claimed CI's "Build app" step carried no `working-directory`. It did, at 12e1dd0 and before — the claim came from re-emitting a yaml block that already contained the line and reading the diff as an addition. Corrected 26 Aug 2026 by the second audit round.)*
  **Pre-existing, found and fixed:** the plan record took a *reference* to the parti's `groupings` list and `attach_garage()` appended to it, so one brief asking for a garage changed every later brief's result in the same process (146.0 → 155.0 on one candidate; the workbench runs every job on one worker). `geometry_report.score` is the same demerit-as-score defect one layer down — engine untouched, direction now stated wherever it is published. Plus four more UI misreadings: a two-sided `between` bound rendered with one edge, the proportion engine's `error` dropped from `RULE_KEYS` so a refusal drew as `null in`, a privacy ramp dividing by 6 where the ranks run 1–5 and drawing an unranked room as the least private, and one style's fault coverage captioned beneath every style's elevation. **OQ 64 raised (seven weights are editorial and unmeasured); OQ 67 raised (the composer's ranking changed and wants a ruling).** See `docs/reports/candidate-score-composite.md`.
- **The rail could report itself available and then fail every turn.** `bool(os.environ.get("ANTHROPIC_API_KEY"))` is true of a key pasted with a trailing newline, and a newline is not a legal header value, so `/api/health` said the rail was on and the SDK raised on each request. `rail.key()` strips and is now the single reader — health, the turn's gate and the client it builds — so no two of them can disagree. `/api/health` also sends `Cache-Control: no-store`: it is the endpoint an operator refreshes to see whether a change took effect, and the client's `fresh: true` had been skipping only the app's own map, not the browser's cache. 5 tests.
- **The 25 Aug merge, and what it changed.** Two parallel sessions had each built WP-2.3 and each issued open-question ids 32–41. Main's block keeps those numbers; the other ten were reissued as 54–63, with a conversion table at the foot of the register. Of the two CP-SAT engines, `build/geometry_cp.py` (dispatched from `geometry.solve()`) survived on Lucas's ruling and `build/solver.py` was deleted rather than kept beside it — the WP-2.3 entry below this section describes the deleted one, and is left as written because it is a record of what that session built. Three of the deleted branch's findings are kept (OQ 54's under-band reporting among them), and one guarantee did not survive: the open-void constraint lived in the deleted engine, so **OQ 55 partly reopened** — only the heuristic's 40-point charge remains, pinned honestly by `tests/test_voids.py`.
- **Drawing-to-record ingestion (WP-5.5).** The Transcription surface (⑪): trace room rectangles over a scanned backdrop that never leaves the browser, or start from `build/ingest_dxf.py`'s extraction of a drafter's DXF (candidates with gaps named, never fabricated); type rooms, place openings, state provenance and trust; check it as a record and put it on the bench. See `docs/reports/wp-5.5-drawing-to-record-ingestion.md`.
- **The workbench (WP-5.2).** Eleven surfaces over a FastAPI skin on `mcp_server/core.py` — phylogeny, style record, kit, brief intake, candidate set, the plan workbench (the drawing with the critique on it), drawing set, details & export, fault corpus, proportions, transcription — with the Drawn Language tokens verbatim, the 19-component design system, SSE compose jobs, the AI rail with validated citations and typed unjudged/refusal cards, and the 24 MCP tools mounted at `/mcp`. The adversarial audit that closed the package fixed a class it called "the product lying about numbers." See `docs/reports/wp-5.2-the-workbench.md` and `docs/workbench.md`.
- **DXF and IFC export (WP-5.1).** `build/export_dxf.py` (layered DXF per sheet kind, inches, the record riding as XDATA under appid `TDL`, a proven round-trip via `build/import_dxf.py` that refuses on linework/record disagreement) and `build/export_ifc.py` (IFC4 — walls, slabs, openings, spaces, plain-gable roof solids; other roof forms ship properties and a `geometry_note`, never a guessed solid). See `docs/reports/wp-5.1-export-dxf-ifc.md`.
- **Missing proportion packs (WP-4.6, closing Phase 4's pack breadth).** Twenty-one packs — every item of WP-4.1's list this corpus can support — taking the corpus to 57 packs and 262 recorded conflicts; rules gained a `quantity` naming what they measure (OQ 48: 139 corruptions found and renamed minimally), `expressed_frame` landed at ontology 0.7.0 (OQ 47), slot-scoped bindings took nodes bound to 132 of 132 (OQ 49), and closing 49 opened **OQ 51** — the lineage cascade delivers packs nobody bound, now the largest open item, ruled adjudicate-first with three ratcheted meters (294 role gaps, 3,367 inherited packs, 233 unendorsed). See `docs/reports/wp-4.6-missing-proportion-packs.md`.
- **Partis to full coverage (WP-4.5).** 12 → 21 partis; 129 of 132 styles native, 0 uncovered; `NATIVITY_W` raised 6 → 20 so a borrowed diagram no longer routinely outranks a native one; check 10 composes every parti against its own first native style, differentially against a control diagram. See `docs/reports/wp-4.5-partis-to-full-coverage.md`.
- **The garage (WP-4.3).** `garage-and-hyphen` as the one attachment mechanism, the dependency ridge at 70%, and the two garage variables the constraint vocabulary had grown for it. See `docs/reports/wp-4.3-the-garage.md`.

## Unreleased (this session, 23 Aug 2026 onward — WP-0.1 through in-progress work)

- **Consolidation (WP-0.1).** v0.6 extracted onto disk as the working tree; `Plan Examples/` (26 reference images) folded in; git initialized; stale duplicates and superseded generations identified and moved off the tracked tree (see `docs/reports/wp-0.1-repo-consolidation.md`).
- **Documentation (WP-0.2).** `README.md` counts corrected and made generatable (`build/gen_readme_counts.py`); `mcp_server/README.md` regrouped to list all 23 tools by layer; `docs/open-questions.md` items 1, 2, 12, 13, 17, 23, 24, 25 resolved and recorded; `docs/README.md` index added.
- **Behaviour tests (WP-0.3).** `tests/` — 35 pytest tests pinning validator, composer, kit cascade, proportion engine, and geometry behaviour against the docs' own claims (two counts corrected where they'd drifted); `build/check_all.py` / `make check` as a single entry point over all data checkers plus pytest. OQ 26 raised (undocumented `EQUIVALENT` room-alias groups).
- **Constraint rule language (WP-1.1).** `schema/constraint.schema.json` (reuses the fault corpus's `test` pattern per OQ 24, extended with `one-of` and `scope`); `build/constraint_vocabulary.py` (68 named variables); `build/check_constraints.py` (new 14th check, wired into `check_all.py`); worked-example migration of 140 constraints across the `english-classical` and `american-colonial` families (106 tested, 34 honestly `scope: judgment`); `docs/constraints.md`. Remaining ~520 constraints across ~25 families not yet migrated. OQ 27 raised (tagging judgment-scope constraints blocked by OQ 22 specifically).
- **Correction to WP-0.2's own record.** `docs/open-questions.md` items 12, 13, and 17 falsely claimed "Executed in WP-1.3" — WP-1.3 had not run. Found and corrected in place (with dated correction notes) while migrating constraints; `docs/reports/wp-0.2-documentation-reconciliation.md` carries a matching correction. See `docs/reports/wp-1.1-constraint-rule-language.md`, "what was found."
- **Kit schema operators (WP-1.3).** Ontology bumped 0.4.0 → 0.5.0 (93 → 95 slots): `wall_thickness_masonry`/`wall_thickness_frame` (OQ 12, the fourth and last trade split), `derives_from_module` cross-references on `cornice`/`frieze`/`modillion_dentil`/`crown`/`chair_rail` (OQ 13). `build/resolve_kit.py`'s `rule_append` merge operator now actually resolves (OQ 16); a new `docs/inheritance.md` section on regional-conditionality mechanism choice, applied to reconcile `service_zone_strategy`'s Tidewater duplication (OQ 17); `--date YEAR` date-conditional variant resolution (OQ 22). `docs/inheritance.md` also had independent stale prose (slot counts, a Constraints section describing a rule language that didn't exist yet) corrected in passing. See `docs/reports/wp-1.3-kit-schema-operators.md`.
- **Validator reads constraints (WP-1.2, closing Phase 1).** `build/plan_check.py`'s STYLE LAYER now evaluates a migrated constraint's `test` object via `mcp_server/core.py`'s `_eval_test` (extended with `one-of` direction support) against `plan.measurements` plus five values `derive_constraint_vars` reads off unambiguous plan structure — a pass is silent, a fail is a finding at a severity `CONSTRAINT_SEV` maps from the constraint's hard/soft/advisory, a constraint with a missing variable is an honest `info` finding, never silently passed. `result["constraint_summary"]` reports present/clear/unjudged. Both shipped example plans' pinned fatal/serious/minor counts are unchanged; the deliberately-wrong-Tidewater-pitch case PLAN-OF-ACTION.md's own acceptance criterion names is pinned directly. `mcp_server/core.py` gained `check_style_constraints` and `measurement_vocabulary` now folds in constraint-sourced variables (23 → 24 MCP tools, new `tdl_check_style_constraints`); `compose.py`'s scoring already covered a failed hard constraint with no code change (verified, pinned). 13 new tests. See `docs/reports/wp-1.2-validator-reads-constraints.md`.
- **Reference corpus (WP-2.1, opening Phase 2).** 14 plan records under `plans/reference/` transcribed from `Plan Examples/` (all 8 Bad Examples files, as 7 records — two files are one two-level house; 7 of 20 Good Examples). Two room-catalogue gaps found and evidenced with pinned tests: `gallery-corridor` is missing from `plan_check.py`'s `EQUIVALENT` entrance-hall alias group, and `primary-bedroom`'s must-adjoin rule for `primary-bathroom` has no `via` allowance for a dressing room — both recur independently across different source drawings. ROOM and FURNITURE layer findings skew markedly toward the bad examples (45 vs 6, 63 vs 23 fatal+serious); ADJACENCY does not, mostly because of the two gaps above plus this transcription's own porch-typing and window-survey limitations, discussed honestly rather than read as a validator weakness. No validator or catalogue file edited — proposals only. See `docs/reports/wp-2.1-reference-corpus.md`.
- **Site layer (WP-2.4).** `schema/plan.schema.json` and `schema/brief.schema.json` gained a `site` object (street bearing, three setbacks, slope direction, cross-slope, prevailing summer wind, piazza bearing, party-wall condition) additive to the pre-existing `context.lot_width_ft`/`lot_depth_ft`/`entrance_faces`, which stayed put rather than move and break the reference corpus. `build/compose.py`'s `footprint()` now caps a candidate's bay count to what the stated lot can hold and drops (not merely outscores) a candidate that cannot reach even its own minimum bay count, recorded in a new `dropped_lot_infeasible` list; `build/geometry.py`'s independent bay-grid solver needed the identical fix a second time, discovered by an end-to-end render smoke test that showed a "24 ft" candidate solved 50 ft wide, straddling its own lot line — both are pinned against a 30 ft-usable-lot brief that reproduces PLAN-OF-ACTION.md's own worked example (a 24 ft town-house parti, not a five-bay Georgian). `build/plan_check.py`'s `derive_constraint_vars` now reads all of `plan.site` into the constraint namespace, which is what actually made the two already-migrated site-scope constraints (the Charleston piazza bearing, the Pennsylvania bank house's cross-slope) evaluable for the first time — the WP-1.2 mechanism was already generic enough, it just had nowhere to read site data from. `build/render_plan.py` draws the lot boundary, a setback envelope, and a north arrow with the street bearing when a plan states one, unchanged when it doesn't. Kit-slot consumption: 5 of the 7 site-and-settlement slots were already flowing through the pre-existing `canonical_choices()`; the other two (`orientation_rule`'s editorial prose, `setback_rule`'s deliberate `open`) gained a new `site_kit_log()` so they reach the decision log instead of vanishing. 29 new tests. See `docs/site.md`.
- **Compositional constraints in the geometry solver (WP-2.2).** `build/geometry.py`'s `solve()` reads `entrance_faces` from the plan's `context` and adds four scoring terms to the candidate search: `entrance_score` (the entry-porch and its hall must reach the entrance wall — weighted at fatal scale, 100, fixing the specific "portico inside the footprint" defect PLAN-OF-ACTION.md names), `principal_and_service_score` (soft preference for principal rooms toward the front and service rooms toward the rear), `ceremonial_score` (a door-connected hop to higher privacy rank must go spatially deeper into the plan, not just be door-connected), and `centre_hall_symmetry_score` (mirror-tolerance scoring gated on the existing spanning-circulation-room test). `composition_parti` is read from the resolved kit per the brief's task list, though no style's kit specifies it yet, so nothing branches on it. Verified against `plans/tidewater-georgian-careful.json` (PLAN-OF-ACTION.md's own acceptance example): entry porch on the entrance wall, service rooms toward the rear, relaxation count unchanged at 11. The acceptance example's third clause ("drawing room and dining room flank the passage on the front") does not hold on this plan and was not forced to — `dining`'s own record declares `exterior_walls: ["N", "W"]`, real hand-authored intent a soft compositional preference should not override; pinned as a documented finding, not silently missed. `build/render_plan.py` now renders door swing arcs. 22 new tests. See `docs/geometry.md`, "Compositional scoring (WP-2.2)."

- **Walls, structure, storeys (WP-3.1, opening Phase 3).** A new `construction/` catalog: `wall-assemblies.json` (ten construction types — masonry two/three-wythe, veneer-over-frame, braced timber frame, balloon/platform frame, log, adobe, rammed earth, SIP — each with exterior/bearing-interior/partition thickness ranges and a bearing role) and `floor-structure.json` (light-frame joist span table, explicitly scoped to styles outside `timber-bay.json`'s applies_to list). A new `build/structure.py` derives, without rewriting a room's clear dimensions: wall thickness from the plan's declared `construction_type` (defaulting to platform-frame with an explicit note when undeclared); an outside-to-outside footprint; every wall line, tagged bearing (exterior, or on the bay grid) or partition; bay-to-bay span checking against a joist member or the bay module's own 20 ft hand-timber capacity; storey heights inverted from `storey-graduation.json`'s own ceiling formula; grade-to-eave/ridge heights against the style's migrated roof-pitch constraint; and stair rise/run/riser-count/landing checked against `storey-graduation.json`'s stair rule and `stair-and-landing-core.json`'s hard landing rule, using the stair-hall's own solved width. A new `build/render_section.py` renders a vertical section and a bearing-line plan diagram, both drawn only from the section record. Three real bugs were found by actually running the module for the first time against the reference corpus rather than by inspection: the four exterior walls had their axis/position swapped in `wall_lines()` (planting a wall's own position into the wrong axis's span break-points and fabricating an out-of-bounds span); `span_check()`'s hand-timber-vs-light-frame decision checked `construction_type` against `timber-bay.json`'s applies_to list, which holds style ids and can never match, silently routing every plan through the light-frame joist table regardless of style; and `graduation_check()`'s third/second-storey band was hand-transcribed as the pack's separate ground-under-piano-nobile band (0.60–0.85) instead of the actual third/second band (0.70–0.88) — fixed by reading the bands from the pack directly rather than re-transcribing them. A fourth item is a genuine tension recorded rather than resolved: the style-based `timber_framed` decision is correct for the acceptance-example plan but produces a real, evidenced disagreement on `spec-builder-colonial.json` (a `colonial-revival`, undeclared-construction plan whose 23.39 ft bay fails the hand-timber cap but would pass cleanly against an engineered joist) — `build_section()` now surfaces a `framing_basis` note naming the tension rather than picking a reading silently. Verified against `plans/tidewater-georgian-careful.json`: outside-to-outside footprint 62.58×42.66 ft from a 60×40.08 ft clear footprint; 29/22 wall lines (9/12 bearing) across the two levels; grade-to-eave 25.44 ft, grade-to-ridge 39.66 ft at 8:12, the midpoint of `tidewater-georgian.c02`'s own 7:12–9:12 constraint; zero spans exceed capacity (a genuine checked pass, not an absence of checking) — **that clause is false as of WP-7.4, 27 Aug 2026, and the reason is worth carrying: `span_check` was never reading the `bearing` flag `bearing_lines` computes, so every partition counted as a support. With partitions removed the plan carries two over-capacity spans on its upper level (40.0 ft and 30.0 ft against a 20 ft hand-framed capacity), and the bearing counts below read 20/8 rather than 9/12. The pass was an artefact of the bug, not a property of the plan**; a real, separately-recorded finding that the stair-hall's own solved geometry cannot fit a single straight flight for the storey's full rise without a shorter-than-required landing. 38 new tests (`tests/test_structure.py`); `build/structure.py` and both reference plans wired into `check_all.py`. See `docs/structure.md`.
- **Roof geometry (WP-3.3).** A new `build/roof.py` derives roof FORM — gable, hip, gambrel, cross-gable — as real plan-view outline geometry and a per-face elevation silhouette, building on top of `structure.py`'s own grade-to-eave/ridge numbers rather than re-deriving them (gable and hip forms share the same ridge height as `structure.py`'s single-ridge simplification; gambrel genuinely differs and is recomputed independently, with the divergence stated explicitly rather than swapped in silently). Covers: roof form resolution (`plan.declared.roof_form`, falling back to the massing's own `roof_default` list with a note); hip-line and cross-gable geometry; gambrel two-slope geometry sourced from a style's own migrated `roof_slope_lower_deg` constraint where one exists, else a family-wide default corroborated by two independently-authored style statements agreeing on the same figures (60–72°/18–30°/55–70% of the half-span); the dependency-and-hyphen wing ridge step-down rule, read and checked against that grouping's own 0.6–0.8 band (explicitly schematic — this corpus's geometry solver has never placed a real second volume, so wing depth is assumed rather than measured, disclosed directly in the record); chimney placement (kit canonical variant, falling back to the massing's own `hearth` field) checked against the style's `chimney_height_above_ridge_ft` structural constraint; three photograph-corpus checks read from the fault files' own bands rather than hand-transcribed (the Cape eave-to-first-floor relation, the gambrel-slope-convergence break, dormer rhythm against the bay grid — the last honestly not-applicable on both shipped plans, since no schema field yet authors dormer position). A new `build/render_roof.py` draws a plan-view roof SVG. Three bugs were caught by direct manual smoke-testing before the test file was written (matching WP-3.1's discipline): `wing_step_down()` read a grouping's reader-facing `statement` prose instead of its separate, regex-matchable `test` field; `chimney_positions()` collapsed three distinct "can't place a chimney" causes into one message, which printed "no gable-end indicated" for `spec-builder-colonial.json` even though its massing genuinely calls for gable-end chimneys — the real cause was an unjudged ridge height, now reported accurately; and dead debug code (a vestigial `if False` ternary) was found and removed from `_cross_gable()`. Verified against both shipped plans: `tidewater-georgian-careful.json` — side-gable, 8:12 pitch, ridge 39.66 ft, two gable-end chimneys at 47.66 ft total height, checked OK against `tidewater-georgian.c01`; `spec-builder-colonial.json` — side-gable, ridge honestly unjudged (`colonial-revival` carries no migrated roof-pitch constraint, a pre-existing corpus gap not introduced here), chimneys correctly identified as gable-end-called-for via the massing-hearth fallback but correctly declined rather than placed at a fabricated height. Hip, gambrel, and cross-gable paths verified against synthetic scenarios, since neither shipped plan declares one. 47 new tests (`tests/test_roof.py`); `build/roof.py` and both reference plans wired into `check_all.py`. See `docs/structure.md`, "Roof geometry (WP-3.3)," and `docs/reports/wp-3.3-roof-geometry.md`.
- **Elevation generator (WP-3.2, closing Phase 3).** A new `build/elevation.py` derives, per face, a bay layout (facade-classical's own odd bay-count formula spaced evenly across the real outside width), window sizing at one head datum per storey (opening-proportion's own corollary applied literally: fix the head from the ceiling rule, fix the sill at the pack's own 28-32 in convention, derive height then width — not the reverse), sash light counts and glass module at the plan's own declared date, an entrance composition dimensioned with the Gibbs Ionic order pack read at door scale (tidewater-georgian's own kit leaves every classical-apparatus slot open; gibbs-ionic is the family's own named default, used as a sourced default rather than invented), the eave cornice as "the style's entablature reduction" (Gibbs's own cornice members regenerated at whatever module sums to facade-classical's domestic envelope, sized to the plan's own real storey height), and water table/belt from brick-course.json where masonry, else facade-classical's generic figures. Roof outline, silhouette and chimneys are read from `roof.py`'s own record, not recomputed. A new `build/render_elevation.py` draws a front-on elevation plus a cornice-profile detail inset; `seg_to()` is a direct port of `orders_template.html`'s own moulding-profile-to-SVG-path function, `profile_silhouette_path()` the same stepped-member walk that page's `buildGeometry()`/`silhouettePath()` do for a column, simplified for a flat entablature run. This file's `measurements` feed a new ELEVATION LAYER in `build/plan_check.py`'s existing FAULT LAYER (plan-declared values still win). Two real bugs found by running the module: window sizing driven width-first put the sill 55 in off the floor (fixed by fixing head-then-sill first, per opening-proportion's own corollary); the eave cornice used a stock 9 ft module regardless of the plan's real storey height, undersizing it (fixed by reading facade-classical's own storey-equals-module convention literally). Three genuine authoring gaps found in the fault corpus itself (conditional secondary tests evaluated unconditionally) were disclosed and worked around locally rather than by editing shared files. A scope bug — this generator's Palladian proportion system was being applied unconditionally to every style, including ones its own source packs were never scoped for — was found because it changed which candidate `build/compose.py` recommends for a `bungalow-small` brief (a spurious classical-cornice fatal on a Craftsman candidate); fixed with a direct `applies_to` membership gate (not the fault corpus's own broader `member_of`/lineage cascade, which reaches the classical family through real but irrelevant historical-influence edges). A second, legitimate ranking change survived scrutiny: `family-georgian`'s own single-pile Centre Passage candidate genuinely produces a 9-bay "institutional, not domestic" front and a flattened-truss roof profile at that brief's scale — both real, previously-invisible findings, not a defect — so `tests/test_composer.py`'s pinned expectation was updated to the new, correct top candidate rather than the elevation layer weakened to preserve the old one; `tests/test_plan_validator.py`'s pinned counts were likewise updated, including a fourth genuine fatal newly evaluable on `spec-builder-colonial.json` (a declared window width with no declared height, now caught). Verified against both shipped plans: 5-bay Tidewater front with a centred door, single head datum per storey, sash lights matching the plan's declared 1765 date, the deliberate half-width shutter on Spec Builder Colonial still caught under real `setdefault` precedence; the elevation layer evaluates 83 of 177 tidewater-georgian-applicable photograph-measurable faults with zero fatal (short of the acceptance text's named 100, disclosed rather than closed by fabricating data for faults this generator has no model for — interior trim, porch/portico members, photograph-texture statistics). 39 new tests (`tests/test_elevation.py`); `build/elevation.py` and both reference plans wired into `check_all.py`. See `docs/elevation.md` and `docs/reports/wp-3.2-elevation-generator.md`.

- **A real solver (WP-2.3, closing Phase 2).** A new `build/solver.py` states room placement to CP-SAT over the same bay grid `build/geometry.py` slices, keeping the heuristic as hint, cross-check and reported fallback. Two tiers, because there are two questions: *does this brief fit* (room minimums only, no objective, and CP-SAT's assumption core names which rooms conflict when it does not) and *where do the walls go* (up to six slicing topologies, cuts placed to proven optimality, best by `geometry.py`'s own scoring). An infeasible brief now returns a deletion-minimised, named conflict set and writes no geometry — *"a 2-bay, 20 x 25.96 ft single pile house cannot hold the dining-room, drawing-room and library at their stated minimums at once, and the lot allows no more bays after its side setbacks"* — where the search previously returned its least-bad plan. Where a drop could not be decided in time it is kept and the set is reported minimal *up to the time budget*, never claiming a minimality it did not prove. Scored by `geometry.py`'s own functions, CP beats the best of 800 heuristic candidates on both shipped plans and both briefs — 540.4 → 469.8, 509.2 → 433.8, 502.5 → 383.7, 350.7 → 316.8 — inside 60 s every time, with the Tidewater plan's transfer beams falling from 16 to 7. `geometry.py` gained `prepare_rooms()`/`derive_footprint()`/`write_record()`, extracted verbatim so both engines size the footprint by one rule and emit one record shape; its numeric behaviour is unchanged, which the pinned relaxation count of 11 proves. `--solver cp|heuristic|both` on both CLIs and a `solver=` argument on the MCP server's `place_plan`; the default everywhere stays the heuristic.
  **The formulation the plan of action named had to be replaced on evidence.** "Integer room rectangles on bay multiples, no-overlap" — loose rectangles whose areas sum to the footprint — needs `sum(w*h) == W*H` over a dozen nonlinear products, and CP-SAT could not decide that on `spec-builder-colonial.json` **in 240 s with four workers while holding a hint that was itself a valid tiling**: not solved badly, unable to tell whether a solution existed with one in hand. `guillotine_tree()` reads the slicing tree back off a heuristic layout instead (this corpus's slicer produces guillotine layouts by construction, so nothing in it had to change), which makes tiling structural rather than arithmetic and leaves only the cut positions — linear, and optimal in under a second. The heuristic proposes the topology; the solver proves the geometry. Optimality is therefore per topology rather than global, and the record says so rather than implying an optimum never established.
  Three further findings. `build/geometry.py` silently places the spec Colonial's dining room at 91 sf against its band's 122 sf floor — 26% short, on every one of six seeds — because `level_score` charges a flat 12 points and a candidate can win while paying it; the plan record still reads 12 x 12 and `plan_check.py` never reads `room.geometry`, so no layer of the critic is looking at the number that changed (OQ 54 raised; nothing changed on it, and the heuristic remains the default engine). A plan's `exterior_walls` are aspirations rather than rectangle edges and cannot all be asserted at once — `passage`, `backhall` and `kitchen` each declare *opposite* walls, so each must span the full depth, while `porch` declares both E and W — so they stay weights at the 14 points `exterior_score` already charged, and room minimums, the entrance front and the spanning passage are the constraints. And `build/check_all.py` was running the suite as the bare `pytest` on PATH while every checker beside it ran under `sys.executable`: in this environment those are different interpreters, all sixteen OR-Tools-dependent tests skipped, and a skipped test reports success. Fixed to `sys.executable -m pytest`. Also: the footprint's depth is now bounded by the massing's own pile (without it a lot-capped footprint grows deeper without limit and the overstuffed probe came back a "feasible" 20 x 75 ft single-pile house), and that bound is what makes an infeasible brief reachable at all. `requirements.txt` added — the first the repo has had; the data and every checker still run on the standard library alone. 26 new tests (`tests/test_solver.py`). See `docs/geometry.md`, "The solver (WP-2.3)," and `docs/reports/wp-2.3-the-real-solver.md`.
- **Constraint migration completed, closing Phase 1 (WP-1.1 continued).** The remaining 25 families (520 constraints across 104 nodes) migrated in 8 parallel batches by `PLAN-OF-ACTION.md`'s own per-family hand-off brief, sized by constraint count rather than a fixed family count. Corpus-wide: **660 of 660 constraints migrated** (up from the worked example's 140), 365 tested / 295 honestly `scope: judgment` — a markedly higher judgment fraction (45%) than the worked example's (24%), a real property of which families came later (Beaux-Arts/French Baroque civic-massing rules, Tudor-Jacobean/Renaissance-classical co-occurrence rules, and two Queen Anne subtypes defined almost entirely by millwork dimensions the vocabulary had no names for). `build/constraint_vocabulary.py` grew from 72 to 87 entries — 15 additions, each requested independently by at least two batches on unrelated style nodes (classical order proportion, a generic clear span, an arch's rise:span ratio, wall material count, ornament coverage percentage, gable count, floor area, a log wall's unsupported run, chimney width, same-storey window-head alignment, courtyard width, and the two garage variables WP-4.3 will need). A reconciliation pass then converted 29 of the newly-named-but-still-judgment constraints to tested using the batches' own quoted numbers, taking hard-constraint coverage from 56.0% to **61.5%** — clearing `PLAN-OF-ACTION.md`'s own ≥60% acceptance bar (the corpus was at 17.7% before this session). Re-verified with zero regressions: both shipped plans' pinned fatal/serious/minor counts unchanged, both pinned composer briefs' rankings unchanged; one stale pinned test (`tests/test_constraints.py`, still asserting the worked-example-only totals) updated to match. A genuine performance finding, not a language defect: `plan_check.py`'s `_load()` helper re-imports `mcp_server/core.py` fresh per `check()` call, defeating `core._data()`'s own cache — a pre-existing pattern the fuller constraint payload turned from unnoticed into measurable (compose() now 30-40s/brief; the full pytest suite no longer fits one 10-minute run), diagnosed and recorded as OQ 28 rather than fixed. OQ 27 (tagging date-blocked judgment constraints) still not decided; the un-tagged pool is now 295 constraints rather than 34. See `docs/constraints.md` and `docs/reports/wp-1.1-remaining-families-migration.md`.

## Unreleased (Phase 4 — breadth)

- **All 132 style/variant kits filled, closing WP-4.2 (wave B).** The remaining 129 of 132 style/variant kits (3 were already-populated exemplars) now carry real, sourced content — **all 159 kit files in the corpus, every family/style/variant, have at least one non-`open` slot**, up from 30 before this pass. Corpus-wide bindings rose from 256 `specified`/18 `extends`/75 `forbidden` to 1,556/330/223. Run as two sub-waves of parallel batches (13 total, 72 then 57 nodes) mirroring WP-1.1/WP-4.1's family-grouped batching; every batch ran `resolve_kit.py --verbose` before writing anything, to add only what the family-plus-lineage cascade didn't already resolve, sourced from each node's own text rather than family summary prose (both waves caught the family blurb overclaiming a trait a member's own text contradicts). A spot-check on `appalachian-log-house` (a variant — the deepest cascade kind) shows a 34-node chain in which every single ancestor now resolves real content, the acceptance criterion PLAN-OF-ACTION.md names directly. OQ 20's fuller question — how many merge problems appear once real content exists at every cascade level, not just one worked chain — is now answered with data: ~26 real problems across 129 nodes (roughly one in five), all of one of three kinds, none a mechanism failure: (1) deep classical content (`antique-classical`'s strict-bilateral symmetry, `renaissance-classical`'s harmonic-mean ceiling rule) leaking into 15 non-classical nodes across five families via a real-but-narrow lineage edge that, once populated, carries the donor's *entire* binding rather than the one trait the edge exists for; (2) `hybridizes_with` edges built to carry one narrow aspect of a donor's practice instead transmitting the donor's whole kit (9+ nodes across three families) — documented as a real, general mechanism limitation for a future pass, not fixed here; (3) one genuine cross-batch collision (`french-eclectic`'s `garage_strategy`, authored against `tudor-revival` in sub-wave 1, broken when sub-wave 2 concurrently gave a nearer ancestor its own binding) — caught by `check_kits.py`'s dangling-diff check, fixed by converting to a full override, and the reason every batch's self-reported "0 errors" was re-verified against a corpus-wide run rather than trusted individually. `test_ontology.py`'s `wall_thickness_masonry`/`wall_thickness_frame` test, which asserted these WP-1.3 slots stay `open`/`empty` on every kit forever, was narrowed (with a dated docstring) to what still holds — the slots exist with the right group — since kit-fill work is now intentionally specifying them with real content (e.g. `colonial-iberian-americas.wall_thickness_masonry`, sourced from its own members' measured ranges). Re-verified corpus-wide: `check_kits.py`/`validate.py` 0 errors, full 291-test pytest suite green. See `docs/reports/wp-4.2-wave-b-kit-fill.md`.

- **Family-level kits and the family cascade mechanism (WP-4.2, wave A).** 27 new `kits/<family-id>.kit.json` files — the first content at this layer ever authored — plus the mechanism that makes them real cascade ancestors: `build/build.py` now generates a kit skeleton for `rank: family` nodes (159 kit files total, up from 132), and its cascade computation splices a style-rank ancestor's own family in immediately after that ancestor, walking `member_of` (organisational placement) as a second, independent axis from `lineage` (real descent) — previously family nodes, which carry no lineage edges at all, could never appear in any `_cascade`, so a style could not "extend against the family" no matter how real the sharing (OQ 20's unbuilt three-level test). `check_kits.py` needed no changes — it already validates any `kits/*.kit.json` file generically. Verified additive and zero-regression: every slot value the three pre-existing populated kits resolved before this change is identical after it, diffed directly. Every family kit is deliberately light rather than deep (the Georgian exemplar was authored before family kits existed and had to specify everything itself) — several batches explicitly found and preserved real cross-member disagreement rather than forcing false commonality (`american-colonial` 4/95, `eclectic-revivals` 1/95: "if a style's provenance shows 0% from the family, fix the family — forcing more here would be exactly that failure mode"), and two caught the family's own summary prose overclaiming a trait one member's own text directly contradicts. The mechanism alone, before any style/variant kit is touched, already resolves 91 of 95 slots for a previously-empty style (`craftsman`) through cascade. `tests/test_kit_cascade.py` gained a `TestFamilyCascade` class; one pre-existing chain-order test updated with a dated docstring for the new four-level chain. See `docs/reports/wp-4.2-family-kits.md` and `docs/inheritance.md`'s rewritten "Resolution order."

- **Proportion-pack bindings for all buildable styles (WP-4.1, opens Phase 4).** Every `rank: style`/`rank: variant` node (132 total) now carries a `proportion_packs` binding except 3 deliberately left unbound — **129 of 132 bound**, in two waves of 15 parallel batches (127 nodes bound this pass; 5 already carried the Georgian-family template binding). Each entry follows `georgian-colonial-american.json`'s own template (`pack`, `role`, `precedence`, an honestly-reasoned `note`, optional `authority`), and every binding placed on a node outside that pack's own `applies_to` list is flagged in the batch's report as a judgment binding with its reasoning stated — never silent. A new `build/check_pack_bindings.py` validates every binding (unknown pack ids, precedence as a total order, the `trim-classical`/`trim-craftsman` mutual exclusion) and is wired into `check_all.py` with `--strict`, now the default check. The 3 unbound nodes — `egyptian-revival` (trabeated/no-arch construction, no order in the library shares its logic), `moorish-andalusian`, `mudejar` (Islamic geometric setting-out, no order or module in the library reaches it) — are named explicitly in a `DELIBERATELY_UNBOUND` allowlist so `--strict` still catches a real future regression while tolerating these three checked, permanent exceptions; `PLAN-OF-ACTION.md`'s own task text ("do not bind the wrong pack") was honored over its literal 132-of-132 acceptance line, and that tension is recorded rather than gamed (OQ 29). A consolidated, deduplicated candidate list of ~25 missing-pack gaps across all 15 batches — most corroborated: an Islamic/Moorish arch-and-ornament system, independently named by three unrelated batches (OQ 30) — feeds the future WP-4.6. Re-verified with zero regressions: `check_pack_bindings.py --strict` and `validate.py` clean corpus-wide, full pytest suite 287/287 (chunked per OQ 28), both shipped plans and the pinned composer brief unchanged. See `docs/reports/wp-4.1-proportion-pack-bindings.md`.

## v0.6 — 19 August 2026

Rooms (58), groupings (16), partis (12, 39 styles native), fault corpus (209 faults, 846 exceptions), plan schema and validator (`plan_check.py`, two worked examples), composer (`compose.py`, four-candidate ranking), geometry solver (`geometry.py`, `render_plan.py`), MCP server (23 tools). Kit schema bumped to 0.2.1 during the Georgian authoring exercise; element ontology to 0.4.0 (93 slots, up from 82).

## v0.5 and earlier — through 18 August 2026

Style graph (164 taxa, four ranks, 476 lineage edges), element ontology (82 slots at this point), massing catalog (40 types), proportion engine (36 packs across Vignola/Palladio/Gibbs/Chambers/Benjamin plus non-classical modules), kit mechanism proved on Georgian Colonial / Tidewater Georgian / English Georgian, image manifest (322 records, format-agnostic, authored before files).

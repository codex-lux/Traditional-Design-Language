# Open questions

Decisions deferred rather than made quietly. Two of these came from authoring the Georgian kit end to end, which was the point of doing it.

Status legend: **OPEN** — awaiting a ruling. **RESOLVED** — ruled, with the ruling recorded inline and a pointer to where it took effect.

## From the taxonomy

1. **RESOLVED — Should the 1877 sketching-tour styles cascade?** Colonial Revival is typed `references` toward Georgian because its transmission is photography and measured drawings. Shingle Style and Queen Anne Free Classic reach early New England building by exactly the same mechanism — the McKim, Mead, White and Bigelow tour of Marblehead, Salem, Newburyport and Portsmouth — and are typed `descends_from`, so they inherit seventeenth-century assembly. Those offices also employed carpenters who still knew the work, which is the counter-argument. **Ruling: apply the `references` reading to all three** — transmission by photography and measured drawing, however skilled the office, is not the same mechanism as a trade lineage, and the model should not blur that distinction for three styles just because the drawings were good. Reflected in `styles/colonial-revival.json`, `styles/shingle-style.json`, `styles/queen-anne-free-classic.json`.

2. **RESOLVED — Is `charleston-single-house` a style or a plan type?** It is a variant of Federal, its origin precedes Federal by sixty years, its floruit runs forty years past Federal's decline, and there is already a `charleston-single` massing. **Ruling: style, under `american-folk-vernacular`** (folk-vernacular family) rather than a Federal variant or a massing-only concept — the piazza orientation constraint, the single-room-deep plan, and the party-wall condition are style facts, not merely volumetric ones, and a massing alone cannot carry `piazza_bearing_deg` as an enforceable constraint. Reflected in `styles/charleston-single-house.json` (`member_of`/`rank`).

3. **LEFT AS A STANDING DISCLOSURE.** Ruled 24 Aug 2026: this is an honest recorded uncertainty about history rather than a defect in the model, and it is already noted on the node. Revisit if WP-4.7's non-Western trunks are ever built. *Original entry follows.*<br><br>**OPEN — `log-vernacular-american` descends from `german-fachwerk` as a proxy** for Germanic *Blockbau*. A chinked hewn-log Appalachian house currently inherits half-timber-with-infill. A `germanic-blockbau` node fixes it.

4. **LEFT AS A STANDING DISCLOSURE.** Ruled 24 Aug 2026: recorded rather than forced. The inconsistency is real but harmless, and typing it uniformly would assert a certainty about transmission the sources do not support. *Original entry follows.*<br><br>**OPEN — Vredeman de Vries's Antwerp engravings are typed both ways** — transmission in `elizabethan` and `jacobean`, quotation in `flemish-vernacular` and `dutch-urban-gable-house`. The model puts pattern books firmly on the transmission side.

5. **LEFT AS A STANDING DISCLOSURE.** Ruled 24 Aug 2026: the gap is honest and is noted on both nodes. WP-4.7 is the package that would give these two real ancestors to point at; until then the graph should say it cannot rather than point at a proxy. *Original entry follows.*<br><br>**OPEN — `shotgun-house` and `cape-dutch` have real ancestors the graph cannot point at** — Yoruba by way of Saint-Domingue, and Cape/Indonesian. Both gaps are recorded on the nodes. A sixth trunk fixes both and the schema extends to it unchanged. Scoping note for a non-Western trunk more broadly: WP-4.7 (not yet run).

6. **LEFT AS A STANDING DISCLOSURE.** Ruled 24 Aug 2026: noted, not fixed. A container whose date span is exceeded by a child it holds is a real modelling wrinkle and not one that misleads anybody reading the node. *Original entry follows.*<br><br>**OPEN — `mid-century-traditional` spans 1930–1975** and holds Neo-eclectic, which runs to 2000.

## From the proportion layer

**7 through 11 are ENVIRONMENT-BLOCKED, not unstarted (recorded 25 Aug 2026).** Every one of them is
the same shape: the text does not give the figure, or the plate numerals are illegible, and closing
it needs a better facsimile rather than more code or more thought. They are therefore blocked on
exactly what WP-4.4 is blocked on. Probed on 25 Aug 2026 from this environment:

```
https://www.loc.gov            -> connection failed (000)
https://archive.org            -> connection failed (000)
https://babel.hathitrust.org   -> connection failed (000)
```

Not a 403 from the proxy as WP-4.4 gets, but a failure to connect at all. Until outbound HTTPS to a
facsimile host is available these five cannot move, and the correct handling of each is what has
already been done: the figure is carried as a judgment rule with a range, or inherited from a source
that does state it, and marked unverified rather than assumed. **Do not close any of them from a
secondary source or a modern redrawing** -- that is precisely how a guess gets laundered as
`measured`, which this corpus forbids. Each entry below says what specifically to look for, so a
person with library access can close them in an afternoon.

7. **OPEN — Palladio's Tuscan entablature is not dimensioned in the text** and the plate numerals are illegible in every reachable scan. Carried as a judgment rule with a range rather than invented members.

8. **OPEN — Chambers's individual mouldings for Ionic, Corinthian and Composite** are engraved minute figures that every OCR renders as noise. They inherit from Vignola rather than being guessed. First thing to correct against a good facsimile.

9. **OPEN — The Composite architrave and cornice corona** in Vignola are the weakest data in the order corpus — three transcriptions all say only "as the Corinthian, fewer and larger details, no brackets." Encoded as two fascias on that authority, with a three-fascia reading recorded as defensible.

10. **OPEN — Benjamin's 1806 first edition** could not be reached in a legible scan. The packs encode the 1816 third edition and the 1830 *Practical House Carpenter*, and whether the 1806 column heights match is marked unverified rather than assumed.

11. **OPEN — Palladio and Chambers overlays exist for all five orders; Gibbs and Benjamin do not cover every order** — Benjamin's Doric is Greek and is encoded standalone rather than as a delta, because encoding it as deltas on Vignola would be fiction.

## From authoring the Georgian kit

12. **CLOSED 24 Aug 2026 — all four splits exist; the ruling's naming was amended to the corpus, not the corpus to the naming.** `window_head`, `corner_treatment` and `window_surround` were split by trade in ontology 0.3.0 (pre-session, Georgian-kit authoring); WP-1.3 added the fourth, `wall_thickness_masonry` / `wall_thickness_frame`, and `elements/slots.json` now stands at 0.5.0 with all eight ids live and none deprecated. The ids read `window_head_masonry`/`window_head_wood`, `corner_quoin`/`corner_board`, `window_surround_masonry`/`window_surround_wood` — not the `_carpentry` naming this ruling wrote. **Deliberately not renamed.** The ruling names trades; the ids name materials, which is what every downstream kit binding, constraint and fault already says. Renaming eight live ids to match the wording of the question that asked for them would spend a migration and eight `deprecated_in_favour_of` records on nothing a reader would notice. The mapping is recorded here instead. `window_surround` was split in two rather than kept single with an added `none` value; the design intent survives by a different mechanism — `window_surround_masonry` carries `none-opening-cut-in-the-mass` as an example value, so masonry's correct answer is still a stated none and not an absence. Pinned by `tests/test_ontology.py`. *Original entry follows.*<br><br>**`window_head` wants splitting.** A gauged brick jack arch is made by the mason, is structural, is cambered by opening/96, is four courses deep, and is laid on a centre before the wall closes. A moulded wood cap is made by the carpenter, is non-structural, and is nailed on afterwards. They share nothing but the opening. The same disease affects `wall_thickness_expression`, `window_surround` (whose correct masonry value is `none`, which is an answer and not an absence), and `corner_treatment` (which conflates the quoin, an order element, with the corner board, a trim element — and that conflation is why `facade-classical` was applying an 18-inch masonry rule to a 6-inch framed condition). **Ruling: split all four by trade.** `window_head` → `window_head_masonry` (jack arch / lintel, structural) + `window_head_carpentry` (moulded cap, non-structural); `corner_treatment` → `corner_treatment_masonry` (quoin, an order element) + `corner_treatment_carpentry` (corner board, a trim element); `window_surround` keeps its id and gains `none` as a legitimate value for bare masonry openings; `wall_thickness_expression` splits by construction trade.
    **Correction, 23 Aug 2026:** this entry previously (and wrongly) said "Executed in WP-1.3." WP-1.3 has not run. Checked directly against `elements/slots.json` while migrating constraints (WP-1.1): three of the four splits already exist, but they predate this ruling and this session entirely — `window_head_masonry`/`window_head_wood`, `corner_quoin`/`corner_board`, and `window_surround_masonry`/`window_surround_wood` were split in ontology **0.3.0**, part of the original pre-session Georgian-kit authoring. They don't match the ruling's own naming: the carpentry slot is `window_head_wood`, not `window_head_carpentry`; the split pair is `corner_quoin`/`corner_board`, not `corner_treatment_masonry`/`corner_treatment_carpentry`; and `window_surround` was split into two ids rather than kept single with an added `none` value — though `window_surround_masonry` already carries `none-opening-cut-in-the-mass` as an example, so the underlying design intent (masonry's correct value is a legitimate none, not an absence) is already satisfied by a different mechanism than the one this ruling specifies. `wall_thickness_expression` has no split, and no slot by any wall-thickness name currently exists at all. Ontology is still 0.4.0 and kit schema still 0.2.1 — neither bumped. WP-1.3 needs to either rename the three existing splits to the ruling's naming (a real migration: `deprecated_in_favour_of` on the old ids) or amend this ruling's wording to match what already exists and document the mapping — and, either way, actually build the missing `wall_thickness_expression` split, which nothing on disk currently does under any name. *(Superseded by the closure above — WP-1.3 has since run and built the fourth split. Kept as the record of what was true on 23 Aug, and of why the naming mismatch was found rather than assumed.)*

13. **CLOSED 24 Aug 2026 — the cross-references exist, and they now enforce the thing they were for.** WP-1.3 added `derives_from_module` to the slot schema and populated it on five slots pointing at `entablature` as the root (`cornice`, `frieze`, `modillion_dentil`, `crown`, `chair_rail`); no regrouping was done, as ruled. `build/validate.py` has checked since then that each reference resolves and that no slot references itself. What nothing checked was the ruling's actual purpose — that a chair rail's module derives *from the same run* as the exterior cornice rather than being independently invented. `build/check_kits.py::check_derived_module_family` now does: within a kit, members of a family that both record a `computed_at` key must agree on its value. It compares only shared keys, so a member derived from the ceiling height alone is not held to be in conflict with one that also knew the opening width — a partial context is not a contradiction. The measured state: only `georgian-colonial-american` binds the family with computed parameters at all, and its two contexts are subset/superset (`ceiling_height_in: 108.0` in both), so the corpus passes today. The check is there for the kit that will not. Ontology stayed at 0.5.0 — nothing about this changed the slot data. Pinned by `tests/test_ontology.py`. *Original entry follows.*<br><br>**The entablature is scattered across three slot groups** — `cornice` and `frieze` in Envelope, `crown` and `chair_rail` in Interior, `entablature` and `modillion_dentil` in Classical Apparatus. They are the same object at different scales and three of them derive from the same module. **Ruling: add `derives_from_module` cross-references between the six slots; do not regroup them.** The lighter-touch option — every existing id and downstream kit binding stays valid, and the proportion engine gains a cross-reference it can use to enforce that a chair rail's module actually derives from the same run as the exterior cornice, rather than being independently invented.
    **Correction, 23 Aug 2026:** this entry previously (and wrongly) said "Executed in WP-1.3." WP-1.3 has not run, and unlike item 12, nothing here has been done under any name, at any version — a corpus-wide search finds zero occurrences of `derives_from_module` in `elements/slots.json` or any kit file. WP-1.3 needs to add the field to the schema and populate it on all six affected slots. *(Superseded by the closure above — WP-1.3 has since run and done exactly that. Kept as the record of what was true on 23 Aug.)*

14. **CLOSED 24 Aug 2026 — keep the three slots; the premise did not survive the data.** The ruling was to keep the three and state the shared conflict once. Reading the three records showed they are not one conflict stated three times but **three different conflicts**: `balustrade` is a 36 in guard (IRC R312.1.2), `porch_rail` is that guard plus the 4 in sphere (R312.1.2, R312.1.3), and `newel_balustrade` is a *handrail* at 34-38 in (R311.7.8.1) — a different requirement, with a different number, for a different reason. Collapsing the slots would have merged three conflicts and lost two. What IS shared is the RESOLUTION — never absorb the difference in the baluster, take it in the plinth, the newel or the ramp — and that is now stated once in `docs/faults.md` rather than implied three times. Original ruling text: The three are genuinely different trades and locations: a stair balustrade, an exterior porch rail and a newel assembly are made by different people, at different times, to different rules. Collapsing them would be an ontology change that re-resolves every kit binding one, for tidiness rather than correctness. What IS duplication is the 4-inch-sphere code conflict, currently stated three times; it moves to one place all three slots reference. WP-4.5's `overlook` room, which had to cite two of these slots for one guard, is the case that surfaced it. *Original entry follows.*<br><br>**OPEN — `balustrade` / `porch_rail` / `newel_balustrade`** are three slots, one assembly type, and one code conflict stated three times.

15. **CLOSED 24 Aug 2026 — the six are now two, and the slot has a typed shape.** (The slot is `room_adjacency_overrides`; the entry's old title named it by the parameter inside it.) A `rules` array joins the kit schema for any slot the ontology types `value_type: rule` — there are 17 — each entry carrying a **kind** (topology, axis, sequence, daylight, element-placement, hierarchy, orientation) and an **effect** against the universal room records (adds, restricts, suppresses). The one-sentence `rule` string stays: prose stays beside the test. **Of the Georgian six:** two were about the adjacency graph and survive as typed rules, the second of them folding in a clause that restated the first. Three were duplication of slots that already say the same thing and are DELETED rather than moved — 'the best room is at the front, away from the service end' is `public_private_gradient`'s own rule sentence, 'no principal room is a through-route to another' is that slot's 'no room is entered through another', and 'windows on two walls of every principal room' is `daylight_strategy`'s `walls_lit_per_principal_room: 2` verbatim. One was not about adjacency at all — where the chimney breast sits in the room — and moved to `hearth_position`. The other seven populated kits are classified in place. **And the executable half:** the slot's own note has said since ontology 0.4.0 that it holds 'only what a style ADDS or SUPPRESSES', and nothing read it — a suppression existed as prose while the validator went on enforcing the rule the kit said should not apply. `plan_check.kit_suppressions()` now reads it across the whole inheritance chain, and `check_kits` refuses a suppression with no machine-readable target or one naming a rule that is not there. **No style declares a suppression yet, and that is stated rather than assumed:** all eight prose statements in the corpus restrict or add, and inventing one to exercise the code is exactly what this corpus forbids. *Original entry follows.*<br><br>**`room_adjacency_rules` holds six rules of four different kinds in one flat set.** No schema change fixes this; it needs the room catalogue. (The room catalogue now exists — `rooms/`, 58 types — so this is unblocked and should be revisited; not yet done.)

16. **CLOSED 25 Aug 2026 — it was already done, and the label was the only thing still open.** Found while deriving the open-question list in a test instead of hardcoding it: this entry's own remaining work was "make `resolve_kit.py` actually honour the two instances already sitting in the data", and it does. `build/resolve_kit.py:156` joins a child's `rule_append` onto whatever `rule` the merge step resolved to, records the join in `_rule_append` with the contributing ancestor and the text it was joined after, and sets `rule_appended_from` in the provenance block; `--verbose` marks the slot `APPEND` and prints `(+ clause from <node>)` under the resolved sentence. Verified on both live instances — `roof_pitch` and `door_surround` on `kits/georgian-colonial-american.kit.json`. **The finding is the staleness itself**: a status word rotted for two days while the code under it was finished, and nothing was watching, because the test that should have caught it asserted against a hardcoded list. That test now derives the open set from this file, so the next rot fails a check instead of waiting to be noticed. *Original entry follows.* **IN PROGRESS — `rule` still has no merge operator.** `extends` fixed three of four restatement cases. The fourth failed because `rule` is one string: a child adding a clause must replace the whole sentence or leave the resolved rule silent about its own change. `rule_append` is in the schema at 0.2.1. This did not need a ruling — the operator's shape is already declared in the schema — so it is being implemented directly in WP-1.3 rather than put to a decision: a child's `rule_append` value is joined onto the resolved parent rule as an additional sentence, with the join point marked so `resolve_kit.py`'s provenance output can still show which ancestor contributed which clause.
    **Correction, 23 Aug 2026:** "is not yet used" was wrong — checked directly while migrating constraints (WP-1.1): `rule_append` is already populated on two slots (`roof_pitch`, `door_surround`) in `kits/georgian-colonial-american.kit.json`, apparently from pre-session `extends`-cascade testing against `english-georgian`. What's actually still missing, confirmed by grep: `build/resolve_kit.py` has no code path that reads `rule_append` at all — the field is authored in data and declared in the schema, but nothing resolves or merges it yet, and no provenance marking exists for it. WP-1.3's real remaining work here is narrower than this entry's original framing suggested: not "start using the operator," but "make `resolve_kit.py` actually honour the two instances already sitting in the data."

17. **RESOLVED and executed — Two mechanisms now exist for regional conditionality** — `applies_when.regions` on a parameter, and variant status on a child node — with no guidance on which to use. `service_zone_strategy` states the same Tidewater fact both ways. **Ruling: use a variant node when enough diverges to deserve its own identity (massing, program, name, exemplars); use `applies_when.regions` when only a single parameter differs and everything else is shared.** This matches how the rest of the taxonomy already behaves — a variant exists because it is materially a different thing, not because one dimension changed.
    **Correction, 23 Aug 2026 (superseding an earlier correction that was itself stale):** an earlier note here said "WP-1.3 has not run," written while WP-1.1 was still in progress and true at that moment — but WP-1.3 ran later the same day. Checked directly, 23 Aug 2026, before starting Phase 4: `docs/inheritance.md` now carries the guidance in full ("Two mechanisms for regional conditionality, and when to use which"), and `kits/tidewater-georgian.kit.json`'s `service_zone_strategy` binding is reconciled — the parent's `strategy_tidewater` `applies_when.regions` parameter was removed, and the variant's own binding now carries the 25-80 ft fact once, with a note recording the reconciliation. Nothing here blocks Phase 4.

18. **HALF CLOSED 25 Aug 2026 — every editorial parameter now says it is one; the SOURCE half is environment-blocked.** The entry's complaint was two things and only one of them needed a library. All **162** parameters that were editorial with neither a source nor a note now carry a note: it states plainly that no source is recorded, quotes the basis the SLOT states for itself, and says which of four forms the figure takes. Nothing was given a source it does not have. The count of silent editorial values in this corpus is **0**.
    **The audit before the writing is the finding, and it caught two errors of mine.** A first pass reported "57 mis-tagged placeholders" with `value: null` and I nearly rewrote them as unpopulated. They are nothing of the kind: **55 carry a `range` and 2 a `set`**, and my script had only looked at `value`. That is the **ninth** count in this work package to change once it was read, and the first that would have DAMAGED correct records rather than merely mis-sized a job. The audit was not in the ruling; doing it anyway is the only reason 57 good records survived.
    **What the 162 actually are — RECLASSIFIED 25 Aug 2026 after an audit, because the first census was wrong in the direction that matters.** It classified a record as a figure whenever it had a `value` key, and called **82** of them "a bare single figure with no band … it reads as precise and is not". Seventy-two of those 82 are not measurements at all: `french-vernacular.wing_strategy.growth_unit` is `"bay-or-building"`, `georgian-colonial-american.frieze.treatment` is `"plain, or pulvinated (cushioned) in the Gibbsian manner on the richest work"`. No citation would sharpen those into numbers, and pointing a researcher with library access at them as the priority would have wasted the scarcest resource this entry has. The true shape: **23 a bare single figure** — the real weakest form, and the real place to start; **82 a categorical call**, which needs a source showing the tradition makes the choice, not a figure; **55 a band**, the honest form for a judgment since the width IS the uncertainty; **2 a set of permitted values**, an enumeration rather than a band (a source would extend the list, not narrow it), which the first pass also mislabelled.
    **And the quoted basis was truncated at a hard 150 characters — mid-word in 148 of the 162.** "…this is the entablature's own m". The note presents that fragment as the reasoning the figure rests on, and several lost their operative clause. It now cuts at a sentence or clause boundary and marks every elision with an ellipsis; 17 are still elided and all 17 say so.
    **And all 162 sit on a slot that already carries a rule or a note.** The reasoning was never missing — it was attached to the slot and not to the parameter, so a reader of the parameter saw a number with nothing behind it. That is what the notes fix.
    **The source half is ENVIRONMENT-BLOCKED**, with OQ 7 through 11: closing it needs measured surveys and period manuals, `loc.gov`, `archive.org` and `hathitrust` all fail to connect from here, and **none of the 162 may be given a source from a secondary work or a modern redrawing.** The 23 bare figures are where a person with library access should start — NOT the 82, which the reclassification above shows are categorical calls rather than measurements.
    *Original entry follows.*<br><br>**STILL OPEN, but measured, corrected where it could be, and no longer hand-counted (24 Aug 2026).** The figure in this entry went stale twice because it was recounted by hand. `build/check_kits.py` now computes and prints the whole provenance census on every run, and `tests/test_provenance.py` pins it, so it cannot drift from this entry again without a test saying so.
    **Where it actually stands: 1,556 parameters — measured 1,161 (74.6%), derived 161 (10.3%), editorial 199 (12.8%), invented 25 (1.6%), code 10 (0.6%).** The number that matters is not `editorial`: an editorial call carrying a note saying what it rests on is the rule working as designed, since 'sources or `kind: editorial`' makes the second half a legitimate answer. What cannot be defended is **editorial with neither a source nor a note — 164, 10.5% of all parameters**. Nothing is unlabelled.
    **156 of those 164 are on one kit,** `georgian-colonial-american`, spread across 67 slots with no slot holding more than 7. That is the deepest-authored kit in the corpus carrying the cost of having been authored first, not a systemic problem across 159 kits — which is a materially different work list from the one this entry has implied since it was written.
    **Two things were fixed rather than described.** (i) Every unsourced editorial number was matched against every numeric threshold its own style states, on the constraint's *expression name* as well as its value. **Exactly one matched**: `water_table.measured_band_in` [24, 36] is `georgian-colonial-american.c05`'s own water-table band, so it was a sourced fact wearing the label for an unsourced one, and it now cites it as `measured`. One hit out of 166 is itself the finding: these numbers are genuinely unsourced, not sourced-and-uncited, and no amount of cross-referencing inside the corpus will retire them. (ii) **Eleven were mislabelled in kind.** `garage_strategy`'s own ontology note says in its first sentence that no historical precedent exists, and the schema distinguishes `editorial` ('our considered convention') from `invented` ('no precedent exists'). A number about the garage door itself cannot be a convention drawn from tradition, because the tradition has no garage. Reclassified to `invented`; the ones left editorial on that slot are the ones carried from the dependency tradition — subordination, ridge ratio, pitch match, cladding continuity.
    **What remains cannot be done from inside this repository, and that is the point.** The 164 want a real second pass against real sources, one number at a time; anything else is laundering a guess as a measurement, which this project's own rules call the worst thing that can be done to the corpus. The honest interim position is the one now in force: every one of them is labelled as our own convention, the count is computed, and it is 10.5% and falling only when somebody sources something. A cheap next step that does NOT require sources: require a `note` on every editorial parameter — it would not make them sourced, but it would make each one say what it rests on, which is the difference between a considered convention and a number someone typed. *Original entry follows.*<br><br>**`kind: editorial` is carrying 166 of 402 parameters** (417 as of the 23 Aug count), which is the migration defaulting honestly rather than claiming a `measured` it could not source. Probably half want a second pass with a source each.

19. **CLOSED 24 Aug 2026 — three enforceable meanings, and they found four things.** The field said what decides a slot and nothing resolved it, because the schema never said what the determination MEANS. It now means three things `build/check_kits.py::check_determined_by` holds the corpus to. **(1) The determiners must be bound.** A slot that says it is whatever the order requires, on a kit whose `order` is empty, is unjudged — and it read as specified, which is this corpus's first discipline inverted. One real case: `scandinavian-log-vernacular.roof_material` is determined_by `roof_pitch`, which is empty, so the choice between sod-on-birch-bark and split-shingle is decided by nothing — and is not a free choice either, since sod needs a shallow pitch to stay on and a shingle a steep one to shed. Now stated on the slot; the warning is left standing because it is true. **(2) No cycles.** None found. **(3) A determined slot may not state a dimensional number as an independent claim** — the schema's own note has always said so ('specifying it separately either restates the order or contradicts it'), so a number must be `derived`, or carry a source or expression, or say in its own note why it is genuinely independent. 17 numbers failed that. Working through them found two data defects rather than missing notes. **`reveal_masonry` and `reveal_frame` on the Georgian kit were byte-identical:** OQ 12's split divided the slot in two and copied the whole parameter set into both halves rather than dividing it, leaving each carrying the other trade's numbers with the per-parameter `applies_when.construction` doing the discrimination the split was supposed to do. **Tidewater's `reveal_frame` was a verbatim copy of its `reveal_masonry`** and therefore stated the MASONRY answer under the frame id — a 13.5 in wall and a 4-8 in reveal — so a frame Tidewater house would have been drawn with a brick wall's reveal. It now extends the parent and states nothing of its own, because nothing in the corpus sources a Tidewater departure and inventing one is what the copy already did once. Also **recorded and deliberately not resolved:** `construction_type.wythe_step_at_floor` reads 13.5 in, which is that slot's own masonry wall thickness repeated, while `reveal_masonry.step_at_floor_in` reads 4.5 in and the rule sentence says a half brick. Correcting a number on a guess is worse than recording that two slots disagree; both now say so. The remaining eleven are notes: which of restatement and independence each number is, and why. *Original entry follows.*<br><br>**`determined_by` is declarative only.** `porch_ceiling` says it is determined by the order and the entablature, which is better than prose, but nothing resolves it because the schema does not say how the determination works.

20. **ANSWERED WITH CORPUS-WIDE DATA (24 Aug 2026, WP-4.2, wave B) — the cascade has now been proved at real depth (up to 34 real, populated levels on a spot-checked variant) across all 129 previously-empty style/variant kits, with a real count of merge problems.** Originally: not a decision so much as a test the kit fill must run; WP-4.2 was written to surface it. **23 Aug 2026 (WP-4.2, wave A)** built the mechanism this test depends on — family nodes had no kit files and no `lineage` edges, so could never appear in `_cascade` before this — and got one real four-level chain with zero merge problems found (`tidewater-georgian → georgian-colonial-american → american-colonial → english-georgian`), but explicitly left the corpus-wide count open until wave B ran. **24 Aug 2026 (WP-4.2, wave B):** all 129 remaining style/variant kits now carry real content (159 of 159 kit files corpus-wide have at least one non-`open` slot; 1,556 `specified` + 330 `extends` + 223 `forbidden` bindings total, up from 256/18/75 before wave B). A spot-check on `appalachian-log-house` (a variant, the deepest kind of cascade) shows a 34-node chain in which every single ancestor now resolves real content — the acceptance criterion PLAN-OF-ACTION.md names directly ("`resolve_kit` shows multi-level provenance on every variant"). The merge-problem count: **~26 real problems found and fixed across 129 nodes (roughly one in five)**, all traceable to three distinct causes, none a mechanism failure: (1) deep classical content (`antique-classical`'s strict-bilateral symmetry, `renaissance-classical`'s harmonic-mean ceiling rule) leaking into 15 non-classical nodes across five families via a real but narrow lineage edge that, once populated, carries the donor's entire binding rather than just the trait the edge exists for; (2) `hybridizes_with` edges built to carry one narrow aspect of a donor's practice instead transmitting that donor's whole kit (9+ nodes in the `continental-baroque-neoclassical`/`british-picturesque`/`low-countries-vernacular` cluster alone, plus `north-german-hall-house` and `swiss-chalet`) — a real, general mechanism limitation, not fixed at the mechanism level in this pass, only patched node-by-node; (3) one genuine cross-batch collision (`french-eclectic`'s `garage_strategy`, authored against `tudor-revival` as the nearest ancestor in sub-wave 1, broken when sub-wave 2 concurrently gave a nearer ancestor its own binding) — the specific risk of authoring `extends` deltas in parallel across a shared cascade, caught by `check_kits.py`'s dangling-diff check and fixed by converting to a full override. Full detail, per-node list, and the case for a future slot-scope allowlist on `hybridizes_with`: `docs/reports/wp-4.2-wave-b-kit-fill.md`.

21. **CLOSED 24 Aug 2026 — the checker is enough.** `check_kits.py` errors on an `op` naming an undefined id, which is the failure mode that actually bit, and the corpus is now fully populated and stable at 159 kits. Constraining the match in the schema would re-resolve every kit that uses an op to prevent a class of error already caught at build time. Not worth it. *Original entry follows.*<br><br>**OPEN — Variant `op`s match on a free string.** A one-character difference turns a `replace` into an `add` and quietly duplicates a record. `check_kits.py` now errors on it, but the failure mode is inherent to the design.

## Structural, unresolved

22. **RESOLVED — No date-conditional resolution.** `applies_when.date_range` existed and was populated (49 dated variant records corpus-wide as of WP-1.3), but nothing selected on it. For a style spanning 1700–1800 — 12/12 to 9/6 to 6/6 sash, fanlights after 1780, butt hinges after 1775 — a resolved kit had to present all the options rather than the ones current at a stated date. **Executed in WP-1.3:** `build/resolve_kit.py --date YEAR` filters every slot's variant list to what `applies_when.date_range` permits at that year (inclusive bounds; an undated variant applies at any date), in the CLI table, the `--slot` detail view, and `--json` (where every variant instead gets an explicit `in_period` boolean rather than being filtered, so a downstream consumer can choose). Nothing excluded is silently dropped — the count and ids of what got left out are always printed alongside what's shown. Real case: `tidewater-georgian`'s `window_head_masonry` correctly flips from `segmental-gauged-arch` to `gauged-flat-arch` across the 1750 threshold the kit's own note names as a dating tell. Only `applies_when.date_range` on **variants** is handled — no kit data currently puts `date_range` on a `parameters` entry (checked corpus-wide), so that path is unbuilt and untested; if a future style needs a date-conditional parameter value rather than a date-conditional variant choice, `eval_parameters` will need the same treatment.

23. **RESOLVED — No room or room-grouping catalogue.** This was the largest remaining gap as of the original drafting of this list. It is no longer a gap: `rooms/` (58 style-independent room types with furniture, clearances, typed directional adjacency, a six-rank privacy gradient, daylight depth) and `groupings/` (16 groupings with `attaches_to`) both exist and pass `check_rooms.py`. `room_adjacency_rules` as the join point is item 15 above, still open on its own terms.

24. **RESOLVED — Constraints are prose with a kind and a severity.** They are written to be enforceable, with numbers, but they were not yet a rule language. **Ruling: reuse the fault corpus's `test` object pattern** (`expression` / `threshold` / `direction`), extended with a `one-of` direction, a `scope` field (`plan` / `elevation` / `site` / `section` / `judgment`), and a named variable vocabulary — rather than invent a separate rule language for constraints. A constraint the sources do not determine numerically gets `scope: judgment` and no test, the same honesty as judgment slots. Executed in WP-1.1: `constraint.schema.json`, `build/constraint_vocabulary.py`, `build/check_constraints.py`, and the migration of a 140-constraint worked example (the `english-classical` and `american-colonial` families, 28 nodes). The remaining ~520 constraints across the other ~25 families are not yet migrated — see `docs/constraints.md` and new open question 27.

25. **RESOLVED — No fault corpus yet.** This was the seed-stage note as of the original drafting. It is no longer a gap: `faults/` now holds 209 named, tested faults with 846 style exceptions (496 numerically bounded), a `test` object on every one, and `check_faults.py` is green. Slot coverage was full (93 of 93) at the time of this ruling; WP-1.3's `wall_thickness_masonry`/`wall_thickness_frame` split (see item 12) added two slots with no fault authored against them yet, so coverage is currently 93 of 95 — not a regression in the fault corpus, a consequence of the ontology growing after it.

## From writing the behaviour-test suite (WP-0.3)

26. **RULED 24 Aug 2026 — document them.** Fold into the same pass as OQ 14's shared code conflict: the seven `EQUIVALENT` groups are validator data in the same sense a fault or a constraint is, and WP-4.5 added an eighth (`gallery-corridor` into the entrance-hall group) on WP-2.1's evidence without any document describing what the list is for. *Original entry follows.*<br><br>**OPEN — Should the `EQUIVALENT` room-type alias groups in `build/plan_check.py` be documented?** Seven groups treat related room types as interchangeable for adjacency purposes (e.g. `{"entrance-hall", "vestibule", "stair-hall"}`) — real, load-bearing logic that changed how a test fixture had to be built during WP-0.3 (a "stair hall standing between two rooms" fixture accidentally satisfied an entrance-hall adjacency rule through aliasing rather than through the two-hop-through-circulation mechanism actually under test). It exists only as an unexplained list in the source; `docs/plans.md` documents the two-hop widening and the `via` intermediary by name but not this. Should it be documented the same way, and are the seven groups as currently drawn the right ones?

## From migrating constraints (WP-1.1)

27. **RULED 24 Aug 2026 — tag them.** Add a `blocked_by` marker during WP-4.6 so the judgment pool can be re-swept cheaply when its blocker clears, rather than re-reading 295 constraint statements to find the ones that were only ever waiting on a mechanism. *Original entry follows.*<br><br>**OPEN — A `scope: judgment` constraint blocked purely on OQ 22 (no date-conditional resolution) isn't distinguished from one that's judgment for a structural reason.** In the 140-constraint worked example, a noticeable fraction of the 34 judgment-scope constraints are judgment *only* because a sash-pattern-by-period or similar rule can't be tested without date-conditional evaluation — not because the rule is genuinely unmeasurable. Once OQ 22 is resolved, these should convert to tested constraints, but nothing currently marks which ones those are; finding them again means re-reading every judgment-scope constraint's statement. Worth either a lightweight tag (`blocked_by: "OQ-22"` or similar) at migration time, or accepting the re-read cost when OQ 22 eventually lands and treating it as a one-time sweep.
    **Still not decided, 23 Aug 2026:** the remaining 25 families were migrated without an answer to this (see item 28's report pointer), so the un-tagged pool is now the full corpus's 295 judgment-scope constraints rather than the worked example's 34. The re-read cost at resolution time is correspondingly larger; still not large enough by itself to justify deciding this unilaterally.

## From migrating the remaining families (WP-1.1, 23 Aug 2026)

28. **FIXED 24 Aug 2026 — `build/modcache.py`, one process-wide cache keyed by real path; every local `_mod()`/`_load()` now delegates to it. Steady-state `check()` 3.06 s → 0.31 s, `compose()` 30-40 s → 7-9 s, the full 291-test suite 3 chunks / ~12 min → one run / 2 min 24 s, restoring WP-0.3's "under five minutes" acceptance line. The profile turned out to understate the problem: a *single* `check()` was executing plan_check 16 times, geometry 8, structure 4, and roof/core/elevation/proportion_engine twice each, because the by-path loads are module-level and nest (elevation → plan_check + geometry + structure + roof + engine; roof → plan_check + geometry + structure; structure → plan_check + geometry; geometry → plan_check), so the whole tree was re-walked at every entry point rather than only `core._data()` going cold. Neither of the two fixes proposed below was taken: caching `core` at `plan_check`'s scope would have fixed one edge of that graph and left the other fifteen loads in place, and an mtime-keyed disk cache would have added a staleness class this corpus does not otherwise have. The delegation approach keeps the by-path convention exactly as it was — every script still runs standalone, every call site keeps its signature — and only stops the re-execution. Pinned by `tests/test_modcache.py` (5 tests, including a counter asserting a warm `check()` executes no module at all). The one behavioural change: a cached module is not re-executed, so a process that mutates a corpus file and re-loads in-process will not see the change — safe here because the mutation tests in `test_ontology.py`/`test_constraints.py` all run their checker as a subprocess, and `modcache.invalidate()` exists for anything that ever needs it. See `docs/reports/oq-28-module-cache.md`.** Original entry follows.<br><br>**OPEN — `plan_check.py`'s `_load()` re-imports `mcp_server/core.py` fresh on every `check()` call, defeating `core._data()`'s own `@functools.lru_cache`.** Each fresh `importlib` module exec gets its own empty cache, so every `check()` call re-reads the whole corpus (styles, faults, kits, rooms, groupings — several hundred files) from disk. This predates this migration, but a smaller constraint payload kept it cheap enough that nobody had profiled it: with all 660 constraints now carrying `id`/`scope`/`test`, a single `compose()` call profiles at 30-40 seconds (up from whatever it cost before, not independently benchmarked pre-migration), and the full pytest suite no longer completes inside a 10-minute run. Nothing is wrong with any answer `check()` or `compose()` gives — every pinned test and both shipped plans' and both reference briefs' composer rankings were re-verified unchanged — this is a cost, not a correctness bug. Two contained fixes suggest themselves (cache the loaded `core` module at `plan_check.py`'s own module scope across `check()` calls within one process; or have `core._data()` itself cache to a file keyed on corpus mtimes) but both touch a loading pattern (`_load()`/`_mod()`, fresh `importlib.util.spec_from_file_location` per call) used the same way across several files as a deliberate isolation convention, and picking one wasn't this package's call to make unilaterally. See `docs/reports/wp-1.1-remaining-families-migration.md` for the profile that found it.

## From proportion-pack bindings (WP-4.1, 23 Aug 2026)

29. **CONFIRMED 24 Aug 2026 — the call stands, and OQ 30's pack will retire two of the three. RESOLVED, with a recorded tension — 129 of 132 buildable nodes bound, not 132.** `PLAN-OF-ACTION.md`'s acceptance line for WP-4.1 says "132 of 132 buildable nodes bound," but the task text two sentences earlier says "where a needed pack does not exist... list it — do not bind the wrong pack." `egyptian-revival`, `moorish-andalusian`, and `mudejar` each have a real, checked reason no combination of the 36 packs applies (trabeated/battered-wall Egyptian construction with no arch; Islamic geometric setting-out with no order or module at all). Binding any of them to hit the literal count would have meant forcing a pack the corpus's own text rules out. Ruling made in the moment rather than escalated: honor the "do not bind the wrong pack" instruction over the numeric target, name the exception explicitly (`DELIBERATELY_UNBOUND` in `build/check_pack_bindings.py`, with the reasoning duplicated in `docs/reports/wp-4.1-proportion-pack-bindings.md`), and keep `--strict` wired into `check_all.py` so a fourth unbound node — a real regression — still fails the build. If a future package (WP-4.6) builds an Islamic/Moorish system and an Egyptian order, this drops to 0 automatically once those three are bound for real; until then it's a permanent, documented 3, not a punch-list item quietly carried forward.

30. **RULED 24 Aug 2026 — build it first in WP-4.6.** The most-corroborated gap in the pack list, and WP-4.5 has now given the styles that want it a courtyard parti to compose with, so the pack is the remaining half of a working pair. Authoring it also retires two of the three `DELIBERATELY_UNBOUND` exceptions recorded under OQ 29. *Original entry follows.*<br><br>**OPEN — An Islamic/Moorish geometric proportion system was independently named as missing by three unrelated batches** (Spanish Colonial Southwest/adobe; Spanish/Mediterranean Revivals; the Moorish/Mudejar cluster itself), each working from a different file list with no cross-visibility into the others' reports. No pack in the 36-pack library encodes a horseshoe arch, a muqarnas transition, geometric star-polygon setting-out, or the tile/plaster/timber ornament stratification real to this whole cluster — the single most-corroborated gap WP-4.1 found, and the strongest candidate to prioritize first when WP-4.6 (missing proportion packs) is scoped. See `docs/reports/wp-4.1-proportion-pack-bindings.md`'s consolidated candidate list for the full set (orders/ornament, facade/massing, and module gaps, ~25 distinct items across all 15 batches) alongside this one.

## From the garage (WP-4.3, 24 Aug 2026)

31. **RULED 24 Aug 2026 — a room-level opt-out.** Option (b): a `daylight.depth_governs: false` flag on the room record. It keeps the decision in the data where this corpus keeps such things, is explicit per room rather than sweeping a whole function_class, and gives the corpus its first vocabulary for *judged, where the judgment does not apply* — which is what this question turned out to be about. *Original entry follows.*<br><br>**OPEN — the daylight-depth rule is unsatisfiable for a garage, and probably for every non-habitable room.** The validator checks a room's depth against the reach of its own window head (`depth_multiplier` from the room record, relaxed by half when cross-lit), which is exactly right for a room people sit in and wrong for one cars sit in. A two-car garage is 20 ft × 22 ft at the *shallow* end of `rooms/garage.json`'s own dimension band — a 16 ft car plus clearance — and no arrangement of windows in a 0-10% glazing band reaches its back wall. So every correctly-sized, correctly-glazed garage the composer produces reports one `serious` daylight finding that cannot be designed away, only ignored. The composer states this in its decision log rather than suppressing it, and the garage is deliberately *not* distorted (made shallower than a car, or over-glazed past the room's own band) to silence it. The question is where the fix belongs, and it is a judgment rather than an engineering call: (a) exempt `function_class: "work"` and `"service"` rooms from the depth rule, which is simple but would also stop flagging a genuinely unlit workshop or laundry, both of which people do occupy; (b) let a room record opt out with an explicit `daylight.depth_governs: false`, which keeps the decision in the data where the rest of this corpus keeps such things, at the cost of a schema field; or (c) leave it, on the argument that a finding a human dismisses in one second is cheaper than a rule that quietly stops looking. Related but distinct from the "unjudged is not passed" principle — this is *judged, and the judgment does not apply*, which the corpus has no vocabulary for yet. Surfaced by WP-4.3; nothing was changed on the strength of it.

## From the workbench (WP-5.2, 25 Aug 2026)

32. **CLOSED 26 Aug 2026 — findings carry a server-minted id built from what they are ABOUT, never from how they are phrased.** `build/plan_check.py`'s `Findings.add` mints an `id` from the layer, the room, and the rule or fault id where the finding has one, with an ordinal disambiguating within a group that has none. Verified unique across both shipped plans (111 and 142 findings, 111 and 142 ids) and readable — `fault:blank-wall-to-the-street`, `adjacency:powder#1` — which matters because it is what a citation will carry.
    **One trap found while building it, and it is the same bug wearing a disguise.** `rule` carries an id on some layers and, on the adjacency layer, a whole paragraph of the rule's reasoning. Admitting it unconditionally produced ids with a paragraph of prose inside them — an id that embeds the very sentence it exists to be independent of. Only an id-shaped value is allowed into the key now.
    The workbench prefers `f.id` and keeps its old `hash(layer|statement|room)` only as a fallback for a response from a server older than this change, with a comment saying why it must never go back to being primary. Four tests, one of which rewords a finding and asserts the id does not move — the regression stated as a test rather than as a hope. *Original entry follows.*<br><br>**OPEN — findings carry no stable id.** A validator finding is `{severity, layer, statement, room?, rule?, fix?}` — nothing to diff one run's findings against the next, cite from the rail, or animate appearing/clearing in the workbench's style-switch interaction. The app derives a client-side key from `hash(layer|statement|room)`, which works until a statement's wording changes, at which point every open row, citation and diff silently breaks. A server-side id (even the same hash, minted in `Findings.add`) would make findings first-class citable objects. Cost: one field on every finding; nothing existing reads finding identity, so it is purely additive. Surfaced by WP-5.2; worked around, not fixed.

33. **CLOSED 26 Aug 2026 — P7 is met: a compromise is counted AND drawn where it falls, in both engines and both renderers.** A relaxation is no longer a bare float. `build/geometry.py`'s cuts each record `{off_ft, axis, at_ft, from_ft, to_ft, level}` — the axis the cut runs across, where the line sits, its extent, and which storey — and `geometry_cp.py`'s counter, which already knew the edge coordinate and threw it away, records the same shape. `geometry_report.relaxations.marks` carries them beside the count that was always honest.
    Both renderers draw them: `build/render_plan.py` and the workbench's `Sheet.jsx` put a hollow triangle on the line itself, dashed, **in ink and never in colour** — colour in this drawing names a material and does not flag a condition. Verified end to end: the Tidewater plan reports 11 cuts and the SVG contains exactly 11 marks, which is P6 and P7 in one assertion — the number on the sheet is the number in the record, not a number the renderer chose. A mark from the CP engine carries no extent, because an edge there is a wall line shared by however many rooms abut it and inventing a span would be a drawn claim nobody measured; it gets a short centred tick instead.
    **A near-miss worth recording.** The first version tested `if round(d, 2)` where the original tested `if d`, which swallowed a sub-half-inch miss and took the pinned relaxation count from 11 to 10. Nothing failed loudly; the number simply changed. It was caught because WP-2.3 had pinned 11 as proof that its footprint-helper extraction changed no numeric behaviour, and the pin is now also a test in `tests/test_measurement_honesty.py`. A count that moves silently is the whole reason these numbers are pinned. *Original entry follows.*<br><br>**OPEN — relaxations are counted but not located.** `geometry.solve()` reports `relaxations = {count, max_off_grid_ft, note}`; the individual cuts' positions, levels and kinds are computed and discarded inside `slice_rect`. P7 says compromises "appear on the drawing, at their location" — the workbench can currently render only the honest tally ("11 cuts off the bay line, worst 4.8 ft") at the plate caption, not a marker where each cut falls. Recording `{level, x_ft, y_ft, off_ft}` per relaxation is additive to the report and would let both the workbench and `render_plan.py` mark them in place. Surfaced by WP-5.2 (the design mockup had invented positioned markers, tagged `[G]`, precisely because the real data lacks them).

34. **CLOSED 26 Aug 2026 — the log carries its structure and keeps its prose, and the judgment inside it is one line of code.** Ruled: structure it and mark the judgment. `build/compose.py` emits `decisions_structured` beside `decisions`; the prose list is byte-identical to what it always was, so every consumer that read it before reads it unchanged.
    **The judgment this entry flagged, and the rule taken.** Which lines are DECISIONS and which are narration — and the log already answers it. A line the composer meant as a decision carries a prefix it wrote itself: `JUDGMENT`, `REFUSED`, `AUTHORED`, `NOT SOLVED`, `KNOWN FINDING`. Those are the corpus's own vocabulary, not a taxonomy imposed from outside, so the classifier is: prefix or no prefix. Everything without one is an assumption taken where the brief was silent — which the composer's own `how_to_read_this` already calls "the assumptions, not facts", so it is a decision too, of a quieter kind. Live counts across both shipped briefs: 45 assumption, 9 judgment, 2 authored, 1 unsolved, 1 disclosure.
    **`field` and `chose` are DERIVED, and every record says so.** They are read off the sentence against a fourteen-shape table built by collecting what the composer actually emits — 58 distinct lines across every brief in `briefs/` — not re-authored at the twenty-five call sites, because rewriting those sentences would change prose that tests and reports quote. 56 of 58 lines get a field and 46 a `because`; the two that get neither are the `NOT SOLVED` and `KNOWN FINDING` disclosures, which settle no brief field, and null is the right answer there. A sentence outside the table keeps null rather than a forced guess: a derived value that is wrong would be OQ 52's defect one layer up, and the statement is right beside it carrying the whole thought.
    The Candidate Set renders it — a judgment now reads as a judgment at a glance, with the field and the choice above the sentence — and falls back to the plain list for a record composed before this shipped. Five tests in `tests/test_measurement_honesty.py`, one of which runs the real composer and fails if its wording drifts away from the table. *Original entry follows.*<br><br>**OPEN — composer decisions are prose lines, not records.** `compose()`'s `decisions` is a list of sentences (`log + _summarise(rlog)`). They read well, but a decision-log *entry* — what was assumed, because which brief field was silent, overridable by whom — wants `{field, chose, because}` structure, and the workbench's decision-log component renders plain prose in the meantime. Structuring the log is a composer change with a judgment in it (which log lines are decisions vs narration?), so it is recorded here rather than done unilaterally.

35. **FIXED 25 Aug 2026 — `core.check_plan` now returns a distinct "could not validate: the jsonschema package is not installed" (with `unvalidated: true`) via an explicit `except ImportError`, and the workbench's brief validation does the same; surfaced by the WP-5.2 adversarial audit and fixed there.** Original entry follows.<br><br>**OPEN — `core.check_plan` reports a missing `jsonschema` as a schema failure.** The lazy `import jsonschema` sits inside the same `try` as the validation call, so with the package absent every plan returns "plan does not match the plan schema · No module named 'jsonschema'" — an environment problem laundered as a data verdict, the exact class of collapse the corpus forbids elsewhere. The workbench's `/api/health` asserts the import instead, but the honest fix is in core: distinguish "could not validate (no validator)" from "validated and failed". One `except ImportError` clause. Surfaced by WP-5.2.

## From deployment (25 Aug 2026)

36. **OPEN — the compose job registry pins the service to a single replica, and serializes compose across all users.** `workbench/server/jobs.py` holds `_JOBS = {}` in process memory (30-minute TTL) and runs its work on `ThreadPoolExecutor(max_workers=1)`. Deployed, that has two consequences the deployment doc records and the platform config currently works around by keeping replicas at 1: a second replica cannot see the first's jobs, so a caller polling `/api/jobs/{id}` after a load-balanced hop gets a 404 for a job that is running fine elsewhere; and because the pool is one worker wide, a second user's compose waits behind the first's rather than running beside it. Neither is wrong at the scale this is being deployed for — a handful of people trying it — and both are the correct simple choice for a local instrument, which is what WP-5.2 built. The question is what to do when it stops being true, and the options differ in kind rather than degree: (a) leave it, cap replicas at 1 forever, and treat compose as a single-user operation, which is honest but puts a hard ceiling on concurrent users; (b) widen `max_workers` and keep the registry in memory, which fixes the queueing but not the replica problem and makes the memory cost of concurrent 250-candidate searches a real question; (c) move job state to something shared — Redis or Postgres — which fixes both and is the first thing in this project that would genuinely need a database, having spent its whole life without one. Worth noting the corpus itself never enters this: it is read-only and identical in every replica. Only *job* state is at issue. Surfaced by the deployment package; nothing changed on the strength of it beyond documenting the replica constraint.

## From the deployment audit (25 Aug 2026)

37. **OPEN — `find_faults` cuts through tie groups up to 104 wide.** `mcp_server/core.py`'s sort key is `(severity_index, frequency_index)`: two coarse enums over 209 faults, so tie groups are enormous, and `out[:limit]` with a default of 25 then keeps an arbitrary slice of them. Measured: for `queen-anne-american` the cut lands inside a tie group of 104 and drops 103 faults the function's own ranking calls exactly as important as the one it kept. It is now *deterministic* — the glob sweep saw to that — but deterministic is not the same as principled, and the caller is told `matches` without being told the cut was arbitrary. The fix `pick_partis` took (expand through a narrow tie, cut a wide one) does not transfer: a 104-wide tie means the ranking has no opinion at all, and the honest response is either a finer key (frequency and severity are not the only things that distinguish faults) or saying in the response that the remainder is unranked rather than unimportant. Both are product decisions about what a search result means, so neither was taken unilaterally.

38. **OPEN — `resolve_kit.choose_pack` reports an unranked tie as the author's ruling.** `sorted(declared, key=precedence or 99)` then `live[0]`: when two packs share a precedence, position in the JSON array decides which governs, and the result is labelled `how: "slot.packs"` — "the author's explicit ruling" — when no ruling was made. The style-level branch already returns `how: "unresolved"` for the analogous disagreement; the slot branch has no such guard. Four real occurrences today, all in `kits/georgian-colonial-american.kit.json` (`window_head_masonry`, `window_head_wood`, `window_surround_masonry`, `window_surround_wood` — brick-course against opening-proportion and sash-light, all at precedence 1). This decides actual dimensions in a resolved kit, so it is not cosmetic; but the fix is either an authoring pass to break the four ties deliberately, or a schema-level statement that equal precedence means unresolved — Lucas's call, not a patch.

## From the export layer (WP-5.1, 25 Aug 2026)

39. **OPEN — the record's vertical opening data is thin and half-dormant.** A plan record states a window's `width_ft`, `height_ft` and sometimes the room's `window_head_ft`; the schema has carried an optional `sill_ft` per window since 0.1.0 but **no plan in the corpus uses it and (until WP-5.5) no build code read it** — the IFC exporter now prefers it when present. Door leaf heights have no field at all. The classical elevation generator derives all of this from its packs — but only inside its own scope gate, and the IFC export (WP-5.1) needs a number for every opening it gives a body. Today it uses editorial defaults (2.5 ft sill, 6 ft 8 in door leaf), each named `editorial` in the element's own TDL Pset rather than laundered as record data. The question: should `window_sill_ft` / `door_leaf_height_ft` join the plan schema as authorable fields, or stay the elevation layer's judgment with the IFC honestly defaulting? Surfaced by WP-5.1; worked around in the open, not silently.

## From the real solver (WP-2.3, 25 Aug 2026)

40. **OPEN — the flat footprint versus declared wings.** `exterior_walls` speaks exposure in the fully-massed house: a porch's three walls, a wing's own corner, a service ell's north light. The corpus uses this idiom everywhere — 10 of 12 partis have two same-level rooms declaring the same corner pair — and one flat rectangle cannot hold it, which is how WP-2.3's first hard-pins build proved both check plans and nearly every composed candidate "infeasible" on facts that are true of the real buildings. The ruling (contested corners downgrade, stated; generalized to any pin-set the solver proves unable to co-hold) bridges the gap honestly, but the structural fix the findings point at is a massing-aware footprint: massings already know their `depth_rooms` and expansion logic — should they carry footprint composition (main block + dependencies/ells) that the solver places rooms into, with `exterior_walls` read against the composed outline rather than one rectangle? That is a schema-and-solver change with a judgment in it, recorded rather than done unilaterally. Surfaced by WP-2.3; worked around in the open (`geometry_report.solver.refinements`), not silently.

41. **OPEN — the solver's door floor sits below the renderers' draw test.** The CP door constraint scales its required shared-wall run to the smaller room of the pair (`min(4 ft, ⌊0.9 · min(maxside)⌋)`, floored at 2 ft) because demanding a parlor's 4 ft of a 2.5 ft linen press would refuse real closets — but `render_plan._shared` draws a door only above 3.2 ft, and the DXF/IFC exporters inherit that test. In the 2–3.2 ft band a door therefore HOLDS as a proven fact and appears in no drawing. The WP-2.3 audit made the gap stated everywhere (the DXF sheet now carries a "N DECLARED DOOR(S) WITHOUT A DRAWABLE SHARED WALL" line and lists them in the export result; the IFC already noted them) — but stated is not resolved. Should the renderers learn narrow doors (a closet door IS narrower than a parlor's), or should the solver's floor rise to the draw test and small-pair doors become a named refusal? A drawing convention judgment, recorded rather than made unilaterally. Surfaced by the WP-2.3 adversarial audit.


42. **CLOSED 24 Aug 2026 — the sentence is corrected, the severity deliberately is not, and measuring it changed the answer.** `types_present` is now aliased for the one question it decides: whether the claim *and the plan models none* is true. Across all 129 composable styles, **542 completeness findings now say the true thing** instead of a false one, and the catalogue's fatal count does not move.
    **What the measurement changed.** The obvious fix — alias it and let the finding fall through to the adjacency branch at the rule's own strength — was the one this entry originally proposed. Measured across the catalogue it turns **170 of those findings FATAL across 87 styles**, which would leave two thirds of the corpus unable to return a clean plan. The cause is that EQUIVALENT is **asymmetric in practice and symmetric in the code**: a primary bathroom satisfies a request for a bathroom, and a hall bathroom does not satisfy a primary bedroom's request for a primary bathroom. Until each pairing states which direction it satisfies in, promoting these is trading 774 false minors for 170 false fatals. So the finding stays `minor`/`completeness` and says what it can actually see: *"wants to adjoin an entrance hall; the plan models centre passage and stair hall instead, which the catalogue treats as equivalent, and does not reach any of them."*
    **And a room cannot satisfy its own rule through its own alias group** — the modelled set is computed without the room doing the asking. Without that, the kitchen's rule to adjoin a scullery was answered by the kitchen being a kitchen: true of the group, nonsense as a statement about the plan. That is why 542 and not 774. The directional question is **OQ 43**. *Original entry follows.*<br><br>**`types_present` is not aliased.** Found under OQ 37 and deliberately not fixed there. `plan_check.py` runs every adjacency target through `_alias()` when asking what a room is next to, but the "the plan models none" branch — which decides whether an unmet rule is a *completeness* finding or an *adjacency* one — tests `rule["room"] not in types_present` against raw types. So `plans/tidewater-georgian-careful.json`, which has a centre passage on its ground floor, still reports "Powder Room wants to adjoin a entrance hall and the plan models none" after `centre-passage` joined that group. The finding is minor and the claim is simply false. The fix is one line and its blast radius is a shift of some minor findings from *completeness* to *adjacency*, which is a change in what the validator SAYS about a plan, not only in how many things it says — so it wants measuring against the reference corpus before it is made, not folding into an unrelated package.

43. **CLOSED 24 Aug 2026 — directional, and the 115 went to 0 by being worked through rather than by being suppressed.** `EQUIVALENT` is now `SUBSTITUTES`, a directed map, and the one function that served both questions is two: `satisfied_by(want)` for "the rule wants this; would anything the plan HAS do?" and `serves(have)` for "this room is next to me; which rules does it satisfy?". Before OQ 43 those were one function, which is precisely the bug.
    **What the promotion actually found.** Of 542 findings where the plan modelled something the rule would take, 294 were the table running backwards and returned to honest absence; 214 became real adjacency findings at their own severity. The predicted 115 fatal across 67 styles appeared exactly as measured — and working through them, **none of the 115 was a defect in a diagram**:
    · **90 were two gaps in the table I had just written.** `hall` — the Anglo-American vernacular hall, the undivided room the front door opens into, whose own record gives its `entered_from` as the exterior and whose aka list carries "living hall" — was missing, so every hall-and-parlor and living-hall diagram was told its parlor could not reach an entrance hall while opening off exactly the room that does the job. And `garret-chamber`, a bedroom inside the roof, so a Cape's closets failed `closet must_adjoin bedroom` while doored to the room they serve. **The third time in three packages that a measurement of mine over-counted its own work list**, and the reason to verify a sample before accepting a number.
    · **Two were rules a correct plan structurally cannot satisfy**, the same shape OQ 37 found twice: an UPPER centre passage required to adjoin an entrance hall on its own storey (now `vertical_ok`, as `landing` already is), and a gallery required to share a door with the stair hall when its own record gives four ways in and the rule accepted one (now `via`). The mirror of that second one failed `good-05-lobby-gallery-mansion`, a plan the reference corpus holds up as GOOD, so `entrance-hall` gained the same `via`.
    · **Six were traditions the universal rules were never written for**, and those are the first suppressions ever declared in this corpus. OQ 15 built the mechanism and its own entry recorded that nothing used it, because inventing one to exercise the code is what this project forbids. A Charleston single's street door opens onto the PIAZZA; a bungalow's opens into the LIVING ROOM, which is the hall the type abolished and whose kit already said so in prose; a dogtrot's opens into the trot, which is open to the weather at both ends; a Creole cabinet is entered from the loggia and never from a chamber. Declared on seven nodes — four of them siblings carrying the same Creole fact, because a suppression is inherited DOWN a chain and does not travel sideways, and hoisting it to a shared ancestor would claim the cabinet-and-loggia range for traditions that do not have it.
    **Result: fatal adjacency findings across all 129 composable styles, 115 → 0.** Total fatal 34 → 32, and every one that remains is a fault-layer finding. *Original entry follows.*<br><br>**The EQUIVALENT groups are asymmetric in practice and symmetric in the code.** Raised by OQ 42, which could not be finished without a ruling on it. `build/plan_check.py`'s `EQUIVALENT` list holds nine groups of interchangeable room types, and `_alias()` treats membership as a two-way relation: if a primary bathroom counts as a bathroom then a bathroom counts as a primary bathroom. That is right for some pairings and wrong for others, and the wrong ones are the load-bearing cases. A primary bedroom's `must_adjoin primary-bathroom` is not satisfied by the hall bath being somewhere in the house; a parlor's `must_adjoin entrance-hall` IS satisfied by a centre passage; `{bedroom, bedchamber, primary-bedroom}` is symmetric for a daylight rule and asymmetric for a privacy one. **Measured cost of leaving it:** 542 completeness findings across the catalogue can state that an equivalent room exists and cannot say whether reaching it would satisfy the rule, and OQ 42 had to hold them at `minor` for that reason — 170 of them would otherwise be fatal, across 87 styles. **Measured cost of getting it wrong:** the same 170. The concrete proposal, so the next person does not re-derive it: replace each flat set with a directed statement — `{"is": "primary-bathroom", "satisfies": ["bathroom"]}` — so a group says which way each substitution runs, defaulting to symmetric where an author does not distinguish. Nine groups, roughly twenty pairings, one judgment each; the code change is small and the judgments are the work. It also wants a second axis nobody has thought about yet: an equivalence can be right for one KIND of rule and wrong for another, and a single directed graph cannot say that.

44. **CLOSED 24 Aug 2026 — reproducible by default, and the measurement is why.** Ruled after measuring the cost on both reference plans at a 20 s budget: the deterministic run costs between 0 and 33% more wall time (17.5 s against 18.1 s on one plan, 22.9 s against 17.1 s on the other), returns the **same score**, and on the busier plan explores **more** topologies — 6 against 4 — because it is not cut off part-way. The old default already reproduced on an idle machine, which is exactly why the defect passed every time anyone checked it. `deterministic=True` is the default of `solve()`; `deterministic=False` and `build/geometry.py --wall-clock` remain for when a person is waiting, and the record says which trade was taken. The MCP tool does not expose the switch: everything arriving there is a plan somebody will keep or compare. *Original entry follows.*<br><br>**The solver's answer depends on how fast the machine is.** Found by `tests/test_solver.py::TestDeterminism` failing intermittently, under a docstring that reads *"a suite that cannot reproduce a layout cannot pin one either."* The test was right and the solver was wrong. `build/solver.py`'s topology loop stops starting new topologies once the next one could not finish inside the budget, and that check reads the **wall clock** — so a loaded machine tries fewer topologies than an idle one and a different layout wins. The same seed, the same plan, a different house. Nothing hid it (`topologies_tried` has always been in the record) and nothing said it either, and it passed whenever anyone checked, because checking one test is exactly when the machine is quiet. **Half-fixed, and the half that remains is a judgment.** `solve(..., deterministic=True)` now runs every topology, bounds each phase by CP-SAT's own deterministic time — work done rather than seconds elapsed — and keeps the wall-clock limit only as a tenfold outer guard; the answer is then a function of the inputs. The default is unchanged, because a person waiting for a plan wants the promise about time kept, and the record now carries `deterministic` and a note saying which trade was taken. What is unruled is **which should be the default**, and it is a real choice rather than an oversight: reproducibility is worth more for anything pinned, compared, re-derived or shipped as a plan record, and wall-clock bounding is worth more for anything a person is waiting on. A third option nobody has costed: make the topology count itself the budget (`topologies=N`) and drop the time bound entirely, which would make the promise about *work* instead of about *seconds* and is the only version in which the two stop competing.

45. **CLOSED 24 Aug 2026 — the four ranges corrected, the type's true size moved to prose, and a check so it cannot recur.** Measured with every room at its catalogue band top: **seventeen of twenty-one partis reach their own declared maximum comfortably** and four could not — `courtyard-and-portal` at 43% of a stated 9,000 sf, `great-hall-h-plan` 82% of 12,000, `living-hall-picturesque` 96%, `tower-villa` 97%. One bad case and three near misses, not a systemic problem, and all four corrected for the same reason rather than the small ones left because they are small. `area_range_sf` now means what the AUTHORED room list can be built at; the fact that a real hacienda runs to 9,000 sf — by having more rooms and a second court, which is exactly what the composer cannot do — is in each parti's description, naming the figure it replaced so the correction records what it corrected. `check_partis.py` gains check 11, which computes the ceiling the way `compose.instantiate` actually builds (rooms marked `repeats_with_bedrooms` counted once per bedroom at the top of the parti's own `bedroom_range`, because a bound computed against a room list the composer never instantiates would be measuring the wrong house). Partis missing their own midpoint by more than 12% went from three to one. The larger option — letting the composer add rooms from the parti's `groupings` when the target demands it — was NOT taken: it means the composer inventing plan content rather than instantiating it, which is the thing it is built to refuse, and it needs its own package and its own rules about what it may invent. *Original entry follows.*<br><br>**A parti's `area_range_sf` describes the diagram and the composer instantiates a fixed room list.** Surfaced by OQ 40, which made the gap measurable rather than creating it. `courtyard-and-portal` declares 1,800–9,000 sf and its seventeen rooms reach about 3,200; `great-hall-h-plan` declares 2,800–12,000 and reaches about 6,500. Both figures are honest about the TYPE — a hacienda really does run to 9,000 sf — and neither is reachable by the only mechanism the composer has for growing a house, which is making the same rooms bigger until each hits its catalogue band. The one thing that does add rooms, `repeats_with_bedrooms`, adds bedrooms and nothing else. So a large brief against a large-range parti returns a house that misses its target by 40% and a decision log correctly saying the diagram grows by having more rooms — with no way to act on it. Three ways out: mark rooms in a parti as `repeats_with_area` so a courtyard gains a second court range and a manor a second wing at stated thresholds; declare the range honestly as what the AUTHORED room list reaches and record the type's true range separately as prose; or let the composer add rooms from the parti's `groupings` when the target demands it, which is the most useful and the largest, since it means the composer inventing plan content rather than instantiating it — the thing it is currently built to refuse.

46. **CLOSED 24 Aug 2026 — `arch` added at ontology 0.6.0, and the migration is eight bindings and not the twenty-two I counted.** The slot holds the arch as a SPANNING MEMBER: its form, its rise against its span, what it springs from, whether it is structural or applied, and whether an arch is permitted at all — Egyptian Revival's c03 is a rule *about* arches which states that there are none, and it had nowhere to live.
    **The 22 was a keyword count and reading them changed it.** Most of the twenty-two `window_head_masonry` bindings that mention an arch are genuinely window-head statements: a stone lintel with a hood mould, a flat gauged arch used as a head, a plain unmoulded band, 'no masonry head at all'. Migrating on the keyword would have been the keyword deciding rather than the content — the same mistake OQ 41's sweep had to avoid, and the second time in two packages that a measurement of mine over-counted its own work list. **Six moved whole** (`italian-renaissance`, `norman-romanesque-english`, `roman-classical`, `gothic-revival-british`, `egyptian-revival`, `italianate-townhouse`), where the content was arch-as-form end to end and the head slot is left empty rather than carrying a pointer, because the same fact in two places is what this migration exists to prevent. **Two were split** — `georgian-colonial-american` and `tidewater-georgian` carried both claims at once, so the arch geometry (rise, camber, depth) moved and the head keeps how it is BUILT (gauging, one datum a storey, keystone, architrave) and names `arch` in `determined_by`, which is the mechanism OQ 19 built for exactly this.
    **And OQ 19's check immediately earned its place on the new arrangement**: it flagged the architrave and keystone widths as editorial numbers sitting on a determined slot with nothing saying whether they restate the determiner or are independent of it. They are independent — an architrave is joinery around the opening and the arch is masonry spanning it, and neither sets the other's dimension — and both now say so. A rule added for one reason tightening another is the ratchet working.
    `proportions/orders/moorish-arch.json`, the pack that found the gap, now targets `arch` instead of `window_head_masonry`. Its impost block honestly stays on `porch_support`: an arcade carrying a loggia is what that slot is for and the corpus has no `arcade` id, which was the third option on this question and is not built. *Original entry follows.*<br><br>**The ontology has no arch slot.** Found while authoring `moorish-arch` for WP-4.6. `elements/slots.json` has 95 slots and none of them is an arch, an arcade or an opening head as a *structural* member: `window_head_masonry` is the nearest and is written for a lintel or a jack arch over a window, `porch_support` for what holds a porch up. So a system whose entire content is the geometry of an arch and its springing had to target the return of a horseshoe at `window_head_masonry` and the impost block at `porch_support`, and both bindings are recorded in that pack's own notes as the compromises they are. It is not only a Moorish problem: the Gothic Revival facade-and-opening pack WP-4.1 named, the Romanesque foliate order, Rome's arcuated pier-arch-pier bay rhythm and the Italianate round-arched window all want to say something about an arch that the corpus currently has no place to put. The question is whether to add an `arch` slot to the openings group (and possibly an `arcade` to the classical apparatus), which is a small ontology change with a large surface — every pack, kit and constraint that currently routes arch content through a window head would want revisiting, and doing it badly would leave the same fact in two places. Raised now because WP-4.6 will hit it again with every arched tradition it reaches.

47. **CLOSED 25 Aug 2026 — `expressed_frame` added at ontology 0.7.0, with the field the slot was actually for.** Ruled after the evidence was corrected downward (see below). The slot holds `member_status`, `face_width`, `spacing`, `projection` from the cladding plane, and `brace_geometry` — and `member_status` is the reason it exists, taking `structural`, `structural-and-expressed`, `applied` or `none`.
    **What the field buys.** Two records state it as a hard rule in nearly the same words: `arts-and-crafts-british` c01, "every visible material must be doing the job it appears to do. Applied half-timbering, veneer stone, false beams and imitation finishes are forbidden", and `arts-and-crafts-american` c01, "exposed rafter tails must correspond to actual rafters at 16 to 24 in on centre… decorative tails applied" are not permitted. Before this the corpus could not tell a frame from a picture of one, which is the single distinction `stick-style` and `tudor-revival` both turn on — and `applied` is now recordable as a legitimate position rather than an accusation, which is what those two styles need it to be.
    **Bound on 14 kits, and the three-way split is the finding.** `structural` on `english-medieval-timber-frame`, `german-fachwerk` and `tudor`; `structural-and-expressed` on `arts-and-crafts-british`, `arts-and-crafts-american` and `mission-revival`; `applied` on `stick-style`, `tudor-revival` and `french-normandy-revival`; and **`forbidden`/`none` on five** — `jacobethan-revival` ("applied half-timbering is forbidden. The variant is defined by its absence"), `prairie-school`, `shingle-style`, `mediterranean-revival` and `spanish-colonial-revival`. That last group is the same argument that got `arch` built at 0.6.0: a rule *about* the member which states that there is none had nowhere to live. Three sibling revival nodes now take three different positions on one member, and the Spanish revival family is told apart at the eave by this field and little else.
    **Migration: 7 rules in 3 packs** off `corner_board` — `timber-panel` (stud spacing, face width, projection, brace ratio), `jetty-overhang` (corner return, dragon beam), `facade-medieval-english` (buttress). The kits gained an EMPTY slot rather than a moved one, and that is worth stating: a keyword sweep proposed 102 kit bindings across 32 slots carrying such content, and reading a sample found essentially all of them correctly placed — `primary_cladding` describing the infill panel, `construction_type` naming the frame. **The dimensional content was not misfiled in the kits; it did not exist there**, because there was nowhere to put it. That is a stronger argument for the slot than a migration would have been.
    *Original entry follows, with its own over-count corrected in place.*<br><br>**OPEN — the ontology has no slot for an exposed structural member on a wall face.** Found while authoring `timber-panel` for WP-4.6, and it is the same shape as OQ 46 one layer out. A half-timbered wall is made of nothing but exposed structural members: the stud, its face width, how far it stands proud of the infill, and the diagonal brace are the four things that distinguish an English close-studded wall from a German Fachwerk from an applied Normandy Revival elevation, and the corpus has nowhere to put any of them. The envelope group's nearest slot is `corner_board` — a board at a corner — so all four rules in that pack are routed through it and all four read slightly wrong. `construction_type` is a categorical and holds no dimension; `primary_cladding` is the panel BETWEEN the members, which the pack does use and which is the other half of the same wall.
    **It is not only a half-timbering problem, which is what makes it worth a ruling rather than a workaround.** `styles/stick-style.json`'s applied stick bands, the Swiss chalet's exposed purlin ends and carved bargeboards, `opening-craftsman`'s exposed rafter tail — already noted in that pack as the member whose structure-or-trim status has to be decided on the drawings — and the Tudor Revival's applied timber are all the same class: a member that is (or represents) structure, shown on the outside of a wall, with a face width and a projection that are the whole of its effect. **CORRECTED 25 Aug 2026, and the correction is the point.** This entry originally said "four packs
in this work package have now routed such a member through a slot written for something else", and
`facade-medieval-english`'s notes later raised that to five. Both are wrong. Measured by READING the
rules rather than by counting keyword hits, the true set is **7 rules in 3 packs**: `timber-panel`
(stud spacing, stud face width, projection proud of the panel, brace ratio), `jetty-overhang` (the
corner return and the dragon beam) and `facade-medieval-english` (the buttress). `trim-sawn`'s
brackets sit on `modillion_dentil`, which is the RIGHT slot -- a modillion is a bracket -- and
`facade-portada`'s estipite is a genuine pilaster, so neither was ever a compromise.
    That is the **sixth** time in this work package that a keyword measurement over-counted its own
work list, and it is the worst of the six, because it happened inside the sentence arguing FOR a
ruling. Inflating the evidence for a change one wants is a different and more dangerous error than
the other five, all of which merely made a job look bigger than it was. The ruling was taken on the
corrected figure.
    The concrete proposal, so the next person does not re-derive it: add `expressed_frame` to the envelope group, holding face width, spacing, projection from the cladding plane, brace geometry, and — the field that would do the most work — whether the member is structural, structural-and-expressed, or applied. That last is the distinction `styles/stick-style.json` and `styles/tudor-revival.json` both turn on and that no slot currently records, so the corpus cannot presently tell a frame from a picture of one. Cost is an ontology bump and a migration of four packs' bindings; the alternative is that every future timber tradition on WP-4.1's list — the Alpine bracket work, the jetty module, the Fachwerk ornament — routes through `corner_board` too and the compromise becomes the convention.

48. **CLOSED 25 Aug 2026 — 139 to 0, by a targeted rename of 74 rules.** The migration this entry said was outstanding is done. At each of the **27 genuinely conflicted addresses** the quantity used by the most packs keeps the original `dimension` and the divergent ones take their own `quantity` as their dimension — so BOTH meanings survive at their own address instead of one being set aside by precedence. 74 rules moved, not the 246 a rename-everything-at-a-conflicted-address pass would have touched, and not the 483 a make-dimension-equal-quantity-everywhere pass would have.
    **`build/elevation.py` needed four lookups updated** and they are the evidence the rename was worth doing, because each was disambiguating by note substring what a dimension should have been saying: the entrance-composition cap was asking `door_surround`/`width` and meaning the whole composition's maximum width rather than an architrave's face; both belt-course lookups were asking `belt_course`/`height` with `note_substr="ordinary case"` and meaning the band's own depth rather than its height above the floor; the bay count was asking `window_grouping_rule`/`count` with `note_substr="bay count"` among five quantities. All four now name the quantity and no longer depend on prose matching.
    **`check_addresses.py` reports 0 collisions and 14 could-not-judge**, and `--strict` has nothing left to fail on. The pinned test now protects zero rather than 139.
    *Original entry, and the measurement that produced it, follow.*<br><br>**PARTLY CLOSED — the mechanism is built, the corruption is visible and counted, and the fix is a migration that is NOT done.** Ruled as proposal (a): an optional `quantity` string on each rule naming what it MEASURES, so `(target_slot, dimension, quantity)` is the real address and a checker compares meanings instead of guessing.
    **All 74 colliding addresses were read, one at a time, and 483 of the 490 rules on them annotated.** Seven were left without a `quantity` on purpose, because the note did not say plainly enough what the rule measured and inventing one would be the laundering this corpus forbids. `build/check_addresses.py` reports those as **could-not-evaluate**, separately from agreement.
    **The real number is 139, not the ~20 the earlier 5% rate predicted.** That estimate came from adjudicating only the 63 pairs involving a WP-4.6 pack; reading all 442 co-binding pairs found **139 addresses where two packs that bind the same node measure different quantities**. A seven-fold under-estimate, and the seventh time in this work package that a measurement was wrong once it was read.
    **The worst of them, with what they were hiding:**

    | address | quantities found | scale of the clash |
    |---|---|---|
    | `window_head_masonry/height` | head height above floor, lintel/arch depth, flat-arch camber, segmental rise, hood-mould depth, archivolt depth | 94 in against 7.6 in against 0.37 in; **44 nodes** bind two or more of the packs |
    | `height_proportion/ratio` | storey diminution, wall height over thickness, roof height over half-span, wall thickness over span, balcony split, elevation h:w, asymmetry lever arm | seven quantities, one address |
    | `porch_support/width` | a post's own width; the CLEAR SPAN between posts | 7 in against 56 in |
    | `chair_rail/height` | chair rail, plate rail, sill band | three members, and two packs' own notes say "IT IS NOT A CHAIR RAIL" |
    | `window_grouping_rule/width` | maximum opening width, minimum solid between openings, mullion width, pier width | four quantities |

    **Two defects in the resolver were found on the way and both are fixed.** `quantity` did not survive `proportion_engine.evaluate`, so the first version of the grouping silently did nothing — the same class of failure the field exists to catch, occurring inside the fix for it. And `resolve_kit`'s `by_slot` is keyed on the SLOT alone, so `choose_pack` was comparing a `count` rule against a `width` rule as though they were alternatives; grouping is now by `(dimension, quantity)`.
    **What the resolver does now.** Where two packs measure different things at one address, precedence still picks a winner — it has nothing better to go on — but the loser is **named** in `other_quantities` and printed by `resolve_kit --verbose` instead of being discarded in silence. `prairie-school` reports 58 quantities set aside, including its own plate-rail and sill-band heights losing to a chair-rail height from an inherited classical pack.
    **The migration is NOT done and should be its own package.** Fixing 139 means either renaming dimensions across ~50 packs — touching every binding note and test that references them — or teaching every consumer to key on the quantity. `check_addresses.py` runs in `check_all` and REPORTS rather than failing, deliberately: a known, counted, documented backlog must not block all work, and what actually protects the corpus is the **pinned count of 139 in `tests/test_wp46_packs.py`**, which fails the moment a new pack adds a 140th.
    *Original entry follows.*<br><br>**OPEN — a `(slot, dimension)` address can hold several rules, and nothing distinguishes a deliberate menu from a silent corruption.** Found by `facade-arcade` and then measured properly by `facade-gable`, both in WP-4.6. **1,922 collision instances across the corpus**, on 75 distinct addresses, in 622 distinct pack-pairs.
    **The first measurement was misleading and reading it changed the question.** The largest entries are packs colliding with THEMSELVES — `room-harmonic` writes `room_adjacency_overrides`/`width` **ten times** and `ceiling_height_rule`/`height` seven times, `storey-graduation` writes `height_proportion`/`ratio` four times, `sash-light` writes `window_lite_pattern`/`count` four times. Those are not defects. They are a **menu** at one address, authored deliberately and labelled as such in their own notes: "SHAPE 1 OF 7 — THE ROUND ROOM", "METHOD 1 OF 3 — THE ARITHMETIC MEAN", "THIRD STOREY OVER SECOND". A pack offering seven harmonic room shapes or three ways to derive a vaulted height is doing exactly what a proportioning system should. **A naive uniqueness check would flag 1,710 correct rules**, which is why this entry exists instead of a checker.
    **So there are three cases and only one is wrong.** (i) Several rules at one address WITHIN a pack: a menu, correct, and the notes say so. (ii) Rules at one address from two packs meaning the SAME quantity: alternatives, and precedence is exactly the mechanism for choosing — `facade-arcade` and `moorish-arch` both give an arch's rise and that is right. (iii) Two packs meaning **different** quantities at one address: a corruption, because whichever resolves last wins and nothing reports it, and precedence cannot help because precedence decides which of two accounts of the same quantity to believe. Four instances of (iii) were found and fixed in WP-4.6 — `porch_support`/`height` meaning both an impost block's height and a springing line; `gable_treatment`/`height` meaning a finial, a parapet and a gable-end silhouette in three different packs; `ornament_vocabulary`/`count` meaning a cusped motif set and a shaped gable's curve count; `rake_condition`/`height` meaning a bargeboard's depth and a crow-step's rise.
    **UPDATED 25 Aug 2026, and the proportion matters.** All 144 cross-pack pairs involving a pack written in WP-4.6 were adjudicated by hand, and **eight** were case (iii) — about **five per cent**. So the mechanism is mostly working, and the alarm the first measurement raised was mostly the menus. The eight: `porch_support`/`height` (an impost block's height against a springing line 84 inches higher); `gable_treatment`/`height` (a finial, a parapet and a gable-end silhouette, in three packs); `ornament_vocabulary`/`count` (a cusped motif set against a shaped gable's curve count); `rake_condition`/`height` (a bargeboard's depth against a crow-step's rise); `rake_condition`/`width` (a crow-step's tread against a bargeboard's stock thickness); `frieze`/`height` (a classical entablature frieze against a suspended spindle valance); `cornice`/`projection` (a cornice moulding against a bracketed eave's overhang); and `eave_condition`/`height` (an eave's height above ground against a verge's overhang depth) — the last on **ten nodes**, and the one that would most easily have shipped silently.
    **A checker was considered and deliberately not built; an authoring aid was.** `build/pack_addresses.py` reports the addresses a named pack shares with packs it co-binds with, and prints both notes side by side so the author can adjudicate. It is not in `check_all` because only a human can tell (ii) from (iii) — the difference is what the rule MEANS — and a build check carrying a 144-entry allowlist that had to be re-adjudicated on every new pack would be a great deal of ceremony for a five per cent hit rate. Run it when you write or bind a pack. It caught six of the eight above.
    **What is not known is how many of (iii) are in the 453 pre-existing pairs**, and finding out means reading them and deciding what each means — which is the work, and it is a package rather than a check. The concrete proposals, so the next person does not re-derive them: (a) give a rule an optional `quantity` string naming what it measures, so `(slot, dimension, quantity)` is the real address and a checker can compare quantities rather than guessing; (b) or accept named dimensions as the convention, which is what WP-4.6 fell into — `upper_ratio`, `springing`, `end_ratio`, `facade_ratio`, `parapet_height`, `curve_count`, `step_rise` — and write a checker that requires any address shared by two packs bound to one node to be declared in an allowlist with a reason. (a) is cleaner and costs a schema field; (b) is free and pushes the problem into naming discipline, which is where it has been failing.

49. **CLOSED 25 Aug 2026 — a binding can be scoped to slots, and the corpus reached 132 of 132.** Ruled as proposed: an optional `slots` array on `proportion_packs[]`, which is OQ 36's mechanism one layer out — that ruling scoped a `hybridizes_with` edge so a donor could transmit one aspect rather than an entire kit; this scopes a BINDING so a pack can supply one rule to a node that is not an instance of its type. Absent means the whole pack, which is the historical behaviour and stays the default. `build/resolve_kit.py::eval_packs` filters on it, which is the only place it can be enforced — everything downstream reads `by_slot` and cannot tell where a rule came from, which is exactly how `role: optional` plus a note in prose failed to scope anything.
    **Slot-level granularity alone would not have served the cases that motivated it**, and finding that out changed the field. `jetty-overhang` writes `material_change_rule` once, so a bare slot id is right there. But `facade-portada` writes `ornament_vocabulary` FOUR times and only its rationing rule applies to `mission-revival`, and `facade-peristyle` writes `column` seven times of which two fit `egyptian-revival`. A scope admitting three rules nobody asked for would have been a smaller version of the problem the field was added to solve, so an entry is either a bare slot id or `slot/dimension`.
    **The three bindings.** `french-normandy-revival` ← `jetty-overhang` scoped to `material_change_rule`: it states that rule almost verbatim and has no jetty. `mission-revival` ← `facade-portada` scoped to `ornament_vocabulary/event_count`: it carries a rule about that pack's subject stating there is none. `egyptian-revival` ← `facade-peristyle` scoped to `column/front_count` and `column/intercolumniation_roman` — **and the exclusions are what make it defensible rather than a way of reaching 132.** The whole pack would have handed it a peristyle count it has no peristyle for, a Vitruvian column-height function, a stylobate curvature, a corner contraction, and an ENTASIS its own c04 forbids outright. Six of the eight `column` rules are excluded, including the one the node prohibits. `DELIBERATELY_UNBOUND` is now empty and is kept rather than deleted, because the mechanism it names is still right for a node that genuinely fits nothing.
    **A checker, for the failure the field could itself cause.** A scope naming a slot the pack does not write is a silent no-op: the node gets nothing and the binding still reads as though it delivered a rule. `check_pack_bindings.py` errors on it.
    **And closing this opened OQ 51**, which is larger. Verifying the `egyptian-revival` binding showed that `facade-peristyle` was ALREADY reaching that node from five ancestors through the lineage cascade, unscoped, entasis included — so the deliberate refusal had been cosmetic all along. See there.
    *Original entry follows.*<br><br>**OPEN — a node can need one rule of a pack without being an instance of the pack's type, and the binding model cannot say so.** Found while binding `jetty-overhang` for WP-4.6. `styles/french-normandy-revival.json` states one of that pack's rules almost verbatim — a vertical change of wall material must occur "at a floor line, a water table, or a jetty, with a minimum 4 in. horizontal offset" — and `styles/tudor-revival.json` states the same rule in nearly the same words. Tudor Revival also jetties, so it is bound and gets the rule. French Normandy Revival does **not** jetty: it names a jetty as one permitted datum for something else. It is a CONSUMER of one rule and not an instance of the type, and binding a jetty module to it would make the corpus assert a framed overhang on a style that does not have one — a false claim about the building, made in order to deliver a true rule about a detail.
    **The workaround available today is worse than the gap.** `role: optional` still says the pack applies; a note saying "bound only for `material_change_rule`" is prose that no resolver reads, and `build/resolve_kit.py` would hand the node the whole pack's fifteen rules including a jetty projection. That is the same class of problem as `hybridizes_with` transmitting a donor's entire kit (the trap CLAUDE.md already records and OQ 36 partly closed for hybrids), arriving now through bindings instead of through lineage.
    **It is not a one-off.** The same shape is visible wherever a pack owns a rule about a device rather than the device itself: `storey-graduation`'s dormer rules are wanted by styles with no graduated storeys, `facade-arcade`'s springing rule by styles with one arch and no arcade, `opening-mullioned`'s light-count quantisation by styles with a single fixed light. Each is currently either bound whole, or left unserved and the rule restated in the node — and restating it in the node is exactly the duplication this library exists to remove.
    The concrete proposal, so the next person does not re-derive it: give a binding an optional `slots` array scoping which target slots it may contribute — `{"pack": "jetty-overhang", "role": "optional", "slots": ["material_change_rule"]}` — which is the same mechanism OQ 36 built for `hybridizes_with`, applied one layer out, and would reuse its checker shape. Cost is a schema field on `proportion_packs[]` and a filter in the resolver. The alternative, and it is defensible, is to say that a rule wanted by nodes outside its pack's type does not belong in that pack at all and should be lifted into a small shared one — which is cleaner in principle and would mean deciding, for each such rule, whether the device or the rule is the real subject.

53. **CLOSED 26 Aug 2026 — the two live wrong dimensions are fixed, a bare ratio can no longer be delivered as a measurement, and the 68 are counted rather than invisible. The referent question survives as named work, stated at the foot of this entry.** All 68 were read, as the ruling required, and reading changed the answer this entry expected. It proposed that the fix was to add `units` to the address key and then decide, one by one, which of the 68 are one quantity in two clothings (convert) and which are two quantities (separate). The first half of that is done. **The second half turned out to be the wrong question**, and the corpus said so in its own records.
    **What reading the 68 showed.** They are one phenomenon: the order packs state interior-trim quantities as a bare ratio, and the trim and module packs state the same quantities in inches. At `casing/width/casing_face_width` on `craftsman`, four rules are in play and **all four say the same thing** — `chambers-ionic` and `palladio-ionic` write `1 / 6`, `palladio-tuscan` and `vignola-tuscan` write `opening_width / 6`. There is nothing to adjudicate between them; the ratio form simply leaves out what it is a ratio OF, and puts it in the rule's prose note ("Of opening_width."), where no evaluator can read it. That is "prose stays beside the test" running backwards: the test was incomplete and the prose was carrying the missing half.
    **But converting them would have been wrong, and this is the part the entry did not foresee.** A ratio rule's `range` is scale-free — 0.1429 to 0.1667 holds at every opening width, where an inch band only holds at one — so rewriting the expression in inches would have forced a band to be re-derived at a single assumed opening and made it worse. And some of the ratio rules are **not** the same account reworded: `palladio-ionic` says in its own note that Palladio does NOT proportion the Ionic pedestal to the column, that "the datum is the OPENING, not the order"; `chambers-ionic` measures its pedestal against the whole order's height and says the classical datum moves down under Chambers. Those are rival accounts of one quantity, which is exactly what precedence is for. Flattening them into inches would have invented a referent the source never gave — the failure this corpus exists to avoid.
    **So the fix is at the resolver, not in the data.** `resolve_kit.choose_pack` now prefers a rule that yields a MEASUREMENT over one that yields a bare ratio at the same (dimension, quantity), and records the demotion in the chosen record (`ratio_demoted`, with its reason) rather than preferring silently — a silent preference would leave the corpus unable to show why one of two agreeing rules was passed over. **135 slot resolutions across 40 nodes now deliver a measurement where a bare ratio used to win** — far more than the 2 this entry predicted, because it counted only where the delivered value was visibly wrong: `chair_rail` 38, `baseboard` 36, `wainscot` 31, `casing` 26, `window_surround_wood` 4. The two named cases are among them: `craftsman` and `craftsman-bungalow` resolve `casing` to `opening_width / 6` — 6 in at a 36 in door — instead of 0.1667.
    **`check_addresses.py` now compares units** and reports a third state beside collision and could-not-judge: two packs that agree on the quantity and differ on the units they say it in. It reports **68** at own scope, which is this entry's own number arrived at independently, and the count is pinned in `tests/test_wp46_packs.py` so the class cannot grow while the referents stay in prose.
    **One thing was deliberately NOT built, having been built and withdrawn.** A first version also flagged every address where every rule is a ratio. It fired **1,463 times across all 132 nodes**, because most such quantities genuinely ARE ratios and are right in that form — `main_roof_pitch`, `opening_height_over_width`, `arch_rise_over_span`, `wall_thickness_over_span`. A checker that cries wolf on the corpus's own correct records is worse than no checker, so only the provable case is flagged: a measurement of this very quantity was on the table and a ratio was delivered instead.
    **The named work this leaves.** The ratio rules still carry their referent in prose. The fix is a machine-readable one — a `ratio_of` field beside `units`, naming the bound variable the ratio is taken of — which would let the engine emit both the scale-free ratio (range-checked as now) and the derived dimension, and would let the address comparison see that a ratio-of-`opening_width` and an expression over `opening_width` are the same account. It is a schema change plus roughly thirty rule edits, it needs no new sources, and nothing is wrong in the meantime: the resolver no longer delivers a ratio where a measurement exists, and where only a ratio exists the value is what the source actually said. *Original entry follows.*<br><br>**OPEN — `check_addresses.py` compares `quantity` but not `units`, and two rules can agree on what they measure while differing 36-fold in what they say. Raised 25 Aug 2026 by the same audit.** OQ 48 made `(slot, dimension, quantity)` the real address on the reasoning that two rules meaning the same thing are alternative accounts precedence can choose between. That holds only if they are accounts in the same unit. **68 own-binding addresses agree on all three and disagree on `units`** — `casing/width/casing_face_width` is `6.0 in` on `benjamin-doric` and `0.1667 ratio` on `vignola-ionic`, four nodes binding both, thirty-six times apart. `resolve_kit.choose_pack` groups on `(dimension, quantity)` and never looks at units, so those land in one group and precedence picks between them as though they were rival measurements of one thing. Across the 132 buildable nodes **659** (node, slot) resolutions choose a rule whose units differ from a peer at the same address, and in **2** of them — `craftsman` and `craftsman-bungalow`, slot `casing` — a `ratio` rule actually wins over inch peers, yielding `0.1667` where `3.5 in` was meant. Those two are live wrong dimensions, not a latent hazard.
    **Why it is raised rather than fixed.** Adding `units` to the address is a one-line change to the grouping key and a much larger change to what it implies: some of the 68 are genuinely the same quantity expressed two ways and SHOULD be converted rather than separated, and deciding which needs reading all 68. Doing that inside an audit would either separate rules that belong together or convert ones that do not. The two live cases are named above so they can be fixed on their own if the larger question waits.
    **A related latent one, same file.** `check_addresses.py` passes a pair on any overlap: `if not (qx & qy)` treats a partial intersection as agreement, so `opening-pointed` {`lights_across`} against `sash-light` {`lights_across`, `lights_high`, `total_lights`} passes while two of the three mean something else. And it skips intra-pack collisions entirely (`if len(sides) < 2`), which is correct for a deliberate menu and wrong for `sash-light`, which writes three different quantities to `window_lite_pattern/count` on its own.

52. **CLOSED 26 Aug 2026 — the twelve invented measurements are gone, the generator now declares what it does not model, and the seven faults decided on them come back could-not-judge.** The fix took the shape this entry wrote out, and the numbers moved exactly as it predicted. `build/elevation.py` supplied twelve measurements as constants; all twelve are removed, and the names live on in a `NOT_MODELLED` dict at the head of the file which carries a stated reason apiece and is FILTERED at the point the measurements are returned — so reintroducing one by a careless `m.update()` cannot put it back in front of the critic, which makes the guarantee structural rather than remembered. To take a name off that list you must model the thing and delete the entry in the same commit; that is the only way out, and it is the point of the list.
    **What moved, on both shipped reference plans identically.** Seven faults left the judged buckets for could-not-judge — `dormer-off-the-bay`, `dormer-wall`, `overscaled-dormer`, `vestigial-chimney-chase`, `capless-stack`, `raking-cornice-that-does-not-match`, `cornice-gutter-without-a-liner`. Five of the seven had been reported PRESENT and two CLEAR, which is this entry's "a fabricated pass sitting beside a fabricated failure" showing up in the arithmetic. Pinned counts: Tidewater serious 40 → 36, minor 62 → 61; spec Colonial serious 70 → 66, minor 59 → 58. **Those numbers going down is the fix working** — four fewer convictions on evidence that never existed, not four fewer defects in the house — and the re-pinning in `tests/test_plan_validator.py` says so in as many words, because a future reader finding a count that fell will otherwise assume a regression.
    **The other three members are done too.** `mcp_server/core.py`'s `_eval_test` now reads a present-but-`null` value as MISSING rather than letting it reach `eval` and become a `status: error` — both are honest states but they say different things, and null is the natural JSON encoding of "I could not judge this", not of "the corpus asked something incoherent". `schema/room.schema.json` no longer permits a null `clearance_in`: the twenty records that carried one all genuinely meant zero and now say zero, so `build/plan_check.py` reads the value instead of coercing it with `or 0`, and an author who forgets one now fails `check_rooms.py` instead of being checked as though the item needed no gap. And `build/roof.py`'s four hardcoded band fallbacks each record themselves in `bands_read_from_fallback` on their own record — the fallbacks are kept, because a check that refuses to run is worse than one that says where it read from, but a run that used one now says so, and on both shipped plans that list is empty.
    **`tests/test_measurement_honesty.py` is the check this entry asked for**, 13 tests in four groups: the generator's declared limits carry a reason each and never leak into the supplied measurements; the seven faults come back unjudged on both plans and none of the five conviction sentences is printed again; a null reads as need_measurements while a real zero still evaluates; and a roof band read from a fallback names itself. *Original entry follows.*<br><br>**OPEN — the elevation generator invents measurements it never took, and the fault corpus convicts houses on them. Raised 25 Aug 2026 by an adversarial audit of WP-4.6.** This is CLAUDE.md's standing "unjudged is not passed" trap, found in six more places, and two of them are firing on both shipped reference plans right now.
    **What is invented.** `build/roof.py:449` explicitly REFUSES to judge dormers — *"No dormers declared on this plan (no schema field authors dormer position yet)"* — and `build/elevation.py:563` then overwrites that refusal with `dormer_count: 0`. The secondary test on `faults/dormer-off-the-bay.json` is `dormer_count % 2 == 1`, so **"Dormers Off the Rhythm" fires as PRESENT (serious) on a house with no dormers modelled**. `elevation.py:558-562` goes further and states five chimney dimensions as constants — `chimney_width_in: 36.0`, `chimney_depth_in: 20.0`, a cap projection, a shadow-line count — on a plan where the neighbouring `visible_chimney_count` is correctly ABSENT because roof.py could not judge it. `vestigial-chimney-chase` is then reported present on evidence `20/36 = 0.5556 against at-least 0.6`, **adjudicated entirely from two numbers nobody measured**, and its primary test PASSES on the same invented figures — a fabricated pass sitting beside a fabricated failure. `raking_cornice_member_count: 0` convicts every gable-roofed plan of a pediment fault; `gutter_outlets: 2, overflow_scuppers: 0` convicts both plans of a gutter fault, and nothing in the corpus models a gutter.
    **And a third state disappears entirely.** `mcp_server/core.py:514` builds its buckets from `evaluated` and `need_measurements` only, so a test returning `{"status": "error"}` lands in NONE of `faults_present`, `faults_clear`, `could_not_judge` or `summary`. It is reachable today: `dormer-off-the-bay`'s primary test divides by `dormer_count`, which is the invented 0, so the primary vanishes without trace and the fault is reported present on its secondary alone. That is unjudged-as-ABSENT, which is worse than unjudged-as-passed because nothing in the output hints that anything was missing. Separately `core.py:440` crashes with a `TypeError` on a measurement supplied as `null` — the natural JSON encoding of "I could not judge this" — because `missing` is built by key presence and the comparison sits outside the try block.
    **Why this is not fixed in the audit that found it.** Every one of these is PRE-EXISTING, none was introduced by WP-4.6, and the fix is not a patch: a measurement that must be ABSENT rather than zero changes what the fault corpus reports on every plan, which moves the reference-corpus expectations, `plan_check` counts and a large number of pinned tests. That is a work package with its own re-pinning, and doing it inside an audit would mean changing the critic's output while nobody was looking at whether the new output is right. It is recorded here in full, with file and line, rather than half-fixed.
    **The shape of the fix, so the next person does not re-derive it.** (a) `elevation.py` must emit no key at all where the generator did not model the thing — the corpus already has the right pattern, since `visible_chimney_count` is correctly absent two lines away. (b) `core.py`'s `_eval_test` must treat a present-but-`null` value as missing, and `check_measurements` must give `status: "error"` a bucket of its own. (c) A test should assert, for every fault the corpus ships, that each measurement it names is either produced by a generator that claims to model it or reported as could-not-judge — the check that would have caught all six at once.
    **Two smaller members of the same family, recorded so they are not lost.** `build/plan_check.py:426` reads `cl = it.get("clearance_in") or 0`, turning an unstated furniture clearance into a measured zero — 20 of 278 catalogue entries omit the key and 15 of those are actually checked; all 15 are pseudo-items where zero is right, so there is no live false pass, but nothing distinguishes "0 by intent" from "author forgot". And `build/roof.py:313, 419, 422, 443` fall back to hardcoded bands when a corpus lookup misses, then report `"computed": True` with an evidence string naming the corpus file — so a reworded rule would produce a confident answer from a stale copy of today's numbers. All four lookups match today; the fallbacks are byte-identical to the corpus values, which is exactly what makes the rot undetectable.

51. **RULED 25 Aug 2026 — adjudicate first, flip to opt-in second. FIRST PASS RUN 26 Aug 2026, AND IT FOUND THE RULING CAN ONLY DO HALF ITS JOB — a decision is wanted on that.** Eleven `storey-graduation` gaps were adjudicated against each node's own record and endorsed: **unendorsed 233 → 222, endorsed 61 → 72**, all three numbers re-pinned. The evidence for each, and the one node deliberately left unendorsed because its own record points both ways (`english-baroque`, whose `distinguished_from` uses graduated storey heights as the tell that separates GEORGIAN from it, while its own ratios graduate a basement against the principal storey), are in `docs/reports/oq-51-first-adjudication-pass.md`.
    **The finding, and it is worth more than the eleven. The ruling gives three moves for a gap — endorse it, bind the right pack instead, or scope the edge — and only the first exists.** Endorsing is one line in `applies_to`. Binding a different pack does NOT remove the wrong one; the cascade still delivers it and the new binding merely competes on precedence. And scoping the edge is not implementable: a lineage edge carries `inherits_kit`, which governs the KIT cascade, and nothing that governs the PACK cascade — `inherits_packs` exists in this register's text and nowhere in the schema or the code, verified by grep. **So a node the pack fits can be settled in a line, and a node it does not fit cannot be settled at all.** The clearest cases are not marginal: `ranch-style` ("everything about it is horizontal: a single storey"), `craftsman-bungalow` ("one to one and a half storeys"), `california-bungalow` and `minimal-traditional` all receive a rule for graduating a stack they do not have, and `ranch-style` is this entry's own worst-case node.
    **Why that matters to the ruling rather than only to the work.** Working the backlog in leverage order will settle the endorsable half and leave the refusable half untouched — and it will fall FASTEST on the nodes where the cascade was already right, because those are the ones easiest to evidence. The corruption the meter exists to measure is concentrated in exactly the cases the meter cannot be moved on, so a falling `unendorsed` does not mean a corpus getting more correct at the same rate, and the number does not say so.
    **The proposal, for a ruling rather than built unilaterally.** The refusal half needs no flip: an additive per-node **declined-packs** list — a node naming a pack that reaches it by descent and does not belong, with a stated reason — would let the wrong half be adjudicated now, strand nothing, and let `check_inheritance` count a declined gap as judged. It mirrors the endorsement exactly and would make "adjudicate first" finishable. The full opt-in flip stays where the ruling put it, at the end.
    **One clean sub-case for whoever takes the next pack:** all 13 of `facade-gable`'s unendorsed gaps arrive from a single ancestor, `gothic-revival-british` — one judgment about one edge rather than thirteen about thirteen nodes. And grouped by ANCESTOR rather than by pack, `english-georgian` alone delivers 46 of the 233. *Original entry follows.*<br><br>**RULED 25 Aug 2026 — adjudicate first, flip to opt-in second.** Of the three mechanisms this entry proposed, (b) is the destination and none of them is the next step. Lucas's ruling: leave pack inheritance as it is for now and work the backlog of gaps **nobody has judged**, in leverage order; where the inherited pack is right for the node, add the node to that pack's `applies_to` — which is the adjudication, not bookkeeping, and moves the gap from unendorsed to endorsed; where it is wrong, bind the right pack on the node or scope the edge. When `unendorsed` approaches zero, add `inherits_packs` and make inheritance opt-in — by then it is a no-op safety net rather than a cliff that strands 294 role-gaps in one commit. The cost is 233 authoring judgments spread across packages plus a small schema change at the end; the accepted risk is that wrong dimensions keep arriving while the backlog is worked, which is tolerable *only* because they are now counted and pinned.
    **What the ruling required, built the same day.** A gap is not automatically wrong: if the inherited pack's own `applies_to` names the node, an author DID judge that the pack belongs there and the cascade merely delivered what was intended. Counting those as faults would make the meter unreadable and the work list wrong. So `check_inheritance.py` now splits the 294: **61 endorsed, 233 unendorsed**, the third number pinned alongside the other two, and `--unendorsed` prints the work list grouped by pack, because adjudicating one pack settles every node under it. The head of that list: `storey-graduation` **38**, `opening-proportion` **23**, `trim-classical` **16**, `chambers-ionic` **15** (on `carpenter-gothic` and both Gothic Revivals), `facade-gable` **14**, `sash-light` **12**, `brick-course` **11**. Two bugs in the checker were fixed while there: `--strict` compared the ratchets as a TUPLE, which is lexicographic and would have let `inherited_packs` double unnoticed so long as `role_gaps` had fallen by one, and its `role_gaps` threshold was 329 against an actual 294 — a ratchet with 35 units of slack in it is not a ratchet. Each number is now its own comparison.
    *The measurement and the three costed mechanisms follow.* **Measured, diagnosed and ratcheted 25 Aug 2026.** Of this entry's three proposals, none was taken yet and a fourth was: `build/check_inheritance.py` now reports what the cascade delivers, names the ancestor that decided it, and pins two numbers so the backlog cannot grow silently. The same shape as OQ 48's handling, and for the same reason — all three fixes are large and the diagnostic is not.
    **The two numbers, pinned in `tests/test_wp46_packs.py`.** `role_gaps` **294**: (node, role) pairs where an ancestor fills a role the node never bound — 58 secondary, 51 massing, 49 primary, 43 facade, 42 opening, 26 interior, 25 room. `inherited_packs` **3,367**: packs arriving purely by descent. Both should go DOWN as nodes are authored; a rise means the cascade has papered over something new.
    **Why the three proposals were all costed and none taken.** (a) scoping `descends_from` would need an authoring judgment on essentially every trunk edge — **127 of 132** nodes receive a pack by descent. (b) opt-in inheritance is the cleanest mechanism and strands ~294 role-gaps in one commit. (c) requiring every node to bind its own roles is the same backlog without the mechanism change. Each is a package; the diagnostic is a morning.
    **What the report shows, and it is worse read node by node than in aggregate.** `python3 build/check_inheritance.py --slots ranch-style`: 69 of 78 dimensioned slots governed by a pack it never bound — its `arch` from `opening-pointed`, a Gothic strike ratio, bound on `gothic-revival-british`; its `baseboard`, `casing` and `chair_rail` from `gibbs-ionic`, bound on `colonial-revival`. `--roles` shows `egyptian-revival` taking **all seven roles** from `greek-revival-american`, which is how a node held out of the binding count on purpose was receiving a full classical kit the whole time.
    *Original entry follows.*<br><br>**OPEN — the lineage cascade delivers proportion packs nobody bound, and "131 of 132 bound" never measured whether a node RECEIVES rules.** Found on 25 Aug 2026 while closing OQ 49, and the way it was found is the point. `egyptian-revival` was the corpus's one deliberately unbound node, held out by WP-4.1 under a note saying nothing in the library fitted its trabeated order. Binding it scoped, then rebuilding and reading the resolver's provenance, showed that **`facade-peristyle` was already reaching it from five ancestors** — `greek-revival-american`, `english-palladian`, `palladian`, `roman-classical`, `greek-classical` — every one of them unscoped, and carrying an entasis rule the node's own c04 forbids in so many words. **The deliberate refusal was cosmetic.** `proportion_packs` being empty on a node means the node's own array is empty; `build/resolve_kit.py::resolve_packs` walks the whole `_cascade` and takes the nearest binder of each pack id, so a node with no bindings of its own inherits its entire ancestry's.
    **Measured, and the number that matters is the second one.** Summed over 132 buildable nodes, **3,367 pack-arrivals come only through the cascade** — that is arrivals, not decisions, and on its own it proves nothing. So the governing decision was measured instead, evaluating each node's slots the way `resolve_kit` does:

    | node | slots dimensioned | governed by a pack the node never bound |
    |---|---|---|
    | `ranch-style` | 78 | **69** |
    | `pueblo-revival` | 79 | **53** |
    | `egyptian-revival` | 75 | **74** |

    *(Corrected 25 Aug 2026 by audit: these read 68 / 52 / 74 when first published. `--slots` was
    reading the node's OWN kit file where `resolve_kit.main` reads the CASCADE-RESOLVED record, and
    `choose_pack` consults the record's `packs` block — a person's explicit ruling — before
    precedence. On any slot whose ruling was inherited rather than restated the two disagree, and
    five of ranch-style's slots took a different branch. The diagnostic for a cascade problem was
    itself reading past the cascade.)*

    On `ranch-style` those include `opening-pointed` — a Gothic arch strike ratio — governing **15** slots and `gibbs-ionic` governing 9. On `pueblo-revival`, `vignola-composite` governs 8. An adobe Pueblo Revival house dimensioned by Vignola's Composite, and a Ranch by a pointed-arch pack, are not subtle drift; they are the compiler producing the wrong building silently.
    **And nothing human decided any of it.** `choose_pack` has three routes: the slot's own `packs` block (a person's ruling), `proportion_packs.precedence`, and unresolved. On all three nodes sampled, **every single slot resolved by precedence alone and not one carried a slot-level ruling** — and the precedence numbers driving it were authored on ANCESTORS, for the ancestors' buildings, with no view of the descendant.
    **What is not known**, and it is most of it: this was measured on three nodes chosen because they had the most inherited-only packs, so they are the worst case rather than the typical one. How many of the 132 are materially wrong, and how many inherited packs are harmless because the node overrides the slot elsewhere or because the rule is generic, has not been measured. Nor has whether the resulting dimensions actually differ — an inherited `sash-light` may give the same answer a bound one would.
    **It is the same class as two things already recorded and larger than both.** CLAUDE.md's standing trap is that `hybridizes_with` transmits a donor's whole kit; OQ 36 scoped that edge; OQ 49 has just scoped a binding. This is the same failure in the third place — the `descends_from` cascade for packs — and it is the one with 3,367 instances rather than twenty-six.
    The concrete proposals, so the next person does not re-derive them: (a) give `descends_from` the same `slots` scope OQ 36 gave `hybridizes_with`, and scope the trunk edges that carry classical orders down into vernacular and modern branches; (b) make pack inheritance OPT-IN — a pack descends only where an edge says `inherits_packs: true`, which is what `inherits_kit` already does for the kit cascade and would make the default honest; (c) leave inheritance alone and require every buildable node to bind, at its own level, every ROLE it needs, so nothing important is ever decided by an ancestor's precedence number. (b) is the cleanest and the largest: it would strand every node that currently relies on inheritance, which is most of them, and the stranding is the point — it would show how much of the corpus's binding coverage is real.

50. **RULED 25 Aug 2026 — stop at the principle; a fault would need the elevation layer to see ornament zones first.** Of the three answers this entry proposed, (c) is taken and (a) is deliberately not: the principle is stated once in `docs/model.md`, with its control case and its limits, so the next author meets it before writing a twelfth pack that rediscovers it. A fault was costed and refused for now — `build/elevation.py` contains **zero** occurrences of an ornament zone or anything like one, so `evenly-distributed-ornament` would mean first teaching the elevation layer where ornament IS, which is a modelling package in its own right and larger than the finding it would police. The entry stays OPEN on that one point: if the elevation layer ever gains ornament zones, the fault should be written, with the eight condemning records as its authority and `italian-renaissance` explicitly exempted. *Original entry follows.*<br><br>**OPEN — ornament works by being bounded, and it looks like a property of this corpus rather than a regional habit. Should it be a fault?** Noticed one pack at a time across WP-4.6 and measured on 25 Aug 2026. `facade-portada` found nine Spanish records saying it and read it as an Iberian characteristic; `corbel-course` found Mudejar, Anglo-American and Scottish records saying it and called it possibly general; `facade-medieval-english` found the Elizabethan frontispiece saying it too. This entry is the measurement that should have followed the second sighting.
    **The keyword sweep proposed 40 of 132 buildable nodes and reading disposed of 14 of them** — ornament that must be *sawn wood*, buttresses only where thrust exists, rafter tails that must be real (that is OQ 47's `member_status`), an arch's geometry. **26 nodes state a genuine placement-rationing rule**: ornament concentrated in named places with a plain field around it, across at least nine unrelated traditions — Iberian and colonial Spanish, English Georgian and Palladian, Victorian American, Scottish, Swiss, Norman, French, Mexican.
    **The strong form of the claim is the inverse, and it holds.** Ten statements across eight nodes explicitly condemn the opposite, in their own words: even distribution "destroys the hierarchy that carries the composition" (`beaux-arts-french`), "converts the complex into" another style (`california-mission-colonial`), is "a revival misreading" (`churrigueresque`), "misreads the tradition" (`mexican-colonial`), "destroys the composition" (`scottish-baronial`), "converts the building into a revival" (`spanish-colonial-american`), "means a later imitation" (`spanish-colonial-revival`), "destroys the style outright" (`spanish-plateresque`). **No node in the corpus requires ornament to be evenly distributed.**
    **And there is exactly one control case, which is what makes it a finding rather than an artefact of how the records were written.** `spanish-plateresque`'s `distinguished_from` names it: "Italian work regulates the whole facade by an order and distributes ornament across it." And `styles/italian-renaissance.json`'s own bay rhythm agrees — "even, additive and non-hierarchical: five to eleven identical bays across a front, with emphasis given only by centring the portal". So the corpus holds one tradition that genuinely distributes, it is named as the contrast by the tradition that most opposes it, and it is not a counter-example to the pattern but the pattern's boundary.
    **What is NOT claimed.** 26 of 132 is not "most of the corpus", and the other 106 are mostly silent rather than disagreeing — a node that says nothing about where ornament goes has not voted. The honest statement is conditional: *wherever a node in this corpus states a rule about where ornament goes, it states concentration, and eight of them say that distributing it destroys the style.*
    The question is what to do with it, and there are three answers. (a) **Author a fault** — `evenly-distributed-ornament`, firing where a composed elevation carries worked ornament in more than N zones with no plain field between, with the eight condemning records as its authority and `italian-renaissance` as its explicit exemption. That is the useful answer and it is the largest: the fault layer would need a way to see ornament ZONES, which `elevation.py` does not currently model. (b) **Leave it in the three packs' notes**, where it is true and inert. (c) **Promote it to a corpus-wide principle in `docs/model.md`**, stated once with its control case, so the next author meets it before writing a twelfth pack that rediscovers it. (c) is nearly free and should probably happen whatever is decided about (a).


## Reissued 25 August 2026 — ids 32–41 collided, and this block moved

*Two sessions worked in parallel without knowing of each other, and both issued the ids 32 to 41.*
*Main's ten (deployment, the workbench, the export layer, the surviving WP-2.3) were merged first
and keep their numbers; these ten — the WP-4.5 and geometry rulings, all of them already RULED or
CLOSED — were reissued as **54–63** when the branches met. Old id → new id: 32→54, 33→55, 34→56,
35→57, 36→58, 37→59, 38→60, 39→61, 40→62, 41→63. Every reference in this corpus was rewritten with
them; a commit message or a report written before the merge still carries the old number, and this
table is how to read it. The corpus's rule is that ids are stable and never reused, which is exactly
why one block had to move rather than the two being merged into one numbering.*

## From the real solver (WP-2.3, 24 Aug 2026)

54. **RULED 24 Aug 2026 — report it in the geometry report.** `geometry_report` gains an explicit under-band list, the way `build/solver.py`'s `unmet_requirements` already works. The search keeps its freedom to trade a room's size against everything else, which is what a heuristic is for, and stops doing it silently. Deliberately NOT the deeper fix of teaching `plan_check.py` to read `room.geometry`: that would have the critic score the drawn house rather than the declared one, which is a larger and separate change every plan layer would feel. *Original entry follows.*<br><br>**OPEN — the heuristic places rooms below their band and nothing says so.** `build/geometry.py` puts the spec Colonial's dining room at 91 sf against the 122 sf floor of its own catalog band — 26% short — and it does so on every one of the six seeds WP-2.3 tried. It can, because `level_score` charges a flat 12 points for a room under its band and a candidate can win the search while paying it. What makes this hard to see is that the plan *record* still declares 12 × 12: only the placement shrinks the room, and `plan_check.py` never reads `room.geometry` at all, so no layer of the critic is looking at the number that changed. `build/solver.py` refuses to do it — the room minimum is the one requirement it will not trade away, and the refusal is what turns an over-stuffed brief into a named conflict instead of a bad plan — but the heuristic remains the default engine everywhere, so the corpus's usual placement still does this silently. Three fixes suggest themselves and the choice is a judgment: (a) make the heuristic refuse too, rejecting any candidate with a room under its band, which is the most consistent but may leave some plans with no candidate at all and no explanation of why; (b) have `geometry_report` carry an explicit under-band list the way the solver's `unmet_requirements` does, which keeps the search's freedom while ending the silence; or (c) teach `plan_check.py` to read `room.geometry` when it is present, which is the deepest fix — the critic would then score the house that was drawn rather than the house that was declared — and much the largest, since every plan layer currently assumes the record's declared dimensions are the truth. Worth noting that (c) is the only one that would also catch this class of defect from any *future* engine. Surfaced by WP-2.3; nothing was changed on the strength of it.

## From partis to full coverage (WP-4.5, 24 Aug 2026)

55. **CLOSED AGAIN 26 Aug 2026 — the CP engine states the open void as a hard constraint, and the tolerance question was the whole finding.** The guarantee this entry was proudest of is back, and restoring it took two changes rather than the one the reopening predicted.
    **The constraint.** `build/geometry_cp.py` now reads each ground room's `_void` fact and states, for every upper room and every unroofed void below it, that the two must be separated on some axis. Roofed voids stay exempt — the Charleston single's upper piazza sits on its lower one — and a void over a void is fine. Measured on a courtyard house with two upper chambers: the CP engine leaves the court clear at **0.0 sf**, where the heuristic places a chamber across it at **296 sf** and pays its 40 points. That difference between scoring and proving is the line this entry always turned on.
    **What the reopening did not foresee, and it is the useful half.** The obvious move was to mirror `geometry.py`'s own overlap tolerance so the two engines agree about what "over" means — it charges only past a 1.0 ft overlap. Doing that was wrong twice over. **A scoring noise-tolerance is not a placement licence**: a foot of bedroom over a courtyard is a foot of floor with nothing under it. And it broke the guarantee downstream in a way that took instrumenting the solver to see — the solver placed a chamber exactly 1 ft into the court, which its constraint permitted, and then `_absorb`, the post-solve pass that grows rooms into leftover space, **grew that chamber straight through the hole**, 310 sf of it. Its keep-out limiter only stops a room that has not already crossed the line. **Proven, and then undone by a post-pass** — the same shape of defect as OQ 52, and just as invisible, because the record it writes looks exactly like a solved plan.
    So the separation is strict (rooms may abut a void's edge and may not enter it by any amount), and `_absorb` gained a `keepout` argument: the ground level is absorbed first and its open voids limit the storey above in all four directions, exactly as a sibling room does. A guarantee that does not survive into the drawing is not a guarantee.
    **A third thing had to move, and it is worth stating because it would have read as a regression.** The coverage floor requires each level to fill 97% of the block. On a courtyard plan that contradicts the new constraint outright — the storey is required to fill the block AND to leave the hole empty — and CP-SAT correctly reported a perfectly buildable brief as infeasible. The hole now comes off the upper level's coverage target.
    **What is still stated rather than solved:** the RING. `courtyard_slice()` states it as a guillotine tree because four thousand random candidates never find one, and the CP engine inherits that through the heuristic hint. That half was named in the reopening as better left stated, and it is left stated. **And the heuristic remains the default engine** (OQ 54), so the 40-point charge is still what most callers get — which is now the sharpest available argument for changing that default. *Original entry follows.*<br><br>**PARTLY REOPENED 25 Aug 2026 by the merge — the CP engine no longer enforces the open void.** This entry closed with the guarantee stated in BOTH engines: the heuristic charged 40 points for a room over an unroofed void and `build/solver.py` stated it as a hard constraint, and that difference between scoring and proving was the line this entry was proudest of. `solver.py` was deleted when two independently-built WP-2.3 implementations met on 25 August and Lucas ruled that `build/geometry_cp.py` — merged first, twice audited, already the default — is the one that survives. The surviving CP engine reads no `_void` fact at all: it contains zero occurrences of the string. So the hard constraint is gone and only the heuristic's 40-point charge remains, which a candidate can buy its way out of. The ring is likewise the heuristic's alone (`courtyard_slice()` states it as a guillotine tree because four thousand random candidates never find one). `_finish()` still derives `void_area_sf` and `heated_area_sf` from the shared footprint, so a CP-produced record does not silently report the gross block as conditioned space — the accounting survived; the placement guarantee did not. `tests/test_voids.py` pins both halves, the surviving charge and the lapse, and the lapse test fails the day somebody teaches the CP engine about voids, which is when this note should be rewritten. **What it would take:** the open-void constraint is one no-overlap between each upper room and each unroofed ground void, which is the shape `geometry_cp.py` already states for same-level rooms; the ring is the harder half and may be better left stated rather than searched, exactly as the heuristic does. *Original entry follows.*<br><br>**CLOSED 24 Aug 2026 — reserved voids are built, in both engines, and building them found three more things.** Option (a) as ruled. An outdoor room whose own record says it sits within the block now takes a rectangle like any other room; the block is sized to hold it; `footprint.area_sf` keeps its meaning as the gross block and `heated_area_sf` states what it heats; the wall between a room and a court is an EXTERIOR wall in the structure pass; the roof pass records the hole rather than spanning it silently; and the drawing hatches it open. Nothing may sit over an unroofed void — charged 40 in `geometry.py`, stated as a hard constraint in `solver.py`, which is the difference between the two engines in one line. A roofed void is deliberately exempt, because a Charleston single's upper piazza sits on its lower one.
    **Three findings from building it, all of which changed the implementation:**
    (i) *Placing a void needed one fact per room that nothing carried* — whether it sits inside the block (a court, a piazza) or is appended at grade (a terrace) — and independently, whether it is roofed. Added as a `void` block on the four outdoor rooms. This is NOT option (b): `function_class` is untouched and nothing was re-typed. A room with no `void` block keeps the pre-ruling behaviour and stays out of placement, because a room nobody has judged must not be silently promoted into the footprint.
    (ii) *On a courtyard massing, `depth_rooms` describes the RANGE, not the block.* `courtyard-full` and `courtyard-u` both read `single-pile`, correctly — the ranges around a court ARE one room deep. Feeding the court's area into a 22 ft depth target produced a **121 x 24.6 ft strip with the patio inside it**: the right area, a shape that is not the diagram, a house nobody could build. The massing's own `footprint` field ('courtyard' has ranges both sides of the void, 'U' one) now sets the block's depth target, and the bay count is re-chosen against `rooms/courtyard.json`'s own 1.0–2.2 proportion band — left to the ordinary growth loops the court came out 16 x 40, in band for area and out of band for the one ratio the room's own record calls the number the diagram turns on.
    (iii) *The heuristic cannot find a ring.* Four thousand candidates put the court in the block's SW corner against two exterior walls every time: isolating one room into the innermost cell of a four-deep nesting is not a thing random guillotine partitions do. A scoring penalty alone would have charged for a defect no candidate could avoid. So `courtyard_slice()` states the ring as a guillotine tree — cut at the court's south and north edges, then cut the middle band at its west and east — and the ordinary search still slices the ranges, which is what it is good at. `solver.py` needed nothing for this: it reads its topology off the heuristic's layout, so it inherited the ring. When the block cannot hold the declared court with ranges deep enough to be rooms, `ring_depth()` returns None, the ordinary slicer runs, and the report says the court is a notch and to read the drawing before believing the plan.
    *Original ruling:* Option (a). Both placement engines carry outdoor rooms as placed and dimensioned *voids* — excluded from the area budget and from the heated envelope, drawn as open. It is the honest answer, and it fixes every porch, piazza and terrace in the corpus rather than only the 19 courtyard nodes that made it visible. It touches both engines, the renderer, and the structure and roof passes that read the footprint, and that cost is accepted. *Original entry follows.*<br><br>**OPEN — the courtyard is a void the geometry engine cannot draw.** `build/geometry.py`'s `prepare_rooms()` and `build/solver.py` both drop every room whose `function_class` is `outdoor` before placement, because an outdoor room is not part of the heated envelope and does not belong in the area budget. That is right for a terrace and wrong for a courtyard: in the 19 nodes whose canonical massing is `courtyard-full` or `courtyard-u`, the court is the thing the house is built around, and a plan that omits it is not a simplification of the diagram but a contradiction of it. The composed courtyard plans therefore score, place and render as a solid block with rooms packed against each other where the void should be, and the drawing — which this corpus insists is a render of the data — shows a house the record does not describe. The problem is pre-existing rather than introduced here (`charleston-single-piazza`'s piazza has the same status today, as does every porch in the catalogue) but WP-4.5 makes it the dominant case rather than an edge one. Three ways out, and the choice is a judgment: (a) let the placement engines carry outdoor rooms as *reserved voids* — placed and dimensioned, excluded from the area budget and from the envelope, drawn as open — which is the honest answer and touches both engines plus the renderer and the structure/roof passes that read the footprint; (b) introduce a `void` function_class distinct from `outdoor`, so a court is handled differently from a terrace, at the cost of a room-schema change and a re-typing pass; or (c) accept that this corpus draws courtyard houses wrong and say so on the drawing. Nothing was changed on the strength of it.

56. **RULED 24 Aug 2026 — add `level_offset_ft`.** `level` stays an integer and keeps its current meaning, driving stacking, vertical alignment and storey heights; an optional `level_offset_ft` sits beside it, defaults to 0, and is read only by the section and the stair check. Every existing parti and every consumer works unchanged. The split-level parti becomes writable once it lands. *Original entry follows.*<br><br>**OPEN — there is no half-storey, so the split-level cannot be expressed.** `schema/parti.schema.json` types `rooms[].level` as an integer and every consumer reads it as a level index — `compose.py` maps `{0: "ground", 1: "upper", -1: "cellar"}`, `geometry.py`'s `prepare_rooms()` keys its working dict by it, `solver.py` inherits that. A split-level Ranch, which `PLAN-OF-ACTION.md`'s WP-4.5 text names among the partis to add, is a house whose defining property is that two of its floors are half a storey apart, and there is nowhere in the record to say so. It was left unbuilt rather than faked: the type unblocks **zero** nodes on this package's own criterion (its two `common` styles are already covered by `ranch-tripartite`, and no style has `split-level` as canonical), so spending a schema change to reach it would have been paying a real cost for no coverage. The concrete proposal for whoever rules, so the next person does not re-derive it: `level` stays an integer and keeps its current meaning, and an optional `level_offset_ft` is added beside it, defaulting to 0 — the level index continues to drive stacking, alignment and the storey heights, and the offset is read only by the section and the stair check. That keeps every existing parti and every consumer working unchanged.

57. **RULED 24 Aug 2026 — let adjacency rules declare a vertical relation.** Add `relation: "above"` / `"below"` to adjacency rules and teach `plan_check.py` to evaluate them across levels. `schema/parti.schema.json`'s own `adjacencies` array already anticipates exactly this and `five-part-palladian` already uses it for its stair. Both of WP-4.5's workarounds come out: `rooms/overlook.json`'s hall rule goes back to `strong`, and the Tudor great chamber stops being typed `library` to dodge a ground-floor rule it can never satisfy. *Original entry follows.*<br><br>**OPEN — adjacency cannot span levels, and no room in the catalogue is an upper-floor state room.** Two findings from WP-4.5 with one cause. First: `rooms/overlook.json`'s defining relationship is that it is open to the hall on the storey *below* it, and `plan_check.py` evaluates adjacency within a level, so a `should_adjoin` written for that relationship can never pass on a correct plan — it reported `serious` on the one arrangement the room exists for, and was held at `preferred` rather than `strong` to stop a right answer being called a defect. Second: all thirteen public and living rooms in the catalogue carry a ground-floor adjacency rule — `drawing-room` must reach a dining room, `parlor`/`best-parlor`/`morning-room` an entrance-hall, `sitting-room` a parlor, `hall` a cross-passage — so the great chamber of a Tudor H-plan, which is the best room in the house and sits over the parlour, can satisfy none of them; typed `drawing-room`, which is what it most nearly is, the plan trips a **fatal** for not reaching a dining room a storey beneath it. It is currently typed `library`, whose rule is `strong` rather than `hard`, so the same impossibility reports as serious and the diagram stays composable — a workaround, and recorded as one. Both would be answered by the same change: letting an adjacency rule declare that it is satisfied vertically (`relation: "above"` / `"below"`, which `schema/parti.schema.json`'s own `adjacencies` array already anticipates and `five-part-palladian` already uses for its stair) and teaching `plan_check.py` to read it. Whether the room catalogue should ALSO gain an upper-floor reception room is a separate and smaller question that this one probably subsumes.


## Ruled with the rest, 24 August 2026

58. **RULED 24 Aug 2026 — spend the schema change: a slot-scope allowlist on `hybridizes_with`.** The problem, found in WP-4.2 and patched twenty-six times at the node rather than once at the mechanism: an edge drawn to carry one narrow aspect of a donor's practice transmits that donor's *entire* kit, because the cascade cannot partition a donor's bindings by which aspect the edge was drawn for. Greek Revival's hybrid edges bring Greek Revival's whole cornice apparatus with them whether or not the edge was about cornices. The ruling is to add an optional slot-scope allowlist to the edge, so `hybridizes_with` carries only the slots it was drawn for and carries everything only when no allowlist is given. That fixes the class rather than the instances, makes the twenty-six node-level patches removable one by one as each edge gains its scope, and — the reason it is worth a schema version — stops the next kit author rediscovering the same thing from scratch. The alternative considered and rejected was narrowing `hybridizes_with` to carry no kit content at all, like `references`: that would be cheaper but would lose the genuine partial inheritance the edge exists to express, and the corpus does have edges that legitimately transmit several slots. Costs a schema bump and a re-resolve of every kit with a hybrid edge in its chain.

59. **CLOSED 24 Aug 2026 — all five repaired, the check that would have caught them added, and two of the five were not the partis' fault.** `build/check_partis.py` gains check 10: every parti is instantiated against the first style in its own `styles` array and run through `plan_check`, and a fatal is an error. It runs in about six seconds for the whole catalogue and now reports **21 of 21 composable**. Everything else in that file checks a parti is well FORMED; this checks the diagram WORKS.
    **The check is differential, and had to be.** Three of `cape-central-chimney`'s four original fatals fire identically for *every* parti composed for `cape-cod-colonial` — they are facts about that style's kit and elevation. A check that blamed the parti for them would generate false accusations against exactly the files it exists to protect, so each parti is compared against a control diagram run on the same style, keyed on the requirement rather than the measured value.
    **What was actually the partis':** `charleston-single-piazza` had no door between its two principal ground rooms (a single house is one room wide with the stair hall on the piazza side, so they share the wall between them and are connected through it); `creole-gallery`'s repeating `chamber` generated `chamber1`/`chamber2`, the ids of two chambers it already named, so every Creole brief for more than two bedrooms built a plan with duplicate room ids; `ranch-tripartite`'s family room reached the kitchen only across the eat-in area, and the validator counts a hop only through CIRCULATION; `five-part-palladian` typed both hyphens as `back-hall`, whose own hard rule is must_adjoin kitchen by direct door — so both wings demanded a kitchen at their own end and only one wing has one. That last is an unsatisfiable requirement produced by naming a room wrongly rather than by drawing it wrongly: a hyphen is a connecting gallery, and it is now `gallery-corridor`, with the service rooms regrouped into the one dependency that has the kitchen.
    **Three validator facts had to change with them, each a rule asking for something a correct plan structurally cannot have.** `landing` → `stair-hall` is now `vertical_ok` (a landing is the top of a stair and the stair hall is a storey BELOW it, so read as a same-floor rule only a house with a second stair hall upstairs could satisfy it — OQ 35 built the mechanism, nothing had used it). `butlers-pantry` → `kitchen` gains `via: [back-hall, gallery-corridor]` (where the kitchen is in a dependency the pantry is in the block and the kitchen is in another building; without it the rule asked the pantry to be in two buildings at once — the same principle the room already embodies at the other end of its own run). And `centre-passage` joins the `entrance-hall` EQUIVALENT group: the front door opens into it and every principal room opens off it, which is the definition that group holds. That last removed 2 and 3 spurious *minor* findings from the two shipped plans with fatal and serious unmoved, which is what says it removed noise and not signal.
    **Two of the five were the corpus's own first discipline inverted — unjudged reported as evaluated-and-failed**, and those are the more dangerous kind. `roof.py` models gable-end stacks only and every branch that returns an empty position list says in its own note that it did NOT model the case; `elevation.py` reported that as `visible_chimney_count: 0`, which handed the fault corpus an evaluated measurement where it had none, and `faults/chimney-omitted.json` duly fired **fatal on a parti named cape-central-chimney for having no chimney**. It is None now, and `plan_check` drops a None measurement rather than letting the evaluator throw on it. Separately, a fault finding quoted `results[0]` — a fault with secondary tests can have its primary PASS and a secondary fail, and the report then printed the passing number as the evidence: *"The House With No Fire: 2 against at-least 1"* on a Cape with two chimneys, a sentence in which every number is right and the claim is nonsense. `core.check_measurements` now returns `failing` beside `results` and the finding quotes the test that failed.
    **Outcome:** a Charleston brief returns the Charleston single, a Creole brief the Creole gallery, a Cape brief the central-chimney Cape. A `ranch-style` brief still returns `bungalow-open-linear`: the ranch parti carries 15 serious findings against the bungalow's 3 and no fatal is involved, so that is the composer's weighting working as designed on a diagram that is genuinely rougher, not a defect of this kind. Left as it is. *Original entry follows.*<br><br>**Five of twenty-one partis carry fatal findings against their own native style.** Measured 24 Aug 2026, each parti instantiated against the first style in its own `styles` array and run through `repair` and `reclaim`: `cape-central-chimney` 4 fatal, `five-part-palladian` 4, `charleston-single-piazza` 2, `creole-gallery` 2, `ranch-tripartite` 1. The other sixteen are clean. A fatal is 100 points and nativity is worth at most 140, so a Charleston-single-house brief gets `centre-passage-single-pile` and a Creole brief gets `bungalow-open-linear` — the composer working exactly as designed on data that is wrong. The failures are door-graph facts inside the parti files, not scoring: `charleston-single-piazza` has no door from its drawing room to its dining room and none from its upper hall to its stair hall. **This is not the borrowed-diagram problem WP-4.5 fixed** — those styles now HAVE a native parti and it is the parti itself that fails. Pre-existing and unrelated to OQ 33 (verified against the pre-OQ-33 tree). `build/check_partis.py` checks the door graph resolves; it does not check that a diagram satisfies the room-adjacency rules `plan_check.py` will hold it to, and that check is what would have caught all five at authoring time. The question is whether fixing the five and adding that check is its own package or belongs to WP-4.6.

60. **CLOSED 24 Aug 2026 as a standing disclosure — ruled: leave it stated.** Same standing as the wing/hyphen simplification this file already carries. The record says what it does not model, which is the discipline working rather than a gap in it; revisit when roof geometry is the subject rather than a dependency. *Original entry follows.*<br><br>**The roof over a court is stated but not derived.** `build/roof.py` computes one ridge over one rectangle. Over an open court it does not: the real roof is ranges around the void, each with its own eave, meeting in valleys, and in the Spanish and Mediterranean cases falling inward to drain to the court. OQ 33 put the hole in the record with its own dimensions and labelled every height figure as computed for a roof that spans the whole block — which over the court it does not — so nothing is silently wrong. But `grade_to_ridge_ft` for a courtyard house is now a number that is explicitly not describing the building. The same is true, less visibly, of `elevation.py`'s silhouette. Deriving ranged roof geometry is a real package (valley lines, four eaves, the inward-pitch case) and it should be ruled whether it belongs to Phase 5 or is left standing as a disclosure the way the wing/hyphen simplification already is.

61. **CLOSED 24 Aug 2026 — four loggia records, as ruled.** `partis/courtyard-and-portal.json`'s walk is now `walk-s`, `walk-w`, `walk-e`, `walk-n`, doored to each other so the ring is continuous, to the court, and to the rooms each range serves. What is lost is the true statement that it is ONE room; what is gained is a diagram whose circulation reaches its own rooms. `courtyard_slice()` reads the assignment rather than guessing it: the i-th walk takes the i-th band (the parti lists them in the engine's own band order, S W E N, so the range holding the entry passage is the one on the street) and every other room goes to the band of the walk it doors to, a room with no walk door following the room it does open off. **Two things had to change with it.** The ranges are not equal — a full courtyard's street and rear ranges run the block's whole width while the side ranges run only the court's depth, so they hold about twice the area — and four rooms in a side range made that corredor swell to 385 sf to fill a band its rooms could not; the rooms are now distributed against the band areas. And the four weights sum to 0.22, not the single record's 0.12: a corredor that actually rings the court is its perimeter times its depth, about 4·√(0.18·G)·7 for a house of gross area G, which is 0.21–0.22 across this parti's whole area range. The old figure was the weight of a walk along ONE side, and understating it does not make the corredor smaller — the ring geometry gives it the band area its rooms do not fill either way — it makes every room in the ranges smaller. *Original entry follows.*<br><br>**A portal range is one rectangle, and a portal range is not one rectangle.** `partis/courtyard-and-portal.json` gives the covered walk a single room record (`walk`, a `loggia`), and the parti's own note says that walk IS the house's entire circulation: "every room is entered from it and from nothing else." A continuous roofed edge around four sides of a court cannot be one rectangle, so the placement engines put it along one side and the other three ranges are entered from nothing. The plan validator does not catch it because the door graph is satisfied by the record, not by the geometry — `plan_check.py` never reads `room.geometry` (the same blind spot OQ 32 named). Three ways out and none of them is obviously right: give the parti four loggia records and lose the fact that it is one room; let a room carry more than one rectangle, which is a schema change with reach into every consumer; or accept it and say so on the drawing. Raised rather than decided, because the first option changes what a room IS in this corpus.

62. **CLOSED 24 Aug 2026 — a real share, with partial coverage allowed.** A room WITH an `area_weight` now takes that share of the brief's target, clamped to its own catalogue band, and is **frozen before the scaling loop runs** — a real share is not renegotiated by a global factor, and that is the whole of what changed. Rooms without a weight split whatever is left, exactly as before, so the **eleven of twenty-one partis that state no weights behave identically to yesterday**. A void's weight is a share of the same number and not of a gross the brief never states: the court is 0.18 of the house that was asked for, and that the house also has a court is what makes the block bigger than the brief (OQ 33) — sizing the court against the block would be circular.
    **Measured, against each parti's own `area_range_sf` midpoint rather than one arbitrary brief:** eighteen of twenty-one land within 12% of target and most within 1%. Three do not, and each says something true. `courtyard-and-portal` misses by 41% at a 5,400 sf brief — **and missed by exactly 41% before this change too**, so it is not a regression: its seventeen rooms cannot be a 5,400 sf house at any weighting, because that diagram grows by having more rooms and the composer instantiates a fixed list. `great-hall-h-plan` went from −0.4% to −12.1%, which **is** the cost of the ruling: freezing weighted rooms at their bands leaves less for the unweighted ones to recover. `hall-and-parlor` moved 12.0% → 14.1%.
    **A weight its own band cannot honour is now reported, not split quietly.** The composer logs a JUDGMENT naming every room, what its weight asked for and what its band gives — *"Sala wants 756 sf, band gives 480"* — so the area miss that follows is an explained number rather than a mysterious one, and it ends with what the reader needs to know: the brief is asking this diagram for a house it does not grow into by making its rooms bigger.
    **The alternative reading, considered and not substituted:** treat the weights as RELATIVE shares and scale them as a group to hit the target, which would keep the ratios the author meant and recover the 11.7 points `great-hall-h-plan` lost. The ruling was the absolute reading — that share of that target — and implementing something else because it measured better would be answering a question that was not asked. Recorded here so the choice stays visible. *Original entry follows.*<br><br>**The composer's `area_weight` is a share of a total nobody states.** Found while fixing the target-is-heated-area bug (OQ 33/39). `instantiate()` sizes each room from its catalogue default, nudges it 10% if the parti gives it an `area_weight` at all, and then scales everything by one factor to reach the brief's target — so a room's `area_weight` never actually sets its share of anything. It is read as a *boolean*: has a weight, or has not. The courtyard parti's weights (0.18 court, 0.07/0.07/0.04/0.04 corredor, 0.14 sala…) read as a considered distribution and are not one; they were tuned in this package against band capacities and the tuning had to be done by measuring the output, because nothing consumes the numbers. Either make the weights real (each room's target area is its weight times the brief's, clamped to its catalogue band) or stop writing them as fractions and say in the schema that the field means "this room is one the diagram sizes deliberately". The first is the honest reading of what a parti author thinks they are writing; it changes the sizing of every parti in the corpus and needs measuring before it is chosen.

63. **CLOSED 24 Aug 2026 — `applies_to_styles` on a test, and six tests scoped. The sweep is six and not twenty, which is itself the finding.** An optional `applies_to_styles` list joins the test object in `schema/fault.schema.json`; **absent means every style the fault applies to**, so every existing fault stays valid. `mcp_server/core.py` filters the test list by it before evaluating, and a test for another style is **not run** rather than counted as passing — a test that is not for this house says nothing about this house.
    **Measured: 17 of 129 styles could not return a clean plan under their own native diagram; it is 15 now**, and the two faults responsible for 11 of the findings — `truss-flattened-pitch` (9 styles) and `chimney-omitted` (2) — no longer fire on styles they were not written for.
    **Only six of the twenty-four candidates were scoped.** Twenty-four secondary tests name a style in their own note, and scoping on that keyword would have been worse than scoping nothing: most name a style as *context* (`muntin-wider-than-its-date` — "Federal and Regency sit at the bottom of it"), as a *reference band* (`six-panel-door-everywhere`), or as **the very case the test exists to discriminate** — `frieze-as-fascia-board` separates a genuine Greek Revival frieze-band window from a collision, and scoping it to Greek Revival would remove the case it is for. The six that name a style as a SCOPE: the Tudor chimney-breadth ratio and the Prairie visual-mass test on `chimney-omitted`; the `PUEBLO REVIVAL ONLY, and inverted` parapet rule, which is the only test in the corpus that fails a house for being level and was failing every correctly-built Territorial and Mediterranean parapet there is; the New England Georgian cornice return band; the Georgian dormer-and-eave rule, which a Shingle Style eyebrow and a Craftsman shed dormer break by design; and the Georgian pitch band.
    **Two things deliberately not done, both recorded rather than guessed.** (i) `truss-flattened-pitch`'s note asks for a per-style *substitution* — "Cape 36.9-45, Tudor Revival 39.8-53.1, Greek Revival 18.4-26.6, Craftsman 14-26.6" — and the ruling was `applies_to_styles`, not a band table. Those styles are now **unjudged** on pitch by that test rather than wrongly failed, which is the right direction and not the destination; the table is the remaining work. (ii) **The same pattern exists on PRIMARY tests**, and the mechanism already covers them because the field is on the shared test object — but scoping them is a data sweep over 209 faults, not six. The clearest example: `gutter-as-cornice` is `universal` and requires a 7 in cornice projection whose own note derives it from `facade-classical` at a 9 ft bay; a Cape's smaller bay gives 6.89 in, so a correct Cape cornice fails a classical threshold by an eighth of an inch. The *fault* is universal and its *number* is not. That sweep wants its own package. *Original entry follows.*<br><br>**A fault's secondary tests are written for one style and applied to every style.** Found under OQ 37. `faults/chimney-omitted.json` carries two `secondary_tests` whose own notes name their scope explicitly: *"The Tudor Revival rule — chimney breadth at least one sixth of the elevation it stands on"* and *"The Prairie test. If the chimney is not the visually heaviest object present, the composition has been inverted."* `mcp_server/core.py::check_measurements` runs every test in `[test] + secondary_tests` against every style the fault applies to, and a fault is present if **any** of them fails. So a Cape Cod colonial is failed for not having a Tudor chimney. That is three of the four fatals `cape-cod-colonial` produces for **every** parti composed for it — the style cannot currently return a clean plan under any diagram, and the reason is not in its own data. `severity_by_style` and `exceptions` both exist and are honoured; neither scopes a *test*. The concrete proposal: an optional `applies_to_styles` on a secondary test, absent meaning all — which keeps every existing fault valid and lets the two chimney tests say what their notes already say. The alternative is to promote them to faults of their own, which is more honest about them being different claims but costs two new ids and a re-check of everything citing `chimney-omitted`. Needs a ruling; measured impact is at least one style unable to produce a clean plan, and the count is not known until the whole fault corpus is swept for the same pattern.

## From the workbench legibility pass (26 August 2026)

64. **OPEN — the loupe magnifies the pen with the drawing, and the Drawn Language says a pen is a pen.** Raised 26 Aug 2026 by the work in `docs/reports/workbench-legibility-pass.md`, and by the fix rather than by the bug. Every sheet in this instrument declares `vector-effect="non-scaling-stroke"` and `sheet/Sheet.jsx`'s own header states the reason: *"strokes are px and non-scaling — a pen is a pen."* A cut line is 2.6 px at any scale because a cut line is a weight, not a width, and the weights are the grammar — cut, profile, medium, fine, hairline. `components/PlateViewer.jsx` magnifies by a CSS transform on the whole plate, which scales the strokes with everything else: at 400% the cut line is a 10 px band and the hairline is as heavy as the medium line was. Nothing is *wrong* on the plate — the ratios between the five weights are preserved exactly — but the sheet at 400% is no longer set in the standard's line weights, and a reader judging poché against partition against grid is reading a ladder that has been stretched. The alternative was built and rejected for this pass, and it is the fork: **magnify by the `viewBox` instead**, which holds every non-scaling stroke at its declared pixel weight while the drawing grows around it. That is the drafting-correct answer and it has two costs, both real — a hairline at 400% is a wisp with a great deal of paper around it, which reads as a fainter drawing rather than a nearer one; and it works only for the SVG, so the plate's title block and caption would stay at their original size while the drawing inside them grew, which is the thing this frame was built to avoid (a plate is a sheet, and a sheet moves under a loupe whole). A third option nobody has costed: transform-scale the plate as now, and counter-scale the stroke widths by the same factor so the weights hold while the type and the geometry grow together — correct in both registers, and it means every stroke width in the sheet becoming a computed value, which is a change to how the drawing is written rather than to how it is shown. **What is not in question:** the magnification itself. Before it, the dimension figures on both the order plate and the plan sheet could not be read at any size the layout offered, which is what was reported. This entry is about what happens to the LINE while they are being read.

## From the adversarial audit of the legibility pass (26 August 2026)

65. **CLOSED 26 Aug 2026 — the pack declares its datum, and the declaration is verified against the pack's own geometry rather than trusted.** Ruled as proposed: `projection_datum: "naked" | "axis"` joins the pack at `schema/proportion-pack.schema.json`, and every authority's published figures stay verbatim — a Gibbs number still checks against Gibbs's own plate, which is the whole reason normalising the twelve was refused. **Seven declarations cover all twenty-six**: the five Vignola bases (Doric and Tuscan `naked`, Ionic, Corinthian and Composite `axis`) plus the two standalone packs, `greek-doric` and `benjamin-doric`, both `naked`; the other nineteen are overlays and inherit through `resolve()`, which now carries the field and lets an overlay state its own where it genuinely differs. None does. `moorish-arch` declares nothing because it publishes no projections at all. Neither of the two standalone packs publishes a shaft body, so their reading was not taken from the Doric pattern — it was taken from the capital, whose widest member is under half the radius in both, and an abacus cannot stand INSIDE the shaft. **The declaration is checked, not believed**: `build/check_orders.py::check_projection_datum` dimensions every resolved pack and compares the declared value against what the geometry reads (a shaft body at 0 or at the semidiameter; failing that, the capital). Declaring the wrong datum on one Vignola base errors on that pack **and on all four overlays that inherit it**, which is verified in `tests/test_drawn_labels.py`. **The consumer that was wrong is fixed**: `build/orders_template.html::buildGeometry` added the naked to both readings, so `dist/orders.html` drew Vignola's Ionic **2.25× too wide** and Gibbs's Corinthian 1.41×, with every Ionic, Corinthian and Composite shaft narrower than its own apophyge; measured before and after, the eleven `naked` packs are unchanged to the pixel, as they should be. **The zeros half is ruled too — unrecorded, and marked.** Eight assemblies in the `axis` packs record every member's projection as 0, and under that reading 0 cannot be a statement: a cornice at radius 0 is at the axis. They are treated as an ABSENT figure, drawn at their naked, and counted on the sheet — the workbench plate says how many of a pack's own edges it does not give, rather than drawing a cornice that recedes behind its own column. That is a reading of a silence, not a fact, and the packs' own sources would settle whether those figures were never transcribed or were meant as flush; that part stays with OQ 7–11 and OQ 18's source half, which are environment-blocked. *Original entry follows.*<br><br>**OPEN — `projection_parts` means two different things in this corpus, split by order, and nothing in a pack says which.** Found by measuring all 26 order packs at a 36-inch diameter after the workbench plate started drawing them as a half section: **thirteen record a member's projection as an offset FROM ITS OWN NAKED** (Gibbs's Doric shaft body: `projection_in` 0, its base plinth 6.0 against a semidiameter of 18) **and twelve record it as an absolute radius FROM THE AXIS** (Vignola's Ionic shaft body: exactly 18.0, its base plinth 25.0). There is no third case and no ambiguous one — the shaft body is either 0 or exactly the semidiameter in every pack that publishes one. The split runs by ORDER and not by authority: every Doric and every Tuscan is an offset, every Ionic, Corinthian and Composite is a radius, across all five authorities. That is the signature of two data-entry passes, not of an architectural distinction, and `check_orders.py` has nothing to say about it because both readings are internally consistent. **What it costs today:** a consumer that adds the naked to the figure draws the twelve at two radii — the shaft's own apophyge, astragal and fillet standing a whole semidiameter clear of the shaft they sit on, which is the fault the plate was fixed for and which `dist/orders.html` (`buildGeometry`, `datum(a.id, m.y0) + m.projIn`) **still commits**, since it is where the rule was copied from. The workbench plate now reads the convention off each pack's own shaft — whose outer face at its foot IS the column's radius, so whichever reading puts it at r0 is that pack's reading — and `tests/test_drawn_labels.py` fails if any pack ever lands between the two. That is a reader working around the data, not the data being right. **The choice for whoever rules.** (a) Normalise the corpus to one convention and migrate the twelve (or the thirteen), which is a 26-file data change plus a re-check of every consumer, and settles it permanently. (b) Add an explicit `projection_datum: "naked" | "axis"` to the pack, which costs a schema bump, keeps both sets of published figures verbatim as their authorities give them, and makes every consumer's reading a stated one rather than a derived one. (c) Leave it and let each consumer derive it, which is what happens now and is one careless consumer away from the same fault again. A further thing to settle under (a) or (b): **eight assemblies across the radius-convention packs record every member's projection as 0** — `palladio-corinthian`'s whole cornice, `palladio-ionic`'s base, architrave, frieze and cornice, several friezes and pedestals elsewhere. Under the offset reading 0 is a statement (flush with the naked); under the radius reading it cannot be one, and the plate treats it as an absent figure and says so on the sheet rather than drawing a cornice that recedes behind its own column. Whether those are genuinely unrecorded or are zeros meaning flush is not decidable from the data, and the packs' sources would settle it.

## From the candidate score (26 Aug 2026) — reissued 66-68 at the 26 Aug merge

66. **OPEN — the score's eight axis weights are editorial and have never been tested against a plan anyone ranked by hand.** The candidate score is now a weighted composite out of 100 (`compose.py`'s `SCORE_AXES`): solecisms 22, rooms 20, connections 17, fidelity 18, area 8, bedrooms 4, canon 6, buildability 5. Each axis's SHARE is measured — it is a real fraction over a real denominator, and a test pins that the published arithmetic is the arithmetic. The WEIGHTS are not measured. They are one judgement about what matters in a house, argued for in the comment above them and stated in `docs/compose.md`, and nothing in the corpus supports or contradicts them. The honest test is cheap and has not been run: take a dozen composed candidates across several briefs, have someone fluent rank them by hand, and see whether the composite agrees. Where it does not, the disagreement is either a wrong weight or a missing axis, and both are worth knowing. Until then the number is a stated opinion with its working shown, which is better than the demerit total it replaced but is not a measurement. Two specific weights to look at first: **fidelity at 18**, which is deliberately not enough to carry a plan that fails everything else and has never been checked against a case where it should be, and **buildability at 5** over a denominator of only two footprint tests, so a single note moves it half its range. See `docs/reports/candidate-score-composite.md`.

67. **OPEN — the composer now recommends different plans, and that was a side effect rather than a decision.** Replacing the demerit total with the composite changed the returned SET on both shipped briefs, not merely the order they are read in. `tests/test_composer.py`'s two identity pins cover position 1 and passed throughout, which is how it went unnoticed until an adversarial audit measured the whole set. The two rankings cannot be reconciled at any weighting: `demerits` charges 1 a minor without limit while an axis charges half a room however many minors land on it, so a plan with many minors concentrated on few rooms is treated very differently by the two. A sweep of fidelity's weight from 18 to 60 reproduces neither brief's old set at any value. **What was done:** fidelity's weight was set from a measurement — the observed 18.0/22.0-point spread of the whole non-fidelity subtotal between clean candidates — so that being the right diagram can decide, which is WP-4.5's ruling; at the original 18 a tidewater-georgian brief returned an octagon and a tower villa over the native side-hall town house. The resulting sets are more native than the demerit total's own answer on both briefs (fit 7.0/7.0/3.6/2.0 and 7.0/3.6/3.6, against 7.0/3.6/2.0/3.2 and 7.0/2.0/2.0). **What is open:** nobody has ruled that the new sets are the right ones. They are defensible by WP-4.5's own criterion and they are not what the composer said yesterday, and only Lucas can say whether a diagram like `five-part-palladian` belongs in a 3,200 sf family brief's four. The cheap test is the same one OQ 64 wants: rank a dozen candidates by hand across several briefs and see which ordering agrees. Until then this is a stated, measured, guarded change and not an endorsed one — `tests/test_score.py::test_the_returned_set_is_native_dominated_not_merely_tidy` fails if it drifts further. See `docs/reports/candidate-score-composite.md`.

## From the second audit round (26 Aug 2026)

68. **HALF CLOSED 26 Aug 2026 — three of its four parts are fixed, two rules still want a ruling, and the entry it replaces was wrong about almost every part of it.** Recorded as "32 derived proportion rules evaluate outside their own band, ten of them comparing inches to a ratio... each needs a decision about what the band SHOULD say, and that belongs to the authority the pack cites". Re-measured from scratch on Lucas's instruction: it is **22 rules, not 32** — the 32 came from evaluating at bindings other than the pack's own defaults — and **not one of them was a wrong number**. Every band in the corpus is as its authority states it. What was wrong was the model around them, in three distinct ways, and all three are now fixed. `docs/reports/candidate-score-composite.md` carries the working; `tests/test_proportion_bands.py` is the guard.
    **TEN were a band on a RATIO compared against a value in inches.** `greek-doric`'s entablature evaluates `column_height * 0.318` to 41.976 in and was checked against `[0.3, 0.34]` — whose own authority note reads *"The Parthenon's measured ratio, about 0.315"*. The same shape in `chambers-composite`, `chambers-corinthian` (entablature and crown), `chambers-tuscan`, `palladio-composite`, `palladio-corinthian`, `palladio-ionic`, `palladio-tuscan` and `moorish-arch`. **Every one of the ten implied ratios lands inside its own band** — 0.3180 in [0.3, 0.34], 0.2500 in [0.2222, 0.25], 0.0694 in [0.055, 0.08] — so the data was right and `range` was one field doing two jobs. **Ruling: split into two rules**, and done: a `*_ratio` rule carrying the proportion the authority states and the band, and the dimension rule beside it multiplying it out into inches with no band at all. The dimension is NAMED (`height_ratio`, `return_ratio`) and not the generic `ratio`, because `facade-arcade` already writes `(arch, ratio)` for `springing_as_fraction_of_height` — a generic name collided with it, `check_addresses.py` caught it in the same hour, and the corpus's own habit (`column_ratio`, `storey_ratio`, `base_ratio`) is the specific name.
    **SIX were the engine's default bindings, and four of them SHOUT the fix in their own authority note**: *BIND storey_height TO THE FULL WALL HEIGHT*, in capitals, on all four Benjamin eave cornices. `DEFAULT_BINDINGS` gives `storey_height` 120 in — one 10 ft storey — and Benjamin's own worked example is *"suppose a house to be thirty five feet high, divide thirty five feet into thirty parts, and one thirtieth will be fourteen inches."* At 420 in all four land squarely inside their bands. The two Gibbs pediments are the same thing at a doorcase: the construction is on the span of the cornice of a principal door, and the default 36 in opening gives a 4.8 in rise. **Ruling: use `calibrated_for`, and refuse to judge outside it** — and the field had been in the schema for exactly this since the chair-rail correction, described in as many words as *"the context in which this rule was calibrated. Outside it the rule still evaluates and should not be trusted"*, on 2 of 751 rules, **read by nothing**. `proportion_engine.evaluate()` now withholds `in_range` outside a stated context and publishes `out_of_calibration` in its place. Judging a rule against a binding its own note tells you not to use is unjudged-reported-as-failed, which is the direction this corpus must never round in. Turning the mechanism on immediately picked up `trim-classical`'s chair rail, which had carried a `calibrated_for` since 0.2.0 that nothing had ever honoured.
    **FOUR missed by 0.00025.** 7/32 = 0.21875 against a floor of `0.219` — the three-decimal rounding of the very expression the band exists to contain — while the Proportions surface displayed the value rounded to 0.2188 and so printed a number that looked inside the band it said was violated. **Ruling: one tolerance, the same idea `check_invariants` already carries** (*"these are ratios written down by hand in the sixteenth century and stored as decimals"*). Implemented as the author's own precision rather than a chosen constant: half a unit in the last decimal place the edge was WRITTEN to, capped at half a thousandth. A floor written `0.219` gets 0.0005 and admits 7/32; one written `0.2222` gets a tenth of that; one written `0.3` gets 0.0005 and still refuses 0.26.
    **The checker gap was the durable half, and it is closed.** `check_orders.py` validated that a range was not inverted and never evaluated a rule against one, so 22 violations sat unreported for as long as the file has existed — while `check_modules.py --eval` and `check_systems.py` have both done exactly this check for years and neither covers `proportions/orders/` or `proportions/overlays/`, where all 22 lived. It evaluates through `proportion_engine.evaluate()` rather than a second implementation, so what it checks is exactly what the MCP tool and the workbench publish, tolerance and calibration included. A violation is a WARNING: two survive deliberately, and failing the build on a finding the corpus is correctly making would be the wrong answer.
    **TWO REMAIN OUT OF BAND ON PURPOSE, and they are the only part of this entry still open.** `vignola-doric`'s cornice derives 9.0 in against its own `[10, 36]`, and `chambers-ionic`'s balustrade derives 28.8 in against `[30, 42]` on a rule whose `quantity` is `guard_height`. Both are the mechanism working rather than failing: a dimension derived from a small order, and a band saying the result is too small to build. The balustrade is the sharper case — a guard at 28.8 in is genuinely too low, and the corpus reporting the conflict between an order-derived dimension and a safety floor is the corpus doing its job. **What is wanted is a ruling on which of the two they are**: a calibration context these rules should declare (as the six above now do), or a finding that should stay red. They are pinned by name in `tests/test_proportion_bands.py` so the count can go down with a ruling and cannot go up silently.

## Reissued 26 August 2026 — ids 64 and 65 collided a second time

The same thing that happened on 25 August happened again, for the same reason: two sessions
ran in parallel and both issued the next free number. `main`'s work on the workbench
legibility pass took **64** (the loupe magnifying the pen) and **65** (the pack's projection
datum); this branch's candidate-score work had independently taken 64 and 65 and had gone on
to a third. **main keeps both of the collided ids**, and this branch's three were reissued:

| written as | now reads | subject |
|---|---|---|
| 64 | **66** | the score's eight axis weights are editorial and unmeasured |
| 65 | **67** | the composer recommends different plans, as a side effect rather than a decision |
| 66 | **68** | derived proportion rules outside their own declared band |

A commit message, a report or a code comment written before this merge may still carry the
old number. The five commits on `claude/plan-ranking-communication-76kl2w` all predate it and
all use the old numbering; every reference inside the tree has been converted.

69. **CLOSED 26 Aug 2026 — the token is configured per test, and a guard sorts last to keep it that way.** Ruled: stop the fixture mutating process-wide state, keep exercising the real gate. The two constraints were never the same constraint. The CLIENT must stay session-scoped — `session_manager.run()` raises if entered twice, so the lifespan may be entered once per process. The TOKEN only needs to exist while a request in that file is in flight, and `monkeypatch` is exactly that: function-scoped, restoring after each test, invisible outside the test that asked. The fixture's original note said env was set directly "because monkeypatch is function-scoped", which was true of the client and merely assumed of the token. **Not done as literally ruled, for a reason worth recording:** a client carrying a default `Authorization` header cannot coexist with `test_bearer_is_required`, which proves the gate by sending no header at all — so the header stays on the requests that mean to be authorised and off the one that does not, and what changed is that the server-side token is configured for one test instead of for the process. **`workbench/server/tests/test_zz_auth_leak_guard.py`** asserts that no gate variable survives into it and names the cause and the remedy in its failure message; the `zz` is load-bearing, because a guard against "something earlier leaked" only works if it runs after everything it guards. Proved by reintroducing the leak: the guard fails naming the variable, beside the downstream 401 it explains. *Original entry follows.*<br><br>**OPEN — a session-scoped test fixture gates every test file that sorts after it.** `workbench/server/tests/test_mcp_http.py::live` sets `WORKBENCH_API_TOKEN` in `os.environ` directly and restores it at **session** teardown. The direct assignment is deliberate and its docstring explains why — monkeypatch is function-scoped, and a function-scoped fixture tore down when the runner left the file and could not re-enter it, turning seven tests into spurious errors. But the consequence reaches outside the file: from the moment that fixture first runs, `auth.required()` is true for the rest of the session, so **every test file sorting after `test_mcp_http` runs against a gated server**, and any plain `client.get("/api/…")` in one gets a 401 that has nothing to do with the route under test. Found because `test_search_index.py` passed alone and failed in the suite; it now states its own unauthenticated baseline explicitly, with the reasoning in place, which is a workaround and not a fix. Three ways out, none obviously right: give the fixture its own `TestClient` with a bearer header and stop touching the environment at all (cleanest, but the fixture's whole point is to exercise the real gate); restore the environment in an autouse function-scoped fixture *within that file only*; or accept it and make "state your own auth baseline" a documented convention for endpoint tests. The first is probably correct and is a rework of a file whose reasoning is sound, so it wants a ruling rather than a drive-by.

70. **CLOSED 26 Aug 2026; the reading was CORRECTED the same day by an adversarial audit.** `placeByHearth` scanned the gazetteer longest-name-first and took the first hit at the finest tier, choosing by an accident of key length rather than by what the sentence says: **25 of 93 hearth placements landed on a place named second or later, `craftsman` among them — placed in Los Angeles while its record reads "Pasadena and Los Angeles", Pasadena being the exact word this whole reading was justified by.** Earliest-mentioned now wins, applied AFTER the anchor filter (sorting first would hand `dutch-colonial-american` to Amsterdam, the bug the anchor exists to catch). A bare `Washington` key also matched a hearth meaning D.C. and drew `minimal-traditional` 3,700 km out; ambiguous names are now flagged regions-only, resolvable from a regions list but never from prose. And `check_gazetteer.py` no longer re-implements the algorithm in Python — it runs `gazetteer.js` through node, because the audit found the 45° rule named as a constant on one side and hard-coded on the other, with a third copy of the no-hearth classifier already disagreeing on one style. *Original closure follows.*<br><br>**CLOSED 26 Aug 2026 — and the check was the smaller half. The corpus already knew where these styles came from.** Ruled: build the check, and chase the hearths too. Chasing them turned out to need no authoring at all, which is the finding. The map placed styles from `geography.regions` alone, and **59 of the 81 country-level placements had a `geography.hearth` sentence naming somewhere finer that nothing was reading** — `craftsman` sat in the middle of Kansas while its own record said "Pasadena and Los Angeles, California"; `prairie-school` likewise, against "Oak Park and Chicago". Placement now takes the finest of what either field names, and the numbers moved **15 → 87 locality, 81 → 24 country**. The remaining 24 are correct: 22 are families and traditions, which are abstractions over styles and have no birthplace, and two are styles whose hearth says so in its own words (`neo-eclectic`: "No design hearth"; `craftsman-bungalow`: "Streetcar suburbs nationwide"). So the gap this question suspected in `geography.hearth` coverage does not exist — the gap was in the reading. **The rule that makes it safe: a hearth may SHARPEN a region, never contradict one.** A name found in the prose is refused if it is more than 45° from EVERY region the style names. Against only the finest region — the first version — it threw away `churrigueresque`'s Madrid because that style's finest region is in Mexico, while Spain sat in the same list. Against all of them it still does its job: `dutch-colonial-american`'s hearth reads "from Nieuw Amsterdam to Beverwijck (Albany)", all six of its regions are American, so Amsterdam is refused and the sentence resolves to Albany, which is what it meant. **`build/check_gazetteer.py --strict`** joins `check_all.py` and ratchets three numbers, all now at zero: unplaced, coarse-for-any-reason-but-the-record's, and hearth sentences it cannot read. It parses the JS gazetteer rather than keeping a Python copy, because a second copy is precisely how the two halves of the citation grammar came to disagree about dots. Proved by adding a style naming a place nobody has heard of: the check fails, names it, and says what to do. *Original entry follows.*<br><br>**OPEN — the map's gazetteer is unversioned interface data about a corpus that grows.** `workbench/app/src/data/gazetteer.js` places all 164 styles today from 180 region-name entries. It is deliberately NOT corpus data — the corpus holds no coordinates and none of these numbers may migrate into `styles/*.json` — and a style whose regions it does not know is reported as unplaced rather than guessed, which is the correct failure. But nothing **fails the build** to tell an author that their new style is invisible on the map: the map simply lists one more name under "not placed", where it can sit unnoticed indefinitely. A `build/check_gazetteer.py` ratcheting placement coverage the way `check_inheritance.py` ratchets its three numbers would close it in an afternoon. Not built, because it is unclear whether the map reading is load-bearing enough to earn a check in `check_all.py`, and that is a judgment about how the tool is actually used rather than a fact about the data. Related: the placement tiers are themselves a finding worth a ruling — **81 of 164 styles name only a country**, so half the corpus has no hearth recorded at all, and that is a gap in `geography.hearth` coverage that the map made visible rather than a gap in the map.

71. **CLOSED 26 Aug 2026, then CORRECTED the same day by an adversarial audit of the closure itself.** The first fix asserted neither the proof nor the count. Its skip matched the sentence `geometry.py` emits for EVERY unsolved status, so a structurally invalid CP model skipped exactly like a loaded machine — the one assertion between the corpus and a silently degraded solver, disarmed by the regression it names (proved by patching `solve_cp` to return `MODEL_INVALID`: skipped, pytest exit 0). And its "proven-or-carried" check pinned two string literals lifted from the source, of which the `PROVEN` branch is never reached at 60 s — 17 restore attempts, 8 OPTIMAL, 9 UNKNOWN, 0 INFEASIBLE — so making `geometry_cp.py` emit `"proven"` unconditionally, i.e. deleting the bookkeeping the test claims to guard, left it green. Now: `geometry.py` names its fallback (`budget` vs `engine`) so the two cases are separable in fact rather than in wording, and the test asserts against `attempts`, the reinstatement pass's own record — a note may claim proof only for a pin that run actually proved infeasible. Verified by mutation. The bound is also tightened from `< declared` (which permitted 34 of 35) to half. *Original closure follows.*<br><br>**CLOSED 26 Aug 2026 — it asserts the proof now, and says so when it cannot judge.** Ruled: assert the proof, not the count. The count was a proxy for the property that matters and a bad one, because how many pins survive the reinstatement pass is wall-clock-dependent — that pass restores each downgraded pin alone at the drawn footprint and needs 2.5 s of remaining budget apiece, so a loaded machine carries pins it would otherwise have restored or individually proved. The property is that a downgrade is never SILENT: every pin must carry a stated refinement naming its room and wall, and that refinement must say which of the two things it is — proven infeasible alone, or carried from the core and explicitly not re-proved. A third, unstated path is the failure the test exists to catch, and so is a pin with no note at all. That holds under any load, being a property of the report rather than of the clock. The other direction keeps a bound, but a load-independent one: fewer pins downgraded than the plan declares, or the model has stopped enforcing walls and started narrating them. **Found while proving the fix, and fixed with it:** under 4x CPU oversubscription CP-SAT does not run at all and the dispatcher falls back to the hill-climb, so the test's other assertion — that the engine is cp-sat — failed. Same species of error, so the same discipline applies: a budget fallback now skips as COULD NOT EVALUATE with the dispatcher's own stated reason, while a fallback for any other cause still fails. Verified at three loads: idle passes, 3 spinners on 4 cores (the realistic full-suite condition that produced the flake) passes, 12 spinners skips. *Original entry follows.*<br><br>**OPEN — a solver pin that depends on machine load.** `tests/test_solver.py::test_check_plans_solve_with_stated_downgrades` asserts at most 8 downgraded wall pins on `tidewater-georgian-careful` and intermittently measures 9. Not a regression and not new: the identical failure reproduces at commit `12e1dd0` in a clean worktree, and the test's own comment predicts the mechanism — *"engine identity near the budget edge is wall-clock-dependent (a loaded machine turns a 30s OPTIMAL into UNKNOWN → heuristic fallback)"*. Observed failing under `build/check_all.py` on three consecutive runs and passing on a direct `pytest tests/` of the same tree, which is the signature. The pin is defending something real — *"downgrade-without-proof is the failure this pin exists for"* — so raising the bound to 9 would blunt it, and the honest options are to give this one test a time budget generous enough to be deterministic on a loaded machine (the 60 s already there is evidently not), to assert on the *proof* rather than the count, or to mark it as a known-flaky check with its own state in `check_all.py` the way the missing-library case got `N/EV`. Raised from WP-5.6, which did not touch the solver and only needed to establish that it had not broken it.

**A SECOND PARALLEL-SESSION COLLISION, and the same rule applied as the first time.** Two
sessions again ran at once and both issued from 64. Main's block — the loupe's stroke weights,
the projection datum, the score's axis weights, and the two after them — keeps its numbers,
because it merged first and is cited under them from `docs/reports/`. This branch's three were
reissued:

| raised on this branch as | now | subject |
|---|---|---|
| 64 | **69** | a session-scoped test fixture gating every file that sorts after it |
| 65 | **70** | the map's gazetteer, and the hearths nothing was reading |
| 66 | **71** | the solver's downgrade pin, measuring the machine rather than the proof |

A commit message or report written before this merge carries the old number; so does the
WP-5.6 report, which was written while they were 64–66. The conversion table at the foot of
this register now has two rows of history, and that is itself the finding: **parallel sessions
issuing ids by reading the file and adding one will keep colliding.** It has now happened
twice in two days, cost a renumbering both times, and will cost a third unless the id comes
from somewhere that is not the working tree.


## From the geometry layer (WP-5.7, 26 Aug 2026)

72. **CLOSED 27 Aug 2026 — ruled: DETECT THE DATUM PER ASSEMBLY-GROUP, from the pack's own evidence, in one place.** `build/profiles.py::pack_geometry` now decides it in `axis_holds_for()` on two signals, either of which settles it: a recorded 0 (impossible under the radius reading — this is what an entablature gives) or nothing in the group reaching its own naked (every member would sit inside the shaft — this is what a capital gives). It only ever downgrades `axis` to `naked`, never the reverse. The entablature is judged as ONE group because its architrave, frieze and cornice share a naked; the pedestal likewise; the column's three assemblies are judged separately because each has its own. `build/elevation.py::eave_cornice` had carried a private copy and now delegates, so the 2.37× divergence between the inset and the two order plates is gone. **Measured: faces drawn flush with their own naked 192 → 81; 60 assemblies across the 14 axis packs now read naked-relative (14 cornices, 14 friezes, 14 architraves, 10 pedestals, 5 bases, 2 subplinths, 1 capital).** `check_orders.py` gained a third reporter, `note()`, and prints for every axis pack exactly which of its assemblies contradict its declaration — the silence was the whole cost of this question. The original entry, and the audit that widened it, follow.<br><br>ORIGINALLY WIDENED 27 Aug 2026 by an adversarial audit: this entry originally scoped the problem to the entablature and to `dist/orders.html`, and it is wrong in both directions.** It is NOT true that "the column family is sound": `gibbs-ionic`'s ENTIRE CAPITAL (volute 3.0, echinus 3.333, bead 2.667, abacus ovolo 3.667, against an upper radius of 5.0) and its ENTIRE PEDESTAL each draw as one flat vertical line, because every one of those figures falls under the radius and is clamped by `max(p, naked)`. `check_orders.py::check_projection_datum` reads the SHAFT only, so it passes the pack clean while the capital contradicts the declaration. And the affected surface is not one but two: the workbench Proportions plate consumes `pack_geometry()`, which takes the PACK-level datum, so it carries the identical clamp — on the product's primary surface, named nowhere. Measured corpus-wide: 14 packs declare `axis`; **192 entablature faces draw flush, 153 of them discarding a published non-zero projection**, and `palladio-corinthian`'s cornice is 5 of 5 flat. 78 of 175 quarter and cyma members corpus-wide have `dx == 0` and take the straight-line branch. `build/profiles.py` now COLLECTS these as `unrecorded` so a surface can at least say how many faces it drew flush for want of a figure, and the elevation inset prints the count — but that is disclosure, not a fix. The original entry follows.<br><br> OQ 65 closed by putting `projection_datum` on the pack and having `check_orders.py` verify the declaration against the pack's own geometry. That verification reads the SHAFT, falling back to base and capital — the column family — and the ruling is sound there. It is not true of the ENTABLATURE in the same packs, and the packs say so themselves: `gibbs-ionic` declares `axis`, yet its frieze face records a projection of **0**, as does its architrave's lowest fascia. A frieze standing on the column's centre line is impossible, so those figures are relief from the naked; the same shape appears in `vignola-ionic`, `vignola-corinthian` and `palladio-ionic`. Reading the pack-level declaration literally across a cornice clamps every member whose figure is smaller than the column radius flush with the frieze — on `gibbs-ionic`'s cornice that is the bed cyma and its fillet, i.e. **the bed mould disappears from the drawing**, and `dist/orders.html` has been drawing it that way since OQ 65 closed, because `outerAt` applies `max(projIn, naked)` to the entablature as well. `build/elevation.py::eave_cornice` now DETECTS the entablature's own datum from evidence (a base member recording 0 cannot be a radius) rather than trusting the declaration, in the manner of `proportion_engine.observed_projection_datum()`, and the elevation's cornice inset reads that. **What is open is whether the fix belongs where it now sits.** Three options: declare the datum per ASSEMBLY in the schema and migrate the 26 packs (most explicit, most authoring); keep detecting it from the pack's own geometry and move that detection into `proportion_engine` so every consumer gets it (cheapest, and it is where `observed_projection_datum` already lives); or rule that the entablature is naked-relative by definition in every pack and record that as a convention. Until then `dist/orders.html` still draws the clamped reading, which is a live wrong drawing on a shipped surface. Raised with, and partly worked around by, `build/profiles.py`.

73. **OPEN — two sourced rules give the eave cornice two different projections, and nothing is entitled to choose.** `eave_cornice()` sizes Gibbs's Ionic cornice so its HEIGHT fills `facade-classical`'s domestic envelope (2 parts of the bay module, 24.56 in here). Gibbs's own rule then fixes its projection: *"The projection of the Cornice equal to its height"*, which he holds for every order but the Doric — so the order projects **24.56 in**. `facade-classical`'s own `cornice/projection` rule says `module / 14`, which at this storey height is **10.53 in**. Both are sourced, both are about the same cornice on the same wall, and they differ by a factor of 2.3. This is the OQ 48 class — two packs meaning different quantities at one address — but at the CASCADE scope OQ 48 explicitly did not close, and it is not a naming collision that a `quantity` field can separate: they genuinely disagree about how far the thing sticks out. The record now carries both (`order_relief_beyond_frieze_in`, `envelope_projection_in`) with a `projection_disagreement_note`, the detail inset draws the order's own profile and the elevation band draws the envelope's figure, and the sheet prints both and says both are sourced. That is disclosure, not resolution. A ruling would say which governs a domestic front — most likely the envelope, with the order read as the profile's SHAPE and not its depth, but that is a judgment about what "reducing an order" means and it belongs to Lucas. Nothing should silently pick one before then.

74. **CLOSED 27 Aug 2026 (WP-5.9) — ruled by doing it: `elevation_profile` carries the visible roof PLANE on a long face, and the front elevation draws its stacks.** This entry said the first option was "almost certainly right and is not a renderer's call to make"; it was, and it was a roof-layer change of four lines. In parallel projection the near roof plane fills the band from eave to ridge, so a side-gable long face is a RECTANGLE, not a flat eave line — the old two-point return was a PERSPECTIVE argument ("the ridge is behind the near plane, not visible") applied inside an orthographic renderer, and it had stood through three work packages that all read it. With the surface there, `_profile_top_at()` answers how much of a stack a roof hides, for a gable triangle and a long-face rectangle alike, with no special casing. **Two things came out of the fix that this entry did not anticipate.** The renderer's own chimney block still asserted the flat eave line in a twelve-line comment — prose asserting what the code no longer does, which is the failure this corpus polices hardest, found in the file being edited. And underneath it were two invented constants of the OQ 52 class: the stack was drawn 3 ft from the gable wall's front corner and based 2 ft below the ridge, against a record stating `y_ft` 21.33 (mid-depth, on the ridge line) and `total_height_grade_ft` — on the sheet that put a brick bar in the sky at the top-left corner, touching no roof, on every gable elevation this corpus has ever drawn. **What is drawn now is the part above the roof only**, and that is a claim about evidence rather than about visibility: the kit makes these stacks gable-end EXTERIOR, so nothing hides the breast, but the only width this corpus states (brick-course's eight courses square, 22 in, itself flagged a judgment) is the STACK's. Drawing 47 ft of 22 in brick asserts a chimney breast nobody measured; the legend says what is missing below. `tests/test_drawn_geometry.py::TestTheStacksAreDrawnWhereTheRecordPutsThem` reads the positions back off the emitted rects. The original entry follows.<br><br>ORIGINALLY: **the front elevation cannot show the chimneys, because the roof's long-face silhouette stops at the eave.** `build/roof.py`'s `elevation_profiles` gives the S face of a side-gable house exactly two points, both at the eave (25.44 ft on `tidewater-georgian-careful`), and models no roof mass above the cornice at all; the gable faces get the real ridge. The cost is specific and large for this style: its kit says the paired stacks joined by an arched brick curtain are *"visible from a mile away and conclusive against New England"*, and the sheet everyone actually looks at cannot draw them. WP-5.7 tried to, by drawing only the portion of a stack clearing the roof silhouette at its plan position — and withdrew it, because with the silhouette flat at the eave that rule puts 22 ft of brick in front of a roof nobody modelled, which is the same error as reporting an unmodelled chimney as zero. The drawing's own hardcoded 36 in stack width WAS fixed in the same pass (see the chimney note in `build/render_elevation.py`). What is wanted is a roof-layer decision: whether `elevation_profiles` should carry the visible roof PLANE on a long face rather than just its eave line, which is a small piece of geometry with consequences for every renderer that reads it, or whether front-face chimneys should be drawn from the plan record and the roof surface inferred at draw time. The first is almost certainly right and is not a renderer's call to make.

## From the adversarial audit of the geometry layer (27 Aug 2026)

75. **CLOSED 27 Aug 2026 — ruled: correct the pitches to their own pack's stated unit.** `benjamin-corinthian`'s modillion pitch 17.5 → **35** and `benjamin-ionic`'s 15.5 → **31**, matching each pack's module block (*"Every figure here is in minutes"*) and its own quoted authority; the halving sentences inside both member notes were corrected with them. The other Benjamin members were swept for the same conversion error and carry none — it was confined to these two. **And the correction that opened this question had itself not landed.** Commit 529310c reported writing widths of 13 and 10.5 and wrote neither: its rewrite loop set the value on reaching `spacing_parts`, then kept iterating and copied the file's original `width_parts` back over it, while the NOTES were rewritten regardless — so two members carried a provenance sentence saying they had been corrected, beside values that had not been. `gibbs-corinthian` landed only because it had no prior value to clobber. Fixed, verified by reading back from disk, and guarded: `TestTheTranscribedWidthsAreTheAuthoritiesOwnFigures` pins all fifteen authored widths against the words each was read from. **The ratio invariant could not have caught any of it** — halving both numbers preserves the ratio, so 6.5/17.5 and 13/35 both read 37%. Original: two Benjamin modillion pitches contradict their own pack's stated unit, and correcting them is a data change nobody has ruled on.** All three Benjamin packs say in their `module.note`, in as many words, *"the DIAMETER is divided into sixty minutes, so the semidiameter carries thirty ... Every figure here is in minutes"* — a part IS a minute. But `benjamin-corinthian`'s modillion records `spacing_parts: 17.5` while its own note quotes Benjamin at *"thirty five from centre to centre"*, and `benjamin-ionic` records `15.5` against *"thirty one minutes from centre to centre"*. Both are exactly half, and both member notes carry a conversion sentence performing that halving ("Thirty-five minutes on centres is 17 1/2 parts of this pack's module"), which the module block contradicts. The error is pre-existing (it predates WP-5.7), and WP-5.7 then **derived new `width_parts` figures from the halved pitches and wrote a provenance sentence around them** — 6.5 and 5.25 where Benjamin says thirteen and ten-and-a-half. The widths were corrected on 27 Aug to their sources; **the pitches were left as found**, because changing a figure this session did not author, in a pack whose own note argues both ways, is a ruling and not a cleanup. The consequence is visible: those two modillion bands now draw 74% and 68% solid, which is what the pack currently says and is almost certainly not what Benjamin drew. Either the pitches are halved and should read 35 and 31, or the module note is wrong for these two packs and should say so. A third possibility worth checking first: whether `spacing_parts` was authored against Vignola's 18-part module and never converted.

76. **CLOSED 27 Aug 2026 — ruled: wire it in.** `repeat_positions()` had two bugs before it had a caller: its `centre_on` branch filled forward only, leaving the whole run before the first anchor bare, and two anchors filling toward each other both claimed the teeth between them, so every middle tooth was drawn twice. Both fixed. The elevation cornice lays its band out tooth by tooth now, anchored on the bay centres — which is what Gibbs's rule (*"always the centre of a Modillion exactly over the centre of each column"*) means on a wall with no columns. **Where a width is unstated the band draws solid AND THE SHEET SAYS SO**, the behaviour the schema, `proportion_engine.py` and `docs/proportion.md` all described and no surface performed; the Tidewater cornice takes exactly that path, because `gibbs-ionic`'s modillion note publishes the pitch and not the width. Worth recording, because it narrows the ruling honestly: the order plates draw a SECTION, and a section cannot show repetition — teeth belong in elevation. Original: nothing calls `repeat_positions()`, so no dentil, modillion, mutule or triglyph band in this corpus is drawn as teeth.** WP-5.7 built the layout function, authored `width_parts` on 17 members from their own authorities' notes, added the field to the schema, passed it through `dimension()` as `width_in`, and documented in three places — the schema, `proportion_engine.py` and `docs/proportion.md` — that a band with no stated width is "drawn solid AND SAYS SO". None of that reaches a drawing: `repeat_positions` has no production caller and `REPEATING` is defined and unreferenced, so every band still draws as the solid smear it did before, and the disclosure the docs promise appears on no surface. The data is now there and correct (the ratios are consistent across every family: dentils 67%, triglyphs 40%, metopes 60%, modillions 29-50%), which means this is a rendering job and not an authoring one — the cornice inset and the two order plates each need to lay the teeth out along the run. Its `centre_on` branch also fills forward only, so an anchored band leaves everything left of the first anchor bare; Gibbs's rule ("always the centre of a Modillion exactly over the centre of each column") needs it to fill both ways.

77. **CLOSED 27 Aug 2026 — ruled: serve the finished paths from Python and retire both copies.** `build/profiles.py` emits `path` per pack and per face in MODEL inches (x out from the axis, y up), and both surfaces apply an SVG `<g transform="… scale(k,-k)">` instead of walking segments. **A model-space path has no handedness for a consumer to get wrong** — SVG mirrors the arcs itself, which is its job, and the flip that used to need detecting is now a negative number in a matrix. `segCmds` (with `silhouettePathFromGeometry`, 106 lines) and `edgeCmds` are deleted; `vector-effect="non-scaling-stroke"` keeps the pen off the scale. Guarded two ways in `workbench/server/tests/test_grammar_agreement.py`, beside the citation-grammar test it is modelled on: one reads both JavaScript files with comments stripped and fails if arc-sweep arithmetic reappears in live code, and one asserts Python is actually serving paths with arcs in them — so the two cannot both pass while a plate is blank. Original: should the drawn-geometry guard extend to the two order plates and the DXF, or is the Python round-trip enough?** `tests/test_drawn_geometry.py` reads `svg_path()`'s output back through the W3C endpoint-to-centre rule and proved, on the day it was written, that all 245 arcs in the corpus were being drawn as their own mirrors. Nothing does this for the two JavaScript surfaces, which hold their own copy of the sweep rule (`orders_template.html::segCmds` and `Proportions.jsx::edgeCmds`) — and the order tool's copy had a second, independent bug: it read only the y-flip, while that page draws its section half with x running one way and its mirrored elevation half with x running the other, so the two halves of every plate contradicted each other on every arc. Both are fixed and neither is guarded. The options are the ones OQ 72 lists in a different context: have the browser walk read a rendered path back the same way (real, slow, and the only check that covers what a viewer sees); assert the JS emits byte-identical paths to Python for a fixture pack (cheap, and catches drift rather than wrongness); or serve the finished path string from Python and let JS stop computing it at all, which is the same "one implementation" argument that motivated the geometry layer in the first place and would retire both copies.

## From the dormer layer (WP-5.9, 27 Aug 2026)

78. **CLOSED 27 Aug 2026 (WP-5.10) — ruled: take the measurement, then guard both.** The fault's own note
    has always said how to choose (*"Choose the test by whether an order is present, not by preference"*), in
    prose no evaluator could read. `build/elevation.py` now derives
    `an_order_is_applied_to_the_wall_carrying_the_eave_cornice` from the style's own `porch_type` and
    `pilaster` slots, and both rivals carry an `applies_when` on it, so exactly one can run.
    **The plausible wrong answer was checked first and is pinned as a test:** `gibbs_order_applies_to_style`
    is True on `tidewater-georgian` and means only that Gibbs Ionic is the order its cornice is GENERATED
    from; reading it as "an order is applied here" selects the entablature test on a house measuring
    **0.4286** and convicts it. Nor is a portico enough — the style's own rule says "where a portico occurs
    it is one bay wide, centred, and carries the bound order", and a one-bay portico's entablature is not
    the eave. Only an order engaging the whole wall qualifies, which is a short and quoted list.
    **`cornice_projection_in` is supplied now, and that is the point of the exercise.** WP-3.2 had
    withheld it to keep the rivals from firing, which silenced the fault on a NAME MISMATCH:
    `elevation.py` published `cornice_projection_past_wall_face_in`, and the fault came back "clear" on
    its wall-height ratio alone while its primary and two of its four secondaries were skipped for want
    of a name rather than a number. Measured: **1 of 5 tests evaluating → 3 evaluated, 1 declined on its
    precondition, 1 still needing an unsupplied measurement.** The same field retired a second WP-3.2
    workaround in the same commit: `solar_array_area_sqft` is supplied at its honest zero and
    `entrance-slope-penetration`'s array secondary declines instead of convicting. The original entry
    follows.<br><br>ORIGINALLY: **`cornice-that-is-a-fascia` carries two RIVAL secondary tests, and
    whichever is right the other fails.** Its first secondary wants `cornice_projection_in / cornice_height_in` between **0.35** and its
    upper bound (*"THE DOMESTIC BOXED CASE. 18 in high and 7 3/4 in out gives 0.43"*); its second wants the
    same expression between **0.85** and its own (*"THE FULL ENTABLATURE-DERIVED CASE — where an order is
    actually applied, on a portico or a pilastered front. A full cornice projects about its own height"*).
    Both notes are explicit that applying either to the other's case is a disaster, and `check_measurements`
    reports a fault present when ANY of its tests fails — so on any house that supplies a cornice projection
    and a cornice height, one of the two convicts it. Nothing in the corpus is being asked which case this
    facade is.

    WP-5.9 built `applies_when`, a precondition on the MEASUREMENTS, and used it to close
    `docs/elevation.md`'s open question 1 for the other two faults that question named
    (`entrance-slope-penetration`'s solar-array secondary, `shutter-on-an-unshutterable-opening`'s
    arch-head secondary). **This one it deliberately did not touch**, because guarding it needs a
    measurement stating whether an order is applied to this facade — something like
    `an_order_is_applied_to_the_facade`, or a count of applied pilasters or columns — and no generator in
    this corpus takes one. `build/elevation.py` knows whether a doorcase carries pilasters and whether the
    style resolves an order pack, so the measurement is derivable; the question is whether it should be
    a boolean the fault corpus tests on, or whether the two secondaries should instead be split into two
    faults with different `applies_to`. That is a fault-corpus editorial decision, not a renderer's.

    Neither reference plan trips it today, and the reason is not reassuring: the elevation supplies
    `cornice_projection_past_wall_face_in` and `main_cornice_height_in`, and the fault tests on
    `cornice_projection_in` — a name nothing supplies. The rival pair is inert because of a name mismatch,
    and the day anyone fixes the name, both houses acquire a serious fault.

79. **CLOSED 27 Aug 2026 (WP-5.10) — ruled: the centre bay is blind.** `_face_bays()` now takes the
    chimney axes for the face it is laying out and marks any bay a stack stands on `blind`; the renderer
    draws no opening there at either storey, because an exterior end stack runs the full height of the
    wall. The gable ends read as two glazed bays flanking a blind centre, which is what a Chesapeake end
    wall is, and the long faces lose nothing — both stacks are at mid-DEPTH, so they stand in the gable
    walls and in neither long wall. **A new fault, `window-on-the-chimney-axis`, catches the collision**
    where a record states both, guarded with `applies_when` on the chimney count; the elevation publishes
    `count_of_openings_on_the_axis_of_a_chimney_stack` as a MEASURED zero, which is the generator saying
    it resolved a collision rather than that one never existed.
    **One thing this does not assert, and it is worth carrying forward:** it blinds the bay where THIS
    RECORD places a stack, not "a Tidewater gable end always has a blind centre". The kit makes
    `paired-and-joined-by-arched-curtain` canonical — *"the tall paired stacks joined above the roof by
    an arched brick curtain"* — which is TWO stacks on one gable end with the space between them spanned
    by an arch, and `roof.py` places a single stack per end at mid-depth instead. Correct that
    simplification and the stacks would flank the centre bay rather than stand on it, and the window
    might come back. That is a roof-layer question. An exception on the fault was drafted for it and
    removed: `check_faults.py` was right that an unbounded exception on the very style that raised the
    problem is a loophole, not a nuance. The original entry follows.<br><br>ORIGINALLY: **a
    gable-end-exterior stack at mid-depth stands in front of the centre bay, and no layer asks whether
    they collide.** `roof.py` places `tidewater-georgian-careful`'s two stacks at `y_ft` 21.33,
    which is exactly half the 42.66 ft depth — on the ridge line, which is right — and the kit's own source
    string calls them `gable-end-exterior`. `build/elevation.py` independently gives each gable end three
    bays with a window in the centre one. Drawn from grade, the stack runs straight down through the centre
    window of both storeys, which is how this was found: WP-5.9 drew it that way for one revision.

    It is not a drawing bug. Either the gable end has no centre window, or the stack is not on the centre
    line, or (most likely for the type) the stack is a broad breast the flanking windows sit clear of and
    the centre bay is blind — which is what a Tidewater end wall usually is. All three are corpus facts and
    the corpus states none of them: nothing relates the chimney record's plan position to the elevation's
    bay layout. WP-5.9 sidestepped it by drawing only the part above the roof, for an unrelated and
    sufficient reason (the breast's width is not stated), so nothing is currently drawn wrongly — but the
    conflict is still in the records, and a `chimney-through-the-window` fault would catch it if the two
    layers were ever asked about each other.

## From the four rulings (WP-5.10, 27 Aug 2026)

80. **OPEN — a node's own MEASURED parameter and a pack rule contradict each other at 133 addresses, and
    nothing was comparing them.** OQ 48 measured pack against pack and closed at 0 own-scope collisions.
    This is the same corruption one layer over and it was invisible to that measurement for a simple
    reason: the kit writes a parameter called `projection_in` and a pack writes dimension `projection`, so
    the two never met under one name. `tidewater-georgian` authored its brick sill's projection as **0–1 in,
    `kind: measured`**, while `sash-light` delivered **2.25 in "sloped about 1 in 6 with a drip"** to the
    same slot — more than twice the node's own figure, in a node whose kit FORBIDS the sloped sill and
    gives its reason. The resolved slot carried both, under two names, and no checker looked.

    `build/check_addresses.py` gained a third reporter, `kit_vs_pack()`, and the first measurement is
    **133 contradicted parameters, 3 could not be judged**. It compares only parameters the node authored
    as `measured` — a `derived` one is a pack's own value copied into the kit, agreeing with itself — and
    compares VALUES rather than quantities, because a kit parameter has no `quantity` field. Ratcheted at
    133 so it cannot grow, in the shape OQ 48's original 139 were handled: a real corruption, found by
    measuring something nobody had measured, too large to fix in the package that found it.

    **The distribution is concentrated and that is the good news:** `american-farmhouse-vernacular` 32,
    `federal-style` 31, `greek-revival-american` 24, `craftsman-bungalow` 16,
    `georgian-colonial-american` 13 — five nodes carry 116 of the 133. The sill that found it is not in
    the count; it was fixed in the same commit.

81. **OPEN — a slot bound `open` inherits the constraint the style declined to make.** `resolve_slots`
    stops its walk only on `specified` or `forbidden`; `open` is skipped entirely and the walk continues.
    So a style that says "I do not constrain this slot" gets its nearest ancestor's constraints in full,
    which is the opposite of what the word means anywhere else in this corpus.

    Found at `colonial-revival`'s `dormer` slot, bound `open` / `status: empty`, which therefore resolved
    the whole slot from `english-cottage-vernacular`: `eyebrow-swept-dormer-within-thatch` **canonical**,
    and `boxed-dormer` — the only dormer such a house is ever built with — **forbidden**, on a `c01, hard.`
    A production Colonial Revival could not declare its own dormer, and the elevation generator drew a
    thatched cottage's on it with full confidence. That instance is fixed (the style now binds the slot
    `specified`), but the mechanism is untouched and reaches all 97 slots.

    It is OQ 51's question in the kit layer rather than the proportion layer, and it should probably be
    settled with OQ 51's opt-in flip rather than before it: making `open` stop the walk today would strand
    every slot that is `open` and relying on the cascade, which is most of them. What is wanted first is
    the count — how many `open` slots resolve to an ancestor's record, and how many of those carry a
    `forbidden` the node would not have written.

## From the adversarial audit of WP-5.10 (28 Aug 2026)

82. **OPEN — `sash-light`'s frame-wall sill rule reaches 27 masonry nodes, and two more pack rules have
    the same shape.** WP-5.10 scoped `sash-light`'s `window_sill/projection` (2.25 in, *"sloped about 1 in
    6 with a drip"*) away from `tidewater-georgian`, on the strength of the rule's own note: *"In a frame
    wall this is a real sill member; in a masonry wall it is a rowlock or a stone and belongs to the
    brick-course pack, not this one."* That fixed one node. The cascade delivers the same rule to **86
    nodes, of which 27 make a masonry cladding canonical** — `english-georgian`, `charleston-georgian`,
    `mid-atlantic-georgian`, `jeffersonian-classicism`, `hudson-valley-dutch`, `italianate-townhouse`,
    `brownstone`-clad `renaissance-revival-american` among them. `charleston-georgian` still resolves the
    2.25 in sloped sill today.

    **Two other rules state a scope their data does not carry**, found by the same sweep:
    `opening-proportion`'s `window_surround_wood/exterior_head_assembly_height` (*"On a masonry front this
    assembly is a flat arch, a jack arch or a stone lintel instead, and the brick-course pack owns its
    coursing"*, `applies_to` 52 nodes, and it resolves onto `tidewater-georgian` and `charleston-georgian`
    — the same node, one address over from the rule just fixed); and `facade-gable`'s
    `gable_treatment/parapet_height` (*"Where the gable is a roof end this rule does not apply at all"*,
    `applies_to` 12 nodes including `tudor-revival` and `dutch-colonial-american`).

    `slots_except` (WP-5.10) is the mechanism and it works per binding. What is wanted is either a
    per-rule construction scope — the thing each of these notes actually describes — or 27+ scoped
    bindings. That is a migration and a schema decision, not a patch.

83. **OPEN — three measurements are still withheld to work around gaps `applies_when` now covers, and one
    is supplied as a constant that is not true.** WP-5.10's commit message says "two workarounds retired
    by one field". Three more are standing:

    - `window_head_radius_in` / `shutter_head_radius_in` (`build/elevation.py`): withheld for exactly the
      reason the solar-array measurement was, and `shutter-on-an-unshutterable-opening`'s arch-head
      secondary **gained** its `applies_when` in the same commit — so the guard exists and the
      measurement that would exercise it still does not, leaving that fault clear on 1 of 2 tests.
    - `plan_offset_at_material_change_in`, `ridge_height_difference_between_volumes_in`
      (`brick-front-vinyl-return`), whose comment names the solar and shutter cases as its precedent.
    - **`total_shutter_leaves: 2.0` and `shutter_leaves_with_a_leaf_width_of_clear_hinge_side_wall: 2.0`
      are unconditional constants**, supplied whether or not the style carries shutters, so
      `shutter-on-an-unshutterable-opening` reads `2/2 = 1.0` and returns CLEAR on a house whose kit makes
      `none` canonical. That is a fault cleared on two invented shutters — OQ 52's class, inside
      `_derive_measurements`, outside `NOT_MODELLED`'s reach and therefore outside the guard that was
      built to catch exactly this.

    **Also counted while there:** 15 `exceptions[].bounds_test` entries divide by a count with no
    `applies_when`. None is live today (their denominators are unsupplied or non-zero), and they are
    listed rather than guarded speculatively — the four WP-5.10 guarded were guarded because they were
    reachable, and guarding the rest without a reachable case would be adding preconditions nobody can
    check.


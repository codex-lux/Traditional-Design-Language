# Geometry

Room rectangles placed in a footprint, both levels solved together.

```
python3 build/geometry.py plans/tidewater-georgian-careful.json \
  --parti centre-passage-double-pile --candidates 900 --svg dist/plans/out.svg
```

## Bay-grid slicing, with the relaxations counted

Rooms snap to the structural bay module the parti declares, because traditional houses **are** built on one — joists span it, windows centre on it, the facade composes from it. Placement is recursive guillotine subdivision along bay lines.

Where a room cannot be made to fit on the grid the cut is allowed off it, and **every such relaxation is counted and reported**. A relaxation is *a joist run that does not land on a bearing wall*, and a window bay that will not centre — a compromise rather than a detail. Note what that definition does and does not say: the bay module is the usual proxy for bearing, not the thing itself. An upper cut that lands squarely on a wall below **does** land on bearing however far it sits from the module, and since WP-7.1 it is not charged. 18 of 30 ground wall lines on the shipped plans are themselves off the bay grid, so the two are genuinely different tests.

Each relaxation is also **located**, and both renderers draw it as a hollow △ on the line itself. The two engines record the position differently: the heuristic knows the cut it made, so its mark carries `from_ft`/`to_ft`; the CP engine reads the line off the finished placement and carries `runs` — the room faces that actually lie on it, measured, and possibly several disjoint pieces. WP-6.3 added `runs`, because until then an extentless mark was drawn as a short tick at **the middle of the plan**, which on the Tidewater placement put a tick and a triangle inside the drawing room with no wall under either. A mark that can be located on no wall of its own level is now named in the caption and **not drawn**; picking a plausible spot for it is the same error in a smaller place. `relaxation_marks` in `build/render_plan.py` and `relaxationMarks` in `workbench/app/src/sheet/derive.js` are the one rule, held together by `tests/test_sheet_symbols.py` and `derive.test.mjs`.

## The move that turns a treemap into a plan

The first working version produced perfectly valid rectangles in entirely wrong places. The fix was one rule:

**A circulation room with exterior walls on opposite sides spans the plan.** That is what a centre passage *is* — a slab from front door to back door dividing the house in two, with rooms sliced either side. Treating it as one more rectangle to pack produces a treemap; treating it as a slab produces a Georgian plan.

Two related constraints followed: only circulation may span (a porch with three exterior walls wants the south edge, not a slab through the middle), and a room whose *type* is a centre passage is placed near the centre, because that is what it is named for.

## Both levels together, not one after the other

Candidate layouts are generated for each level and scored **in pairs** on vertical alignment — upper wall lines that continue to a wall below, and wet rooms that sit over wet rooms. An upper layout that would score better on its own is rejected when it leaves walls unsupported.

**WP-7.1 made the upper level level-aware, and that is a bearing fix and not a stacking one.** `slice_rect` is now called for level 1 with the ground layout, and `snap()` prefers a wall line below over a bare bay line — so an upper cut lands on bearing where it can. Measured over 14 composed plans: transfer beams 166 → 109, relaxations 96 → 76. The relaxation definition was corrected with it: a cut that lands on a wall below is not a compromise, whatever the bay module says, which is this file's own prose finally executed. **`stacks_over` did not move (26/47 → 27/47 broken).** Do not expect the generator to fix it; that is a different problem and it is open.

**"The stair landing over the stair" stood in that list until WP-6.1 and was never true — and this paragraph then went stale twice in the other direction, which is worth more than the paragraph.** It read *"`stacks_over` is read by neither engine"* for three packages after WP-7.4 made both engines charge it, and it described a landing sitting anywhere over its own stair on a plan that now declares the claim. Both are corrected here. **WP-7.4 charges `stacks_over` in both engines** (soft, `geometry.STACK_W = 40`, never a hard pin), and **WP-11.6 authored `landing.stacks_over = "stair"` and `upperpassage.stacks_over = "passage"` onto `plans/tidewater-georgian-careful.json`** from the parti that had always declared both. Measured on the heuristic the day it landed: the plan goes from 3 claims (1 kept) to 5 (3 kept), and `plan_check`'s `landing-off-well` branch — which runs only where a room's `stacks_over` names the stair's own room, and which therefore had never fired in the life of the corpus — fired on the first run.

Nor is it only the stair, and the scope is far larger than the two shipped plans. **`stacks_over` is declared by 14 of the 21 partis — 50 claims — so every plan composed from one carries them**, and measured across every such parti composed against its own first native style, **26 of 49 are drawn broken on the heuristic and 23 of 49 on CP-SAT**. Roughly half, on both engines — and the clause that used to follow this one, *"because neither reads the field"*, was already false when it was written down: **WP-7.4 made both engines charge `stacks_over`**, soft, at `STACK_W = 40`. It is a preference the search trades off, not a blindness. **On the two shipped plans it is 3 broken of 7 claims** (heuristic, re-measured at WP-11.6, which authored two of the seven); it was three of six before. Each is a waste stack with nothing under it, declared in the record and contradicted by the drawing. WP-7.1's level-aware generator did not move this number (26/47 → 27/47 on the composed set); it is a different problem from bearing continuity.

*(One correction worth keeping, and it has now expired, which is the point of keeping it: this paragraph once put `landing.stacks_over = "stair"` on the Tidewater plan record when only `partis/five-part-palladian.json` carried it — right measurement, wrong mechanism. Since WP-11.6 the plan record carries it too, so the sentence that was wrong when written is true now. A dated correction is a fact about a moment, not a rule.)*

**EVERY MASSING ELEMENT HAS ITS OWN ENVELOPE (WP-11.9).** OQ 40 made a dependency a second element and `blocks_for` has placed one since 3 Sep, but SIX layers below the placer still read `footprint.width_ft`/`depth_ft` as the whole building. `build/elements.py` is the one reader — a LEAF, on `stacking.py`'s precedent, because `geometry.py` loads `plan_check.py` and `structure.py` loads `geometry.py`, so the six sit on three rungs of one ladder. Ruled by Lucas 5 Sep 2026: **per-element envelope with the union reported beside it; the lot cap on the BUILT EXTENT, elements only, gap excluded; a hyphen is an element and abutment is a constraint; `touches` is measured against the room's own element's face.** Taught, each measured on a hand-tagged Tidewater: `openings` places outward openings on the room's own element (18 → 23 placed, refusals 11 → 6, five authored windows recovered); `structure.wall_lines` takes an `elements=` list and emits four exterior walls per element, scoped to the elements holding rooms on that level (4 → 12 on the ground, 4 upstairs); `vertical_score` counts only ground walls under the upper level's extent (7 upper walls had been credited to walls under no upper floor); the lot cap reserves the dependency and the main block is sized from its OWN rooms (extent 97 → 77 ft, `geometry_report.lot_extent`); `plan_check.drawn` measures `touches` against the room's element (rooms reaching no exterior wall 8 → 1); `export_ifc` writes one slab per element per storey. **The whole shipped corpus is byte-identical** — placement, footprint, openings, fixtures, furniture, findings — because every plan here is one rectangle. The roof spans the UNION bounding box, which is what ruling 1 gave it, and `engine="cp"` still refuses a multi-element plan: `oq/the-proving-engine-cannot-place-a-second-massing-element`. Report: `docs/reports/wp-11.9-the-six-layers-that-read-one-rectangle.md`.

**AND THE SEARCH HOLDS THE SAME BAND, BY RANKING (WP-11.8).** `SHAPE_W = 6.0` charged a room drawn past its own ceiling and a candidate won while paying it. Rejection is not available to this engine — measured over 250 slicings of the Tidewater ground floor, the number of rooms out of band runs 3 to 10 and **zero candidates conform**, and the search has no conflict set and cannot decline to place. So the band is the FIRST KEY of the candidate acceptance and the score is the second: a candidate leaving fewer rooms outside their band beats one that scores better. **The key counts the band in BOTH directions** -- `band_violations` for the proportion ceiling and `under_band` for the room's own area floor, the same reader the report uses. The second half was forced by a guard rather than designed: ranked on the ceiling alone this took under-band rooms 15 → 21 and put `spec-builder-colonial`'s dining room back at 87 sf against a 122 sf floor, undoing WP-7.4 and caught by the assertion WP-7.4 left behind for exactly that. A ranking that honours one band by breaking the other is one term winning, not the record. Corpus-wide on `heuristic`, rooms drawn outside their own band **77 of 219 → 30**, under their own floor 15 → **13**, serious 745 → 681, minor 1031 → 992, fatal 113 → 136; the ceiling-only key read 28 / 21 / 695 / 1006 / 129 and is kept in the code comment because it is a correct measurement of a wrong key. The span-charge prune is tiered with it, because pruning across violation tiers would skip a better-conforming candidate for scoring worse.

**The structural price, which no critic layer reports:** over-capacity clear spans **13 → 25** and the worst **40.0 → 60.0 ft**, relaxations 64 → 86. A squarer room puts fewer cuts on the bay module and cutting on it is the only way this slicer makes a bearing line — WP-7.4's own mechanism running backwards. The 20 ft capacity and `bearing_lines`' 0.75 ft tolerance are untouched; `plan_check` has no span finding, so this cost appears in WP-11.8's report and nowhere a reader of a sheet would see it (OQ 98).

Every one of those twenty-three new fatals is `unreachable`: a squarer room shares less wall, so its declared doors lose the run they need. **A door-seating count was built and ranked above the band to protect that, and measured worse on every axis at once** (68 out of band, 131 fatal, 780 serious) — it is a proxy for the drawn layer's rule rather than the rule, and was deleted rather than reported. What settled the trade is the move registry: all 81 `unreachable` fatals carry `adjacent_placed`, so `add-the-grammar-door` reaches every one and the revision loop runs by default, while a room outside its band has no move at all. `geometry_report.shape_band` states the residual on both engines, written from `_disclose`.

**Two solver budgets since WP-11.8.** `BUDGET_BATCH_S = 40.0` is `solve()`'s default — `check_all`, `corpus.drawing()`, the CLI, the reference plans. `BUDGET_INTERACTIVE_S = 25.0` is passed by name by `workbench/server/evaluate.py` and `mcp_server/core.py`'s `place_plan`, the routes a person waits on. One number had served both, and WP-11.7's harder model left `spec-builder-colonial` five seconds short of its own proof.

**THE ROOM'S OWN PROPORTION BAND IS A HARD CP PIN, AND IT OUTRANKS THE RECORD'S `exterior_walls` (WP-11.7).** `geometry_cp.py` holds each room inside its type's `dimensions.proportion` ceiling — the band `plan_check`'s drawn layer already convicts a room for leaving, read through `GEO.shape_band`, which the soft aspect term had always used. It was planned as per-room side bounds at an authored tolerance τ and is the band instead, because the corpus already states the rule: measured first, **all 23 dimensioned rooms on the Tidewater record declare a shape inside their own band**, so the pin contradicts no authored record and there is no τ to source. Two rooms — `terrace` and `landing` — state no band at all, so their drawn shape is unconstrained here and unjudged by the drawn layer too; the model says so in `reqs.notes` rather than passing over it.

**The band and the declared walls cannot both be hard.** Measured: band held with all 22 wall pins released → OPTIMAL; band held with the pins held → INFEASIBLE at every footprint to ten bays; and the ladder gives up 14 or 15 of the 15 shape pins at **every** coverage floor from 0.97 down to 0.60, so coverage and packing are not the blocker. The first conflict core names three literals — the Back Hall's declared N wall, its declared S wall and its 7 × 16 ft programme — and a room declaring an opposite pair must span the 40 ft depth, which at 112 sf is 2.8 ft wide against a band of 5. **Ruled 5 Sep 2026: the band wins**, which is what CLAUDE.md has said since WP-2.2 (*"exterior_walls are aspirations, not rectangle edges … do not promote them to constraints"*) and what this engine had never done. A released wall pin keeps the heuristic's own 14 points in the objective.

**`_RANK = ("wall", "axis", "shape")`** is the downgrade ladder, lowest authority first, and a round releases the WHOLE of a rank rather than the pins the core happens to name — the narrow version needs eight rounds and its intermediate states go UNKNOWN at any bench-sized budget, while `_reinstate` already exists to win back an over-release. Sizes, doors, the entrance and capacity are absent from the ladder and still never downgrade.

**The axis pin states the spanning half and refuses the centring.** The room is the one the front door opens into — the relation the entrance-axis census itself walks — and where that picks out no room or more than one the axis is unjudged and nothing is pinned. `2x + w == W` was built and measured INFEASIBLE in 0.9 s with every wall pin already released: a 10.2 ft passage centred on a 60 ft front leaves two strips holding 2,000 sf of programme in 1,992 sf of floor.

Measured on `plans/tidewater-georgian-careful.json`, `auto`: **serious 79 → 56, stacks kept 0 → 3 of 5, no room drawn outside its own band** (the worst had been a 13 × 16 ft bedroom drawn 45 × 7), and the passage 22 × 19 → 9 × 40, spanning again. The cost was on the other shipped plan: `spec-builder-colonial` proves in 29.6 s against what was then a 25 s budget and fell back to the search. **WP-11.8 closed that the next day by splitting the budget** — the batch default is 40 s and the plan is CP-solved again, FEASIBLE in 40.7 s with 0 rooms outside its own band, so both shipped reference plans are drawn by the proof. `oq/a-shipped-plan-needs-thirty-seconds-and-the-budget-is-twenty-five` is closed.

**`stacks_over` and `wet_stack_with` are two fields since plan schema 0.8.0 (WP-11.6).** `stacks_over` is STRUCTURAL: the room on the level exactly one below whose plan this room sits over, judged by strict positive rectangle intersection in both engines and in `plan_check`'s drawn layer. `wet_stack_with` is SERVICING: the room this room's wet stack shares a chase with, on any level including its own, and its target need not itself be wet. Until 0.8.0 one field carried both duties — its description read *"for plumbing and structure"* — and the Tidewater record used it for a ground-level powder room naming a ground-level cellar stair, which **three readers dropped with a bare `continue` and no note**. The verdict now lives in `build/stacking.py`, a leaf (`geometry.py` loads `plan_check.py`, so `plan_check.py` may never load `geometry.py`, and those two must agree); `geometry_report.stacking` carries `claims == kept + broken + unjudged` with a reason on every unjudged entry, from a closed set; and `build/check_stacking.py` fails the build on a claim the record makes that no reader can answer.

**THE OVER-CAPACITY SPANS ARE ON THE RECORD AND IN THE CRITIQUE (WP-11.12).** `geometry_report.span_capacity` carried a COUNT and a CHARGE from WP-7.4 and nothing a reader could act on, and `plan_check` had no span finding at all — so a 60 ft unsupported joist run on the Tidewater upper floor reached no sheet, no critique and no `revision_report` (OQ 98's reporting half). `_disclose_spans` writes `marks` beside the count, on `relaxations`' own precedent (*counted AND locatable*) and in `_disclose` so both record writers carry it; `over_capacity_spans` and `spans_over_capacity` are two adapters over ONE arithmetic, and `span_check` is called from exactly one place in the tree with an `ast`-reading test holding it there. **Every published count is a FLOOR and the record, the finding and both plates say so** — `span_check` credits a bearing wall across the whole plate however short it runs, which is OQ 98's unruled measurement half. Deterministic corpus figure: **23 over-capacity spans, worst 60.0 ft** (`engine="heuristic"`; `auto` gave 28 on one run and is not reproducible — WP-11.8's published "13 → 25" was an unlabelled `auto` reading and the deterministic pair is 11 → 23). Report: `docs/reports/wp-11.12-the-span-nobody-was-told-about.md`.

**THE PROVING ENGINE PLACES A SECOND MASSING ELEMENT (WP-11.11).** `geometry_cp` built every room as `x = NewIntVar(0, Wi)` with `x + w <= Wi` — one rectangle, one non-negative coordinate space — and REFUSED a tagged plan rather than flatten two elements into one (which flatters the fatal count: rooms crammed into one rectangle are all trivially reachable). Refusing beat lying, and it meant the engine that PROVES was unavailable on exactly the plans `multi_element` discloses. `geometry_cp._element_boxes` is the one reader now, off `geometry.blocks_for`, and every statement the model makes about "the block" is made about the room's own element: containment, the coverage floor, a declared exterior wall, a spanning room's through-axis, an exterior door reaching the envelope, the bay grid, the span capacity, and `_absorb`'s four growth limits. **Ruled 7 Sep 2026, as `oq/the-proving-engine-cannot-place-a-second-massing-element` framed it: ONE coordinate space (CP-SAT integer variables take negative lower bounds, so a west dependency at x = −34 needs no second origin); an element boundary is NOT downgradable (a plain `m.Add`, never a `reqs.lit`, so it creates no assumption literal and cannot enter a conflict core); and it is affordable — a three-element fixture is proved OPTIMAL in 0.47 s.** The integer box is a SUBSET of the stated element, never a superset: this model is integer at 1 ft and a dependency's edges are not, and rounding to the nearest foot let a room sit half a foot outside the mass the record states. **The guarantee is that the MODEL for a one-rectangle house is byte-identical** — four `CpModel` proto hashes in `tests/test_cp_elements.py`, because CP under a wall-clock budget is not reproducible and pinning what it FINDS would pin the machine. **And the tags on the shipped record are still refused, for a better reason**: `engine="cp"` proves the hand-tagged Tidewater infeasible with a minimized core of one door, while the search reports the same tagging as an improvement. Report: `docs/reports/wp-11.11-the-prover-learns-the-massing.md` · new question: `oq/the-coverage-floor-is-an-exact-cover-per-element`.

**AN AT-GRADE APPENDAGE IS NOT A MASSING ELEMENT (WP-11.10).** `rooms/terrace.json` is the only record in the corpus with `void.within_footprint: false`, and its own note says leaving it out of placement *"is the right answer rather than a limitation"* — which is true of the BLOCK and was read for a year as true of the drawing. `build/appendages.py` is a LEAF that places it OUTSIDE the block, on `plan.threshold`'s precedent: derived after the solve, drawn open by both renderers, in `plan.appendages`, read by no structural layer. **Ruled 7 Sep 2026: it is not a massing element** — no walls, no storey, no roof plane, not in the built extent the lot cap is measured on (`geometry_report.lot_extent.at_grade` carries the ground it covers beside that cap). It writes no `block` tag, so `geometry.py`'s CP refusal never fires and no plan trades its proof for a search; `oq/the-proving-engine-cannot-place-a-second-massing-element` is untouched, and the gate `PLAN-OF-ACTION.md` put on this package applied only to the massing-element reading of it. **The room keeps `geometry` ABSENT**, and that is the whole mechanism: `structure.wall_lines` filters on `geometry`, `plan_check`'s `rooms_unplaced` on `takes_a_rectangle` and `geometry._record_prep` on `is_placed`, so all three stay blind by construction rather than by six more readers being taught. The pass runs BEFORE the level loop because `_place_interior` is the first pass in it and could not otherwise seat the door the appendage exists for. Measured: 4 placed, 2 refused by name, the corpus placement byte-identical and the openings moved on the five plans that declare a terrace. The cost is that the drawn rectangle is held against no band — `oq/an-at-grade-appendage-is-drawn-and-not-judged`. Report: `docs/reports/wp-11.10-the-terrace-that-is-not-a-massing-element.md`.

**The placer places two levels, and says so on the record.** `PLACED_LEVEL_INDICES = (0, 1)` in `build/stacking.py` is the ceiling both engines are written against — `_finish` does `best["ground"] if idx == 0 else (best["upper"] if idx == 1 else {})`. `plans/reference/bad-03-narrow-lot-townhome.json` declares three levels and its one level-2 room came back with **no geometry, no finding and no note** until WP-11.6; `geometry_report.multi_level` states it now, on `multi_element`'s precedent, and `plan_check.drawn`'s `rooms_unplaced` names any room the placer never reached. Placing a third level is a package of its own — `oq/the-placer-places-two-levels-and-says-nothing-about-the-third`.

Every misalignment that survives is named in the report: *"10 upper wall lines do not continue to a wall below; each is a transfer beam."* That is a cost, and it should be visible before anyone prices it. The figure was 21 on that plan before WP-7.1.

## When it will not fit

Grow the footprint first — add a bay before compromising a room. A room below its furniture minimum is a defect that survives the whole life of the building; a slightly larger house is just a slightly larger house. Only then shrink rooms toward their bands, and only then drop optional rooms.

Footprint depth comes from the **massing's own pile** — single-pile 22 ft, double-pile 36 ft — rather than from an invented aspect ratio. Getting that wrong produces a house of the right area and the wrong shape, which was the first version's most obvious failure.

## The drawing is a render of the data

`build/render_plan.py` emits SVG from the coordinates: rooms, walls, bay lines, windows on exterior walls, door marks where two rooms share an edge, dimensions, a scale bar. Nothing is drawn that is not in the plan record, so the drawing and the data cannot disagree — the same discipline as the order tool.

Since WP-9.1 the placed record is a record: plan schema 0.4.0 carries `geometry`, `footprint`,
`geometry_report`, the stair and per-opening positions on the plan itself, and the ONE list of
what is placement output and what is authored lives in `build/openings.py` —
`PLACEMENT_PLAN_KEYS`, `PLACEMENT_ROOM_KEYS`, `PLACEMENT_DOOR_KEYS`, `PLACEMENT_WINDOW_KEYS`
and `strip_placement()`. The DXF exporter and the revision loop both import it; a window's
`wall` is authored and stays, a door's is solver output and goes. A second spelling of that
list is refused by a source-reading test.

## The real solver (WP-2.3)

`geometry.solve()` now dispatches to **CP-SAT** (`build/geometry_cp.py`, OR-Tools) by default, with the randomised slicing kept as the fallback and cross-check the package text asked for. The division of labour, per the 25 Aug rulings:

**HARD — satisfiable or refused, never traded:** no-overlap, containment, near-total coverage; declared doors imply geometrically touching rooms (a shared run at least the pair's programme-scaled door overlap — all four interval bounds, since two end-gap inequalities alone admit a sliver narrower than the door); a threshold room with an exterior door is the entry and must reach the entrance front; each room at roughly its program size; each room's declared exterior walls. The absorb pass that tiles the ≤3% coverage void is capped at each room's own size band — residual void stays honest empty floor rather than a closet drawn at 3x its proof.

**Wall pins carry a stated refinement**, because `exterior_walls` speaks *exposure in the fully-massed house* — porches protrude, wings hold their own corners — which one flat rectangle cannot always hold (10 of 12 partis double-claim a corner on some level):

- a **protruding** room (3+ walls, or a non-circulation opposite pair) and a room in a **contested corner** harden to "reach at least one declared wall", the rest scored at the heuristic's own 14 points;
- any other wall pin stays fully hard **unless the solver proves a set of pins cannot co-hold** — exactly those pins downgrade the same way, and every downgrade is stated in `geometry_report.solver.refinements`. Doors, program sizes, the entrance and capacity **never** downgrade: they are what infeasibility is for. Because a solver core is *sufficient, not minimal*, a **reinstatement pass** then restores each downgraded pin alone at the footprint actually drawn: a pin that holds returns to being a hard fact, a pin that provably cannot keeps its downgrade with an individual proof, and a pin the budget could not re-prove says "carried, not proven" in its refinement note — never "(proven)".

**SOFT — the WP-2.2 compositional terms**, now weighted objectives rather than search preferences: bay snapping (relaxations stay counted, never forbidden), zoning, ceremonial depth, wet-over-wet stacking, symmetry-adjacent nudges, plus the level-score terms (area error, aspect sanity) mirrored term for term.

**On infeasibility** the caller gets both halves of the ruling: a **plain-language minimal conflict set** (CP-SAT's assumption cores, iterated to a fixpoint then greedily minimized within a budget — a set that ran out of minimization budget says so) *and* the heuristic's least-bad drawing, labelled `INFEASIBLE AS DECLARED` on the sheet itself. The footprint grows a bay before any requirement is blamed. A non-planar door graph — five rooms all pairwise doored — is the canonical true refusal: no arrangement of touching rectangles can realize K5, and the conflict set names the door pairs.

Solving is two-phase: a hard-only pass finds or refutes a placement fast, then the weighted objective polishes it, hinted both by the full heuristic search (soft-optimized, hard-repairable) and by the hard-only placement; every hard-valid result is scored with the heuristic's own scorers and the best is kept, the status saying which. Determinism: one worker, fixed seed — the same record yields the same drawing (exact reproducibility holds when the solve reaches OPTIMAL; a wall-clock-limited polish can land differently under different machine load, and the status says when that is the case).

**The workbench runs the heuristic per edit gesture, deliberately** — a proof takes seconds and the bench re-scores on a 400 ms debounce — and carries a *prove placement (CP-SAT)* control for the explicit act, which surfaces the conflict panel. The CLI, MCP `tdl_place_plan` and everything non-interactive default to the real solver.

## What this does not yet do, stated plainly

It produces **valid, dimensioned, drawable plans with every compromise reported**. It does not yet produce plans an architect would sign.
- **No wall thickness, no structural grid, no roof.** Rooms are clear dimensions. Turning them into a framed building is WP-3.1.
- **Doors are centred on the shared wall and now drawn with a swing arc**, but door position is still not placed *by rule* in the fuller sense PLAN-OF-ACTION.md's WP-2.2 brief describes — never in a window bay, never visible from a lavatory. See "What was deliberately not done" below.
- **`room-harmonic` proportions are not checked.** A style that binds that proportion pack does not yet have its principal rooms scored against it — that is a materially separate integration (reading a resolved proportion pack's ratio into a placement score) and is deferred, not silently skipped without a record.

## Compositional scoring (WP-2.2)

`build/geometry.py`'s `solve()` now reads `entrance_faces` from the plan's own `context` (mapped to this file's four-wall N/S/E/W model — a diagonal entrance such as `SE` is satisfied by either adjacent cardinal) and adds four scoring terms to the candidate search, alongside the pre-existing `level_score`/`exterior_score`/`adjacency_score`:

- **`entrance_score`** — the entry-porch (`function_class: threshold`) and whatever circulation room it opens into must reach the entrance-facing wall. This is the specific defect PLAN-OF-ACTION.md's WP-2.2 brief names — *"the current rendered Tidewater plan puts the portico inside the footprint"* — and it is weighted at the same order of magnitude `compose.py`'s own `SEV_W["fatal"]` uses (100), so no candidate in the 250-candidate search can win with the porch off the entrance wall when any candidate has it right.
- **`principal_and_service_score`** — a soft preference (3.5/2.0/3.0 points, an order of magnitude below `entrance_score`): principal rooms (`function_class` `public`/`living`/`dining`) toward the entrance front, service rooms (`function_class` `service`/`work`) toward the wall opposite it, with a room actually sitting ON the entrance wall penalised harder than one merely not-at-the-rear.
- **`ceremonial_score`** — checks the door-connected hop from a room to a higher-`privacy_rank` room actually goes spatially deeper into the plan (farther from the entrance wall), not just that a door connects them. Scoped to hops where privacy rank actually increases, not an arbitrary-length whole-plan traversal — see "What was deliberately not done."
- **`centre_hall_symmetry_score`** — gated on the same spanning-circulation-room test the bay-grid slicer already uses for a centre passage (`spanning()`): rewards a same-type room roughly mirrored across the spanning room's centreline within `18%` of the footprint's own width, with a small (not punitive) penalty per unmirrored room, since plenty of correct centre-hall plans carry one asymmetric service room.

**Verified against `plans/tidewater-georgian-careful.json`**, PLAN-OF-ACTION.md's own acceptance example (entrance faces S): the entry porch lands on the S wall, `kitchen` and `pantry` land in the rear half, and the relaxation count stays at 11 — unchanged from the pre-WP-2.2 baseline, well inside the "no more than two more" acceptance bar. `tests/test_composition.py`'s `TestSolveIntegration` pins all three.

The acceptance example's third clause — *"the drawing room and dining room flank the passage on the front"* — does **not** hold on this specific plan, and this package did not force it to. `dining`'s own record in `tidewater-georgian-careful.json` declares `"exterior_walls": ["N", "W"]` — the rear — which is real, hand-authored intent (dining paired with the breakfast room and kitchen at the back is a legitimate period Georgian arrangement), not an oversight this solver should paper over. A room's own declared `exterior_walls` is authoritative over every compositional preference in this package by design: `exterior_score`'s existing 14-point-per-missing-wall penalty already dominates `principal_and_service_score`'s much softer 3.5-point front preference, and it should — the room stated what it needs, and a soft compositional nudge does not get to override that. `tests/test_composition.py::TestSolveIntegration::test_dining_room_front_claim_is_a_documented_data_conflict_not_silently_forced` pins the conflict itself as a finding, not a bug: whoever next touches this reference plan (or the acceptance text) has an explicit, evidenced choice to make, rather than a silently-missed clause.

**`composition_parti` is read from the resolved kit** (`C["kits"][style]["slots"]["composition_parti"]`), per the brief's own task list, but as of this package no style's kit actually specifies it (`status: empty` across all 132) — so nothing in the scoring above branches on its value yet. It is read and named in the solve so the moment a future kit-authoring pass gives it real content, this file already has the hook.

**On the reference corpus.** PLAN-OF-ACTION.md's acceptance also asks that "the solver's re-placement of a good plan's topology scores within one severity band of the transcription." This is not independently meaningful in the current architecture: `plan_check.py` validates a plan record's own declared `width_ft`/`length_ft`/`exterior_walls`/`doors` — the fields a transcription or the composer already fixed — and never reads `room.geometry` or `geometry_report` at all. Running `solve()` on a reference-corpus plan cannot change what `plan_check.py` reports, because solving only adds coordinates for rendering; it does not touch, drop, or retype a single room. The clause is trivially true under the current separation between the critic and the geometry pass (nothing solving does can move a plan between severity bands), which is worth stating plainly rather than fabricating a before/after score that has no mechanism to differ.

### What was deliberately not done

- **`room-harmonic` proportion checking.** Reading a resolved proportion pack's ratio into a placement score is a materially separate integration from the scoring terms above and was not attempted this pass.
- **Door placement beyond centring.** Doors are still centred on the shared wall (unchanged) and now drawn with a swing arc, but "never visible from the lavatory" and "never in the bay a window occupies" are not checked — both need the elevation-scale bay/window layout WP-3.2 (the elevation generator) actually produces; attempting them against clear-dimension room rectangles alone would be guessing at a bay grid that does not exist yet at this layer.
- **`ceremonial_score` is scoped to direct, rank-increasing door hops**, not an arbitrary-length path search across the whole plan for "no backtracking anywhere." A full graph traversal is a reasonable next step but a distinctly separate piece of work from the direct porch→hall→principal-room case PLAN-OF-ACTION.md's own acceptance example names.
- ~~**This remains a heuristic hill-climb.**~~ No longer — WP-2.3 built the CP-SAT engine (see "The real solver" above); the slicing search remains as the fallback and cross-check, and as the interactive engine the workbench re-scores with per edit gesture.

## Next

WP-2.3 landed (see "The real solver" above and `docs/reports/wp-2.3-the-real-solver.md`). What the solver still does not model — wings and ells as real geometry (OQ 40), door position by rule, `room-harmonic` proportions — is stated in that report's "deliberately not done".

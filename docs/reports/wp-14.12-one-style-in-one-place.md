# WP-14.12: one style in one place

*25 September 2026. Branch `wp-14.12`, taken from `0ad8b49`. WP-14.11 (`111fa18^..9f639f5`) is
cherry-picked onto it at the lead's instruction, because this package extracts the lineage block
14.11 rewrote. Then the lead's tip `6946bcb` is merged in, carrying WP-14.10, 14.11, 14.13 and
14.14, in merge commit `d176a95`. Every verification figure in §IV is of the MERGED tree. Scope: PRD `docs/prd/phase-14-the-dossier-and-the-journey.md` §D, §E.1–E.4 and
§J.1, and the package text in `PLAN-OF-ACTION.md`, plus one item the lead added mid-package: route
a `brief:<id>` citation to the example it names. Every figure below was measured in this worktree.
Cite this report by filename: the Phase 14 numbers are this line's, and the number is not what
disambiguates a package in this repository.*

## What it is for

A style had two pages, and each was reached by its own address:

- the **Style Record** (`#/style/<id>`), one long page;
- the **Kit** (`#/kit/<id>/<slot>`), which reached the same style by a different address.

Neither page could be reached without a style. A bare `#/style` opened Tidewater Georgian, and so
did a bare `#/kit`, because both surfaces carried a `DEFAULT_STYLE`. So a link somebody pasted
opened a record the link did not name.

The package puts everything about one style in one citable place, the **Style Dossier**. It is
read section by section, in the order the corpus is consulted:

1. identify
2. filed under this
3. lineage
4. kit
5. proportions
6. plan types
7. constraints
8. faults
9. evidence

The dossier draws only the section in view and omits every empty one. A bare `#/style` is an
outline to recognise a style by, and never shows a record. `#/kit` becomes an alias for the
dossier's kit section.

## I. What was built

### The place

The place is decided by `dossier/sections.js::placeOf`, and nowhere else. It follows the PRD's
order (§E.2):

- **A style is present:** the dossier. With no section named, that is `identify`.
- **No style, the kit section and a slot:** the slot panel, at `#/style/-/kit/<slot>`. This is one
  slot across every style, on the `/api/slots/{id}` route nothing had used.
- **Anything else:** the Styles index.

### The dossier (`surfaces/StyleDossier.jsx`)

**The head.** It carries:

- the rank, as a `Term`;
- what the style is filed under, as a link;
- its years;
- its name, with its id as a margin note;
- its tradition's swatch;
- a `StylePicker` whose pick writes the URL;
- "Design a house in this style →" to `#/brief?style=`, for buildable ranks only;
- "place in the phylogeny →".

**The strip.** The strip is the dossier payload's own section list:

- It is in `DOSSIER_SECTIONS` order, whatever order the rows arrive in.
- Each section is labelled by its `section-<id>` glossary record.
- Each section carries the payload's count.

`listedSections` holds the server's zero-omission rule a second time on the way in. So a section
ever served at zero is still not offered.

**A section the strip does not list.** A URL may name such a section. It is REFUSED by name ("This
record has no Kit section"), and the page draws identify rather than an empty section.

**The style in hand.** `prefs.setStyleInHand` is called when the resolved payload is for the id the
page names. That is the only thing the dossier does with it:

- the store is written here;
- the index offers it as a link;
- nothing reads it into a render.

### The sections (`dossier/*.jsx`)

Each section is EXTRACTED from `StyleRecord.jsx` or `KitSurface.jsx`, not rewritten.

**Identify.** It shows, in order:

1. summary cards, one per listed section, each with its payload count;
2. the description;
3. the diagnostic tells;
4. `distinguished_from`, as links;
5. the defining characteristics.

**Members.** It lists `members` and `buildable_at` from the payload, each with a rank `Term`.

**Lineage.**
- The lineage block of `StyleRecord` is extracted with its `EdgeGlyph` call exactly as WP-14.11
  left it: `<EdgeGlyph edge={e} width={44} />`, at `workbench/app/src/dossier/Lineage.jsx:41`.
- Descendants are links now, where they were buttons that swapped the record in place.
- What a descendant's edge carries is read from the edge's SERVED flag through
  `lineage/carry.js`: `carriesKit` for the colour, and `carryTermOf` for the glossary word. It is
  not keyed on the edge's type (§II.4).
- It adds the cascade ladder, lifted into `relations.cascadeRows` so that the kit section and
  lineage read one spelling.

**Kit.** This is `KitSurface` embedded. `KitSurface` no longer has:

- a `DEFAULT_STYLE`;
- a picker;
- its own style state.

The dossier owns the style and the URL owns the open slot. The side panel's packs and massing
affinities went to the Proportions and Plan types sections. The cascade ladder stays in
`PANES.kit`, which is kept.

**Proportions.**
- The record's proportional-system prose comes first.
- Then `/api/styles/{id}/packs`, as its five provenances: bound, opted in, delivered, withheld and
  declined. Each group has a `pack-*` `Term` heading.
- The nearest ancestor's delivered group is shown. The farther ones fold behind the reader's own
  `delivered` fold in `state/prefs.js`.

**Plan types.** A LIST only (`oq/a-brief-cannot-name-a-parti`):

- massings, as `VariantPill`;
- partis and groupings, named with their ids and not linked.

**Constraints.**
- Each row's state is `judgment.constraintStateOf`, and its name is that state's glossary record.
- A judgment row keeps its hatched mark.
- `?constraint=` marks the row and scrolls to it.
- The row's own id is a link to exactly that place.

**Faults.** It reads the dossier payload:

- the verdict here;
- the faults licensed through the lineage;
- a link to the universal count, for style and variant ranks only.

**Evidence.** It shows the exemplars and sources, extracted. It adds the image records from
`/api/assets?style=`, each with its harvest status.

### The Styles index (`surfaces/StylesIndex.jsx`)

The index is `styles/styleTree.js`'s outline:

- traditions come in the server's order, and siblings by floruit;
- an orphan is listed rather than dropped.

`?q=` narrows the outline and keeps each match's filing chain above it. The index never fetches a
record.

### The relations panel (`components/RelationsPanel.jsx`)

The panel shows four things:

- Filed under;
- Filed under this;
- Neighbours, which is `relations.siblingsOf`: filing, not lineage;
- On the bench.

It sits in `PANES.kit` beside every section but the kit. On the kit section the pane is the kit's
own ladder, as it was when the Kit was a surface.

### The router and the citations

**`#/kit/<style>/<slot>` is an alias.**
- It is listed in `router.js::LEGACY_PATHS` and READ positionally, with the same `-` placeholder.
- It is never WRITTEN: `formatHash` answers the canonical address.
- `state/nav.js::canonicalize` rewrites the address bar by `replaceState`, as it already did for
  `#/cite/`.
- A bare `#/kit` is the index, because it named no style.

**`kit:` and `slot:` citations route into the dossier.** `kit:<id>#<slot>` opens the kit section on
that slot, and `slot:<id>` opens the slot panel. `citeFor` is the inverse of both.

**`names/names.js` takes the one constraint-id reader.** It is `citations.js::constraintStyleOf`,
where it had a local `lastIndexOf('.')`. A guard refuses the local copy.

**The palette's "The Kit" entry is merged into the style surface's entry.** The kit's words stay in
its haystack, so "kit" and "slots" still find it (`search-unit`).

**`brief:<id>` opens the intake holding that example.** The lead added this mid-package: WP-14.14
found that a guided example's brief citation opened an EMPTY Brief Intake. The cause was that
`routeCite` threw the id away, while WP-14.10's intake reads `?example=<id>` from
`place.params.example`. The fix is additive, and no grammar regex moves:

- `routeCite` returns `params: { example: <id> }`. The id rides as a param because that is where
  the intake reads it; as a selection key, `parseHash` would move it out of params.
- `citeFor` takes an optional third argument. It answers `brief:<id>` for a non-empty example, and
  null for the empty intake, which no citation names. `citeForPlace` passes the argument through.
- A citation's own params travel through the three writers and readers of a target: `nav.cite`,
  `hrefFor`, and the `#/cite/` branch of `parseHash`. They win over a link's query, just as a
  citation's selection keys win over carried context. Filters still never travel.

`brief` was the one documented exception to the router's identity. It is in the identity list
now.

### The server: one addition, on the workbench route

`/api/styles/{id}` reads `corpus.style()` now, which is `core.get_style()` with each `descendants`
row given its edge's `inherits_kit` and `slots`. Two properties hold:

- **The values are COPIED off the edges `corpus.phylogeny()` serves.** They are not re-derived, so
  the rule that decides the flag is spelled once. The join walks the styles in the order both
  functions walk them, and it REFUSES a row whose id or type disagrees with its edge.
- **`mcp_server/core.py` is byte-identical to the base.** `git diff 0ad8b49 -- mcp_server/` is
  empty. The MCP tool `tdl_get_style` serves `core.get_style`, and MCP payloads are held
  byte-stable in this tranche. The enrichment was first written into core, which is §II.5.

### Tests

**`src/dossier.test.mjs`** has 27 tests over the two pure modules. They cover:

- the three places, driven from `parseHash` addresses, and `placeOf` held to the site map's
  `stylePlaceKind` (§II.8);
- the strip's order, zero omission and deduplication;
- the refusal;
- the section address round trip;
- neighbours, pack groups, the fold, the ladder and the bench;
- that every word the dossier names is a glossary record;
- that a descendant is read by its edge's served flag, never by its type. A `hybridizes_with`
  descendant carrying the kit reads `carries-the-kit`, and a scoped one reads
  `carries-named-slots`.

It also holds the surfaces to six source facts:

- the style in hand is written once and read nowhere;
- the index fetches no record;
- no file on the style path names a default style;
- no new file sets text in `--ink-4`;
- every section component is drawn only under `view.id`;
- the lineage section carries no table of edge types, and it calls `EdgeGlyph` with `edge={e}`.

**`router-unit` is 128 checks.** It drives the alias table in both directions, including a cold
load that must leave the address bar rewritten. It also covers `brief:family-georgian`, whose
round trip asserts that the params survive the URL. A new check requires that no param
`routeCite` produces is spelled like a selection key, and its premise is asserted.

**`workbench/server/tests/test_dossier_routes.py`** gains three tests:

- every descendant's `inherits_kit` and `slots` is held to `/api/phylogeny` edge by edge, over
  every style some edge points at, and the premise is asserted both ways (some descendant carries,
  some does not);
- `core.get_style` rows stay `{id, type}`, and `corpus.style` adds the flag without reaching back
  into core's copy;
- the join's refusal is DRIVEN, by handing `corpus.style` the edges rotated by one, because both
  functions walk one order and the corpus cannot reach that branch.

## II. What was found

### Three pages said something the corpus does not say, and a guard is behind each now

**1. The bare addresses showed a record the URL never named.** `StyleRecord` and `KitSurface` each
defaulted to `tidewater-georgian`. A pasted `#/style` opened a record nobody chose, and the W5
check could only look for the absence of one word in the first 400 characters. Two things replace
that:

- the structural check (the index is on screen and no dossier head is);
- `placeOf`, which has no branch that opens a record without a style.

Two mutations are red:

- `placeOf` opening the dossier on a bare `#/style`;
- the dossier filling a missing style from the style in hand.

**2. StyleRecord told three styles they were unbound.** It told `egyptian-revival`,
`moorish-andalusian` and `mudejar` they were *"deliberately unbound to any proportion pack — a
named exception, not a hole (OQ 29)"*. The claim came from a list written into the page. Measured
against `/api/styles/{id}/packs` today, **all three bind packs of their own**:

| style | packs it binds |
|---|---|
| `egyptian-revival` | 1 (`facade-peristyle`, OQ 49's scoped binding) |
| `moorish-andalusian` | 3 |
| `mudejar` | 3 |

The list went stale when OQ 49 closed. The Proportions section reads the packs payload, so it
cannot say this.

**3. The constraints column carried *"295 of 660 constraints are that kind"*.** That is a
corpus-wide count typed into JSX: *a figure typed into a page is true the day it is typed*. It
is gone. The copy ratchet's two baseline rows for the retired files are removed, so the ratchet
only shrinks.

**4. StyleRecord's descendants carried a fourth copy of the edge-type table.** WP-14.11 removed a
type→carries table from three surfaces, because a type cannot say what an edge carries: a
`hybridizes_with` edge may or may not carry the kit. `StyleRecord` coloured its descendant rows
from a `CARRIES` table keyed the same way, and `/api/styles/{id}` served a descendant as
`{id, type}` only. So the extracted section had no flag to read. The coordinator's instruction
gave two options: serve the flag, or draw descendants with no carry claim. The flag is served. The
dossier test refuses any edge-type name in the lineage section's source.

**5. The first place the flag was served changed an MCP tool's payload.** The enrichment went into
`mcp_server/core.py::get_style` first. That function is what `tdl_get_style` serves, and MCP
payloads are held byte-stable in tranche 1. The lead's reminder caught it. It lives on the
workbench route now (`corpus.style`), and `mcp_server/` is byte-identical to the base. A test fails
if the enrichment moves back into core. That is mutation S5, and it is red.

**6. The first version of the fix made a second spelling of the rule, and WP-14.11's own test
caught it.** To let `style()` share the rule, it was lifted out of `phylogeny()` into a helper, and
`phylogeny()` was made to call the helper. The app suite went to 392 of 393:

- `lineage.test.mjs` models the server by reading `corpus.py`'s source;
- its premise regex pins the one line `phylogeny()` serves the flag with;
- a rewording fails there rather than letting the test model a server that no longer exists.

The failure was pointing the right way. The rule already had one spelling, in `phylogeny()`. So
`style()` now copies each descendant's flag off `phylogeny()`'s own edges, and `phylogeny()` is
byte-identical to 14.11's. That is the move `test_dossier_routes.py` then holds edge by edge.

**7. At 1280 px the dossier scrolls sideways by 100 px, and that is the shell's stated floor, not
a defect.** In the 1280×800 screenshots the rail's right edge and the search box are cut off.
`document.documentElement.scrollWidth` is **1380** on all seven addresses measured, on this tree
and on a `git archive` control of `0ad8b49` alike. At 1440 every page is exactly 1440. I first
wrote this down as a shell defect. Reading the merged `App.jsx` corrected that: `tokens.css` sets
`#root{min-width:1380px}` (WP-14.8), and only the front door and the Glossary release it
(`#root[data-reflow]`). The style surfaces are working surfaces and keep the floor like every
other.

**8. Two packages wrote PRD §E.2's order, and the merge showed that they disagreed.** WP-14.13's
`nav/navModel.js::stylePlaceKind` decides index, slot panel or dossier for the rail, the crumbs
and the page head. This package's `dossier/sections.js::placeOf` decides it for the surface. Each
was written without sight of the other. Swept over selections and addresses, they agree on every
address the router writes and part on a blank id: on `#/style/%20` the crumbs said the Styles
index while the surface fetched a record named " ", and a slot of spaces splits the same way.
`placeOf` reads `stylePlaceKind` now. Under the merge rule the lead's spelling survives, and its
reading is also the right one, because a blank id names no record. A test holds the two to one
answer and names the blank cases, and restoring the old body of `placeOf` turns it red.
`navModel.js`'s comment said "until that retirement lands" about `#/kit`. It landed here, and the
comment now says so.

### The kit table had no room for the column it exists for

The table has four fixed columns: group 132, binding 74, source 190 and faults 52 px. Beside the
ladder pane at a laptop's width, the slot column collapsed to one character ("a…"). The same
layout was on the base, where the Kit surface's side pane was `PANES.kit` at its 340 px default.

Two changes answer it:

- the table keeps a 640 px floor and scrolls sideways;
- on the kit section the relations panel steps aside.

`components/SlotRow.jsx` was not touched.

### Every count on the dossier reconciles with the payload it came from

Measured on `tidewater-georgian` against the API:

| section | on the strip | the payload beside the section |
|---|---|---|
| Kit | 94 | `/api/kit/tidewater-georgian?only_specified=true`: 94 slots of 97 |
| Proportions | 35 | the packs payload: 4 bound + 4 opted in + 26 delivered + 1 withheld + 0 declined |

`north-american` lists 9 members and 85 buildable styles, which are the dossier payload's own
lists. The walk holds each of these to the API rather than to a literal.

## III. What was deliberately not done, and what was done outside the listed files

- **`Chrome.jsx`: superseded at the merge.** Before the merge, this branch removed one line there,
  the rail's "The Kit" item. With `kit` gone from `SURFACE_PATHS` its click wrote `#/style`, and
  the walk's rail-reachability loop failed it. 14.13 rewrote the file from `nav/navModel.js`,
  which has no kit item, so the merge takes 14.13's file whole.
- **`e2e/walk.mjs`: one line outside the listed blocks.** In the filter round trip, `visit('#/kit')`
  became `visit('#/style')`. `visit` refuses an address the router does not WRITE, and it no
  longer writes `#/kit`, so that line would have failed on this tree alone. No `engineClaim`-pinned
  line was touched.
- **`Overview.jsx` is not touched.** Before the merge, its "The Kit" door (`nav.go('kit')`) went
  through `formatHash`'s alias and landed on the Styles index. 14.14 rewrote the front door, and
  on the merged tree no file but `router.js` names the kit surface.
- **`nav/navModel.js`: one comment corrected, and `dossier/sections.js::placeOf` now reads that
  module's `stylePlaceKind`** (§II.8). Nothing else in 14.13's files moved.
- **`EdgeGlyph.jsx`, `Phylogeny.jsx`, `FaultCard.jsx`, `Proportions.jsx`, the journey, the brief
  intake, the candidate set and `session.js` are not touched.** The lineage section's `EdgeGlyph`
  call is 14.11's own, `<EdgeGlyph edge={e} width={44} />`, at
  `workbench/app/src/dossier/Lineage.jsx:41`. 14.11 was cherry-picked, so `StyleRecord.jsx` was
  deleted here after 14.11 had edited it: the conflict was resolved by the deletion, and the
  edited line lives on in `Lineage.jsx`.
- **`workbench/server/app.py`: one route changed.** `/api/styles/{style_id}` returns
  `corpus.style(...)` where it returned `core.get_style(...)` (§I, the server). No other route
  moved.
- **`state/nav.js` and `router.js` also changed for the brief citation.** In `nav.js`, `nav.cite`
  passes a citation's params. In `router.js`, `hrefFor` and the `#/cite/` branch of `parseHash` do
  the same. These files were already this package's, for the `#/kit` alias. The
  selection/params split, the grammar regexes and `SELECTION_KEYS` are untouched.
- **Two things were left as they were extracted:**
  - `KitSurface`'s slot-detail fetch still builds `/api/kit/${styleId}/slot/${openSlot}` without
    `api/client.js`'s `seg()` encoding;
  - `SlotRow`'s group column is set in `--ink-4`.
  Neither is new in this package. Each is one line for whoever owns those components.
- **No new open question.** Every finding above was fixed by construction, or belongs to a package
  already named.

## IV. Verification

**Suites and checkers:**

All on the merged tree (`d176a95` plus this package's follow-ups):

| run | result |
|---|---|
| `node --test` (app suite) | 480 of 480 (402 before the merge) |
| `router-unit` | 128 checks |
| `search-unit` | 13 checks |
| `check_frontend.py` | OK: entry 579 KB, `three` 564 KB in its own chunk, the glossary not bundled |
| `pytest test_dossier_routes.py test_grammar_agreement.py` | 17 + 9 passed |
| `check_glossary` | OK, 121 records, 482 quotations verified |
| `check_ids` | OK |
| `check_citations` | 3,073 citations over 716 files, 0 dangling |
| `check_counts` | 133 claims, 0 stale |

**`npm run build`.** It printed one warning: the pre-existing chunk-size notice for
`coastlines-fine`, 1.19 MB.

**Mutations.** Twenty-one were run. Each was asserted LANDED before its colour was read, and each
was restored byte-for-byte. **All twenty-one went red.**

The dossier and the router, run on the tree before the 14.11 cherry-pick. They were not re-run
after it, and 14.11 touched none of those lines:

1. `placeOf` opens a record on a bare `#/style`.
2. The dossier fills a missing style from `prefs.get().styleInHand`.
3. A zero count is listed.
4. An unlisted section is drawn.
5. A count literal is placed in JSX (copy ratchet).
6. `canonicalHash` answers null (router-unit).
7. `nav` does not rewrite a legacy address in the address bar (router-unit).

The S and B sets were run just before the merge. `git diff 7868c1c d176a95` is empty over every
file they mutate and every test they run.

The descendant flag, run against `test_dossier_routes.py -k descendant`:

- S1: the flag is keyed on the edge type, as a second spelling.
- S2: the flag is a constant.
- S3: the scope is dropped.
- S4: the route reads `core.get_style` again.
- S5: the enrichment moves into core, which changes the MCP payload.
- S6: the join's identity check is deleted.

The brief citation, run against `router-unit`:

- B1: `routeCite` drops the id.
- B2: `citeFor` cannot recover it.
- B3: the `#/cite/` reader drops params.
- B4: `hrefFor` drops params.
- B5: `nav.cite` drops params.
- B6: the id rides as a selection key.
- B7: `citeFor` accepts an empty example.

The merge's reconciliation, run against `dossier.test.mjs` on the merged tree:

- P1: `placeOf`'s own second spelling is restored.

**The walk.** Run on the final tree, on port 8271: **282 ok, 0 FAIL, 4 COULD NOT EVALUATE, exit
3.** The four are the base's own four, word for word, and none is this package's:

- the fallback caption;
- the gable-end stacks;
- the stoop;
- the drawing set's face caption.

Before the 14.11 cherry-pick the same walk read 260 ok with the same four. The 22 more arrive with
the cherry-pick, which adds 14.11's lineage and fault-card blocks. `walk.mjs` did not change
between the two runs in any other way.

**Screenshots** are at 1440×900 and 1280×800, of:

- `#/style`
- `#/style/tidewater-georgian`
- `#/style/tidewater-georgian/kit/cornice`
- `#/style/tidewater-georgian/proportions`
- `#/style/north-american`

They are in the session scratchpad, `shots-14.12/`.

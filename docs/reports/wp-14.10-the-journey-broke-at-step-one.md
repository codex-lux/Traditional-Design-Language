# WP-14.10 — The house journey, and the step that broke it

*Status: COMPLETE on branch `wp-14.10` from `0ad8b49`, 25 September 2026. Not pushed. Built to
`docs/prd/phase-14-the-dossier-and-the-journey.md` §G, §G.1 and §I.5. Re-derive the figures below
on your own tree rather than quoting them: each is a statement about the tree it was measured on.*

## I. What was asked, and what was built

Brief → candidates → plan → drawings → export was spread over three rail groups with static
metas and one sentence on the Overview naming the order. A reader on the Drawing Set could not
tell that the house in front of them had been refused, that the compose it came from had failed,
or which candidate the bench's plan even was. §G gave that a pure reading (`journey/journey.js`,
built by WP-14.6); this package draws it and makes the session hold the two facts it needs.

- **`components/JourneyBar.jsx`** — one bar, mounted once by `App.jsx` above `<main>` on the six
  house surfaces (brief, candidates, workbench, drawings, export, transcription) and never in full
  screen. A `nav` labelled *house journey*; each step an anchor carrying `data-step`, the current
  one `aria-current="step"`; the state words stand OUTSIDE the anchor, so a glossary `Term`
  (refused, fatal, serious, unjudged) is never inside a link. A blocked step is a
  `<span data-step data-blocked>` and never an anchor. Next is an anchor only where the step can
  proceed; otherwise it is the journey's own reason. The provenance line names the plan on the
  bench and links back to where it came from.
- **`journey/bar.js`** — the bar's own decisions as pure functions: where it is shown
  (`showJourneyBar`), which step is a link, which is blocked, what Next offers, and whether the
  origin may be linked (`barView`). A step made FROM the plan (drawings, export) is a link only
  while the step before it can proceed, so a refused house shows *blocked: refused* on both and no
  link to a drawing the refuse-to-draw contract forbids is on the page to be followed. A plan from
  an EARLIER compose names its candidate and does not link, because the Candidate Set shows the
  session's current job, which would be somebody else's candidate.
- **`journey/sessionWrites.js`** — every write the journey's reading depends on, pure:
  `composeHandlers` (the ONE handler set both compose streams hand `jobEvents`), `reattach`,
  `jobExpired`, `composeStart`, `planFromOf`, `briefNameOf`, and the two sanitisers that read
  `planFrom` and `jobError` back from storage.
- **`state/session.js`** — `planFrom` and `jobError` per §G.1; the persisted object is
  `{ brief, jobId, planFrom, jobError }` under the unchanged `'tdl-workbench-session'` key;
  `recordPlanFrom(kind, plan, extra)` is the one writer the three surfaces call.
- **`journey/briefForm.js`** and **`surfaces/BriefIntake.jsx`** — step one, which is where the
  journey broke (§II.1). The budget tiers are the brief schema's own enum; the style is chosen with
  the `StylePicker` every other surface uses and is never a constant; `?style=` seeds it and
  `?example=<name>` loads a shipped brief by name and then leaves the address; Compose is a real
  `<button>`, disabled until the brief states the two fields its schema requires, and that test
  is the journey's own brief step rather than a second spelling of it.
- **`surfaces/CandidateSet.jsx`** — its reattach stream records a failure, releases its
  `EventSource` when the surface goes away, and opening a candidate records `planFrom`.
- **`surfaces/PlanWorkbench.jsx`** and **`surfaces/Transcription.jsx`** — one import and one
  changed line each: the example load and "send to the bench" record where the plan came from.
- **`App.jsx`** — the import and the mount.

## II. What was found

**1. STEP ONE REFUSED THREE OF ITS FOUR OWN BUDGET CHOICES.** `BriefIntake.jsx` offered
`entry`, `move-up`, `custom` and `estate`; `schema/brief.schema.json` admits `value`, `mid`,
`custom` and `unlimited`. Re-derived here with `jsonschema` on `briefs/family-georgian.json`:
`entry`, `move-up` and `estate` are REFUSED (*"is not one of ['value', 'mid', 'custom',
'unlimited']"*), `custom` is admitted. `jobs.submit` validates the brief before a job exists, so
a reader who chose any of the three was told the whole brief was refused. The form now reads the
enum from `GET /api/schema/brief` (`briefForm.budgetTiers`); a tier an older persisted draft
carries that the schema does not admit is SHOWN as off the schema rather than silently dropped
from the reader's brief (`offSchemaTier`).

**2. A REFUSED BRIEF ERASED THE LAST GOOD RESULT.** `compose()` did
`session.set({ result: null, progress: [] })` BEFORE `api.compose`, so a brief the server refused
at submit wiped the candidates the previous brief had produced. `composeStart(job_id)` is written
only once the server has accepted the job; a refused brief now leaves the last candidates
standing, which is what they still are.

**3. A FAILED COMPOSE WAS REPORTED NOWHERE, BY TWO DEFECTS THAT HID EACH OTHER.** Brief Intake's
stream was the one that looked at `error` — into its own component state — and `compose()` calls
`go('candidates')` immediately, so that stream was closed by Brief Intake's own unmount effect
(the leak fix an earlier adversarial audit made there, whose comment says exactly this) as soon as the navigation rendered —
before a compose of any length could report anything. The stream that actually listened was the Candidate Set's reattach, whose `error`
was `() => {}`; and a job the server had already failed (`api.job` status `error`) was forgotten
with `session.set({ jobId: null })`, its reason discarded. Either way the Candidate Set read the
job as gone or still composing. `composeHandlers` writes `jobError {state: 'failed', reason,
jobId}` on the stream's `error` for both sites; `reattach` writes it from `api.job`'s own error;
a 404 is `expired`, named, rather than a silent forget.
**The guard that existed could not see it**: `sse_handlers.test.mjs` read each call site's handler
KEYS and asserted every compose event was registered — and `error: () => {}` registers `error`.
A key that is present and does nothing IS the defect. The file now holds both call sites to
`composeHandlers(session, <job id>)` with nothing spread over it, and DRIVES the handler set
(what each handler writes), which is what the mutations below bite on.

**4. EVERY VISIT TO THE CANDIDATE SET DURING A COMPOSE LEAKED A STREAM.** The reattach called
`jobEvents(...)` for its side effect and discarded the handle, so nothing could close it; a
reader moving between the bench and the Candidate Set during a long compose opened a new
`EventSource` each time, against a browser's six per origin. Brief Intake had already learned
this and closes its own. The handle is kept and the effect's cleanup releases it; a source guard
in `sse_handlers.test.mjs` says so, and says that it is a reading of source (a React effect
cannot be driven under `node --test`).

**5. THE BRIEF WAS NAMED BY ITS CITATION.** The Candidate Set's eyebrow printed `result.brief` —
`brief:family-georgian` on the shipped example — because the result carries the citation and not
the name. `briefNameOf(result, brief)` reads the name the brief itself states, falling back to the
id in a code span. **Two neighbours of this are not in this package's files and are named for the
lead**: `routeCite('brief:x')` (`citations.js`) discards the id and lands on a bare Brief Intake,
so a brief citation reaches no brief; and `RecordLink` prints a `brief:` citation raw, because the
search index carries no briefs to name it from.

**6. THE JOURNEY CANNOT ATTRIBUTE AN EVALUATION WHOSE CHECK ERRORED, AND THE BAR IS WHERE THAT
BECOMES VISIBLE.** WP-14.6's §III.1 found it in the pure module and wrote it down before any
surface drew the bar: `core.check_plan`'s two error payloads carry no plan id, so on a record
whose check errored and whose placement was refused, the journey reads the plan as *not yet
evaluated* and the drawings and export steps as *ready*, and the bar makes both links. **Measured
here, the shipped corpus does not reach it today**: each of the sixteen plan records was run
through `evaluate.py` exactly as the bench route calls it, and on `engine="auto"` **0 of 16
checks errored** and 16 of 16 name their plan, while **11 of 16 placements came back refused**
(`engine="heuristic"`: 0 of 16 errored, 4 of 16 sketches refused). The common half of the
precondition is common; the other half is absent today, and has been present three times before
(WP-11.3, WP-11.4 and WP-13.6 each shipped a placed record failing its own schema). The fix is a server line and a
journey line and the server is outside this package's files; writing the journey half alone would
read a key nothing writes. Filed as `oq/an-evaluation-whose-check-errored-names-no-plan`.

**7. `planFrom` IS RECORDED BY THE THREE WRITERS §G.1 NAMES AND NO OTHER, AND THE IDENTITY RULE
KEEPS THE REST HONEST.** A pasted record, an undo, and Transcription's DXF-complete load (which
loads the bench directly rather than through *send to the bench*) do not write it. §G's rule —
`planFrom` counts only when `planFrom.planId === plan.id` — makes a record with another id read
*origin not recorded*, which is true. The limit is a paste that keeps the SAME id as an example
the reader loaded earlier: it inherits *an example*. That is one line per path and each is in a
file this package may change by two lines at most; it is named rather than spent.

## III. Verification

- **App suite** (`node --test` in `workbench/app`): **392 of 392** pass. A `git archive` of
  `0ad8b49` gives 361 of 361; the +31 are this package's own — `journeyBar.test.mjs` 11,
  `sessionWrites.test.mjs` 7, `briefForm.test.mjs` 8, and `sse_handlers.test.mjs` +5 net.
- **The browser walk** (`HEAVY_CALLS_PER_HOUR=10000 WALK_PORT=8251 bash
  workbench/scripts/walk.sh`): **273 ok, 0 FAIL, 4 COULD NOT EVALUATE, exit 3.** The four are
  exactly the base's four — the fallback caption on a sheet drawn by the corrective rounds, and
  `good-03-parlor-drawing-room-house`'s absent stack, absent stoop and absent
  `date_of_representation` — so there is no FAIL to attribute. **21 check sites are this
  package's**: the Brief block's 7 (the budget offers tiers; every option is the schema's; all
  four of them; Compose is a button; `?style=` seeds the style by name; `?example=` loads a brief
  by its name; the address then names its style), the refused-record block's 4 (the bar is above
  the surface and outside `<main>`; the drawings and export steps are spans reading *blocked:
  refused* with no `href`; the plan's Next is its reason), the refusal contract's 3 (the Drawing
  Set and Details & Export each read their own step as blocked, and the Drawing Set offers no
  link on to the export), and the journey block's 7 (the router knows all six house surfaces; the
  bar is on exactly those six; the last step offers no Next; Next on four surfaces is a link only
  where its step can proceed). Exit 3 is the walk's own *an unjudged walk is not a green one*.
- **`npm run build`**: builds, 137 modules; the one warning is the pre-existing chunk-size note on
  `coastlines-fine` (1,189.51 kB). **The walked bundle is the current code**: the walk ran on a
  `dist/` built before the mutation harness last rewrote (and byte-identically restored) six
  sources, so their mtimes were newer than the bundle; rebuilding afterwards produced asset names
  IDENTICAL to the walked ones (`index-SBCICQ9P.js`, `three-scene-Ci05hVoI.js`), and vite's names
  are content hashes.
- **Checkers**, each run alone for its own exit code: `check_frontend` 0 (router-unit 124,
  search-unit 13, the lazy tiers hold), `check_glossary` 0 (121 records, 478 quotations),
  `check_ids` 0, `check_citations` 0 (3,067 citations over 708 files, 0 dangling), `check_counts`
  0 (122 claims over 15 files, 0 stale); `gen_open_questions.py --check` current.
- **One red, predicted and not this package's to clear**:
  `tests/test_wp46_packs.py::test_claude_md_open_question_list_is_derived_from_the_file_not_asserted_against_a_literal`
  fails here and passes on a `git archive` of `0ad8b49`, because the register now holds 126 open
  of 221 entries and CLAUDE.md, which this package may not edit, lists 125 of 220 (§VII).
- **Python tests that read the files this package touched**: `tests/test_proof_and_search.py`'s
  bench-text guards, 6 of 6 pass.
- **The merge with the lead branch**: a trial `git merge-tree` against `ff2e168` conflicts in
  `e2e/walk.mjs` ONLY, and only because WP-14.11 and this package each appended a block after the
  Glossary block. The resolution is both blocks, this one first, each with its own closing brace;
  that file syntax-checks clean under `node --check`.

## IV. Mutations — 17, all red, each asserted LANDED and restored byte-for-byte

Each was applied by a harness that requires the old text to occur exactly once and the file to
change, runs the named test file for its own return code (no pipe), and proves the restore by
hash before the next.

| # | Mutation | Red in |
|---|---|---|
| M1 | a budget option outside the enum (`estate` appended) | `briefForm.test.mjs` |
| M2 | the bar's blocked rule deleted (`blocked = false`) | `journeyBar.test.mjs` |
| M2b | the step drawn as an anchor whatever the bar says | `journeyBar.test.mjs` |
| M3 | the Candidate Set's `error` spread back to `() => {}` over the factory | `sse_handlers.test.mjs` |
| M3b | the Candidate Set's own literal handler set, `error: () => {}` | `sse_handlers.test.mjs` |
| M3c | the factory's `error` swallowing the failure | `sse_handlers.test.mjs` |
| M4 | a plan from another job linked to the current candidates | `journeyBar.test.mjs` |
| M5 | the bar shown in full screen | `journeyBar.test.mjs` |
| M6 | App mounting the bar unguarded | `journeyBar.test.mjs` |
| M7 | the session persisting the result as well | `sessionWrites.test.mjs` |
| M8 | a server-side failure keeping the job | `sessionWrites.test.mjs` |
| M9 | the default style restored to `tidewater-georgian` | `briefForm.test.mjs` |
| M10 | Compose back to a `Chip` | `briefForm.test.mjs` |
| M11 | the brief's readiness re-spelled beside the journey | `briefForm.test.mjs` |
| M12 | a `planFrom` of any kind read back | `sessionWrites.test.mjs` |
| M13 | the Candidate Set's stream no longer released on unmount | `sse_handlers.test.mjs` |
| M13b | the Candidate Set's stream handle discarded again | `sse_handlers.test.mjs` |

**M13 WAS BLIND ON ITS FIRST RUN**: the stream-release fix had no guard at all, so the mutation
had no test to name. The guard was written and the mutation re-run red, and M13b — the handle
discarded, as it was before this package — was added beside it. The table is ONE run of all
seventeen on the committed tree, not an accumulation of passes. It is a source guard and
says so; the property is structural to one React effect.

## V. Screenshots

Four, at `scratchpad/shots-14.10/`, taken against this worktree's own server and bundle:
`brief-1440x900.png` and `brief-1280x800.png` (`#/brief?style=tidewater-georgian`), and
`workbench-1440x900.png` and `workbench-1280x800.png` (`#/workbench`, the Tidewater example
loaded and solved).

**What a practitioner sees on the brief.** Under the masthead, one line: *A HOUSE — 1 Brief
stated · 2 Candidates none yet · 3 Plan no plan on the bench · 4 Drawings needs a plan · 5 Export
needs a plan | Trace a drawing → 3*, and at the right end, in italic, *compose the brief first*:
the Next is its reason and not a link, because no candidates exist yet. The style field reads
*Tidewater Georgian* by NAME, seeded from the address; the two shipped briefs are offered by their
own names (*Small bungalow for two · Family house, Tidewater Georgian*); the budget tier offers
exactly `value`, `mid`, `custom`, `unlimited`; Compose is a bordered button at the top right. The
glossary definition each step carries is in the DOM for its description and is not drawn.

**What a practitioner sees on the bench.** *3 Plan refused · 1 fatal · 61 serious · 4 unjudged*,
the four terms dotted-underlined as glossary Terms, then *4 Drawings blocked: refused · 5 Export
blocked: refused*, neither a link, and *blocked: refused* as the Next. A second line names the
house: *On the bench: Tidewater Georgian, five bays, carefully planned
`tidewater-georgian-careful` · the example*. Before the sheet is read, the reader knows the house
is refused, how badly, and that no drawing of it may be taken away. The brief and candidates steps
still reading *stated* and *none yet* is TRUE rather than stale: this plan came from an example
and not from that brief, and the origin line says so. The bar is **57.6 px** tall at both widths.

**At 1280 the right end of the bar is off the screen, and the bar is not why.** Both 1280 shots
scroll **100 px** sideways and the right-aligned Next sits beyond the window. Measured: hiding the
bar leaves the same 100 px, and the Overview and Brief Intake show the same 100 px with no bar,
fresh-loaded or resized from 1440. `#root { min-width: 1380px }` in `theme/tokens.css` is the
shell's deliberate floor, which the Glossary alone releases with `data-reflow` (0 px there). A
reader at 1280 therefore meets the bar's one sentence of advice only by scrolling. That is the
shell's floor meeting a right-aligned item, named for the shell's package rather than worked
around in this one.

## VI. Deviations from the brief, stated

- **`journey/journey.js` gains one word**, `next`, in `JOURNEY_WORDS`. The bar's Next link needs a
  word and the rule is that every word is a glossary record's or the journey's; adding it to the
  journey's own table is the one place it may live. The file is WP-14.6's.
- **`App.jsx` is two hunks, not one**: the import at the head of the file and the mount above
  `<main>`. A mount cannot be written without its import.
- **`jobError.reason` may be `null`**, where §G.1 types it `string`: an `error` event that carries
  no text is still a failure, and inventing a reason for it would be the fake in the other
  direction. The bar prints the journey's *failed* and nothing after it.
- **`composeStart` is written after the server accepts**, not before the request as §G.1's
  writer table reads (§II.2 is why).
- **Full screen is not driven by the walk**: only the Phylogeny surface offers a full-screen
  control today, and it is not a house surface. `showJourneyBar(surface, full)` is driven under
  `node --test` instead (M5, M6).

## VII. Not done, deliberately

- **The server half of §II.6** (`evaluate.py` stating the plan id) — outside this package's files.
- **`routeCite('brief:x')` and `RecordLink`'s raw `brief:` citation** (§II.5) — WP-14.12's and
  WP-14.8's files. **The address a brief citation should route to is `#/brief?example=<id>`**:
  that is the key Brief Intake reads (`place.params.example`; `formatHash('brief', {}, { example:
  'family-georgian' })` is exactly `#/brief?example=family-georgian`), it loads
  `/api/briefs/examples/<id>`, and on both shipped examples the brief's own `id` is its file stem,
  so `brief:family-georgian` maps with no table. Once loaded, the parameter leaves the address and
  `?style=` takes its place, so a refresh keeps the reader's edits rather than reloading the
  example over them.
- **`planFrom` on a paste, an undo and the DXF-complete load** (§II.7).
- **`PLAN-OF-ACTION.md` and `CLAUDE.md`** are not edited, as instructed. **One consequence the
  lead must act on**: `tests/test_wp46_packs.py::test_claude_md_open_question_list_is_derived_from_the_file_not_asserted_against_a_literal`
  holds CLAUDE.md's open-question list to the register, and this package adds an open entry, so
  that test is red on this branch until the list and its count gain
  `oq/an-evaluation-whose-check-errored-names-no-plan`.
- **The full `check_all.py` was not run.** What was run is listed in §III.

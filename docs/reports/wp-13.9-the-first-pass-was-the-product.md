# WP-13.9 — the first pass was the product

**Status: COMPLETE (19 September 2026).** Ruled by Lucas the same day, against the bench's own
Tidewater sheet.

Lucas read the Plan Workbench and found sixty drawn findings — most of them the `unreachable`
fatal — beside a revision panel reading `REVISED · 0 ROUNDS · 0 MOVES APPLIED · CONVERGED —
NOTHING LEFT A MOVE COULD ANSWER`. His words:

> During plan solving, there doesn't seem to be any level of recursive self-improvement
> directed towards resolving any of the fatal issues that arise during the generation. So
> what's produced and surfaced is basically a first pass that has all of the problems that the
> program already identified but didn't actually bother to resolve itself. There should be at
> least one or two steps of recursive self-improvement based on the criticisms identified
> before the plan is surfaced to the user.

That screen was four defects, and **none of them was the loop failing at what it had been asked
to do**. Each is below with its measurement.

---

## I. What was on the screen, mechanically

1. **Solve never ran the loop at all.** `re-solve`, an example chip and an undo each ran exactly
   one `POST /api/plan/evaluate` (`PlanWorkbench.jsx:145-173, 270-277, 470`). The loop was
   reachable only from two chips submitting a job. **The panel in the screenshot was a leftover**
   — the `revision_report` of an earlier `revise (proof)` job, riding on a record that had since
   been loaded and re-solved.
2. **On the search engine the door move was never offered.** See §II.
3. **Two placements on one screen.** The sheet's sixty rows were judged on a hill-climb
   placement `typefacts` had refused; the panel's key was measured on a CP placement from the
   job's own separate solve. `RevisionPanel.jsx` admitted "the two can differ" and the reader
   was left holding both.
4. **The `unreachable` sentence was false on five of ten rooms.** See §V.

---

## II. THE RULING'S FIRST HALF: A STRANDED ROOM WITH A PLACED NEIGHBOUR IS ANSWERABLE ON EITHER ENGINE

`critique.classify` tests `_is_placement` **before** it asks `_intended_move`, and that function
returned `engine != "cp-sat"` for the `unreachable` and `cut-off` kinds. So on the only engine
that produces these fatals in bulk, `add-the-grammar-door` — the move the 1 Sep ruling granted
for exactly this finding — was never offered, and `revise._choose` reads
`assessment["actionable"]` alone.

Measured on `plans/tidewater-georgian-careful.json`, `engine="heuristic"`:

| | classed `placement` | classed `actionable` | classed `architect` |
|---|---|---|---|
| as shipped | **10 of 10** | 0 | 0 |
| the same findings relabelled `cp-sat` | 0 | **9** (`add-the-grammar-door`) | 1 |

All ten carry a non-empty `adjacent_placed`. The one that stays `architect` is `primary`, which
already declares a door to all three of its placed neighbours — the move can only ADD one.

**Four files said otherwise in prose and nothing asserted it.** `CLAUDE.md:3565`,
`PLAN-OF-ACTION.md:1750`, `docs/geometry.md:44` and WP-11.8's own report all carried
"`critique._intended_move` answers every one with `add-the-grammar-door` and the revision loop
runs by default". The *evidence* half re-derives exactly; the *consequence* half described a
counterfactual. It survived eleven days and an adversarial audit because the one test near the
behaviour asserts that `adjacent_placed` is a **list**. All four are corrected, WP-11.8's by an
addendum rather than an overwrite.

**The lever is not lost.** What the old class said — a proof or a wider pool might have seated
the door the author already declared — is still true on the search, so `classify` attaches it to
the row (`lever`, `lever_move`, `lever_why`) and `revise`'s `remaining` publishes it for both
classes. The class says what the loop will DO; the lever says what an engine setting could still
change.

This closes the `unreachable`/`cut-off` half of
`oq/a-placement-finding-is-classed-by-what-the-engine-is-for-five-kinds`. **The other four kinds
are untouched and the question stays open on them.**

---

## III. THE RULING'S SECOND HALF: THE LOOP READS THE REFUSAL

`build/revise.py` had **no reading of `geometry_report.refused` anywhere**. `_improves` guarded
`could_not_evaluate` and the key. So the loop could move a room, re-place, watch the key fall,
accept, and hand back a record WP-13.4 forbids any surface to draw — reporting a lower key on it.

A refusal is the drawing's `fatal`, and it is guarded the way a fatal is:

```python
if _newly_refused(new, old):
    return False
```

**The direction is the point.** A placement already refused when the loop starts may still be
improved — refused → refused on a lower key is accepted — because refusing to work on a refused
house would leave the reader with the first pass and nothing else, which is the complaint. What
is refused is *making* one. Both shipped plans are refused before the loop starts, so this is
the live case rather than a shape.

The report carries `placement_refused: {before, after}` and `summary.refused_before/after`; a
round rolled back for a new refusal says `rolled_back_by_refusal` rather than wearing the
measurement's verdict, which would be a refusal with one message for two causes.

---

## IV. THE ROUNDS RUN BEFORE THE PLAN IS SURFACED

`evaluate.evaluate` takes `revise_rounds` (default **0**) and `revise_budget_s`. On an explicit
solve the bench sends 2; the wall drag sends none, because a gesture cannot wait for a loop any
more than it can wait for a proof. **This supersedes WP-9.3's "a per-edit critique: deliberately
not done" for the explicit solve only**, on the 19 Sep instruction.

**It is inline rather than the job route because of WP-6.4's rule — one drawing set is one
building.** The job hands its record back stripped and the bench re-solves it, which is what put
sixty findings of one placement beside a key measured on another. Here the loop's final PLACED
record is what `check_plan` judges, what `placement_summary` is projected from, and what
`rooms_meta` reads; the declared record rides beside it as `revised_plan` for the bench to load
as one undo step.

The loop runs on **the engine that placed**, never `auto`: a round is judged by re-placing and
comparing keys, so judging on another engine compares two houses — and `auto` would re-attempt
the proof at 25 s inside every round on a record where it has just been measured to fall back.

### The budget was wrong once, and the correction is the measurement

`BUDGET_REVISE_INLINE_S` was first set to **50.0** on the reasoning that fifty seconds is "two
interactive proofs plus their checks". Measured at `engine="auto"`, the loop then ran for
**69.6 s**: `revise()` tests its budget at the TOP of each round, so round one ended at about
35 s, 35 is less than 50, and round two started and ran to 70. **A ceiling that is not a ceiling
is worse than none, because it reads as one.**

**30.0 is derived from that measurement rather than chosen**: one round on a CP placement costs
about 35 s, so a second round starts only where the first was cheaper than this.

| engine | rounds run | loop time | stop reason |
|---|---|---|---|
| `heuristic` | 2 | **2.9 s** | round-cap |
| `auto` → CP-SAT | 1 | **36.4 s** | budget |

So the inline loop runs both rounds where they are nearly free and one where each costs a proof,
and says so. That is the ruling's "at least one or two" landing on either engine rather than a
number that means two on one of them and a minute of somebody's time on the other. The chip
routes keep their own far larger budgets: a job is not a person waiting.

---

## V. THE `unreachable` SENTENCE WAS FALSE ON FIVE OF TEN ROOMS

It read "The record declares N door(s) to it and the placement realised none of them" for every
stranded room. Measured on the Tidewater plan at `engine="heuristic"`:

| what the placement did | rooms | the sentence they were given |
|---|---|---|
| realised **none** of the declared doors | 4 | correct |
| realised **all** of them | 3 | **false** |
| realised **some** | 2 | **false** |

`primary` declares three doors, its `unplaced_pairs` is empty — all three were placed — and it
was told none were. The room is cut off because the cluster it belongs to is. **The number to
say it with was already in hand**: `unplaced_pairs` is computed by the same loop and was attached
to the finding as evidence while the prose contradicted it. Three sentences now, and
`realised_doors` rides as evidence.

---

## VI. What it buys, measured against a `git archive` control of HEAD

Every shipped plan, `engine="heuristic"` (deterministic), two rounds, the same harness run
against a control checkout and against this tree.

| | fatal | serious | minor | faults | `unreachable` | moves | doors added | fatals opened |
|---|---|---|---|---|---|---|---|---|
| as composed | 158 | 727 | 1077 | 266 | 93 | — | — | — |
| control, after 2 rounds | 138 | **638** | **1035** | 261 | 77 | 73 | **0** | 0 |
| this tree, after 2 rounds | **109** | 673 | 1096 | 261 | **48** | 78 | **45** | 0 |

**The loop clears 49 fatals where it cleared 20, and 45 of the 45 stranded rooms it clears are
cleared by the door move that could not previously be reached.** No plan opens a fatal on either
tree, which is the acceptance rule's own guarantee.

**The cost is published rather than netted off: serious is 35 worse and minor 61 worse than the
control's outcome.** Two causes, both structural rather than a regression. `_choose` sorts by
severity first and caps at six moves a round, so fatal-clearing moves now crowd out the
serious-clearing ones the control spent its rounds on; and `add-the-grammar-door` requires a
re-place, so each accepted door re-runs the search and the hill-climb scrambles. The key is
lexicographic and `[109, 673, …] < [138, 638, …]`, so the corpus's own arbiter prefers this
trade — but a reader comparing serious counts alone would read it backwards, which is why both
rows are here.

On the Tidewater plan specifically: `[13, 65, 102, 23] → [6, 67, 108, 22]`, `unreachable`
10 → 4, six doors added, in 2.9 s.

---

## VII. What was deliberately not done

- **A root-cause finding naming the one unseated pair that strands a cluster.** On this plan
  `('passage','stair')` strands the stair and the library, and four pairs on `upperpassage`
  strand the upper floor — ten fatals a reader has to reconstruct into two causes.
  `plan_check` already has the shape for it (`outside-is-only-an-appendage`). Left out because
  it is a new finding kind and this package already moves every drawn statement; it is the
  obvious next package.
- **Rewriting `_is_placement`'s other four engine-name kinds.** `drawn-vs-declared`,
  `stack-broken`, `landing-off-well` and `stack-unplaced` keep the reading they had. The ruling
  was given about the stranded room; extending it by analogy is the laundering this corpus
  refuses.
- **Running the rounds on the wall drag, or on undo.** The drag cannot wait; an undo that
  revised would re-apply the revision it was pressed to remove.
- **Making the chip routes return the loop's own placement.** They still strip and re-solve, so
  the two-placements divergence survives there — raised as
  `oq/the-chip-driven-revision-is-judged-on-a-placement-the-sheet-does-not-draw` rather than
  fixed, because it is a second surface's contract and this package already changed one.
- **Lowering the interactive cost of a CP round.** 36.4 s of somebody's wait buys one round and
  one move on the Tidewater plan. Solving that means a cheaper re-place, which is the placer's
  question and not the loop's.

## VIII. The guards, and the one that was blind

Twenty-four mutations, each asserted to have LANDED before its colour was believed and each file
restored byte-for-byte in a `finally`. **Twenty-three red on the first run; one was blind.**

`test_the_rounds_are_bounded_and_an_unreadable_value_asks_for_none` posted `revise_rounds: 999`
and asserted the report came back inside `core.REVISE_MAX_ROUNDS` — **which passes with the
route's bound deleted**, because `core.revise_plan` bounds `rounds` itself before the loop ever
runs. It was asserting the inner bound through the outer one and could not fail. Re-cut onto the
reader itself (`app._revise_rounds`, `app._revise_budget` as pure functions), where what the
route decides and nothing else does — that 0 is expressible, and that an unreadable value means
none rather than the default — can actually go red. Three further mutations then bit it.

**And one of this package's own new assertions was vacuous when written**: a loop in
`test_no_round_opened_a_fatal` read `assert not rd.get("opened") or all(i not in ... for i in [])`,
and `all()` over an empty generator is True, so that half could not fail at any state of the
code. Removed rather than repaired, with the reason in the test's docstring.

`tests/test_critique.py` (5), `tests/test_revise.py` (9), `tests/test_plan_check_unreachable.py`
(6), `workbench/server/tests/test_inline_revision.py` (10), `workbench/app/src/revision.test.mjs`
(4), and five new checks in the browser walk.

### The five walk checks are UNREACHABLE today, and that is pre-existing

**The browser walk throws before it reaches them, and it does so identically on a `git archive`
control of HEAD**: eight checks, then `TimeoutError` on `svg[role="img"]`. The cause is WP-13.4
working as ruled -- `tidewater-georgian-careful` is refused on `auto`, so `/api/plan/evaluate`
answers with `placement_refused` and no `placement`, `drawable` is false, and the conflict set
stands where the plate would be. Nothing after that line in the walk runs, on either tree. **It
is not this package's**, and this package deliberately does not fix it: making the walk judge a
refused record is its own ruling about what the walk is for.

So the five checks added here are written against a surface the walk cannot currently reach.
They are kept rather than deleted -- they bite the moment that throw is fixed, and deleting them
would lose the statement -- but **they are unverified by the walk and this report may not claim
otherwise.**

**They were verified live by a focused probe instead**, which waits on `[data-panel="revision"]`
rather than the plate, and all eight of its checks pass against a real browser and a real
server. The probe is not committed: it duplicates the walk's checks, and a second walk is a
second thing to keep current.

### AND THE PROBE FOUND A DEFECT BOTH SUITES WERE STRUCTURALLY BLIND TO

The panel read:

> Key serious 61 -> 59 . minor 106 -> 110 (on the proved placement, 36.6 s). **The sheet above
> is the placement this key was measured on** -- the loop's own, drawn as it ended.
>
> The placement the loop stopped on is still refused -- bearing, hearth, tiling are downgraded:
> no sheet, export or model is drawn from it.

**Two sentences, one screen, and the first promises a sheet the second says does not exist.** On
a refused placement there is no plate above the panel at all. Every assertion in `tests/`,
`workbench/server/tests/` and `revision.test.mjs` was about the REPORT -- the rounds, the counts,
the fields -- and the defect was in the sentence rendered from it, which is the one thing none of
them reads. The noun follows `refusedAfter` now ("the findings above", which is true either way),
with a JS guard on the adapter's ability to see the refusal and the walk check re-cut onto the
claim the two wordings share.

**Render the surface and read it** is this repository's highest-yield technique, and it earned
itself again here.


**The walk's click-to-sheet wait went 90 s → 150 s**, and that is measured rather than
precautionary: an example chip is an explicit solve now, so the path is a 25.9 s solve plus a
36.4 s round before the sheet is drawn. **And this package weakened one of the walk's existing
checks before it strengthened it**: the panel is now already on the page when the `revise
(search)` chip is clicked, so waiting for `/stopped:|converged/` alone would have matched the
SOLVE's panel on the first tick and asserted nothing about the chip at all. The discriminator is
the sentence each path makes, and the chip's — "a fresh solve … the two can differ" — is now
required before that loop exits.

---

## IX. The build on the settled tree, with every red attributed against a control

`python3 build/check_all.py` (the bare run, not a shard): **`2 of 53 checks failed`**, which is
the state this branch was already in. `pytest tests/` is **25 failed / 2,595 passed / 47 skipped
in 55:20**; `pytest workbench/server/tests` **254 passed / 15 skipped**; `node --test
workbench/app` **215 passed**.

**Seventeen of the 25 are the WP-13.1 gate and are red by design.** The other eight were each
run against a `git archive` checkout of this branch's parent through the same ids:

| failure | on the control |
|---|---|
| `test_composer.py` × 3 (the Georgian set) | **identical** |
| `test_parti_composability.py` | **identical** |
| `test_refusal.py` (the DXF selftest's skip clause) | **identical** |
| `test_score.py` (native-dominated set) | **identical** |
| `test_solver.py` (the deliberately-red half bound) | **identical** |
| `test_element_awareness.py` (the finding digest) | **THIS PACKAGE'S** — see below |

**Not one red is this package's except the digest, and that one is a re-pin the test's own
assertion demanded.** `test_a_one_rectangle_plan_is_UNTOUCHED_finding_for_finding` hashes every
finding's kind, room and statement, so the corrected `unreachable` sentence moves it. Its message
says *"diff the findings row by row and attribute every one before re-pinning; a net count can
hide any number of substitutions"*, and that is what was done, on a harness first proved to
reproduce BOTH old digests to the character:

- **Tidewater: 1 row out, 1 in** — the same room (`chamber3`), the same kind, the sentence alone.
  That room declares two doors and the placement realised one.
- **spec-builder-colonial: 5 out, 5 in** — the same five rooms (`dining`, `foyer`, `kitchen`,
  `powder`, `stair`), **every one of them a room that had been told none of its declared doors
  were placed when some or all were.** Five of five, on a plan nobody had looked at for this.
- Row counts UNMOVED at 203 and 239; both layers this class watches unmoved at 13/18 and 11/17.

`24b6a6a640f637b8 → cf857d78072bf00c` and `afa4d7a9c173615d → 37018ccbc24734ff`.

**The collection count reconciles exactly**: 2,667 collected against the 2,647 last recorded, and
20 is this package's own new tests to the test — 5 in `test_critique.py`, 9 in `test_revise.py`,
6 in `test_plan_check_unreachable.py` — so nothing was silently lost or skipped. The server suite
gains this package's 10.

**One methodological note worth carrying.** The first full run of this build STRADDLED the
package's own documentation edits: it started before them and its three prose-reading checkers
(`check_ids`, `check_citations`, `check_counts`) therefore judged a tree that no longer existed —
which is the trap `CLAUDE.md` records against WP-13.8, met by the session that was re-reading it.
That run was killed rather than quoted, the tree was settled, and the figures above are from a
single run of the final state.

---

## X. THE ADVERSARIAL AUDIT OF THIS PACKAGE (19 September 2026)

Five read-only auditors over the committed diff, on four angles: regressions and callers,
whether the new tests can fail, second-order risk, and a second occurrence of each pattern
elsewhere. Every figure they reported was re-derived here before it was acted on, and one of
their headline claims was found overstated that way. **The audit found more than the package
did.**

### X.1 The sharpest finding: nine of the package's own guards could not fail

An auditor ran **48 mutations against an isolated `git archive` checkout of the commit** and
reported 37 red, 11 green, of which 2 are equivalent mutants and **9 are genuine blind guards**.
The package's own report had claimed "24 mutations, 23 red, 1 blind", and that is not
reproducible as a completeness claim. The blind ones include **this package's own headline
defect** and **its own budget ruling**:

| blind mutation | what it proved |
|---|---|
| restore `_is_placement`'s pre-package `return engine != "cp-sat"` | **all 10 tests in `test_inline_revision.py` stayed GREEN.** `add-the-grammar-door` went from 6 applications to ZERO through the real route, and the file's fatal-count assertion survived on a margin of ONE incidental `light-the-far-end`. A route test that asserts a loop RAN is not a test that it does the thing the route exists for. |
| drop the inline budget default | 54 tests green, and `core.revise_plan`'s own 120 s then applies — **quadrupling the ceiling on the one route a person waits on.** `BUDGET_REVISE_INLINE_S` appeared in no assertion anywhere in the repository. |
| revert `remaining`'s lever publication to `if c == "placement"` | 93 tests green across three files. The package's lever test asserts it on `classify`'s in-memory assessment; the PANEL reads `report["remaining"]`. Two spellings of one reading, and only the one nobody renders was guarded. |
| delete every set-site of `rolled_back_by_refusal`, of `refused_the_drawing` on the move entry, and of `rnd["refused_after"]` | green, three times. All three were asserted **only on the JS adapter, fed hand-built dicts carrying those names** — producer and consumer pinned to each other's FIELD NAMES and to nothing else. `RevisionPanel.jsx` renders "the placement it produced may not be drawn — rolled back" off a field no producer test covered. |
| move `_attach_revision` below the schema-error early return | 10/10 green. No test drives a record that fails `check_plan` with rounds asked for. |
| revert `rooms_meta` to the unrevised record | 54 green — on the shipped corpus the loop does not change the room SET, so the defect the four comment lines describe is unreachable from data. |
| `refusedBefore: null` in the adapter | all 215 JS tests green, because `refusalCleared` read `pr.before` AGAIN rather than the field beside it. One quantity, two readings, and no test could see it while both happened to be right. |
| per-round `refusedAfter` → a constant `false` | green, and the panel rendered neither that field nor `rolledBackByRefusal` at all. |

Every one is fixed: producer-side guards driven through a scripted critique
(`TestTheRefusalTravelsOnTheRoundAndTheMove`), the door move asserted **through the route** by
name and by a falling `unreachable` count, the budget read off the call rather than the clock,
the lever asserted on `report["remaining"]`, the schema-error and `rooms_meta` cases DRIVEN, one
derivation in the adapter, and both round fields given a reader in the panel and in the live
strip.

### X.2 And one of the package's own new tests was VACUOUS

`test_the_loop_did_not_make_the_drawing_refused` read
`assert not (pr["after"] and not pr["before"])` — and on its fixture `pr["before"]` is truthy
(its sibling test asserts so), so `not pr["before"]` is False, the conjunction is False for every
value of `pr["after"]`, and the assertion is `not False` **by shape**. It stayed green under
seven mutations including the deletion of the `_newly_refused` clause it claimed to guard.

**It shipped in the same class whose neighbour records removing exactly that shape** — the
`all(... for i in [])` half of `test_no_round_opened_a_fatal`, removed with a paragraph
explaining why. Writing that paragraph did not stop the same mistake being made forty lines
below it.

A second one was blind for the other reason: `test_the_refusal_alone_does_not_accept_a_round`
offered `[4, 44, 70, 19]` against `[3, 44, 70, 19]`, which fails on the key comparison two
statements before the clause the test is named for — a guard on the key wearing a refusal test's
name. The keys are EQUAL now, with the premise asserted.

### X.3 The route-level findings, and the one that is not ours

**Measured, not argued**: on the shipped Tidewater record a revising solve returns **606,771
bytes, of which the revision report is 147,583 — twice**, once as `out["revision"]` and once
inside `revised_plan.revision_report`, to a bench that reads only the second. 24.3% of the
response. `jobs.py` had already met this and says so in its own comment. One copy now, on the
record, where the job route also puts it.

The other end of the same measurement: the revised record is **162,651 bytes of which 147,583
are the report — 91%** — and the bench re-POSTs its whole document on every debounced wall drag.
`api.evaluate` strips it; `api.exportCad` deliberately does not, because `build/export_dxf.py`
turns it into the plate's `revision_summary` XDATA.

`core.revise_plan` forwarded `time_limit_s` on a bare truth test, so a caller asking for 0 had
the request **dropped in silence** while `bounded` said nothing — the one knob on that call that
was pass-or-vanish while every other was bounded and reported.

And the debounce read its two solve flags **four hundred milliseconds before the evaluate that
spends them**, so any re-run of the effect inside the window silently lost the corrective
rounds. They are read in the callback now.

### X.4 The `realised_doors` arithmetic this package shipped was wrong

`realised = max(0, declared - len(unplaced_pairs_for_this_room))` subtracts a deduplicated
**PAIR** count from a **RECORD** count. A pair is minted by whichever room declared the door, so
a neighbour's unplaced door was charged to this room. Measured on `spec-builder-colonial`: the
Family Room declares two doors, the kitchen one WAS placed, the stair's own unplaced `family`
door put `('family', 'stair')` in the set — and the room was handed **the very sentence this
package exists to remove**. The `max(0, …)` clamp was the tell: it can only bind when the set
holds a pair the room never declared.

**The test guarding it restated the production expression verbatim**, so it could not fail, and
a sibling selected its rows by the miscomputed value and thereby certified the false wording.
Both are re-cut to derive the answer independently, and the plan that exposes it is in the
fixture list. One row moves: `CORPUS` finding digest `37018ccbc24734ff -> 554b84e823b1c057`,
diffed row by row against a control that reproduces the old digest to the character.

### X.5 Two pre-existing defects the audit reached, one fixed and one raised

**The `cut-off` layer was silent on every plan with no placed exterior door.** Its dedup guard —
"a room already reported UNREACHABLE is not also reported cut off" — tests `rid not in seen`,
and `seen` is seeded from `outside`. With no outside, `seen` is empty and the line skipped every
room, so the whole layer said nothing on exactly the houses where a room joined to nothing most
needs saying. Nothing was reported unreachable there either (the `no-outside` info says why), so
there was no restatement to avoid. Measured over the sixteen plans on `engine="heuristic"`:
**`cut-off` rows 2 → 26**, the 24 new ones on five plans — `bad-01` 7, `bad-05` 10, `good-03` 3,
`good-05` 3, `bad-04` 1 — every one a room the drawing really does join to nothing. Silence that
reads as a pass is the first thing this corpus forbids, and it was hiding behind a guard written
for the opposite situation.

**`build/compose.py` ranks a house that may not be drawn first.** `_sort_key` is applied a
second time at `:1767`, after the loop writes `refused` onto every candidate at `:1763`, and
does not read it. Measured on `briefs/family-georgian.json` at one round on the search: the
candidate ranked **first** is refused for `bearing, stacks`, and the second-ranked one is
drawable. That is WP-13.9's own rule one layer up — "a house nobody may draw is not a better
one" — and it is **not fixed here**, because three readings are available and the choice between
them has a ruling behind it: `oq/the-composer-ranks-first-a-house-that-may-not-be-drawn`.

### X.6 THE PROCESS FINDING: three "blocking" findings were my own contamination

An auditor reported three deployment blockers in `workbench/server/evaluate.py`, the first being
an uncommitted `loop_engine = "auto"` sitting directly under a comment reading "THE ENGINE THAT
PLACED, NEVER `auto`", proved red through the route. **It was a mutation another auditor had
written into the shared tree and not yet restored.** I had dispatched a mutating auditor
alongside four reading ones, in one checkout.

The mutating auditor caught it itself — its own report opens by naming the concurrent writer,
reconstructs the timeline to the second, and proves by content scan and by `git diff --quiet` on
the file it mutated most that none of the dirt was its own. That is the discipline working. What
did not work was mine: **a mutation harness and a reading auditor may not share a checkout**,
and this file already records that an interrupted mutation run makes every figure taken
afterwards meaningless. The rule extends: it makes every figure taken CONCURRENTLY meaningless
too, and the reader has no way to tell.

Verified afterwards: `evaluate.py` carries the correct engine selection, its tests are 7/7 green,
and the loop is deterministic at 3.9 s, 7 moves, `[6, 67, 108, 22]`.

### X.7 The browser walk ran to the end for the first time since WP-13.4

The five checks this package added to `workbench/app/e2e/walk.mjs` were **unreachable**, and so
were about a hundred and eighty others: `waitForSelector('svg[role="img"]')` sits at top level
with no `try`, both records the bench offers as examples are refused, and a `TimeoutError` there
terminates the process. The walk was reporting eight checks of about a hundred and ninety, and
**the refusal contract's own assertions at the foot of this file — the ones `refusal.test.mjs`
says in its docstring are the walk's — were among the ones never reached.**

Three things had to be true for it to run:

1. The refusal is asserted **where the reader meets it**, on the example chip, and the sheet
   section below is given a subject that draws.
2. `page.goto(BASE + '#/workbench')` when the page is already on that hash is a **same-document
   navigation** — the browser fires `hashchange` and the app never re-boots, so `planDoc` never
   re-read localStorage and the walk sat on the refused record it already had, dying on a plate
   the contract forbids. `page.reload()` is the honest spelling.
3. A negative assertion over a locator that missed **passes vacuously**: `!/fresh solve/.test('')`
   is true of an empty string, so one of the five was a silent fake pass and could not tell
   "the panel correctly omits this phrase" from "there is no panel". The premise is asserted
   first now.

And then the checks below it, written against the Tidewater record, convicted the surface of not
being a house it was never looking at: a literal floor of thirteen rooms, of ten door marks, and
a plate title asserted to contain the words "tidewater" and "georgian". **Worse, the walk's own
API probe fetched `tidewater-georgian-careful` by name while the bench showed the subject** — so
the engine caption, the stack census and the stoop census were measured against a record the
plate is not of, which is WP-6.4's "one drawing set is one building" broken inside the instrument
that checks it. Every expectation is derived from the record on screen now, and a record that
places no stack and no stoop reports COULD NOT EVALUATE by name rather than failing.

### X.8 A disclosure the rounds cost, named rather than fixed

Making the walk's fallback check read the page instead of a second solve surfaced a real
consequence of this package. An explicit solve now runs the corrective rounds, and the loop
re-places **on the engine that placed, asked for BY NAME** — so the record the sheet is drawn
from carries no fallback reason, and the caption's sentence *"The proof was attempted and did
not answer — …"* is gone. A bare `auto` evaluate of the same document still falls back and still
says so.

Nothing on the sheet is false: the caption describes the placement it is a caption for, and that
placement really was a search asked for by name. What a reader loses is the answer to a
different question — *was a proof attempted for this house, and what did it say?* — which the
first pass used to answer and the revised sheet does not.

It is not fixed here. The remedy is to carry the FIRST solve's `solver.reason` forward onto the
record the loop ends on, and that is a change to what `geometry._disclose` means by `reason`:
today it is a fact about the placement in hand, and this would make it a fact about the act that
produced the house. That is a disclosure ruling, not a defect with an obviously right repair,
and this pass does not take rulings. The walk reports it as COULD NOT EVALUATE, by name, with
both readings in the message.

### X.9 And one coupling the new subject exposed, on a surface this package did not touch

`DrawingSet.jsx` renders the line that names which face is drawn —
*"E elevation · the entrance front is W · drawn for 1760 · glass module 9″"* — gated on
`result.date_of_representation`. Asked of the API directly, `good-03-parlor-drawing-room-house`
returns `date_of_representation: null`, so on that record the whole line is absent and a reader
looking at the east elevation is told neither which face it is nor where the front is.

The face and the entrance front are facts about the DRAWING; the date is a separate disclosure
about the record. One being unavailable should not take the other two with it. The walk reports
it as COULD NOT EVALUATE with the reason rather than failing, because the surface is doing
exactly what it was written to do, and nothing here changes it: splitting that paragraph is a
change to a surface outside this package, on a record this package only made visible.

**The walk's final tally: 215 checks green, NONE failed, and FOUR COULD NOT EVALUATE — exit 3,
which is the walk's own "an unjudged walk is not a green one" — against EIGHT checks reported
before the fix.** The four are named in its output with their reasons: three are facts about the
record on the bench (it places no stack, no stoop, and states no `date_of_representation`) and
one is the two-acts finding of §X.8. **Its footer no longer asserts a cause it cannot know** —
it read *"because this server does not yet answer WP-13.4's refusal contract"*, which was the
only cause when the line was written and is now one of several, and a summary naming the wrong
one sends a reader to the wrong place. Each line says its own reason.

**And the discriminator for that third state took three goes, each miss matching something else
on the same page.** "the entrance front" is also the text of the face CHIP that names the front,
so the first guard matched a radio label and never took the branch. "glass module" is also drawn
INTO THE PLATE, in the sheet's own margin schedule — the walk printed
`tidewater-georgian · 3 BAYS · side-gable 8.0:12 · GLASS MODULE 9.0 IN (undated)`, which is the
SVG and not the caption, and whose `(undated)` is the very null the branch exists for. **A
three-state check whose third state never fires is a two-state check with a longer comment**,
and the only thing that found either miss was making the check PRINT what it had matched. Every one of the two hundred below the sheet wait — the stair, the
fixtures, the furniture, the door types, the relaxation marks, the wall handle, the drag, the
working-sketch banner, the Round's cameras and overlays, the Drawing Set, and the refusal
contract's own assertions at the foot of the file — had not run since WP-13.4.

### X.10 The build on the audited tree, with every red attributed

`python3 build/check_all.py` whole (the bare run, not a shard), started after every edit had
landed so no prose-reading checker judged a tree that no longer existed:

    2 of 53 checks failed
    pytest tests/                    23 failed, 2605 passed, 47 skipped   (0:55:14)
    pytest workbench/server/tests    263 passed, 15 skipped               (0:13:46)
    node --test workbench/app        221 tests, 221 pass, 0 fail

**Sixteen of the twenty-three are the WP-13.1 gate and are red by design.** The other seven are
the set the WP-13.9 entry already names as pre-existing: three `test_composer.py` rows,
`test_parti_composability` (which is `check_partis.py`'s own conviction seen through a second
reader, so counting them as two findings would double-count one), `test_refusal`'s DXF skip
clause, `test_score`'s returned-set row, and `test_solver`'s deliberately-red half bound. **Not
one red is this audit pass's**, and the two failing CHECKS are `check_partis.py` and the suite,
which are one finding and not two: `side-hall-townhouse` composed against `adam-style` trips
`even-bay-front`'s 3-to-7 band at one placed upper opening
(`oq/a-side-hall-front-is-convicted-by-a-band-written-for-centred-fronts`).

**And the collection count reconciles exactly, by name rather than by arithmetic.** 2,667 →
**2,675**, +8: ten test ids added and two renamed away, diffed id by id against a `git archive`
checkout of the parent —

    +6 / −2  tests/test_plan_check_unreachable.py   (three for the `cut-off` layer, one
             premise test split in two, two renamed)
    +4       tests/test_revise.py                   (TestTheRefusalTravelsOnTheRoundAndTheMove)

Nothing was silently lost or skipped, which is what that reconciliation is for.

**Eighteen mutations over every guard added or re-cut in this pass, and all eighteen are red.**
Two were blind on their first run and both are recorded rather than patched away:

1. The BATCH's own `rolled_back_by_refusal` site is shadowed by the single-move retry's
   `setdefault` whenever the batch holds one move — one state, two causes, and only the second
   guarded. The fixture offers two issues now, and drives a batch refused FOR THE DRAWING whose
   moves, retried alone, are refused on the KEY: only the batch's own line can say so.
2. `_record`'s `refused_the_drawing` annotation was **dead**. That function is called only with
   `accepted=True`, at both of its call sites, so the branch could not fire — a third spelling
   of a verdict the lever branch and the single-move retry already write. Deleted, because an
   annotation that cannot apply reads as coverage.

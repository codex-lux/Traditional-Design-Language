# WP-14.13: every page says where it is

*25 September 2026. Branch `wp-14.13`, taken from `0ad8b49`. Scope: PRD
`docs/prd/phase-14-the-dossier-and-the-journey.md` §F (the site map, the crumbs, the title and
§F.4's rule), §I.11 (the assistant's name and the narrow fold), §I.12 (the shell's other
attachments) and the rows of §J that name this package. Every figure below was measured in this
worktree or in a control worktree of `0ad8b49` made for the purpose. Cite this report by
filename: the number names two packages in two other phases already, and may in this one.*

## What it is for

A reader of the workbench could not tell where they were. The tab title never changed. The
masthead's only persistent content was the bench plan's id and style as bare text. The rail was
a card catalogue of eleven surfaces in the order they were built, each labelled in words
`Chrome.jsx` wrote. The palette's `short` lines and the Overview's DOORS labelled the same places
in different words again. A link handed to a colleague opened on a slot's figures inside a kit
inside a style inside a family, with nothing on screen naming the product or the way back.

This package makes each page say three things: where it is (the crumbs and the title), what it is
(its page head, from its own glossary record), and what comes next (the rail as a site map with
the house's steps numbered). It does all of that from records and the API, and writes no word or
figure of its own.

## I. What was built

**`nav/navModel.js`, the site map.** It is one pure module and holds ids only. `NAV` is §F.1's
frozen table: four groups (start, styles, a house, the library) and thirteen items, each naming
its surface and the glossary record it is called by. `navModel({lookup, counts, glossaryCount,
inHand, dossier, journey, place})` returns §F.2's shape exactly.

- Every label is that record's `term`. A record the glossary lacks comes back as `label: null`
  and `missing: <id>`, and the rail prints `noEntry(id)`.
- Every meta is the API's figure or a journey word. The count beside Find a style is
  `counts.styles`. Beside Proportions it is the sum of `counts.proportion_packs`, beside Faults
  `counts.faults`, and beside the Glossary the glossary's own `count`. Beside a house step it is
  `journey/journey.js`'s words for that step, and beside a dossier section it is that section's
  `count`. A test fails if any numeric literal in the module's code is other than 0 or 1.
- Step numbers are `JOURNEY`'s index plus one. Trace a drawing is not a step and has none.
- The style in hand is a link to that style's dossier. Its sections are listed only while that
  very dossier is the page shown. At most one item is current, and it is the one §F.2 names.
- The four §F.2 exports (`NAV`, `navModel`, `headTermFor`, `guidedExampleStyle`) keep exactly
  §F.2's signatures, as the lead asked. Six helpers are added beside them: `wordFor`,
  `normalizePlace`, `sectionOf`, `stylePlaceKind`, `inHandFrom` and `flatItems`. They are
  additive, and the rail, the crumbs, the palette and the walk read them rather than keeping
  their own copies.

**`nav/crumbs.js`, the trail and the title.** `crumbsFor(place, {lookup, dossier, names,
styles, journey})` returns §F.3's table, place by place.

- The style chain follows `member_of` and never lineage. It is taken from the served
  `dossier.chain` when that dossier is this style's. Before the dossier arrives, it is
  `chainOf(styleId, styles)` over `/api/styles`, whose `in` field is the same fact. So the trail
  is whole on the first render, and not only once the dossier fetch answers.
- `titleFor(crumbs, lookup)` reads the non-group labels backwards, joins them with ` · `, and
  appends ` — ` and the `about-tdl` term. On the front door it is that term alone.
- The PRD's own example is held to `styles/*.json` and `elements/slots.json` by a test: *Styles ›
  North American › American Colonial › Georgian Colonial American › Tidewater Georgian › Kit ›
  Main cornice*.

**The shell** (`Chrome.jsx`, `App.jsx`):

- **The rail.** Items are `<a data-nav href aria-current>` inside the existing
  `nav[aria-label="surfaces"]`. Each carries `useTermDescription(termId)` and the step number or
  meta from the model. The shell writes no label. A label wraps and is never cut short, and the
  style in hand carries its id as a margin note under its name (the model's `note`).
- **`CrumbStrip`.** A `<nav aria-label="crumbs"><ol>` between the masthead and the pane row, with
  the last item marked `aria-current="page"`.
- **`PageHead`.** WP-14.8's component is mounted once, in the centre column above `<main>`, with
  `headTermFor(place)`. The Glossary surface's own mount is removed, so no page has two heads.
- **The masthead.**
  - The wordmark is a link to `#/`.
  - The "the workbench" eyebrow and the inert Export and Settings icons are gone.
  - "On the bench: *name*" links to `#/workbench`. Beside it are fatal, serious and unjudged as
    three counts, each word a `Term`, and a count the check did not state reads "not counted" and
    never 0. Before a plan is evaluated it says so instead.
  - A "Keys ?" button opens the existing `ShortcutCard`.
  - The tab title is `titleFor`'s, and `index.html`'s static title is the product's name.
- **`lastEval` belongs to its plan.** It clears when `plan.id` changes, unless the evaluation in
  hand is already of that plan. The bench loads a revised record and sets its evaluation in the
  same tick, and a blanket clear would have thrown that evaluation away.
- **A dead server is said, not blanked.** A failed `/api/health` renders
  `role="status" data-boot-status` with "Cannot reach the server · Retry".
- **`data-reflow`** is set on `#root` for the Overview and the Glossary, which releases WP-14.8's
  1380 px floor on those two pages only.
- **`ColdLinkBanner`** shows once per browser, when the first place loaded is not the front door
  and `prefs.seen['front-door']` is unset. It shows:
  - the `about-tdl` term and definition;
  - the crumbs;
  - a Front door link and a Guided example link, each named by its record;
  - a Dismiss button.

  Dismissing it, or visiting the front door, sets the flag.

**The assistant** (§I.11):

- **Pane head.** `AiRail` takes a `title`. `RailHost.assistantName(glossary)` passes the
  `assistant` record's term, or `PANES.rail.label` while the glossary loads, or
  `noEntry('assistant')` if the glossary answered without the record.
- **Folded spine.** The spine carries the same name, and its label stays `PANES.rail.label`.
- **Greeting and off-state copy** now say it is an AI assistant. `rail.py` and what it is sent
  are untouched.
- **The narrow fold.** `state/layout.js` exports `NARROW_FOLD_PX = 1500`. `readStored` records a
  stored choice exactly as §I.11 defines one: an own boolean `collapsed.rail`. The store's first
  `clampAll` folds the assistant below that width when there is no choice, and publishes the fold
  with `save = false`. No later resize folds or unfolds anything.

**The palette and search:**

- **Options.** Each carries `data-id` and, for a record, `data-cite`. The opening hand is the site
  map's order, front door first. `KIND_LABEL.term` is "glossary terms", and `kit` is gone from
  `KIND_ORDER`, which gains `term` last.
- **Surface entries.** `staticEntries(lookup)` derives the surface entries from `NAV`. Each entry
  is named by its record's `term`, or shows its id where the record is missing. The kept synonym
  haystacks go with each entry, and the kit's (`elements`, `bindings`) are now the style entry's.
  The app-written `short` lines are gone.

**The copy:**

- **Three stale fault counts.** "209 faults" appeared three times against a corpus of 210. The
  Fault Corpus now reads the length of its own list, and Export reads `counts.faults`. Export's
  download filename is kept.
- **Quiet eyebrows.** Four quiet eyebrows in `RevisionPanel` take the default tone.
- **The copy ratchet loses six rows** and gains none: the masthead's unjudged tooltip, the rail's
  "9 sections" and "4 formats", and the three "209 faults".
- **`match.js`.** Its comment no longer pins 665. The index this tree serves holds 762 entries,
  measured through `corpus.search_index()`.

## II. Verification

| Check | Result |
|---|---|
| `npm test` (the app suite, no npm install needed) | **393 of 393**; the control at `0ad8b49` is 361 of 361, and +32 is exactly the new tests (14 navModel, 9 crumbs, 9 layout) |
| `npm run build` | built; the one warning is the pre-existing coastlines-fine chunk over 900 kB. Entry 529.44 → **547.48 kB** (169.78 → 175.56 gzip), against a control build of `0ad8b49`: +18 kB for two pure modules, the shell and the banner |
| `node e2e/router-unit.mjs` | 124 checks passed |
| `node e2e/search-unit.mjs` | 13 checks passed |
| `python3 build/check_frontend.py` | OK — entry 535 KB, three 564 KB in its own chunk, glossary not bundled |
| `pytest test_grammar_agreement.py tests/test_check_frontend.py` | 36 passed |
| `check_glossary` / `check_ids` / `check_citations` / `check_counts` | exit 0 each (121 records, 478 quotations; ids clean; 3,064 citations, 0 dangling; 122 claims, 0 stale) |
| server, MCP, corpus | `git diff 0ad8b49 HEAD -- workbench/server mcp_server build glossary schema` is empty, so `rail.py`, `core.overview()`, `/api/health` and every MCP payload are byte-unchanged |

**The browser walk** (`HEAVY_CALLS_PER_HOUR=10000 WALK_PORT=8281 bash workbench/scripts/walk.sh`,
on the final tree, built first): **296 ok, 0 FAILED, 4 COULD NOT EVALUATE, exit 3.** Exit 3 is
the walk's own "an unjudged walk is not a green one" and is **not a pass**. The four unjudged
checks are exactly the four the lead's baseline at `0ad8b49` names, with the same reasons:

- the fallback caption: the corrective rounds' placement names no fallback, where a bare `auto`
  probe does;
- `good-03` places no stack;
- `good-03` places no stoop;
- `good-03` returns no `date_of_representation`.

No check failed, so none needed attributing against a control. This package's own share:

- the rail block (5 checks);
- the re-cut reachability loop (14);
- the assistant's name (1);
- the palette's `data-id` (1);
- the Phylogeny and Fault Corpus waits re-scoped to `main` (Finding 1);
- **40 appended checks**:
  - the crumbs follow the filing and the front door has no trail;
  - each place is headed once by its own record;
  - no two of 17 places share a title;
  - the masthead: home link, no "the workbench", no inert icons, no build ids, the bench link and
    its three counts, a Keys button that opens the card;
  - full screen hides and restores the crumbs and the head;
  - the cold-link banner: it shows, names where you are, offers both ways in, dismisses, stays
    dismissed across a reload, never shows on the front door, and never shows to a browser that
    has seen the front door;
  - the narrow fold: 1280 px folds with a named spine and no stored write, widening does not
    unfold, 1680 px is open, narrowing does not fold, and stored choices win both ways;
  - the unreachable server is said, and Retry restores the shell.

All green.

The first walk on this tree did not get that far. It ran 90 checks and then died on a
TimeoutError. That is Finding 1.

## III. Mutations: 21 of 21 red

The mutations ran in a detached worktree of this branch. The baseline was confirmed green for
every suite used before any mutation. Each mutation:

1. asserted its target occurred exactly once;
2. read the file back to confirm it landed;
3. ran the suite, and was counted red only on a non-zero exit;
4. restored the original bytes and checked the hash.

Every red was then re-run to print the failing test's name, because this repository has twice
counted a mutation that landed as a syntax error. **None did here.** Each fails on a named
assertion.

| # | Mutation | Red on |
|---|---|---|
| R1 | the chain read from `lineage[0].target` before `member_of` | the PRD example; the filing-not-lineage sweep; the crumb links; the title |
| R2 | a narrow first `clampAll` with nothing stored does not fold | starts folded at 1280; 1499/1500; non-choices; no write |
| R3 | a stored open assistant folded at 1280 (the stored-choice test dropped) | a stored open assistant stays open; a narrow window borrows |
| R4 | the fold performs a localStorage write | the fold performs no write |
| R5 | the style meta a literal `164` | a meta is the API's figure; no numeric literal but 0 and 1 |
| R6 | the title the product's name alone (two places, one title) | the title reads the trail; no two places share a title |
| X1 | the title keeps only the last crumb | the same two |
| X2 | another style's dossier enters the trail | the URL alone decides the trail |
| X3 | the served chain ignored | the PRD example; the filing sweep |
| X4 | every `clampAll` folds, not only the first | only the first folds |
| X5 | a non-boolean stored `rail` counts as a choice | what is not a choice does not stop the fold |
| X6 | the threshold `<=` | 1499 folds and 1500 does not |
| X7 | the faults meta written in when the API gave none | absent when the API gave none; the literal guard |
| X8 | a house item's meta a fixed word | a house item's meta is the journey's words |
| X9 | step numbers from zero | step numbers are the journey's order plus one |
| X10 | the Styles index current on a dossier too | at most one item is current |
| X11 | the slot panel's head read as the index | the page head is §F.2's record |
| X12 | the guided example taking a fragment cite | the guided example is the first plain style cite |
| X13 | sections listed under any dossier | at most one item is current |
| X14 | the palette naming a place by its id | `search-unit.mjs` |
| X15 | the guided term written before the name | the style in hand is named, term after |

**X12 was blind on the first draft of its test.** The first fixture's first `see` entry was a
plain style cite, so `!c.fragment` agreed with the answer and decided nothing. The fixture now
leads with a brief and a fragment cite, so both conditions decide it. This is WP-11.15's rule
about a fixture where both branches return the same answer, met in a filter.

## IV. Findings

**1. The shell's page head broke the walk at check 90, and the 210 judgments behind it never
ran.** The first walk on this tree died on `waitForSelector('text=compressed')` on the
Phylogeny. The head now sits above `<main>` on every place. After the first visit it folds
its "How to read this page" list, and that list is kept in the DOM with `hidden`. On the
Phylogeny one hidden line mentions the compressed axis, it is first in DOM order, and a global
`text=` wait resolves to the first match. The wait timed out on a hidden `<li>`. Being a
top-level wait with no `try`, it took down every check after it. That is WP-13.9's finding about
a top-level wait, met again.

The damage was swept rather than assumed. Of the walk's nine global `text=` waits, a page head
can answer exactly two:

- `compressed`, on the Phylogeny;
- `dishonest`, on the Fault Corpus, where the wait is soft and would only have waited fifteen
  seconds in silence.

In both, the word is only in the hidden list. The sweep read every `glossary/surface-*.json`'s
term, lede, read list and try, case-insensitively, as Playwright's unquoted `text=` matches.
Both waits are `main >> text=` now, which is what each was written to prove. The other seven
cannot collide today. **They will collide the day a surface record's read list takes one of
their words**, and four of them are in blocks other packages own. Scoping every walk wait to
`main` is the durable fix. It is named here rather than done across other packages' blocks.

**2. Any layout stored before this package reads as a choice to keep the assistant open.** This
follows from the ruled rule and is not a defect in it. `persist` writes the whole layout object,
so every browser that ever dragged or folded any pane has `collapsed.rail: false` stored. Under
§I.11 that is a stored choice. The narrow fold therefore reaches only a browser with nothing
stored: a new reader, which is the reader the ruling was for. An existing reader on a 1280 px
laptop keeps an open assistant they never chose. The PRD states the whole-object write in its own
words ("that is a stored choice from then on"). The part it does not say is that the rule
applies to layouts written before the rule existed. It is left as ruled.

**3. The style in hand is always the guided example on this tree.** The shell reads
`prefs.styleInHand` and never writes it. The write belongs to the dossier (WP-14.12's
`StyleDossier`), which is not on this branch. So until the two merge, the in-hand item is the
guided example's style, correctly labelled as such. That is the behaviour §F.2 specifies for
"prefs holds none".

**4. `lastEval` outlived its plan.** Loading a different plan left the previous plan's fatal,
serious and unjudged counts in the masthead, beside the new plan's name. This was found by
reading, and fixed as above. The obvious fix, clearing on every plan change, would have discarded
the evaluation the bench sets in the same tick as a revised record.

**5. A failed `/api/health` rendered nothing.** `locked` stayed `null` for ever, so a stranger
handed the URL during a slow boot saw a blank page. The walk now aborts `/api/health` in a fresh
context and asserts the status line and its Retry.

**6. The fault count was written into the app three times, and all three were wrong.** The copy
said 209 and the corpus holds 210. The Fault Corpus now reads the length of the list it shows,
and Export reads `counts.faults`. No number is written in their place.

**7. My own screenshot harness waited on a selector that carries two states.**
`[data-bench-counts]` holds the three counts or the words "not yet evaluated". A wait on it
therefore resolved before the evaluate answered, and the first bench screenshots showed the
unevaluated masthead. The harness waits for `[data-count]` now. It is the selector class this
repository keeps meeting, met in a harness that is not a check.

**8. Two names were cut short, and only the screenshots showed it.** The first pass truncated
both with an ellipsis, and every test and walk check passed over them:

- the assistant's pane head read "ASK THE CORPUS · AI ASSIS…" at its shipped 344 px, because the
  record's term is letterspaced in capitals;
- the style in hand read "Tidewater Georgian · the guided exa…" at the rail's shipped width.

A name the reader is not given in full is not a name. Both wrap now. The walk's pane-head check
reads `textContent`, which holds the whole string whatever the CSS cuts, so it could not have
seen this. That is the gap between what a check reads and what a reader sees.

**9. At 1280 px the masthead's right end is off the screen on every working surface.** WP-14.8's
1380 px floor on `#root` still applies everywhere except the front door and the Glossary, which
is the ruling. So at 1280 px on the kit, the proportions plate and the bench, the page scrolls
sideways and the search box is cut at the window's edge. The "Keys ?" button is off the screen
entirely and is reached only by scrolling. The screenshots show it. It is not fixed here, because
the floor is `tokens.css`'s and §J.3 gives that file to WP-14.8 alone. Two fixes are possible and
both need someone to choose one: a masthead that holds the viewport's width inside the floor, or
reflow for more surfaces.

**10. Signed out, the boot probes `/api/overview`, and this package adds no call.** The probe is
the base tree's, unchanged here. When `/api/health` says a password is set, the only way to
learn whether this browser already holds a session is one gated request. `/api/overview` is
that request, and when the answer is yes it is also the data the shell needs. It answers 401
with no corpus text. A different probe would need `/api/health` to report the session, and
§J.4 forbids changing `/api/health`. The one change this package makes to `boot()` adds no
request: a non-401 failure of that probe, or a failed `/api/health`, now shows the status line
instead of a blank page. Retry calls `boot()` again, and only when the reader clicks it. WP-14.14's
Gate block tolerates exactly this one 401. I leave the probe as it is.

**11. The rail has no Kit item, and that follows §F.1.** The site map's thirteen places are
§F.1's. The kit is a section of a style's dossier, `#/style/<id>/kit[/<slot>]`. It is listed as a
child of the style in hand on that style's own dossier. Once WP-14.12's dossier lands, every
dossier's section strip reaches it too.
The slot panel with no style (`#/style/-/kit/<slot>`) is a place the crumbs name ("Styles › Find
a style › Kit › *slot*"), not a rail item. The front door's map drawing thirteen items and no Kit
is therefore intended.

## V. Deviations from the brief and the PRD

- **`walk.mjs` outside the permitted blocks, three times.**
  - The two scoped waits in Finding 1 are in the Phylogeny and Fault Corpus blocks. They were
    needed because this package's head caused the crash.
  - The rail-reachability loop is WP-14.7's §J.1 loop, not one of the four blocks named for this
    package. It was re-cut because the rail's items are anchors now.
    - It judges each item by its `data-nav` id and its own `href`, not its position.
    - It allows `in-hand:identify` as the current mark on the in-hand style's own dossier.
    - Its pre-visit, which moves off a current item, never lands on `style` or `kit`, because
      that would mark a different item current.
  - None of the three touches an `engineClaim`-pinned line or the full-screen `goto('#/kit')`.
- **`layout.test.mjs`'s "a narrow window borrows" states an open assistant.** Its fixture is
  seeded with `{collapsed: {rail: false}}`. Otherwise the narrow fold, a different property with
  its own cases, would fold the pane that test measures. Its assertions are unchanged.
- **`glossary.test.mjs`'s premise is re-cut.** It asserted that `Glossary.jsx` ships a literal
  `<PageHead termId>`. That mount moved into the shell, where the id is a variable, so the
  premise now reads `RailHost.jsx`'s `assistant` record. That is the one literal id the shell's
  own files ask the glossary for.
- **`search-unit.mjs`'s `elements`/`bindings` synonyms now point at `style`.** §J.1 gives that row
  to WP-14.12. It had to move here because the site map has no `kit` item, so the old assertion
  had nothing to find. The change is the same as the one 14.12 specifies.
- **`useStyles` maps `member_of` from `/api/styles`' `in`.** `crumbsFor` takes `styles` beside
  §F.3's four options, so the trail is whole before the dossier arrives. Both changes are
  additive.
- **`normalizePlace` reads the legacy `kit` surface as the kit section of that style's dossier.**
  Until §E.1's retirement lands, the rail, the crumbs and the head then agree with the address the
  reader is sent to. Once WP-14.12 makes the router answer `style` for every kit address, this
  branch is dead and may go.
- **The chrome writes three bits of furniture that are not records.** They are "Dismiss", "You
  are at:" and "Cannot reach the server · Retry". The last is the PRD's own wording. None of the
  three defines anything, and each is named as furniture in its component's header.

## VI. Not done

- **Scoping every walk wait to `main`.** This is the durable fix for Finding 1. It is left because
  it spans blocks other packages own.
- **§F.4's other halves.** Removing the defaults `DEFAULT_STYLE` (`StyleRecord`, `KitSurface`),
  `DEFAULT_PACK` (`Proportions`) and `BriefIntake`'s default style belongs to WP-14.12, WP-14.9
  and WP-14.10.
- **`JourneyBar`.** It is WP-14.10's. The centre column has its slot, between `PageHead` and
  `<main>`, and nothing is mounted there.
- **Reflow below 1380 px for the working surfaces.** This is by ruling. At 1280 the bench, the
  kit and the plates keep their minimum widths and scroll sideways, and the screenshots below
  show it.
- **No open question is raised.** Finding 2 is the ruled rule applied to old data and is recorded
  here. Finding 1 is an instrument defect with a named fix.

## Screenshots

Twelve shots at 1280×800, 1440×900 and 1680×1050 of `#/`,
`#/style/tidewater-georgian/kit/cornice`, `#/proportions/trim-classical` and `#/workbench`. Each
was taken in a fresh browser context, so nothing was stored. They are in the session scratchpad
under `shots-14.13/`, and `workbench/app/e2e/shots-shell.mjs` retakes them.

**What a first-time practitioner sees:**

- **Every page says what product this is before anything else.** A deep link opens under the
  cold-link banner: the `about-tdl` term and its definition ("a pattern book for traditional
  houses, written so that a machine can read it and a person can still argue with it…"), then
  "You are at:" the trail, then Front door, the guided example and Dismiss.
- **The crumb strip says where you are.** On the kit it reads *Styles › North American ›
  American Colonial › Georgian Colonial American › Tidewater Georgian › Kit › Main cornice*, each
  ancestor a link to its own dossier. The tab reads the same trail backwards and ends
  "— Traditional Design Language".
- **Every page is headed by its own record.** "KIT: What this style specifies, slot by slot, and
  where each value came from", then "How to read this page", open on the first visit, with four
  lines and a Try link.
- **The rail is a site map in the reader's words.** Start; Styles (Find a style 164, then
  "Tidewater Georgian · the guided example" with `tidewater-georgian` as its margin note, then
  Family tree & map); A house, numbered 1 to 5, each step's state in the journey's words ("to
  do", "none yet", "no plan on the bench", "needs a plan"); Library (Proportions 57, Faults 210,
  Glossary 121). On the in-hand style's own dossier, its sections open beneath it with their
  counts, and the one shown is marked.
- **The masthead on the bench** reads "On the bench: Tidewater Georgian, five bays, carefully
  planned · refused · 1 fatal · 60 serious · 4 unjudged". Each count's word is a defined term,
  and the unjudged count is hatched.
- **The narrow fold, measured.** At 1280 and 1440 px, with nothing stored, the assistant starts
  folded to a spine reading "ASK THE CORPUS · AI ASSISTANT". At 1680 px it is open with that name
  as its head, wrapped whole.
- **The front door reflows**, measured by the harness: `data-reflow` set, 0 px of sideways scroll
  at every width.
- **The working surfaces do not reflow.** At 1280 px they scroll 100 px sideways, and the
  masthead's search box and "Keys ?" go past the window's edge. That is Finding 9. At 1440 and
  1680 px nothing scrolls sideways.
- **The bench's example is refused**, so the bench draws no plate and shows the refusal and the
  inline revision panel. That is the refusal contract working, and not this package's.

## VII. For the lead: where this branch meets the others

**`App.jsx`** (J.3 gives 14.13 "the rest"):

- The import block: two lines changed (`CrumbStrip`, `PANES`, `assistantName` joined to their
  existing imports) and twelve added. WP-14.10's hunk adds an import of `JourneyBar` beside
  them, and the resolution keeps both.
- `boot()` and the new `bootFailed` state.
- A block after `const Active = SURFACES[surface] || ...`. The `SURFACES` map itself is not
  touched, so it is 14.12's alone.
- The render tree:
  - `Masthead`'s props;
  - `ColdLinkBanner` and `CrumbStrip` after it;
  - `LeftRail model busy`;
  - `<main>` is wrapped in a centre-column `<div>` with `PageHead` above it. **This is where 14.10's
    `<JourneyBar surface lastEval />` goes: after `PageHead`, before `<main>`, inside that div.**
    14.10's hunk was written against the tree before this wrapper existed, where `<main>` sits
    directly in the pane row. It will therefore probably conflict in the text, and the element
    belongs inside the wrapper.
  - `PaneStub`'s `spine`.

**`search/staticEntries.js`.** The file is rewritten whole. WP-14.12 turns the `kit` entry into
the `style` surface with its synonyms kept. Here there is no `kit` entry at all: entries come from
`NAV`, and the kit's hay (`elements`, `bindings`, …) is in `SURFACE_SYNONYMS.style`. Take this
file's version and check that the style entry's hay holds everything 14.12's kept.

**`e2e/search-unit.mjs`.** The synonyms block's `→ style` is identical in intent to 14.12's row.
The `kinds` list and the names block are this package's.

**`e2e/walk.mjs`:**

Line numbers are on this branch:

- the imports at the top (16-20);
- the rail block (130-177);
- the reachability loop (207-255);
- the Phylogeny and Fault Corpus waits (1152-1155, 1257);
- "rail present on every surface" (1642-1653);
- the palette's `data-id` check (1704-1707);
- the blocks appended before `browser.close()` (from 2324).

The appended blocks visit only `#/style/...` addresses and use `#/phylogeny` for full screen. They
never visit `#/kit`, so they survive 14.12's kit retirement. The reachability loop avoids
`style`/`kit` on its pre-visit for the same reason.

**`palette/CommandPalette.jsx` and `surfaces/Glossary.jsx`** are changed as described in §I. The
second is 14.8's file, and the only change is the removed mount.

**`navModel.js` exports** are exactly §F.2's four, plus six additive helpers. None of §F.2's
signatures changed, and neither module's exports have changed since `81e04fb`, which WP-14.14 is
built on.

**A trial merge of the lead branch (`ff2e168`, WP-14.11 merged) into this branch** was run in a
throwaway worktree and discarded:

- `FaultCorpus.jsx` auto-merges. The merged file keeps both packages' changes and carries no
  "209".
- `walk.mjs` conflicts in one hunk only: both packages appended a block before
  `await browser.close()`. The resolution keeps both blocks. Close this package's block with its
  own `}`, then take WP-14.11's block, which ends with the closing brace the two shared.
- On the resolved tree the app suite is **408 of 408**: this branch's 393 plus WP-14.11's 15.
  `router-unit` passed 124 checks and `search-unit` 13, and `node --check walk.mjs` is clean. The
  browser walk was not run on the merge.

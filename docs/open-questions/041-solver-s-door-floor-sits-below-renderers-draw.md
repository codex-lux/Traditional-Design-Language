# OQ 41 — the solver's door floor sits below the renderers' draw test

*Status: CLOSED 27 AUG 2026 · Raised in: From the real solver (WP-2.3, 25 Aug 2026)*

**CLOSED 27 Aug 2026 — both sides now measure a door by its own leaf and jambs, and the branch this entry quoted turned out never to have fired.** The renderers learned narrow doors in WP-6.1; WP-6.3 replaced the solver's floor with the same figure, `openings.required_wall_ft(width) = width + 2 x 0.35`, read from the record — which supplies a width on 469 of the corpus's 471 doors. One number, in `build/openings.py`, used by both renderers, both exporters, the CP model and the arbiter that judges its output; the 2-3.2 ft band in which a door held as a proven fact and appeared in no drawing is gone.
**What closing it found is larger than the question.** This entry quotes the solver's rule as `min(4 ft, floor(0.9 x min(maxside)))`, floored at 2 ft, and says it 'scales to the smaller room ... demanding a parlor's 4 ft of a 2.5 ft linen press would refuse real closets'. **That branch has never once fired.** `maxside` is `max(width_ft, length_ft)` — the LONGER side — so dropping below 4 needs a room whose long side is under 3.34 ft, and there are **0 such rooms in all 16 plan records** (238 measured). Every interior pair in the corpus got a flat 4 ft: the 3 x 5 linen press the comment names got a parlour's requirement, and so did `bed2cl`, which is literally 2 x 6. The comment, and this entry quoting it, described behaviour that did not exist — and `hard_fact_violations` carried a second transcription of the same dead expression, which is how an arbiter comes to convict placements the solver proved legal.
**And fixing it fixed the plan.** A stricter rule for most pairs (102 tighter, 3 looser, 128 unchanged) made the model EASIER, because the flat 4 ft had been demanding a parlour's shared wall for every closet. `tidewater-georgian-careful` had never once been solved by CP-SAT; it now solves OPTIMAL, and its openings go from 20 placed / 11 with nowhere to go to **30 placed / 1** — the kitchen gets all six of its doors, the chamber bath gets its door, and the three fatal unreachable-room findings go to zero. Cost, stated: 17 declared exterior-wall pins downgraded, each with its own refinement, and relaxations 11 → 13. *Original entry follows.*<br><br>**HALF CLOSED 26 Aug 2026 — the renderers learned narrow doors, which is the half this entry preferred; the solver's floor is WP-6.3's.** Ruled by building it: the renderers now test a door against its own leaf and jambs (`width + 2 x 0.35 ft`) rather than against a flat 3.2 ft applied to a 2.2 ft closet leaf and a 6 ft cased opening alike, so a closet door IS narrower than a parlour's and draws. `build/render_plan.py`, `workbench/app/src/sheet/derive.js` and both exporters take the same test from one constant, and `tests/fixtures/sheet_symbols/` holds all of them to it. The 2-3.2 ft band in which a door held as a proven fact appeared in no drawing is gone. **What remains:** the solver's own floor is still a flat `MIN_DOOR_OVERLAP`, so the two numbers agree by being generous rather than by being the same number; WP-6.3 makes the CP constraint the per-pair figure from the record's now-real widths, at which point this closes. *Original entry follows.*<br><br>**OPEN — the solver's door floor sits below the renderers' draw test.** The CP door constraint scales its required shared-wall run to the smaller room of the pair (`min(4 ft, ⌊0.9 · min(maxside)⌋)`, floored at 2 ft) because demanding a parlor's 4 ft of a 2.5 ft linen press would refuse real closets — but `render_plan._shared` draws a door only above 3.2 ft, and the DXF/IFC exporters inherit that test. In the 2–3.2 ft band a door therefore HOLDS as a proven fact and appears in no drawing. The WP-2.3 audit made the gap stated everywhere (the DXF sheet now carries a "N DECLARED DOOR(S) WITHOUT A DRAWABLE SHARED WALL" line and lists them in the export result; the IFC already noted them) — but stated is not resolved. Should the renderers learn narrow doors (a closet door IS narrower than a parlor's), or should the solver's floor rise to the draw test and small-pair doors become a named refusal? A drawing convention judgment, recorded rather than made unilaterally. Surfaced by the WP-2.3 adversarial audit.

# OQ 35 — `core.check_plan` now returns a distinct "could not validate: the jsonschema package is not installed" (with `unvalidated: true`) via an explicit `except ImportError`, and the workbench's brief validation does the same; surfaced by the WP-5.2 adversarial audit and fixed there

*Status: FIXED 25 AUG 2026 · Raised in: From the workbench (WP-5.2, 25 Aug 2026)*

**FIXED 25 Aug 2026 — `core.check_plan` now returns a distinct "could not validate: the jsonschema package is not installed" (with `unvalidated: true`) via an explicit `except ImportError`, and the workbench's brief validation does the same; surfaced by the WP-5.2 adversarial audit and fixed there.** Original entry follows.<br><br>**OPEN — `core.check_plan` reports a missing `jsonschema` as a schema failure.** The lazy `import jsonschema` sits inside the same `try` as the validation call, so with the package absent every plan returns "plan does not match the plan schema · No module named 'jsonschema'" — an environment problem laundered as a data verdict, the exact class of collapse the corpus forbids elsewhere. The workbench's `/api/health` asserts the import instead, but the honest fix is in core: distinguish "could not validate (no validator)" from "validated and failed". One `except ImportError` clause. Surfaced by WP-5.2.

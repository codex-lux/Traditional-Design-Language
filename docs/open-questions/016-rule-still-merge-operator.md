# OQ 16 — `rule` still has no merge operator

*Status: CLOSED 25 AUG 2026 · Raised in: From authoring the Georgian kit*

**CLOSED 25 Aug 2026 — it was already done, and the label was the only thing still open.** Found while deriving the open-question list in a test instead of hardcoding it: this entry's own remaining work was "make `resolve_kit.py` actually honour the two instances already sitting in the data", and it does. `build/resolve_kit.py:156` joins a child's `rule_append` onto whatever `rule` the merge step resolved to, records the join in `_rule_append` with the contributing ancestor and the text it was joined after, and sets `rule_appended_from` in the provenance block; `--verbose` marks the slot `APPEND` and prints `(+ clause from <node>)` under the resolved sentence. Verified on both live instances — `roof_pitch` and `door_surround` on `kits/georgian-colonial-american.kit.json`. **The finding is the staleness itself**: a status word rotted for two days while the code under it was finished, and nothing was watching, because the test that should have caught it asserted against a hardcoded list. That test now derives the open set from this file, so the next rot fails a check instead of waiting to be noticed. *Original entry follows.* **IN PROGRESS — `rule` still has no merge operator.** `extends` fixed three of four restatement cases. The fourth failed because `rule` is one string: a child adding a clause must replace the whole sentence or leave the resolved rule silent about its own change. `rule_append` is in the schema at 0.2.1. This did not need a ruling — the operator's shape is already declared in the schema — so it is being implemented directly in WP-1.3 rather than put to a decision: a child's `rule_append` value is joined onto the resolved parent rule as an additional sentence, with the join point marked so `resolve_kit.py`'s provenance output can still show which ancestor contributed which clause.
**Correction, 23 Aug 2026:** "is not yet used" was wrong — checked directly while migrating constraints (WP-1.1): `rule_append` is already populated on two slots (`roof_pitch`, `door_surround`) in `kits/georgian-colonial-american.kit.json`, apparently from pre-session `extends`-cascade testing against `english-georgian`. What's actually still missing, confirmed by grep: `build/resolve_kit.py` has no code path that reads `rule_append` at all — the field is authored in data and declared in the schema, but nothing resolves or merges it yet, and no provenance marking exists for it. WP-1.3's real remaining work here is narrower than this entry's original framing suggested: not "start using the operator," but "make `resolve_kit.py` actually honour the two instances already sitting in the data."
